# CI-144「之间」的形式：计划与逐条审查（待联合审查）

> 2026-09-17 ｜ 状态：**Proposed，等人类联合审查通过后才动手**
> 用途：确认「之间」（器官与器官之间那一层）的**形式**，并逐条对照十项工程哲学审查其**可行性 / 哲学自洽 / 逻辑闭环**。

---

## 1. 问题陈述（一句）

**零硬耦合**的实现方式是器官之间不共享类型、只走线 ⇒ 类型信息在线上消失 ⇒ **编译器在接缝处失明**。
后果不是"谁做错了"，而是**这块地没有工具**：同类坑被不同的人反复重新发现。

已核实的三条实证（见 `CI-144_合拢坑记录.md`）：

- `span_id` 在六个器官中**零实现**，而 README 已写明 `traceparent: 00-<trace-id>-<span-id>-01`
- `helix_mind.proto` **两份副本**，且注释**已分叉**；`flowmodus.proto` 在 FlowModus 内部**就有一套半双源**
- 两个**静默接缝**：人类摘要占机器槽位；字段一路传到位而**接收方从未读它**

---

## 2. 「之间」的形式：**四件东西，各自单一职责**

核心判据来自你的哲学：**解耦是"器官之间没有依赖"，不是"器官与事实之间没有依赖"。**
⇒ **共享"事实"，不共享"代码"。** 这是让"唯一事实来源"与"极致解耦"同时成立的那把钥匙。

| # | 东西 | 唯一职责 | **不是**什么 |
|---|---|---|---|
| **①** | **契约事实源**（每协议一份，住在自己的身体里） | 声明**线上事实**：字段号、帧布局、错误码、通道分配、动词集 | **不是**代码、**不是**共享库、**不是**任何一个器官的依赖 |
| **②** | **生成**（每器官各自 build 时执行） | 从①生成**自己的**绑定 | **不是**运行时依赖；器官 A 与 B **互不引用** |
| **③** | **一致性校验器**（按需运行） | 比对①与各器官产物是否一致 | **不是**常驻服务；不在热路径 |
| **④** | **合拢验收**（按需运行） | 跑**行为**层面能证伪的验收（trace-id 贯穿等） | **不是**单元测试；它测"之间"，不测器官内部 |

**家族层（`.github`）只放"索引 + 坑记录 + 冻结账"**，**不放事实本身**——否则就会长出一个新的巨石（违单一职责）。

### 为什么正好是这四件

它们一一对应**四类可失败之处**：

| 失败处 | 谁抓 | 何时抓 |
|---|---|---|
| 形状写错（字段号/布局） | ② 生成 ⇒ 器官内**编译期** | 最早 |
| 两份产物漂移 | ③ 校验器 | 提交/按需 |
| 语义接错（两端各自自洽） | ④ 合拢验收 | 联调/按需 |
| 新的、还没人想到的 | 家族层**坑记录** | 事后一次，不再重复 |

---

## 3. 逐条对照十项工程哲学

| 原则 | 本形式如何满足 | 有无冲突 |
|---|---|---|
| **唯一事实来源** | 每个**线上事实**恰有一处：①（每协议一份）。**不是"所有事实一处"**——那会变成巨石 | ✅ |
| **单一职责** | ①②③④ 各一件；家族层只管索引与账 | ✅ |
| **极致复用** | ②生成 ⇒ **不可手抄**，K-002/K-003 从根上消失（抄不了生成物） | ✅ |
| **极致解耦** | 器官之间**零引用**；器官各自依赖**事实**，而事实不属于任何一方 | ✅ **关键澄清见 §2 的钥匙** |
| **按需加载** | ①是数据（不加载即不占资源）；③④按需触发，非常驻 | ✅ |
| **按需驱动** | ③④由变更/联调**驱动**，不轮询 | ✅ |
| **物理事实优先** | **PFP（4B 冻结）= 物理事实** ⇒ 冻结+版本锚点+生成；SAP（可演化）= 语义 ⇒ 可加不可改 | ✅ **且你的 PFP/SAP 拆分正是这条原则的实现** |
| **确定性优先** | ③④**无网络、无时钟、同输入同判决**；生成产物可复现 | ✅ |
| **0硬编码** | 线上常量（字段号/通道/错误码/动词）是**声明的数据**，不是代码里的字面量 | ✅ |
| **按需渲染** | ③④的结果只在被索要时呈现，不推不改器官 | ✅ |

**结论：十项全部满足，且无一项需要例外。** 唯一需要"解释"的是 `唯一事实来源 × 极致解耦`——已在 §2 用"共享事实、不共享代码"说清。

---

## 4. 逻辑闭环

```
        ① 事实源（声明）
             │ 生成
             ▼
        ② 各器官绑定 ──编译期──▶ 形状错误在此暴露
             │
             │ 比对
             ▼
        ③ 校验器 ────────────▶ 漂移在此暴露（按需）
             │
             │ 联调
             ▼
        ④ 合拢验收 ──────────▶ 语义接错在此暴露（按需）
             │
             │ 失败
             ▼
        坑记录（家族层）──────▶ 下一次冻结的输入
             │
             └──────── 回到 ① ────────┘
```

**闭环成立的条件**：④失败 → **必须**落进坑记录 → **必须**回灌①。**缺任一"必须"，环就断成一条直线**，坑就白踩——这正是当前状态（坑有，家没有；现在家有了）。

---

## 5. 对外部三份文档的严肃审查（三处必须修正）

### 5.1 ⚠️ **顺序自相矛盾：最贵的一类坑，配了最慢的探测器**

其《合拢期方法论》§4 把坑分为三类，并明确：

> **类型一：形状坑（不可逆）**——插头尺寸错了，所有设备都插不上。**这类必须先定，因为改起来最贵。**

但其第六章"最小动作"给出的**唯一**手段是**契约测试**（§3：「类型的编译器用不了 → 用行为的编译器」）。

**矛盾**：形状坑最贵，却用**最晚、最弱**的探测器（行为测试）去抓。
**修正**：形状坑应由**②生成**在**编译期**抓——这与"改起来最贵 ⇒ 必须最早发现"一致。**行为测试只该负责它独有的那一类（静默语义坑），不该兼任形状守卫。**

### 5.2 ⚠️ **"契约测试"必要但不充分**

契约测试测**行为**（"跑起来对不对"），测不出**形状声明本身是否符合标准**（"这个字段号是不是标准里那个"）。后者需要**比对声明与标准**——即 ③ 校验器。
⇒ **两者都不可省**：③ 管"声明 vs 标准"，④ 管"行为 vs 声明"。其文档只提了 ④。

### 5.3 ✅ **命名优先级须按实测重排**（已在坑记录 §2 记录）

其实测表称 Helix-Mind「全旧名」、FlowModus「全对」；**实测相反**：Cellrix **27** / Anaphase **8** / Mind **2** / FlowModus **1** / Tuck 0 / tentacle 0。
⇒ **先扫 Cellrix，不是 Mind。**

---

## 6. 可行性（含一条关键的"机制已存在"）

| 要素 | 现状 | 判定 |
|---|---|---|
| **②生成机制** | Anaphase 与 helix-mind **都已在 `build.rs` 里跑 `tonic_build`** | ✅ **机制早已存在——错的只是"输入的位置"（副本而非事实源）** |
| ①的形态 | 各协议已是**独立仓库**（`commonintents/*/.git`，含 `.github`） | ✅ 有地方放 |
| ③的形态 | 已有先例：`tools/adr_anchor.js`（锚定表校验器）已在 Cellrix 网内 | ✅ 有模式可循 |
| ④的形态 | 三条验收已可证伪（见 §7） | ✅ |
| **阻塞项** | **版本基线未定**：全员对齐 `v1.0.0-RFC-4`（无 PFP/SAP）还是跳 `v2.0-alpha`（有 PFP/SAP，未批准） | 🔴 **这是全表前提** |
| 成本 | 形状部分**基本机械**（生成 + 校验）；真正的难处是**基线决策**与跨仓协调 | 🟠 |

**关键结论**：这件事**不需要新发明机制，也不需要新构建体系**——`build.rs` + 生成 + 校验器都是**本生态已有的东西**。所以它不违"极致复用"，它是**复用**。

---

## 7. 计划（分期，每期可独立验收）

| 期 | 内容 | 验收（可证伪） | 依赖 |
|---|---|---|---|
| **S0** | **裁定版本基线**（RFC-4 还是 v2.0-alpha），并记入家族账 | 一条 ADR，写明基线 + 迁移态度 | **无（但它是全部的前提）** |
| **S1** | 坑记录 ✅**已完成**（`6f2c086`）；家族层建账（索引 + 冻结账） | 坑记录可提交、可追溯 | 无 |
| **S2** | **命名对齐**（按实测优先级 Cellrix→Anaphase→Mind→FlowModus） | 旧名命中数 **37 → 0**，且校验器可复核 | S0 |
| **S3** | **①事实源 + ②生成**：`helix_mind.proto` 与 `flowmodus.proto` 单一来源化 | **副本数 2 → 1**；两器官由生成获得绑定；**编译期**即抓形状错 | S0 |
| **S4** | **③校验器**：声明 vs 标准、产物 vs 声明 | 人为植入漂移 ⇒ **变红**（变异注入） | S3 |
| **S5** | **④合拢验收**：三条行为验收 | ①trace-id 不换 ②两次调用 span-id 不同（**K-001 的探测器**）③重连无空洞 | S2、S3 |
| **S6** | **回灌**：④失败 → 坑记录 → ① | 至少一次完整的"踩→记→改标准"闭环留痕 | S4、S5 |

**顺序理由**：S0 是前提（否则一切在流沙上）；S2 是纯文档但**必须先扫对地方**；S3 让**最贵的形状坑最早被发现**（修正 §5.1 的矛盾）；S4/S5 才是外部文档说的"契约测试"，**排在形状之后**——先让贵的可逆，再让静默的可见。

---

## 8. 待你联合审查的关键问句

1. **S0 基线**：`v1.0.0-RFC-4`（已冻结、**无** PFP/SAP）还是 `v2.0-alpha`（有 PFP/SAP、**未批准**）？
   → 我的建议：**冻 PFP 层（物理事实）到 RFC-4 的稳定形状，把 SAP 明示为演化**——即**不必二选一，而是按层选**：形状跟 RFC-4，演化容量跟 v2.0-alpha 的设计。
2. **①的粒度**：每个协议**自治**（各自仓内一份事实），家族层只放索引——**是否同意"家族层不放事实"**？（我反对家族层持有事实，那会长出巨石）
3. **②是否接受"生成"**：本生态已有 `build.rs`/`tonic_build`，但**事实源位置要从副本改为单一来源**。接受否？
4. **③的判决权**：校验器发现漂移时——**红灯阻断提交**，还是**只报告**？（我建议阻断，否则它会变成装饰）
5. **④的三条验收是否即为"跑通"的定义**？若同意，过了才冻 PFP 层。
6. **是否认可 §5.1 的修正**（形状坑由生成在编译期抓，而非契约测试）——这**改动外部方案的顺序**。

---

## 9. 诚实清单

1. 本文的**形式设计是我的推演**，不是 CI-144 既有规定。CI-144 已有的只有 `traceparent` 字段与 PFP/SAP 的**帧内**用法。
2. 我**没有逐条读**外部三份文档引用的全部源码；本文只对**可机器复核**的断言下判定（命名计数、span-id 零命中、proto 副本），其余列为待裁定。
3. **"版本基线"我没有替你选**——它决定其余全部，且涉及未批准规范，**不该由我拍**。
4. 我在本轮**先犯过一个错**（误判 `commonintents` 不是仓库、"CI-144 没有家"），由人类更正后已修正。**那次误判本身与 K-005 同族**：从错误的模型出发下结论，两端各自自洽而合起来不成立。
