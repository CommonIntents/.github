# CI-144 决策索引（唯一入口）

> 建立：2026-09-17 ｜ **第十一版：2026-09-17（拆出更正史；H6 并入 H4；B0 完成）**
> 用途：**回答"现在该做什么"**，而不是增加论述。
> 起因：本目录文档已达 17 份而代码未改一行。审查指出——**文档增长远快于代码**；若不加以约束，它会成为**第 18 个器官**。
>
> **✅ 第十一版执行了它自己的刹车**：§6（更正史，178 行）**已迁至 `CI-144_更正史.md`**，本文件从 384 行降到 214 行。
> **本文件此后只装三样东西：A 类（待裁）/ B 类（待执行）/ 计数口径。**
> **下一次需要追加更正时，追加到 `CI-144_更正史.md`，不得回填本文件。**
>
> **⚠️ 代价仍需写明**：本轮至今 **6 次提交、24 条 K、0 行代码**。文档已拆成各自停止增长的 4 份（索引 / 更正史 / 坑记录 / 实现矩阵），**但"文档快于代码"这件事本身没有被解决，只是被分散了。**

---

## 0. 先钉住两件事（否则后面每个数字都会漂）

### 0.1 规范读哪一份（**本版新增，因为此前每次读都隐含选了一份却没声明**）

**工作区里有同一 remote 的两份 BIND-19 检出**：

```
/Users/jason/Doubao/chats/Jasonmilk/BIND-19               HEAD=4321c09  branch=v2.0-alpha  tag=(无)
/Users/jason/Doubao/chats/Jasonmilk/commonintents/BIND-19 HEAD=bbc35c6  branch=v2.0-alpha  tag=v2.0-rc.1
origin 两者相同：https://github.com/CommonIntents/BIND-19.git
```

**判据在规范自己身上**（两处，源码逐字）：

```
SAP-xCF14 §10「权威来源声明」: 本规范的唯一权威来源在 CommonIntents/BIND-19/docs/spec/sap-xcf14.md
PFP/SAP 页眉「权威来源」    : github.com/CommonIntents/BIND-19/blob/v2.0-rc.1/docs/spec/...
                            「对应 BIND-19 版本」: v2.0-rc.1
```

**⇒ 本索引及一切行号判断，一律以 `commonintents/BIND-19/docs/spec/` 为准。**
**理由（可复核）**：该目录带 `v2.0-rc.1` tag；根目录那份无 tag，且缺少 `commonintents` 那份独有的 CC BY-ND 版权行。

| 文件 | 根 BIND-19 | **commonintents/BIND-19（准）** | 差异 |
|---|---|---|---|
| `docs/spec/pfp-xcf14.md` | 316 行 | **317 行** | 仅版权行 |
| `docs/spec/sap-xcf14.md` | 261 行 | **262 行** | 仅版权行 |
| `spec/BIND-19.md` | 195 行 | 195 行 | 仅版权行 |

> **`CI-144_更正史.md §6.2` 的「PFP 317 行」因此复核通过**——它一直指的是 `commonintents` 那份。
> **本版之前，"317"是个未声明范围的数字；现在它有了来源。**

### 0.2 仓数与清单（此前写"14 个独立 git 仓"，实测 **20**）

```
find . -name .git -maxdepth 3 | wc -l   →  20
```

- **commonintents（7）**：`.github` / `BIND-19` / `CAPABILITY-13` / `INTENT-7` / `INTENT-7-SECURE` / `PFP-xCF14` / `SAP-xCF14`
- **器官（13）**：`BIND-19`(根) / `Cellrix` / `FlowModus` / `Helix-MCP-Learner` / `HelixECO-Glove` / `Tuck` / `anaphase-helix` / `helix-mind` / `helix-tentacle` / `lodestone-md` / `lodestone-spec` / `lumtract` / `phyt-DNA`

> **注**：`commonintents/PFP-xCF14` 与 `commonintents/SAP-xCF14` **只有 README + CONTRIBUTING，无 `spec/`**（实测）。规范正文住 BIND-19。⇒ **"规范在哪个仓"这个早先悬着的问题，答案是 BIND-19。**

---

## 0.3 归类规则（硬约束）

| 类 | 含义 | 要求 |
|---|---|---|
| **A. 待决策** | 有人类决定才能推进 | 必须写明**决定的形式**；**且必须"一行可答"**（答复函数式：对任意输入只需一条规则，而非对每条输入各答一行） |
| **B. 待执行** | 无需决策，可直接做 | 必须写明**完成判据**，且判据须**可证伪** |
| **C. 背景** | 已完成的记录 | 只作追溯；**含"不做，因为 X"的显式条目** |

---

## 1. A 类：待人类决策（**5 个决策位，按依赖排序**）

| 序 | # | 问题（一行可答） | 依赖 | 若答 X 则… |
|---|---|---|---|---|
| 1 | **H4** | **权威裁定（两层，一次答完）**：<br>**① 两份 BIND-19 检出中，哪一个是本家族的工作副本？**<br>**② 该仓内部，`spec/` 正文与 `src/` 参考实现谁是线格式权威？** | — | ① 答"commonintents 那份" ⇒ B12 快照有唯一目标（内容判据见 §0.1）<br>② 见下方「H4 第二层的证据」 |
| 2 | **H5** | **`reflex.rs` 是求生机制，还是安全闸门？** | — | 答"求生" ⇒ 反射 fail-open **保持**，K-007 #3 归「未接线」，B5 撤销、替代 R1；答"闸门" ⇒ B5 原样恢复 |
| 3 | **H1** | **紧急处置动作集中，哪些是 veto 型（延续现状即可），哪些需要"放行"？** | — | 放行型 ⇒ 落白名单（**一个尚未被命名的功能**），不是 PFP 的一个 bit。**⚠️ 所选机制必须说明其可信来源**——否则会与 `Replay-Enable` 同构（一个无签名字段降级另一个），见 `CI-144_更正史.md §6.7.1` |
| 4 | **H2** | **谁能写记忆？写入是否经过与 `suggested_actions` 相同的审查？** | — | 若写入无审查 ⇒ 被守卫的与被执行的是两批数据，中间隔一次记忆 |
| 5 | **H2b** | **`safety_rules` 应当静态（源码即边界）还是运行时可更新？** | **← H5** | 答"静态" ⇒ 须修 `reflex.rs:7` 的 `loaded from L2` 注释（与实现不符） |
| 6 | **H3** | **B1–B3 通读后新发现的缺陷，纳入本次冻结 / 仅安全类纳入 / 全部推迟？** | **← B1–B3 ✅** | 一行规则，非逐条答 |

> **原 H6 已并入 H4**（不新增决策位）。理由：H4① 与 H4② 是**同一类问题（权威裁定）的两个层次**——
> ① 是"哪一份检出"，② 是"那一份里 spec 还是 src"。**分列会让同一个裁定被答两次，而两次可能不一致。**
>
> **H4 第二层的证据（B0 产出，详见 `CI-144_实现矩阵.md` 与 `CI-144_更正史.md §6.10`）**：
> - `src/frame.rs:1-20` 自陈 BIND-19 **v2.0** 把 PFP/SAP 存在位放进 `Flags`；
> - `spec/BIND-19.md:2`（**权威正文**）的 8 字节头是 `FrameType+ChannelID+Flags+SequenceID` + 4 字节 `PayloadLength` ⇒ **头 8 字节全被占满，放不下 v2.0 的字段**；
> - `Tuck` 有**单测钉住**自己的另一种布局（`frame.rs:409-411`）⇒ 三方各有一套；
> - `docs/v2.0-upgrade-plan.md`（自称权威、`READY_FOR_ALPHA`、定义 24 字节 PAL）**在 `spec/` 与 `src/` 中零命中**，却仍活在 5 处文档中 ⇒ **第三套**。

### H1 的背景（原 A1/A4，已并入）

反射已证实是 **veto-only**（`reflex.rs:13-15` 只返回 bool）；且 `hard_reflex` 的实际检查面是 `suggested_actions`，**不是**执行路径（见 K-009）。
⇒ **任何需要"放行"的紧急动作都不是反射，而是一个尚未被命名的功能。**

### H5 / H1 之下的机制选择（原 A4：E1 vs E2）——**已裁，附条件**

> **裁定：暂定 E1（PFP 只能否决 + 紧急动作走白名单；PFP 154 的放行位不再被要求）。**
>
> **理由（第七版重写）**：不是"E2 不可行"（那是第三轮被撤回的越界结论），而是——
> **已实证：被守卫的数据与被执行的数据不是同一批**（K-009）。任何依赖"输入可信"的机制，在已实证的那条路上都不安全。**E1 正好不依赖输入可信。**
>
> **升级条件**：若「`execute_calls` → PFP 帧构造」这一段被证明与 LLM 文本隔离，**可重开 E2**。
> ⇒ **该路径至今未读**，故 E1 为**审慎默认**，非定论。

**E3（直接清除 154）** 仍不作选：若 H1 枚举出真实需求，E3 不完整。

---

## 2. B 类：待执行（无需决策，附**可证伪**判据）

| # | 动作 | 完成判据（可证伪） | 归属 |
|---|---|---|---|
| ~~B0~~ | **✅ 已完成（2026-09-17）** ⇒ 产物：**`CI-144_实现矩阵.md`**（协议 × 消费端逐格、带行号证据；含 §3 单向依赖表、§5 免疫归谁、§1.1 PAL 待裁） | 判据达成：每个协议 × 每个消费端都有格子；**N-A 全部带理由，无留白**；能表达单向依赖。**⇒ 产出 K-021～K-024（4 条新坑）** | CI-144 |
| ~~B0′~~ | **✅ 已完成（2026-09-17）** ⇒ 产物：**`CI-144_默认放行分类表.md`**（11 处，五列，含计数口径与"不做，因为 X"） | 判据达成：产出**分类表**而非翻转清单；**当前生效 8 · 未生效 2 · 未定 1**；**应翻者仅 1 处（#2）**。**⇒ 产出 K-025 / K-026** | Anaphase |
| ~~B1~~ | **✅ 已完成（2026-09-17）** —— 通读 SAP 全文 262 行。**规范级答案见 `CI-144_更正史.md §6.7`** | 判据达成：答案由规范原文给出，非推断 | CI-144 |
| ~~B2~~ | **✅ 已完成（2026-09-17）** —— PFP 317 行**全文**读完（此前只读 128–185）。**"全文无来源规定"可断言**；但**新发现 5 处规范内部缺陷**（K-013～K-017） | 判据达成：逐段读完 + 用规范自带伪代码重编码 5/5 向量通过（**阳性对照**） | CI-144 |
| ~~B3~~ | **✅ 已完成（2026-09-17）** —— BIND-19 `spec/BIND-19.md` 195 行全文读完 | 判据达成：**帧头没有 Version 字段**（协商在连接级握手，`bind:4`）。**负对照**：BIND-19 全文零命中 `PFP/SAP/xCF14`。**⇒ 新发现 K-018～K-020** | CI-144 |
| **B4** | **`is_high_risk` 改判据维度**：从**命令名**改为**能力声明** | 变异证明：`sh -c` / `python -c` / `env` / `xargs` **注入后必须被列高风险**（当前实现全部漏报，见 `hitl.rs:82-92` 单测词表）。**注：`check_approval(&c.tool, &[])` 的"空切片丢上下文"是本项的症状，不独立立条** | Anaphase |
| ~~B5~~ | **撤销**。理由：K-009 + K-011 ⇒ 反射不覆盖执行路径，改 allow-list 不保护任何东西。**替代为 R1** | — | — |
| **B6** | `HardOverride` 补 **reason + 带外触发条件** | 变异证明：**无带外触发源时 `HardOverride` 不可达** | Anaphase |
| **B7** | **闸门缺省复盘**：`security_gate: None` 与 `PermissiveGate` | 变异证明：**不配闸门 ⇒ 高风险动作必须被拦**；配 `PermissiveGate` 后通过。**注：`PermissiveGate` 生产零实例化（全仓仅 `security.rs` 自测引用）** | Anaphase |
| **B8** | S2 命名对齐（**Cellrix 27 → Anaphase 8 → Mind 2 → FlowModus 1**） | 旧名命中 **37 → 0** | 全生态 |
| **B9** | S3 事实源单一化 + 生成 + **golden 对照** | 副本数 2 → 1；生成物与手写物**字节/行为等价** | CI-144 |
| **B10** | S4 校验器 + 基线豁免清单（含责任人与期限）。**← B0 · ← B15**（基线含 period 文件命名，B15 已改） | **两个 fixture**：① 人为植入漂移 ⇒ 变红（清单内豁免、逾期转阻断）；② **K-027 的 `AMBIGUOUS` 三态**——"命中 >1 ⇒ 不猜、报错并带候选"，**"不会说我不知道的校验器会产出假结果"**。**无 B0 则无可比对对象** | CI-144 |
| **B11** | 器官侧补丁：复合游标 `(period_id, seq)` + 物化快照。**← B15**（`period` 若仍是 `fnv64(input)` ⇒ 复合游标退化成裸 `seq`） | S5③ 可过；**且 `(period_id, seq)` 的消费者由本项建立**（B15 只把数据与契约备好，T5 是其契约测试） | Anaphase + Cellrix |
| ~~B15~~ | **✅ 已完成（2026-09-17）** —— `period_id` **分配**（`allocate_period_id`：时钟+计数器），文件按 `{period_id}` 命名、**去掉 `.truncate(true)`**；每行携带 `period_id` + `job_id`；`list_periods` **从行内读身份，不再从文件名反解**；解析返回 `None/One/Ambiguous`，**歧义报错并带候选**；端点区分 `missing` 与 `ambiguous`。**引用坑 ID：K-006** | **判据达成（四条，全部实跑）**：① 同名输入两轮 ⇒ 两轮内容可取回、id 不同、旧 `parent` 仍 join（T1）；② 不同输入 ⇒ 同样通过（T2 阳性对照）；③ 变异①按 `job_id` 命名（**等价于恢复 K-006 的触发条件**，不是"等价于旧 `truncate`"）⇒ T1/T2/T5/T8 **红**；变异②不读行内 id ⇒ T1/T8 **红**；变异③歧义静默挑最新 ⇒ T8 **红**；变异④ identity 回退为派生 ⇒ **T10**/T1/T5/T8/C9 **红**（四轮均已跑并回滚）；④ `list_periods` 与 Cellrix 三处消费者改名后仍列出全部轮次（**Cellrix 侧为 T7，见"未做"**）。**测试 277 → 283，全绿**（含 **T10**：重放比对 body、**排除 `period_id`**，且变异④证明它非空转）。**⚠️ K-006 未闭** —— 面板侧见 B16，未闭前不得宣称已解决 | Anaphase |
| ~~B15′~~ | **✅ 已随 B16 完成**（同为 Cellrix 侧，B16 的硬约束即它的判据）—— `period_normalize.js:219` `byId[job_id]` **已静默去重**；`session_list.js:143/171` 用 `job_id` 作选择键 | 同名输入两轮在面板上**出现两行**、可分别选中，**且复用 T1 的用例** | Cellrix |
| **B12** | **打 `v0.9-draft` + changelog（落 `commonintents/.github`）+ 生成式家族快照** | 快照为 CI-144 各成员仓的 `{路径, remote, 分支, commit, tag, spec 版本串}` 清单，**由命令生成、可重跑字节一致、禁止手写**。**← H4** | CI-144 |
| **B13** | **约束"回落路径的命令来源"（两个旋钮，主次分明）** | **主 `tool_command`**（`Option<String>`，生产无调用者、无配置键，但有完整执行接线 + `tests/integration_test.rs:193` 证明其活）；**次 `execution_placeholder`**（默认字面量 `"echo"`，无校验）。判据：**任何"命令"值不得无条件进入 `tool.execute(command, ...)`** | Anaphase |
| **B14′** | **`security_gate` 缺席的两种原因（未配置 / 构造失败）当前折叠成同一个 `None`** | 判据：**"闸门缺席"必须可区分于"闸门通过"**；构造失败不得静默降级为未配置。**同族阳性对照：Tuck 已就 `None ≠ 空表` 立过条款，anaphase 侧复发且无人写条款** | Anaphase |
| **B20** | 🆕 **身份词汇映射（B0 新增一行）** —— 现有四个词：`job_id`（派生重放句柄）· `period_id`（分配身份）· `trace_id`（**Tuck 在用，实际 = `job_id`**）· `span_id`（**零实现**，K-001）。**⇒ `trace_id` 与 `period_id` 的关系未定义**；B18 之后若并存，下一个人会造第五个词 | 四个词各有**一行定义**与**一处权威**；**且已定：`trace_id` 与 `job_id` 保持同一事实（不替换值）；`period_id` 是独立词**（前缀关系只是构造性质，不是词汇关系 —— K-031 说明它对任意入参不成立）；**且能判定任意两者是否同一事实**（"书同文"层，正是 CI-144 剩下的职责） | CI-144 |
| **R1** | **`ReflexArc` 归位（三条）**：① 模块头标注**「当前未接入执行路径 / 占位」**；② `safety_rules` 改名以表达自保语义；③ 修 `reflex.rs:7` 的 `loaded from L2` 注释 | 三条均落地；②③ 有变异证明（改回旧名/旧注释 ⇒ 校验变红） | Anaphase |
| ~~B16~~ | **✅ 已完成（2026-09-18）** ⇒ `Cellrix: 1fbab6e`：面板改按 **`period_id`** 取键（`period_normalize.js` 的链走查 + `session_list.js` 的选择态/行身份/重命名/续接列表/banner）；三处测试消费者**不再从文件名读身份**（`prove_track_nodes_test.js` / `pt_replay.js` / `ab_verify.sh` 改为**按行**解析样本）。**⚠️ 实际只验证了 2/3**：`pt_replay.js` **改动前就红**（夹具/计量漂移，见 K-029）⇒ **一个预红的测试无法验证任何改动**，它那处是待修夹具**引用坑 ID：K-006** | **判据达成（实跑）**：① `chain_merge_test.js` 新增同名输入用例——两轮必须保持两个 period、共享摘要**不得**解析出任何一个；**变异：`byId` 改回 `job_id` ⇒ 两断言红、链返回空**；② **现场 E2E**（`:18932` + 真 Anaphase + Tuck）：`/api/sessions` 对同一 `job_id` 返回**两行**、`period_id` 各异；`/api/events?job_id=<共享摘要>` ⇒ **`ambiguous=true` 且带两个候选**；按 `period_id` ⇒ 正常返回 8 事件。**⇒ K-006 关闭条件已满足**。回归网与基线逐条相同（既有 2 红非本次引入） | Anaphase + Cellrix |
| **B17** | 🆕 **治理层可自检**：三条前提（Anaphase endpoint / `tuck_endpoint` / Tuck `audit_path`）任一不满足时，必须显式处于**未治理**状态且可查询（启动日志 + 状态端点 + 面板可见）；**闸门须测能力而非端口**（`health.rs:137` 现在只 `tcp_reachable`） | 关掉 `audit_path` ⇒ 系统**报告"未治理"**，而不是静默继续；只开端口不落盘 ⇒ 同样报告未治理 | Anaphase + Tuck |
| ~~B17′~~ | **✅ 已完成（2026-09-18）** —— 分配器改为**会拒绝**：`job_digest` 校验（前缀/空/超长/非 hex 全部显式失败），`try_allocate_period_id` 返回 `Result`，生产调用点不再依赖 panic 版本；16 处测试夹具由非 hex id 改为合法 id；引用坑 ID：**K-031** | **判据达成**：非 hex ⇒ 显式失败（不是过滤）· 全非 hex ⇒ 不得产出 `run--p…` · 非 ASCII ⇒ 不 panic · **撞车用例**（`run-abcd&` 与 `run-abcd` 必须不撞）。**变异：把 filter 换回 reject ⇒ 必须红（已验）**。`anaphase-helix: eba9ff3` | Anaphase |
| **B18** | 🆕 **身份传播到 Tuck：送两个 id，且载波带外** —— **① 载波不得进帧、不得进 PFP**（帧层有两个不兼容格式 K-021、PFP 是已满的冻结层；且"帧"正是 B15 卸掉的那一半轮子）。**⇒ 走带外的 metadata / `_meta` / header**，并在实现里写死该约束，否则最省事的做法就是往帧里塞字段。**② 必须同时送 `job_id` 与 `period_id`，不是一个**：`period_id` 是分配值 ⇒ 重放时不同 ⇒ **只送它则重放的执行 join 不回原始审计记录**；`job_id` 管**重放分组**，`period_id` 管**join**。**③ 换键前先查旧键上挂着什么**：`trace_id` 上已有 **1255 条**审计记录（K-028），迁移须说明旧记录怎么办 | **判据（四点）**：① 同一输入两轮 ⇒ 审计链**两条可区分**记录，各自 join 回对应 period；② **阳性对照**：不同输入两轮 ⇒ 正常 join（证明判据不是只会失败）；③ **变异**：只送 `job_id` ⇒ 判据 **必须红**；④ **帧与 PFP 字节不变**（改前改后同一帧 ⇒ 同一字节，证明载波确在带外）；⑤ **header 缺失或不匹配时必须显式失败或标记 `unknown`，不得 fallback 到 `"local"`** —— 因为 `"local"` 是**合法值**，会让"关联键丢了"伪装成"这是一次合法本地调用"（K-032）。**变异：删掉该 header ⇒ 必须可观测地失败，而非静默得到 `"local"`。****✅ 已裁定：选「加」，不选「换」** —— 已核三事实：① 载波是 HTTP header（完全在帧/PFP 之外 ⇒ "不进帧"天然满足）；② **`period_id` 以 `job_id` 为前缀**（按构造成立，生产路径恒 16 位 hex；**对任意入参不安全**，见 K-031）⇒ 若换值则零新增字段；③ **但换了会破坏精确查询**：`audit_api.rs:57` 的 `?trace_id=` 是精确匹配，1255 条旧记录与查询契约都按 `job_id` ⇒ **换值后按 `job_id` 查不到新记录**。**⇒ 保持 `trace_id = job_id` 不变，新增独立字段；代价：B20 词汇表是"两个独立词"，不是包含关系。**（原始核查）：载波是 **HTTP header `x-tuck-trace`**（`http_reasoning.rs:113` 发 → `gov.rs:76-81` 取 → `ledger.rs:30-37` 写），**完全在帧/PFP 之外** ⇒ **"不进帧"天然满足**，B18 只需在既有 header 旁**多送一个 `period_id`**；**新增字段不破坏已有 1255 条记录，改字段名才会** | Anaphase + Tuck |
| **B19** | 🆕 **计量完整性**（K-029，**已完整定性**）—— 不只是"修一条红测试"。**首条 `assistant/usage` 被拒收 ⇒ 任何基于 usage 的聚合系统性低报**（token 总量 / 成本 / **缓存命中率**），而**丢的那条恰是唯一的 cold-cache 观测点 ⇒ 命中率的分母从一开始就缺了最该计入的部分**；**它是 FlowModus（度量衡）的输入来源**。根因：`cached_tokens: Option<u64>`（`usage.rs:36`）为 `None` ⇒ JSON `null` ⇒ `event_family.js:262` 的 `['number']` 不接受 `null` ⇒ `assembly.js:168` 整条拒收。**对照**：旧资产（`HEAD~1`）结果相同 ⇒ **非 B16 引入**；`prove_track.node.js:145-153` 有 `cached == null` 分支 ⇒ **下游懂 null，是校验器过严**。**引用坑 ID：K-029** | **判据（四点）**：① **可选字段接受 `null`**；**阳性对照**：`cached_tokens` 为数字时行为不变；**变异**：把 `null` 重新判为 invalid ⇒ 必须红。<br>② **拒收账本必须有消费者** —— `tape.rejections()` 现在**有账本但没人读**；"拒收有账本没人读"与"有测试没跑"是同一件事。<br>③ **契约测试**：`usage 节点数 == 文件内 assistant/usage 行数`（含 `null` 与缺省两种形态）。<br>④ `pt_replay.js` **由红转绿**。**⚠️ 不接受"改写入端不写该字段"** —— 那是把"明确的无值"降级为"字段不存在"，属 K-013 同族 | Cellrix |

**新增机制（本链产出，建议入家族规则）**：
- **阴性结论必须带阳性对照，且逐目标施加**（本链 6 次同族失败，第 6 次由机制自己抓出）
- **单引一条原则的论证不予通过**（须命名张力 + 残余风险）
- **变更 ① 必须引用坑 ID**
- **计数必须带口径**（"生效 / 未生效 / 未读"三分，禁止合并计数）

---

## 3. C 类：背景（含"不做，因为 X"）

| 条目 | 性质 |
|---|---|
| `CI-144_合拢坑记录.md` | K-001～K-024；**K 类唯一账本**（其中 K-007～K-010 为器官内部，见该文 §5 的裁定请求） |
| 🆕 `CI-144_实现矩阵.md` | **B0 产物**：协议 × 消费端逐格证据、单向依赖表、免疫归谁、PAL 待裁 |
| 🆕 `CI-144_默认放行分类表.md` | **B0′ 产物**：11 处默认放行分支、分类而非翻转、计数口径、"不做，因为 X" |
| 🆕 `CI-144_形状类CI检查规格.md` | **CI 机制规格**：阈值已实测锁定（300 NCLOC）；**CI-4 与 CI-6 均已落地并可红**；CI-4 首批 5 条豁免**已交付删除**（33→28，余下按 category 分 5 批至 2026-12-11）；CI-6 含 **DATA-ONLY 验证器**与 7 个变异 |
| 🆕 `CI-144_更正史.md` | **§6 迁出**：对《收敛纪要》的全部更正，只增不改 |
| `CI-144_之间的形式_计划与审查.md` | S-1～S6 分期（已并入 §2） |
| `CI-144_之间_审查答复.md` ~ `答复六` + `HANDOFF_审查意见_第四~六轮.md` | **六轮往返记录。本版起停止新增轮次** |
| **F（原 H6）：默认路径的闸门责任归谁** | **不做，因为** B13 若钳住两旋钮 ⇒ 回落路径无可配置命令 ⇒ 本问题无对象。仅当 B13 保留旋钮时重提 |

---

## 4. 必须记住的边界（防止误以为收尾）

> **本链是「决策链收敛」，不是「缺陷清点完成」。**

| 未读面 | 状态 |
|---|---|
| **SAP 262 行** | ✅ **全文已读**（B1，2026-09-17）⇒ 结论见 `CI-144_更正史.md §6.7` |
| **PFP 317 行** | ✅ **全文已读**（B2，2026-09-17）⇒ 结论见 `CI-144_更正史.md §6.8` |
| `BIND-19/spec/BIND-19.md` 195 行 | ✅ **全文已读**（B3，2026-09-17）⇒ 结论见 `CI-144_更正史.md §6.9` |
| `commonintents/BIND-19/src/pfp.rs` | **部分已读**（解码/规则6/reserved 相关行）；`src/sap.rs` **未读** |
| **PFP "optional" 的真实语义** | ✅ **已读（B2）** ⇒ **规范自身三处不一致**，见 `CI-144_更正史.md §6.8.1` |
| `tests/test_vectors/ci-144-v2.0-test-vectors.json` | ✅ 33 条已逐条核（B2）；**无任何测试加载它** ⇒ K-013 |
| `hitl.rs` | 仅读 `:35` 签名与 `:82-92` 单测 |
| `security.rs` | 已通读（108 行） |
| `reflex.rs` | 已通读（23 行） |

**下一个读规范的人可能在第 200 行发现第 4 处缺陷。**

### 4.1 ✅ 计数口径（**B0′ 已完成落地；本表只是指向，权威在产物文件**）

> **权威计数在 `CI-144_默认放行分类表.md §2`。** 本表不复制它，**因为两处计数必然漂移**（这正是本链栽过两次的坑）。
> 只记结论：**当前生效 8 处 · 未生效 2 处 · 未定 1 处**；**其中属「安全判定」的 2 处：1 处 fail-open（#2）+ 1 处 fail-closed ✅（#11）**；**真正"应当翻"的仅 1 处（#2 `security_gate: None`）**。
> **依赖降级 4 处依 Axiom C 保持 fail-open——翻它们等于"记忆不可用即停机"。**

**⇒ 早先"Anaphase 有 N 处 fail-open"的说法一律作废**，改为按口径说话（生效 / 未生效 / 未定）。
**⇒ 本条已不依赖 K-013**（B0′ 用的是源码直读，不是向量）；**K-013 仍是规范侧的开放项。**

---

## 5. 本索引自身的检验

- **A 类**：**6 个决策位**（H1–H6，含 H2b）—— **H6 是 B0 产出的，不是我加的**（见 `CI-144_更正史.md §6.10`）
- **B 类**：**12 条未完成**（B4 / B6–B14′ / B17 / **B18 / B19 / B20** / R1；**B17′✅ 已完成**；**B0✅ B0′✅ B1✅ B2✅ B3✅ B15✅ B16✅（含 B15′）B5 撤销**），每条有**可证伪**判据
- **C 类**：明确降级，含一条**"不做，因为 X"**

**已完成闭环**：**B0 · B0′ · B1 · B2 · B3 · B15 · B16**（**K-006 已闭**）（B0/B1/B2/B3 结论在 `CI-144_更正史.md §6.7–§6.10`；B0′ 产物为 `CI-144_默认放行分类表.md`）。
**⇒ 三份规格全部通读（774 行） + 实现矩阵已建。**

> ⚠️ **本文件 315+ 行，已超过它给自己定的"导航牌"体量。** 下一版应把 §6（更正史）迁出为独立文件，本文件只留 A/B/C 三类 + 计数口径。**在迁出之前，本文件的增长必须停止。**

**若本文件将来也变成"既不决策也无动作"，它应被降级为 C——包括它自己。**

---

## 6. 更正史 → **已迁出**

> **§6 的全部内容（对《收敛纪要》的更正，含 B0/B1/B2/B3 通读带来的新更正）已迁至 `CI-144_更正史.md`。**
> **迁出理由**：本索引长到 300+ 行，已开始变成它自己警告的那个器官（见文首）。**本索引只留"该做什么"，不留"曾经记错什么"。**
> **本索引自身设的刹车**：下一次需要往更正史追加时，追加到 `CI-144_更正史.md`，**不得回填本文件**。

---

## 7. 撤回留痕（**不静默重写**）

| 撤回项 | 原表述 | 撤回理由 |
|---|---|---|
| C1 | `with_tool_command` 零调用点 | **只 grep 了 `src/`**；`tests/integration_test.rs:207` 有调用。失效的是**范围**，不是 grep 模式（阳性对照 `with_pipeline` 命中） |
| C2 | `tool_command` 恒为 `None` | 由 C1 推出，随 C1 作废 |
| C4 | `command` 不由 `action_str` 派生 | **表述错**：命令来自 `tool_command`，`action_str` 是同一调用的实参 |
| — | K-008「拼接比逐个动作更松」 | **证据不足**。并集不删除已有子串 ⇒ 不可能更松。**保留两条**：无逐动作归因；注释自称 `O(1) hash matching` 实为线性 |
| — | "K-007 扩到五处" | **计数口径错**（把 4b 与未读项并进主计数）。已改为 §4.1 三分口径 |
| — | "删除 `execution_placeholder`" | **成本高于收益**：会使"回落路径"概念消失，牵连向后兼容测试 |
| — | "E2 当前不可行" | **越界**：从"反射输入被污染"跳到了"帧字节不可隔离"，跨了一整层 |
| — | "H4 两个副本未定正统、tag 已分叉" | **本版推翻**：两仓 HEAD 同属 `v2.0-alpha` 链，差异仅版权行 + 一个 README 提交；规范自己声明了权威来源。⇒ H4 降为"工作副本约定确认" |
