# CI-144 合拢坑记录（跨组件）

> # ⚠️ 本文件已转为**存档**
>
> **不要再往这里追加新条目。** 未结项请写 `CI-144_未结清单.md`——那是**唯一入口**。
>
> **为什么**：本文件是按时间追加的叙事（58 条 K），**读它必须从头读**。
> 而"还有什么没做完"是一个**短列表**，混在叙事里的结果就是**没人读**（K-029）。
> 同一件事刚在 `ci/check_line_budget.py` 上做过：**叙事留在原处，可变的那一半拆出去。**
>
> **本文件继续保留全部历史**，供追溯"某条结论是如何被推翻或确认的"。

> 建立：2026-09-17 ｜ **第二版：2026-09-17（新增 K-007～K-012 与 §5 裁定请求）**
> 用途：**「两个组件之间」出的问题，写在这里，不写在器官里。**
> **本版注**：K-007～K-010 是器官内部的坑，见 **§5**（我自觉违反规则 1，并给出两个选项）。

---

## 0. 为什么要有这个文件

`cargo check` 只看器官内部。器官之间只走"线"，类型信息在线上消失 ⇒ **编译器在接缝处失明**。

后果不是"做错了"，而是**这块地没有工具**：同类坑由不同的人反复重新发现，因为它们的家在"之间"，而每个器官的 `GROWTH.md` / `ADR` 都不收它们。

**规则（唯一一条）**：凡事发生在**两个组件之间**的坑，记在这里。器官内部的坑，照旧记在该器官的 `GROWTH.md`。

**记账格式**：坑 → 证据（可复核）→ 归属 → 现状。**证据必须可复核**，否则它只是一句印象。

---

## 1. 已核实（一手证据，可复核）

### K-001 ｜`span_id` 在六个器官中**零实现**（🔴 未修）

**坑**：CI-144 组织 README 第 7 节规定
`traceparent: 00-<trace-id-128bit>-<span-id-64bit>-01`，并要求它绑定进 INTENT-7-SECURE 的 TLS Session ID、嵌入 HXR 写入 L3 记忆。**但没有任何器官实现 span-id。**

**证据（2026-09-17 复核）**：

```bash
grep -rn "span_id\|call_id" --include=*.rs --include=*.proto \
  anaphase-helix/src helix-mind/crates Tuck/src helix-tentacle/src FlowModus
# → 零命中
```

**归属**：CI-144（信封层 / PFP）。
**影响**：同一轮内第 1 次与第 2 次工具调用**无法区分**；Tentacle 执行完即忘，事后不可追。
**现状**：**未修**。这是本表**最具体、最便宜**的一条——标准已写好，只需把现有 `job_id`（一轮一个）对到 `trace-id`，再补一个逐调用的 `span_id`。

### K-002 ｜`helix_mind.proto` 有两份副本，且已分叉（🟠 未收口）

**坑**：同一份协议**住在两个器官家里**，没有一处是不属于任何一方的。

**证据（2026-09-17 复核）**：

```
helix-mind/crates/helix-mind-api/proto/helix_mind.proto
anaphase-helix/proto/helix_mind.proto
```

**一手经历**：为给 `RememberRequest` 加 `parent_ids`，**必须同时改这两份**；改的过程中发现**两者的注释文字已经不一样了**（同一 message、同一字段集，注释各自演化）。字段集当时还一致，但**没有任何机制保证它继续一致**。

**归属**：CI-144（契约必须住在不属于任何一方的地方）。
**现状**：**未收口**。收口方式二选一：① 抽出独立契约仓并由两边引用；② 保留副本但加**一致性校验**（比对字段集，进了"之间"的测试网）。

### K-003 ｜FlowModus 契约双源（🟠 未收口）

**坑**：FlowModus 的 proto 同时存在于两处。

**证据（2026-09-17 复核）**：

```
FlowModus/flowmodus-rs/proto/{gossip,routing,metrics,supplier,registry}.proto
FlowModus/schemas/{gossip,routing,...}.proto        ← 同名文件第二次出现
anaphase-helix/proto/flowmodus.proto                 ← 消费方还留了第三份
```

**归属**：CI-144（契约单一真相源）。
**现状**：**未收口**。至少要先判定 `flowmodus-rs/proto/` 与 `schemas/` 谁是真相源。

### K-004 ｜"人类可读摘要"占用了"机器可读父指针"槽位（✅ 已修）

**坑**：Anaphase 把一个 period 的 `resume_from` 算成
`.or(context.resume)` —— 在调用方没给 job id 时，**把人类可读的续接摘要写进了机器可读的父指针字段**。该字段经 `/v1/sessions` 成为面板的 `parent` ⇒ 一段散文成了假父节点 ⇒ 线程断裂。

**证据（一手，实测）**：139 个 period 中 **12 条** `parent` 不是 id（10 条散文 + 2 条我验证时手写的），**83 条成孤根**。

**为什么它是"之间"的坑**：字段本身跨了 Anaphase ↔ Cellrix 两个器官；**写入方与读取方都各自"自洽"，只有线上合起来才看出来。**

**归属**：CI-144（语义漂移）。
**现状**：**✅ 已修**（`anaphase-helix: d403817`）：写入侧删掉回退（无 id 即无父）；读取侧拒绝非 period id；API 侧把悬空父指针归一为空。线上复查非 id 父指针 **12 → 0**。

### K-005 ｜一个字段被一路传到位，**接收方从未读它**（✅ 已修）

**坑**：`activation_vector`（proto 字段 13）在 ADR-0042 中"接线完成"——定义、存储、API 映射、proto 保留位全部就位。**但 Anaphase 侧从未读取它**（grep 零命中），白盒一直显示 Mind 持久化的 `heat` 列，而**那一列没有任何代码写过**（实测 `heat != 0.5` 的节点为 **0 / 565**）。

**为什么它是"之间"的坑**：两端各自都"做完了"。发送方真的发了，接收方真的收得到——**只是没人读**。`cargo check` 对此完全沉默。

**归属**：CI-144（端到端未被验证）。
**现状**：**✅ 已修**（`anaphase-helix: 9d69d15`）：改读 `activation_vector`，字段正名 `heat → activation`。

### K-006 ｜`job_id` 由输入派生 ⇒ 相同输入撞车 ⇒ 覆写被引用的父（🟠 已定位，未修）

**坑**：period 的 `job_id` 由输入派生，同问题重问 ⇒ 同 id ⇒ 写入 `truncate` **覆写**；而**别的 period 仍以被覆写的 id 为父** ⇒ 父的内容变成另一次更晚的执行。

**证据（一手，实测）**：`run-9e901b965a772d51` `first_ts=17:01:07`，其子 `run-32c4be74a996a40d` 为 `06:19:10` ⇒ **父比子晚 11 小时**；51 条血缘路径中 **5 条**时间戳非单调。

**归属**：CI-144（标识语义）。
**现状**：**✅ 已闭（2026-09-18）** —— 写入端（B15）与消费端（B16）均落地：
- `anaphase-helix: 519da1f`（`period_id` **分配**、文件按 `{period_id}` 命名、**去掉 `truncate`**、行内携带身份、解析返回 `None/One/Ambiguous`）
- `anaphase-helix: a15a9eb`（歧义报错并带候选，端点区分 `missing` / `ambiguous`）
- `anaphase-helix: fbecc01`（T10：重放比对 body、**排除 `period_id`**）
- `Cellrix: 1fbab6e`（面板与三处测试消费者改按 `period_id` 取键）

**现场 E2E 证据（`:18932` + 真 Anaphase + Tuck）**：
```
.helix/events/run-8bba24c5ee368a4a-p006aac4877000000.events.jsonl
  首行: period_id=run-8bba24c5ee368a4a-p006aac4877000000  job_id=run-8bba24c5ee368a4a  seq=0
/api/sessions  ⇒ 同一 job_id 两行、period_id 各异
/api/events?job_id=run-8bba24c5ee368a4a ⇒ ambiguous=true
  error: matches 2 periods (run-8bba24c5ee368a4a, run-8bba24c5ee368a4a-p006aac…); use a period_id
/api/events?job_id=<period_id>          ⇒ 正常返回 8 事件
```

**⚠️ 残留（不影响 K-006 关闭，但已登记）**：`ADR-0041` 仍是 `Proposed`，且其 **D2/D3** 与实际实现有偏差（见该 ADR §七，待人类裁定）。
**⇒ K-006 在工程上已闭；在"文档与实现一致"意义上仍欠一条 ADR 状态。**

---

## 2. 对本轮外部《接线表 v0.2》的**更正**（实测 vs 其结论）

外部审查给了"四器官四种命名写法"的表。**核数之后，优先级与结论都要改**：

| 组件 | 《接线表 v0.2》 | **实测：含旧名的文件数** | 判定 |
|---|---|---|---|
| **Cellrix** | 🟠 混血名 | **27** | **最严重；原表把重点放在 Mind 上是错的** |
| anaphase-helix | 🟠 旧名 + 版本号 | **8** | 次之 |
| helix-mind | 🔴 **全旧名** | **2** | 原表称"最旧"，实测**最少** |
| **FlowModus** | ✅ **全对** | **1** | **并非全对，原表此处不成立** |
| Tuck | ❓ 未标版本 | **0** | 在命名上**干净** |
| helix-tentacle | ❓ 未标版本 | **0** | 同上 |

**复核方式**：`grep -rlE "\b(CIB/1\.0|CIC13|CIN7|CIB19|CIS/|CISS/)" <repo> --include=*.rs --include=*.md --include=*.proto --include=*.toml`

**这条更正的意义**：原表会让人**从 helix-mind 开始扫命名**；实测说**先扫 Cellrix（27 : 2）**。**优先级错了，先做的那件事就会是错的那件。**

**其 ❓ 之一已解**：`commonintents/` 下确有 **`PFP-xCF14` 与 `SAP-xCF14`**（连同 `BIND-19` / `CAPABILITY-13` / `INTENT-7` / `INTENT-7-SECURE`，共 6 个），故"是否在组织内"不再是问题。

---

## 3. 待人类裁定（阻塞后续，不阻塞记录）

1. **版本基线**：全员对齐 `v1.0.0-RFC-4`（已冻结，**不含 PFP/SAP**）还是集体跳 `v2.0-alpha`（含 PFP/SAP，**未批准**）？—— 全表剩余格子的前提。
   - **2026-09-17 追加实测**：`v1.0.0-RFC-4` 这个 tag **只存在于根 `BIND-19/`**（该仓 branch=`v2.0-alpha`，tag 在 main 上）；`commonintents/BIND-19/` 的 tag 是 **`v2.0-rc.1`**。两仓 HEAD 都在 `v2.0-alpha` 链上，remote 相同。
   - **规范自身已给出读哪一份的判据**：PFP/SAP 两处「权威来源」均指向 `CommonIntents/BIND-19/docs/spec/…` @ `v2.0-rc.1` ⇒ **以 `commonintents/BIND-19` 为准**（详见 `CI-144_决策索引.md §0.1`）。
2. **安全层与能力层的上下顺序**：组织 README 与 BIND-19 仓库图**两处不一致**。（**2026-09-17 仍不一致，未核新状态**）
3. **K-002 / K-003 的收口方式**：抽独立契约仓 vs 保留副本 + 一致性校验。
4. **🆕 K-012 的处置**：PFP 4 字节无签名保护 + SAP 规则 6 可降级 `CATASTROPHIC` —— **是"规范缺陷"还是"未实现的机制"？** 前者改规范（但 PFP 自称冻结不朽，改动须出新版本），后者加机制（在 SAP 里覆盖 PFP 字节，或其他带外方式）。
5. **🆕 §5 的规则 1 边界**：器官内部的坑是否应移入器官侧账本（选项 A / B）。

---

## 4. 规则

1. **只在"之间"的坑写这里**；器官内部的坑仍记在该器官的 `GROWTH.md`。
2. **每条必须有可复核证据**（命令 + 输出，或源码路径 + 行）。**没有证据的印象不记**——它会让这张表失去可信度。
3. **状态只有三种**：🔴 未修 / 🟠 已定位未修 / ✅ 已修（附提交号）。
4. **更正不抹平**：若外部结论被实测推翻（如 §2），**记录更正本身**，不静默替换。

---

## 5. ⚠️ 本文件对规则 1 的一处**自觉违反**（2026-09-17，请裁定）

**规则 1 说：器官内部的坑记在该器官的 `GROWTH.md`。**
**而下面 K-007 ～ K-010 全部发生在 `anaphase-helix` 内部**（反射弧 / 安全闸门 / 审计的默认方向）。按规则 1，它们的家是 `anaphase-helix/docs/GROWTH.md`。

**我仍记在这里，理由与代价都写明如下**，请裁定后二选一：

| 选项 | 含义 | 代价 |
|---|---|---|
| **A（本文现状）** | 留在本表，**并标注"器官内部"** | 违反规则 1 ⇒ **规则一旦被例外，就不再是规则** |
| **B（建议）** | 移入 `anaphase-helix/docs/GROWTH.md`，本表只留一行指针 | 需要承认**"器官侧账本"也是一个机制，也必须被建**；否则这批坑会再次无家可归 |

**我倾向 B，但 B 需要一个新的 B 类动作（见下）**：

> **B15（建议新增）：器官侧坑账本** —— `anaphase-helix` 需要一个与本文同构的"坑 → 证据 → 归属 → 现状"账本。
> **完成判据**：K-007～K-010 在本表只留指针行；器官侧每条有可复核证据。

**为什么这条值得单列**：K-002/K-003（契约双源）是**"之间"的坑，因为它们跨仓**；而 K-007～K-010 是**"同一个器官内部，四处独立决定过默认方向"** —— **它是"内部的"，但它的成因（每处单独决定）与"之间"的坑同构。**
⇒ **规则 1 的边界可能划错了：该按"成因"划，而不是按"位置"划。** 这是本链第 3 次出现"同一个病在不同位置复发"。

---

## 6. 第二组 K（2026-09-17 新增）｜**器官内部（见 §5 裁定）**

### K-007 ｜Fail-open 的默认方向在四处被**分别决定**过（🔴 未修）

**坑**：不是"某处写错"，而是**每一个 fail 点都被单独决定过**，且方向不一致。

**证据（2026-09-17 复核，`anaphase-helix`）**：

| # | 位置 | 语义 | 当前是否生效 | 归类 |
|---|---|---|---|---|
| 1 | PFP optional | 缺省=无守卫 | **未读** | 待 B2 |
| 2 | `pipeline/mod.rs:97,118` `security_gate: None`（生产从不构造，`main.rs:694` 无 `with_security_gate`） | 缺省=无守卫 | ✅ **生效** | 安全判定 |
| 3 | `reflex.rs:14` `!safety_rules.iter().any(...)` | 缺省=无守卫 | ⚠️ **未接线**（守 `echo` 参数，见 K-009/K-011） | 4a |
| 4 | `run_cycle.rs:1182` `Fear prediction failed, default allow` | 失败=放行 | ✅ **生效** | 待 H5 |
| 5 | `run_cycle.rs:1247-1250` `Safety audit failed, fallback allow` ⇒ `Ok(Success)` | 失败=放行 | ❌ **4b 不可达** | 4b |

**计数口径（禁止合并计数）**：**生效 2 处（#2/#4）· 未生效 2 处（#3/#5）· 未读 1 处（#1）⇒ 总数未定，待 B2。**

**决定性对照（同一作者、相邻两个 match、相反选择）**：

```
run_cycle.rs:1211   match self.safety.audit(...)      Err ⇒ Ok(Success)   ← fail-open
run_cycle.rs:1258   match self.hitl.check_approval(..) Err ⇒ Ok(Failure)   ← fail-closed（注释自陈）
```

**归属**：Anaphase 内部（成因与"之间"同构，见 §5）。
**现状**：**🔴 未修**。

### K-008 ｜反射把**动作集拼接成一个字符串**再子串匹配（🔴 未修）

**坑**：`run_cycle.rs:1155` `suggested_actions.join(", ")` → `reflex.rs:14` `action.contains(rule)`。

**证据（源码逐字）**：
1. **无逐动作归因** ⇒ 只能整批拒 / 整批放；跨分隔符 `", "` 的边界命中会**误伤**（方向为更严，非更松）。
2. **注释与实现不符**：`reflex.rs:11` 自称 `O(1) hash matching`，实为 `iter().any()` **线性扫描**。

**已撤回项（留痕）**：曾主张"拼接比逐个动作**更松**"——**证据不足，已撤回**（并集不删除已有子串 ⇒ 不可能更松）。

**归属**：Anaphase 内部。
**现状**：**🔴 未修**。

### K-009 ｜**被守卫的数据 ≠ 被执行的数据**（🔴 未修）

**坑**：反射检查 `suggested_actions`（记忆侧），执行走 `context.calls`（模型侧）。**两者不相连。**

**证据（源码逐字，中间那条边已补）**：

```
run_cycle.rs:778    context.suggested_actions ← MemoryAdapter.query（记忆侧）
run_cycle.rs:262-266  字段注释自陈：Legacy unstructured action suggestions (from MemoryAdapter.query).
                      Retained for P11b compatibility; Execution prefers the structured plan.

context.calls 全仓仅两处赋值：
run_cycle.rs:722    ← contract::parse_structured_command(user_input)（结构化命令，bypass LLM）
run_cycle.rs:1104   ← parse_reasoning_output(output)（模型产出）
                    ⇒ 两处均不经过 suggested_actions
```

**⇒ 反射不覆盖执行路径；而它覆盖的路径执行的不是 `action_str`**（见 K-011）。

**归属**：Anaphase 内部。
**现状**：**🔴 未修**（修法 = R1 归位，不是改 allow-list）。

### K-010 ｜`safety.audit` 失败 ⇒ 放行，而 `SafetyAdapter` **只有一个实现**（🟠 已定位未修）

**坑**：`run_cycle.rs:1247-1250` 审计失败被当成成功放行；**但该分支当前不可达**。

**证据（源码逐字）**：

```
adapters/mod.rs:257   async fn audit(&self, action: &str, content: &str) -> Result<bool, String>;  ← trait
adapters/mod.rs:263-266  impl SafetyAdapter for NoopSafetyAdapter { ... Ok(true) }                ← 唯一实现
anaphase-helix/src/adapters/ 目录：flowmodus / http_reasoning / mind / mod / tentacle / usage
                              ⇒ 无 safety 真实 adapter
```

**⇒ 它是"被接上引信就会炸的哑弹"**：接任何会返回 `Err` 的真实 adapter 的那一刻，它变成活的 fail-open。
⇒ **关联 B14′**：`security_gate` 缺席的两种原因（未配置 / 构造失败）也折叠成同一个 `None`。

**归属**：Anaphase 内部。
**现状**：**🟠 已定位未修**（修法须先定"审计失败时该不该阻断"，这是一条**职责**决定）。

### K-011 ｜**安全子系统在 Anaphase 只有形状，没有实现**（🔴 未修）

**坑**：比"五处 fail-open"更根本 —— **这些 fail-open 是在数一个尚未实现的子系统内部的默认方向。**

**证据（2026-09-17 复核）**：

| 能力 | 真实 adapter | 生产接线 |
|---|---|---|
| 记忆 | `adapters/mind.rs` | ✅ |
| 推理 | `adapters/http_reasoning.rs` | ✅ |
| 工具 | `adapters/tentacle.rs` | ✅ |
| **安全审计** | **无**（仅 `NoopSafetyAdapter`） | ❌ |
| **安全闸门** | 仅测试（`tests/security_gate.rs`、`tests/tuck_gate.rs`） | ❌ **生产从不构造** |

```
security_gate 全仓用法：pipeline/mod.rs:97/118/126/165（定义与缺省）
                      + tests/security_gate.rs、tests/tuck_gate.rs
main.rs:694 AgentLoop::new(...) ⇒ 无 with_security_gate 调用
main.rs:787-788 resolve_pipeline(...).await ⇒ 失败即整体 None
```
**⇒ 闸门构造失败 = 整个 pipeline 变 `None` = 回落路径。这不是 fail-open，是 fail-degraded**（三个缺省值叠加，恰好把"安全子系统缺席"退化成了"执行路径也无对象"）。

**同族阳性对照**：Tuck 已就 `None ≠ 空表` 立过条款（`ADR-0005`），**anaphase 侧复发且无人写条款**（⇒ **B14′**）。

**归属**：CI-144（默认放行策略）+ Anaphase（实现缺口）。
**现状**：**🔴 未修**。

### K-012 ｜**PFP 的 4 个字节没有任何密码学保护，而 SAP 的签名覆盖的是别的东西**（🔴 规范层已坐实）

**坑**：SAP-xCF14 被读作"PFP 的安全证明层"，但**它的签名只覆盖 PAH（传感器读数）**。

**证据（规范原文，2026-09-17 全文读完 SAP 262 行）**：

```
sap:1.2  栈：[BIND-19 8B] + [PFP 4B] + [SAP 28B (可选)] + [Payload]
sap:8    「Tuck 读取：PFP=必须 / SAP=不依赖（可选验证）」
sap:3.6  PAH-Hash 的输入 = 「传感器读数（姿态/临边/模态/时间戳/设备 ID 等）的序列化字节」
sap:3.7  PAH-Signature = Ed25519(PAH) 的 SHA-256 截断 64 位
pfp 全文  grep "签名|防伪|篡改|覆盖" ⇒ 6 行命中，无一条是 PFP 自身的签名机制
pfp:1.1  「物理事实优先 | 所有字段由传感器驱动生成，AI 无权修改」   ← 声明，非机制
```

**⇒ 结论（规范级）**：`Risk-Level` / `Override-Flag` / `Replay-Enable` 全在 SAP 之外 ⇒ **帧构造方（AI）可无签名构造它们**。

#### 连带：**自我降级链**（本题真正危险处）

```
sap:6（规则 6）  当 PFP.Replay-Enable == 0：
                    effective_risk = RiskLevel::Medium;   // 丢弃原始高优先级
                    「永远无法触发 CATASTROPHIC 硬覆盖」
pfp:3.7         紧急处置唯一机制 = Risk-Level==CATASTROPHIC && Override-Flag==HARD_OVERRIDE
                （自称"协议的不可协商部分"）
```
**⇒ 不可协商的紧急机制，被同一个未签名 4 字节里的 `Replay-Enable` 位规范化地降级。**

**归属**：CI-144（PFP ↔ SAP 之间）。
**现状**：**🔴 规范层已坐实**；**代码层未核**（`commonintents/BIND-19/src/pfp.rs`、`src/sap.rs` 是否实现规则 6、是否校验覆盖范围 —— **未读**）。

---

## 7. 第三组 K（2026-09-17 新增）｜**B2 完成：PFP 317 行全文读完**

> **读法**：全文逐段读完（1–59 / 60–143 / 144–185 / 186–317），非 grep 命中行。
> **阳性对照**：规范第 5 节的解码伪代码，我用它把 5 条 `pfp_codec` 向量**逐条重编码**，5/5 与 `expected` 一致 ⇒ **伪代码可执行、向量与伪代码自洽**。
> **正因对照做了，才发现下面的缺陷不是"向量写错"，而是"声称与实现分叉"。**

### K-013 ｜**测试向量集存在，但没有任何测试加载它**（🔴 未修）

**坑**：`tests/test_vectors/ci-144-v2.0-test-vectors.json` 有 **33 条向量 / 8 个分类**，`metadata.description` 自陈：

> 「CI-144 v2.0 协议家族测试向量，**供其他兼容实现验证使用**」

**而全仓无任何测试加载它。**

**证据（2026-09-17 复核）**：

```bash
grep -rn "test_vectors\|ci-144-v2.0\|\.json" commonintents/BIND-19/src commonintents/BIND-19/tests --include=*.rs
# → 零命中
```

**⇒ 为什么这条比其他规范缺陷都重**：**它是"互操作性"这一层的地基。**
- 向量是**跨实现**协议（"供其他兼容实现验证"）的**唯一可执行判据**
- 没有加载 ⇒ **C1–C4 那一族失败（声称做了、实际没跑）在契约层重演，且这次是在 CI-144 自己的仓里**
- **顺带解释了 K-014：向量没被跑，所以它自己的 hex 与语义对不上也没人发现**

**归属**：CI-144（契约的可执行化）。
**现状**：**🔴 未修**。修法：加载 + 逐条断言；**未加载前，不得声称"测试向量已提供"。**

### K-014 ｜**Reserved 位非零时该怎么办——规范内部有三处规定，且互相矛盾**（🔴 未修）

**坑**：同一件事（解码时遇到 `Reserved != 0`）在三个地方有三种处理，且**参考实现与规范正文相反**。

**证据（四份材料，2026-09-17 逐条核）**：

| # | 位置 | 规定 |
|---|---|---|
| 1 | `pfp:3.9`（规范性正文） | 「未分配前，接收端检测到保留位非零应**触发版本协商流程，不得静默忽略**」 |
| 2 | `pfp:5`（解码伪代码，规范内） | `if reserved != 0 { return Err(Error::ReservedBitNonZero); }` ⇒ **硬错误** |
| 3 | `src/pfp.rs:255-266, 293-295`（参考实现） | **解析出 `reserved` 字段并返回 `Ok`**，另提供 `has_unknown_reserved()` 供调用方判断 ⇒ **不失败、不协商、只询问** |
| 4 | `pfp:7` 测试向量表 | 把 `pfp-all_one` 列成 `cf14 ff 0f` ⇒ **byte3 `0x0f` 的 reserved = 1，非零** ⇒ **规范性表格自己给了一条违反 #1 的向量** |

**⇒ 三种语义（协商 / 拒绝 / 接受），没有一个实现能同时满足 #1 与 #2。**
**⇒ 且 #4 是一条规范正文里的自相矛盾**：`Reserved` 字段定义（`pfp:2.2`）写"强制为 0"，而"全一"向量带着非零 reserved 出现在规范表里。

**为什么这条与 K-012 同族**：都是**"声明"与"机制"分叉**——K-012 是"AI 无权修改"无机制，本条是"不得静默忽略"与"return Err"无共同机制。

**归属**：CI-144（PFP 冻结层内部）。
**现状**：**🔴 未修**。⚠️ **PFP 自称冻結不朽 ⇒ 任何改动须出新版本（PFP-xCF15），不得原地改** —— 这条本身就是一个待裁定项。

### K-015 ｜`pfp-all_zero` 自称"全零"，实际编码不是零；且四处声明互不相同（🟠 已定位未修）

**证据（四处，全部可复核）**：

| 来源 | 内容 |
|---|---|
| `pfp:7` 规范向量表 | `pfp-all_zero` … **期望编码 `cf14 00 00`** |
| `tests/test_vectors/README.md:63-66` | `pfp-all_zero` … **期望编码 `cf14 00 00`** |
| `…test-vectors.json` | `pfp-all_zero.input` = `{modality:Cognitive, risk:Low, body_stance:**Unknown**, edge:Safe, dest:Internal, override:Normal, replay:false}`；`expected.encoded_hex` = **`cf143000`** |
| `src/pfp.rs:302-310` | `default()` = Cognitive / Low / **BodyStance::Unknown** / Safe / Internal / Normal / replay=true |

**⇒ 三件事**：
1. **JSON 的 `input` 与 `expected` 自洽（我重编码 5/5 通过），但它的 hex 是 `0x3000` 不是 `0x0000`** ⇒ **"全零"这个名字与内容不符**（`body_stance=Unknown=3` 被编码进 byte2 高位）。
2. **规范表与 README 写 `cf14 00 00`** ⇒ 那解码出来是 `body_stance=SEATED`，**与"Unknown"不符** ⇒ 两处文档与 JSON **不是同一个向量**。
3. README 自己的位布局行（`:60` / `:259`）写 `[Output-Dest(1)|Override-Flag(1)|Replay-Enable(1)|Reserved(5)]`，**与 `pfp:2.2` 的 `[Reserved(5)][Replay][Override][Output]` 位序一致**（都是 Output 在 bit0）⇒ 位序无争议，**争议只在取值**。

**归属**：CI-144（规范 ↔ 向量 ↔ 实现三源不一致）。
**现状**：**🟠 已定位未修**（K-013 修好后会被自动抓住）。

#### 🆕 2026-09-18 实测补充：不一致已证实，**权威待裁定**（勿据实测改规范）

**实测（CI-4 首批 5 条向量断言通过）**：

```
清单 input : body_stance = "Unknown"
清单 expect: cf143000        ← Unknown = 3 编进 byte2 位 4-5
规范表 pfp:7: cf14 00 00     ← 解出来是 SEATED，与 "Unknown" 不一致
README 表   : cf14 00 00     ← 同上
```

**⇒ 可断言的是：四方不一致（已由实测证实，这一半可闭）。**

**⚠️ 不可断言的是：哪一方是权威。** 这需要**显式裁定**，理由三条：

1. **`H4` 已裁定 `spec/` 是权威** —— 现在实现与 spec 冲突时判"spec 错"，**那是一次权威反转，不能由实测顺带得出**；
2. **这正是 K-023「不能计票」的翻版** —— 那里是 2:1 多数偏离伪代码被拒；这里是 **1 实现 + 1 清单 vs 2 份规范文本**，同样是计票；
3. **第三种可能未排除**：`cf14 00 00` 可能是**另一条向量的值被抄错了位置**（张冠李戴），而不是"写错"。

**⇒ 处置**：**"哪一方权威"进 A 类**（本轮加 H7）。**在裁定前，任何人不得据这条实测去改规范表** —— 那是用一个可能不权威的来源去改权威。

**另需注意**：`pfp-all_one` 实测 `cf14ef07`，其 byte3 `0x07` 的 `Reserved = 0` **只因 0xef 的高位覆盖了 proximity_edge**；而规范表列 `cf14 ff 0f`（`Reserved = 1`）⇒ **首批通过不代表规范一致**（K-014 仍在）。

### K-016 ｜硬实时路径的时延数量级写错约 **3000 倍**（🔴 未修）

**坑**：`pfp:6` 原文：

> 「仅需 2 次位运算 + 1 次比较，约 3 个 CPU 周期（**~0.3 ps**）」

**证据（纯算术，可当场复核）**：
```
3 CPU 周期 @ 3 GHz  = 3 × 0.333 ns ≈ 1 ns     ← 0.3 ps 的约 3000 倍
3 CPU 周期 @ 100 GHz = 30 ps                   ← 仍比 0.3 ps 大 100 倍
```
**⇒ 在现役任何时钟频率下，3 个周期都不可能是 0.3 ps。**

**为什么它值得记**（不只是笔误）：
- 这个数字是 **PFP 核心卖点（硬实时、~3 cycles）的一部分**，写在**冻结层规范正文**
- PFP 自称「冻结不朽，一旦定稿永不变化」⇒ **一个写错的数量级会被永久冻结**
- 而 §6 同段的 "2 次位运算 + 1 次比较 = 3 周期" 本身是对的 ⇒ **错的只是单位换算，不是算法**

**归属**：CI-144（PFP 冻结层内部）。
**现状**：**🔴 未修**（同 K-014 的约束：冻结层改动须出新版本）。

### K-017 ｜规则 6 的向量**避开了它唯一要紧的那个组合**（🟠 已定位未修）

**坑**：`rule6_downgrade` 有 3 条向量，`catastrophic_detection` 有 3 条 —— **没有任何一条同时具备 `Risk=CATASTROPHIC` 与 `Replay-Enable=0`。**

**证据（JSON 逐条）**：

| 分类 | 向量 | 关键组合 |
|---|---|---|
| `rule6_downgrade` | `rule6-catastrophic_replay_disabled` | 输入是**抽象枚举**（`original_risk_level: "Catastrophic"`, `replay_enable: false`）—— **不带 PFP 字节**，不经过编解码 |
| `rule6_downgrade` | 其余 2 条 | Critical+0 / Low+**1** |
| `catastrophic_detection` | `catastrophic-catastrophic_override` | `cf14ee07` ⇒ **Replay-Enable=1**（我按 `pfp:5` 伪代码解码复核） |
| `catastrophic_detection` | 其余 2 条 | 同上，均 `Replay-Enable=1` |

**⇒ 后果**：`src/pfp.rs:284 effective_risk_level()` 有两个效果 —— (a) `Replay=0 ⇒ Medium`；(b) 其余原样。
**当 `Risk=CATASTROPHIC` 且 `Replay=0` 时，(a) 与 (b) 冲突**，而这**正是 K-012 那条"自我降级链"的唯一触发条件**。
⇒ **测试集合系统性地避开了它自己最危险的那一格。**

**归属**：CI-144（测试覆盖 ↔ 规范风险不对齐）。
**现状**：**🟠 已定位未修**（K-013 修好并补齐这一格后，K-012 的代码侧影响才可测）。

---

## 8. 第四组 K（2026-09-17 新增）｜**B3 完成：BIND-19 `spec/` 195 行全文读完**

> **读法**：全文（1–70 / 95–145 / 145–195）。**阳性对照**：§4 协商算法（最高共同版本 + 空交集即断连）逐条可核对。
> **负对照**：`grep -n "PFP\|SAP\|xCF14\|CF14" spec/BIND-19.md` ⇒ **零命中**。

### K-018 ｜**"版本协商"这个词在 CI-144 里指两件不同的事，而 PFP 引用的是不存在的那一件**（🔴 未修）

**坑**：PFP `:176` 规定保留位非零时「应**触发版本协商流程**」，但**该层在协议栈里没有任何版本协商机制**。

**证据（四份材料）**：

| # | 材料 | 事实 |
|---|---|---|
| 1 | `bind:2` | 固定 8 字节头 = `FrameType(1) + ChannelID(1) + Flags(1) + SequenceID(1) + PayloadLen(4)` ⇒ **没有 Version 字段** |
| 2 | `bind:4` | 版本协商在**连接级握手**（`Handshake` 帧 0x05，TLS 1.3 风格），协商内容为 `client_supported_bind_versions` + `client_supported_intent_versions` + `client_supported_capabilities` |
| 3 | `bind:1` | 「BIND-19 remains **entirely oblivious** to the specific business contents of its payload」 |
| 4 | `bind` 全文 | **零命中** `PFP` / `SAP` / `xCF14` / `CF14` |

**⇒ 三条结论**：
1. **帧头没有 Version 字段** ⇒ B3 原问题的答案是：**帧头 Version 不存在，更谈不上可协商**（协商是连接级）。
2. **握手版本清单里没有 PFP 版本**（只有 BIND + INTENT-7）⇒ **即使 PFP 出了 xCF15，也没有协商通路。**
3. **BIND-19 声明对载荷完全无知** ⇒ **它不可能去检查 PFP 的保留位**；而 PFP 却把"接收端检测保留位"写在自己规范里。

⇒ **PFP 把责任委托给一个既不认识它、也没有该机制的层。** 这与 K-014（三处互斥）**同一族**：**都在引用一个不存在的机制。**

**顺带（位宽对照，可复核）**：`bind:2.1` 的 `Flags` 是 **1 字节、仅定义 3 位**（FIN `0x01` / CON `0x02` / SEC `0x04`），其余位"发送方置 0、接收方忽略"。
⇒ 但 PFP 的 `Proximity-Edge` 在**字节 2 的位 6-7**（`pfp:2.1`）⇒ **它是第二个字节的高 2 位**（我先前写作"高半字节"，**表述错，此处更正**）。⇒ **BIND-19 的 Flags 装不下它，PFP 必然是独立 4 字节** ⇒ 与 `pfp:1.2` 的栈图一致。

**归属**：CI-144（BIND-19 ↔ PFP 之间）。
**现状**：**🔴 未修**。

### K-019 ｜CI-144 的跨规范引用是**单向**且**未被任何一方核**（🟠 已定位未修）

**坑**：PFP/SAP 各自声明"在 BIND-19 之上"，而 **BIND-19 从不提及它们**。

**证据**：
```
pfp:1.2  「PFP 插入在 BIND-19 传输头之上、SAP 安全证明层之下」
sap:1.2  栈图同
bind     grep PFP|SAP|xCF14 → 零命中
sap:10   「所有规范变更的 PR 必须提在 BIND-19 仓库，同时更新 docs/spec/ + src/ + tests/」
```

**⇒ 这是"之间"的坑的教科书形态**：**引用是单向的，而被引用方不知道自己被依赖。**
⇒ 与 K-002（`helix_mind.proto` 双副本）、K-003（FlowModus 契约三源）**同族**：**没有一处是属于"之间"的**。
⇒ **对 B0（实现矩阵）的直接要求**：矩阵必须能表达**单向依赖**，否则 PFP↔SAP↔BIND-19 这三格填不出来。

**归属**：CI-144（跨规范引用）。
**现状**：**🟠 已定位未修**。

### K-020 ｜**帧头 Flags 与 PFP 的治理规则方向相反**（🟠 已定位未修）

| 层 | 遇到未分配位 | 方向 |
|---|---|---|
| `bind:2.1` Flags | 「Unassigned bits MUST be set to 0 by the sender and **ignored by the receiver** to ensure forward compatibility」 | **宽容**（忽略 ⇒ 前向兼容） |
| `pfp:3.9` Reserved | 「检测到保留位非零应**触发版本协商流程，不得静默忽略**」 | **严格**（不得忽略） |

⇒ **同为"未分配位"，同一家族的两个协议给了相反的规定。**
⇒ 这不是"谁错了"，而是**家族内缺少一条统一的"未知位"政策**——而 `flags` 与 `Reserved` 是同一个问题的两个实例。

**归属**：CI-144（家族级政策缺失）。
**现状**：**🟠 已定位未修**。修法建议：在 `.github` 级写一条**家族"未知字段/未知位"政策**，两个协议各自引用。（**这正是 P1「共享事实，不共享代码」的适用点。**）

---

## 9. 第五组 K（2026-09-17 新增）｜**B0 完成：实现矩阵产出**

> 证据全部来自 `CI-144_实现矩阵.md`（**逐格、逐行号**）。本组是 **B0 的产出**，不是它的失败。

### K-021 ｜**BIND-19 帧层有两个不兼容的格式，且两者的 SAP/PFP 载荷完全一致**（🔴 未修）

**坑**：同一"CI-144 帧"，参考实现与 Tuck 的**头 8 字节含义不同**。

**证据（逐字段，`Tuck/crates/tuck-core/src/frame.rs` vs `commonintents/BIND-19/src/frame.rs`）**：

| 字段 | 规范 `bind:2` / 参考实现 | Tuck |
|---|---|---|
| magic `0xCF14` | **字节 2+3** | **字节 0+1**（`frame.rs:104`） |
| FrameType | 字节 0 | **字节 3**（`frame.rs:112`） |
| Channel ID | 字节 1 | **无该字段** |
| Sequence | **1 字节** | **`u16`**（`frame.rs:113`） |
| Payload Length | **4 字节，上限 64MB** | **`u8`**（`frame.rs:91/115`） |
| PFP/SAP 存在 | **Flags 具名位 `0x08`/`0x10`** | **独立字节 `present`**（`frame.rs:114`）+ 高位约定 |
| **SAP 28 字节载荷** | — | ✅ **逐字段一致**（`sap.rs:35`） |

**⇒ 三条硬事实**：

1. **上限差 6 个数量级**：Tuck `u8` ⇒ 255 − 40 = **载荷约 215 字节**；规范 `u32` ⇒ **64MB**。
2. **`with_payload` 静默截断**：`frame.rs:315` `payload.len().min(255) as u8` ⇒ **超限不报错，改数字**。
3. **注释与实现不符**：`frame.rs:30`「if >255 use extended」、`:90`「extended length for large payloads」⇒ **`grep -i extended` 零实现**（仅这两行注释命中）。

**⇒ 同族阳性对照（这条才让它站得住）**：**SAP 载荷逐字段一致** ⇒ **分歧只在帧层，不在载荷层** ⇒ **不是"Tuck 整体没对齐"。**

**归属**：CI-144（BIND-19 帧层）。
**现状**：**🔴 未修**。**修法前置**：须先裁 K-022。

### K-022 ｜**规范正文与它自己的 v2.0 参考实现不是同一版**（🔴 未修）

**坑**：`commonintents/BIND-19/src/frame.rs:1-20` 自陈「BIND-19 **v2.0** 在 v1.0 的 8 字节固定头部基础上，增加 PFP/SAP 可选扩展层」，并把 PFP/SAP 存在位放进 **Flags**（`0x08`/`0x10`）。
**而** `spec/BIND-19.md:2`（**权威正文**）的 8 字节头是 `FrameType + ChannelID + Flags + SequenceID` + 4 字节 `PayloadLength` ⇒ **头 8 字节全部被 `PayloadLength` 占用，没有位置放 FrameType/ChannelID**。

**⇒ 参考实现与它自己引用的权威正文不是同一版。**
**⇒ 这与 K-021 是两件事**：K-021 是"Tuck 与参考实现不同"；K-022 是"**参考实现与权威正文不同**"。
**⇒ 若 K-022 成立，则 K-021 的裁定前提是**：`spec/BIND-19.md`（195 行，v1.0.0-RFC-4）与 `src/frame.rs`（v2.0）**谁定义线格式**。

**归属**：CI-144（BIND-19）。
**现状**：**🔴 未修**。⚠️ **本条须与 `docs/decisions/0008-ci144-v2-bind19-frame-type-conflict.md`（该文件名为"frame type conflict"）对照后再裁定——该 ADR 未读。**

### K-023 ｜**规则 6 的强制降级只在一个实现里没生效——而那个实现把"没生效"写成了测试**（🔴 未修）

> **本条曾在 2026-09-17 被写成"2:1 与规范伪代码相反"。该表述已撤回**（见 `§10 更正`）——
> **方向是反的**：`sap:6` 第 1 条原文写着「有效风险等级强制视为 `MEDIUM`。**永远无法触发 CATASTROPHIC 硬覆盖**」⇒ **规范要求这个分支在 `Replay=0` 时不可达**，Tuck 与 Cellrix 遵守了它。**偏离的是参考实现。**

#### 三个实现的实测（逐行）

| 实现 | 判定用哪个风险 | 位置 | 该判定有调用方吗 |
|---|---|---|---|
| 规范 `pfp:6` 伪代码 | **原始** `risk_level` | `pfp:6` | — |
| 规范 `sap:6` 第 1 条（**规范性强制**） | — | 「永远无法触发 CATASTROPHIC 硬覆盖」 | — |
| **Tuck** | **`effective_risk_level()`** | `tuck-core/src/lib.rs:427-430` | ✅ `decide()` 主路径 |
| **Cellrix** | **`effective_risk_level()`** | `protocol/src/pfp.rs:292-294` | ✅（未经 `decide`，方法公开） |
| **`commonintents/BIND-19`（参考实现）** | ⚠️ **原始 `risk_level`** | `src/catastrophic.rs:327-334`、`src/pfp.rs:276-279` | ✅ `CatastrophicManager::handle_frame`（`catastrophic.rs:345`） |

#### 参考实现的关键证据：它有测试钉住"降级不生效"

```rust
commonintents/BIND-19/src/frame.rs:752-770
    RiskLevel::Catastrophic,
    ...
    OverrideFlag::HardOverride,
    false, // replay_enable = 0              ← 显式构造规则 6 的触发条件
    ...
    // 生产模式：CATASTROPHIC 硬覆盖触发
    assert!(frame.is_catastrophic_override());   ← 断言硬覆盖"仍然触发"
```

⇒ **参考实现不仅没实现规则 6 的效果，还把它写成了预期行为。**
⇒ **同文件 `:772-785` 另有 `test_is_replay_disabled()` 断言同一条件的帧 `replay_enable == 0`** ⇒ **同一文件内，两条测试各自都对，合起来违反 `sap:6`。**
**⇒ 这是"偏离被固化进测试"的形态** —— 与 Tuck 那条（`lib.rs:409-411` 钉住自己的帧布局）**同一族**：**两处都是"测试保护了错误"**。

#### ⇒ 裁定问题被改写（比"谁对"更根本）

> **不是**「谁实现了伪代码」，**而是**「`pfp:6` 的伪代码与 `sap:6` 的第 1 条互相矛盾，谁是规则 6 的规范语义？」——
> - 若以 `sap:6` 为准（Tuck / Cellrix 路线）⇒ **参考实现须修，且它的测试须改**
> - 若以 `pfp:6` 伪代码为准 ⇒ **规范第 1 条须改**（而它写着"从根本上杜绝利用重放发动高危物理攻击"，改它等于放弃该防护目标）

**⇒ 且"计票"被彻底排除**：这不是 2:1 的问题，是**规范两份文本自相矛盾**（与 K-014 的保留位、K-018 的版本协商同族：**规范内部多处互斥**）。

#### 与 K-012 的关系（这一条才让它致命）

`Replay-Enable` 本身**在未签名的 PFP 4 字节里**（K-012：SAP 只签 PAH）。
⇒ **参考实现的行为 = 攻击者可置 `Replay=0`（跳过防重放检查）而仍然触发紧急放行通道** ——
⇒ **恰好使 `sap:6` 第 1 条声称要杜绝的那件事成立。**

**归属**：CI-144（PFP `pfp:6` ↔ SAP `sap:6` ↔ 参考实现）。
**现状**：**🔴 未修**。**⇒ 关联 K-017**：向量系统性避开这一格，**因为把这格写进向量，就会暴露两份规范文本的矛盾**。

### K-024 ｜**v2.0 升级计划定义的 24 字节 PAL 被 PFP 4 + SAP 28 取代，而没有任何文档说这件事**（🔴 未修）

**坑**：`docs/v2.0-upgrade-plan.md` 自称**权威版本**、状态 **`READY_FOR_ALPHA`**、维护者 Helix-Mind，定义：

```
pal:61-80   PAL 固定头部 = 24 字节 / 192 bits，含 Modality/Risk-Level/Body-Stance/Proximity-Edge
            /Output-Dest/Override-Flag/PAL-Version/Replay-Enable/Reserved/Physical-Context-Hash/PAH-Signature
pal:83      帧结构 = [ 8 字节 BIND-19 传输头 ] + [ 24 字节 PAL ] + [ 可变长加密载荷 ]
pal:88-91   BIND-19 传输头新增 `PAL-Present`(1 bit)；`Seq-Counter`(16 bits) 复用**原第 6-7 字节（原 Reserved 区域）**
```

**而实际发布的是另一套**：

| 材料 | 内容 |
|---|---|
| `docs/spec/pfp-xcf14.md` | PFP **4 字节**（冻结层） |
| `docs/spec/sap-xcf14.md` | SAP **28 字节**（演进层） |
| `README.md:73` | `[8B 头] + [PFP 4B (optional)] + [SAP 28B (optional)] + [Payload]` |
| `git log -1 3c73907` | 「规范文档权威来源建立 — PFP/SAP规范 … v2.0-rc.1」 |

**⇒ 三处消失/矛盾，全部实测**：
1. **`PAL` 在 `spec/` 与 `src/` 中零命中**（`grep -rn "PAL" spec/*.md docs/spec/*.md src/*.rs`）⇒ **PAL 已不在规范与源码中**。
2. **`PAL` 仍活在 5 处**：`docs/v2.0-upgrade-plan.md`（36 次）、`docs/decisions/0001`、`0003`、`0009`、`PLAN.md`、`README.md`。
3. **没有任何提交或文档声明"PAL 被 PFP+SAP 取代"**——`3c73907` 的提交信息只写"建立权威来源"，**不写取代关系**。

**⇒ 而 PAL 的卖点正是它被去掉的理由**：
```
pal:35  「Tuck 只读取 PAL 头部（24 字节明文），不解密载荷；24 字节 = 3×64-bit 对齐 SIMD」
pal:304 「固定 24 字节头部、固定加密路径、无分支」
```
⇒ **一个以"24 = 3×64-bit 对齐、利于 SIMD"为核心论证的头部，被换成 4+28=32 字节的两层结构，而没有任何记录讨论过这个性能论证是否失效。**

**⇒ 且这直接与附录 D 的自我声明矛盾**：
```
pal:360  「附录 D **不修改**协议规范的任何字段、偏移、规则语义。PAL 头部、BIND-19 帧结构、所有规则（1~8）文本保持**完全不变**。」
```

**归属**：CI-144（v2.0 计划 ↔ v2.0-rc.1 规范）。
**现状**：**🔴 未修**。**⇒ 裁定项**：`docs/v2.0-upgrade-plan.md` 是**现行权威**、**已废草案**、还是**待执行的下一版**？
**⇒ 若它是"已废"，则 5 处引用它的文档（含 `PLAN.md` 与 `README.md`）需要更正。** 本条不裁，只记事实。

---

## 10. 本文件自身的更正（**不静默重写**）

| 撤回项 | 原表述（2026-09-17 早） | 更正理由 |
|---|---|---|
| **K-023 的方向** | 「**2 个实现**（Tuck/Cellrix）与规范伪代码相反 …… **多数实现可能一起偏离伪代码**」 | **读反了。** 规范 **`sap:6` 第 1 条**（规范性强制）原文：「有效风险等级强制视为 `MEDIUM`。**永远无法触发 CATASTROPHIC 硬覆盖**」⇒ **规范要求该分支在 `Replay=0` 时不可达**。Tuck 与 Cellrix **遵守了规则 6**；**偏离的是参考实现**（`catastrophic.rs:327` 用原始风险），**且它把偏离写成了测试**（`frame.rs:752-770`）。⇒ **裁定问题随之改写**：不是"谁实现对了"，是 **`pfp:6` 伪代码与 `sap:6` 第 1 条互相矛盾**。 |
| K-023 的"死代码"推断 | （审查稿提出）「那 2 个实现里的 CATASTROPHIC 硬覆盖**是死代码**」 | **部分成立、部分不成立**：<br>• **"不可达"成立**（降级后恒 ≠ CATASTROPHIC）—— 但这是**规则 6 的设计目的**，不是缺陷；<br>• **"无人发现"不成立**：**Tuck 有显式测试**（`lib.rs:515-516`「CATASTROPHIC + HardOverride with Replay-Enable=0 → effective MEDIUM → Pass (not HardOverridePass)」）⇒ **它知道，并写下来了**。 |

---

## 11. 第六组 K（2026-09-17 新增）｜**B0′ 完成：默认放行分类表产出**

> 证据全部来自 `CI-144_默认放行分类表.md`（11 处，五列，含计数口径）。**本组是 B0′ 的产出。**

### K-025 ｜**同一个"闸门判不出"，两条路径给出相反答案，而弱的那条是默认路径**（🔴 未修）

**证据（源码逐字，`anaphase-helix/src/run_cycle.rs`）**：

| 路径 | 守卫 | 判不出时 |
|---|---|---|
| **结构化**（有 pipeline） | `hitl.check_approval` | **阻断**（`_ =>` ⇒ `Failure`，`:1561-1566`） |
| **回落**（无 pipeline，**默认**） | `hitl.check_approval` | `Err` ⇒ **阻断**（`:1254-1258`，自陈 fail-closed） |
| | `safety.audit` | `Err` ⇒ **放行**（`:1247-1250`，自陈 `fallback allow`） |

**⇒ 同一个作者、相邻两个 `match`、相反的两个选择** ⇒ **印证"每一处都被单独决定过"**（不是疏忽）。
**⇒ 且结构化路径的 `_ =>` 更强**：它把 `Err` 与 `Ok(false)` **一视同仁地阻断**；回落路径的 `safety.audit` 把 `Err` 当放行。
**⇒ 复合后果**：`resolve_pipeline` 失败（K-011）⇒ 落到**守卫更弱**的默认路径。

**归属**：Anaphase 内部。
**现状**：**🔴 未修**。

### K-026 ｜**`is_high_risk` 既决定"要不要问人"也决定"能不能不问就执行"**（🔴 未修）

**证据（源码逐字，`anaphase-helix/src/hitl.rs`）**：

```rust
:35     pub fn is_high_risk(command: &str) -> bool          ← 只吃一个字符串
:66-71  pub fn check_approval(&self, command: &str, args: &[String]) -> Result<bool, String> {
            if !Self::is_high_risk(command) {
                return Ok(true);                            ← 低风险 = 不经人类，直接放行
            }
            (self.approver)(command, args)                   ← args 只在"已判高风险"之后才用
        }
```

**⇒ 两条推论**：
1. **`args` 只在"已判高风险"之后才被使用** ⇒ 结构化路径传的 `&[]` **不是丢上下文，是这个函数本来就用不到** ⇒ **印证**把 `&[]` 降为 B4「症状」的裁定。
2. **代价比"漏报"更大**：`is_high_risk` 判错（如 `sh -c` / `python -c` / `env` / `xargs` **按既存单测 `:82-92` 均不在词表**）**不是"漏问一次"，是直接放行**。⇒ **这就是 B4 的完整代价。**

**归属**：Anaphase 内部。
**现状**：**🔴 未修**（修法即 B4）。

---

## 12. 第七组 K（2026-09-17）｜**B15 落地过程中发现**

### K-027 ｜**接收方把"多"说成"零"——错误分类丢失**（✅ 已修，但形态可复用）

**坑**：`GET /v1/sessions/<period>` 原先把**所有** `Err` 都归为 `missing: true, events: []`。
⇒ **"有多个 period 共享这个摘要，请指明一个"** 被报成 **"没有数据"**。

**证据（源码逐字，B15 修改前）**：

```rust
// anaphase-helix/src/main.rs（修改前）
Err(e) => Json(serde_json::json!({
    "configured": true, "missing": true, "events": [],
    "error": e.to_string()
})),
```

**⇒ 这是 K-006 的镜像**：

| | K-006（原版） | K-027 |
|---|---|---|
| 情形 | 两份数据被当成**一份**（覆写） | 两份数据被当成**零份**（missing） |
| 方向 | 把"多"说成"一" | 把"多"说成"零" |
| 共同病 | **接收方从错误中恢复不出真相** | 同 |

**⇒ 同族第三例**（前两例：`.or()` 拿摘要顶机器 id；"歧义时静默挑最新"）：
**失败形态是「错误分类丢失」** —— **错误被压平成一个桶，调用方无法区分该重试、该改键、还是该放弃。**

**为什么值得单独立 K（而不只写在更正史里）**：
1. **可复用**：任何"一个 `Err` 分支兜住所有失败"的地方都会复发；
2. **它给 B10 提供了第二个真实 fixture** —— B10 的三态（`PASS` / `FAIL` / **`AMBIGUOUS`**）正是它的对策：

| B10 三态 | K-027 的对应 |
|---|---|
| PASS | 命中 1 个 |
| FAIL | 键无效 |
| **AMBIGUOUS** | 命中 >1 ⇒ 不猜，报错并带候选 |

⇒ **B10 的第一个 fixture 是"人为植入漂移 ⇒ 变红"；这是第二个。**

**修法（已落，`anaphase-helix: a15a9eb`）**：新增 `ambiguous` 分支（`missing: false` + 候选 `period_id` 列表）；`rename` 同构处理，且**先解析再写**（写操作上猜比读操作上猜更贵）。

**归属**：Anaphase（传输层）↔ CI-144（错误分类契约）。
**现状**：**✅ 已修**（B15 同批）。**但形态本身未机制化**——见 B10。

---

## 13. 第八组 K（2026-09-18）｜**B16 现场验证中发现**

### K-028 ｜**Tuck 审计链的关联键是内容摘要 ⇒ K-006 在跨来源侧重演**（🔴 未修）

**坑**：Anaphase 只把 `trace_id = fnv64(user_input)` 送出去；Tuck 审计链以它为关联键。
⇒ **同一输入跑两轮 ⇒ 两侧各一条 `trace_id=X` ⇒ 跨来源 join 把两轮合并成一条。**

**现场证据（2026-09-18，`Tuck/gateway-audit.jsonl` 第 1255 条真记录）**：

```json
{"kind":"response","trace_id":"run-8bba24c5ee368a4a",
 "data":{"caller":{"api_key_id":"system"},"demap_miss":0,"session":"default","status":200}}
```

**⇒ 与 K-006 同形**：

| | K-006（文件侧） | K-028（跨来源侧） |
|---|---|---|
| 两份数据被当成一份 | ✅ 已修（B15/B16） | ❌ **未修** |
| 机理 | `job_id` 同名 ⇒ 覆写 | `job_id` 同名 ⇒ **join 合并** |

**⇒ 这一条曾被用作"必须保留两身份"的辩护理由，那个论证是循环的**（用 K-006 论证 K-006）。
**正确修法不是二选一，是换 join 键**：join 应用 `period_id` ⇒ 需其传播到 Tuck（`traceparent` 层）。**动作位 = B18。**

**归属**：Anaphase ↔ Tuck（跨来源标识）。
**现状**：**🔴 未修**。**⇒ 在 B18 完成前，跨来源 join 是已知未修缺陷，不得表述为"设计约束"。**

### K-029 ｜**每轮第一条 `assistant/usage` 被校验器拒收 ⇒ 计量少算一次调用**（🔴 未修）

**起点**：`Cellrix/web/tests/pt_replay.js` 长期为红，而它正是**三处 `.events.jsonl` 消费者之一**（`:55`，最相关的那一处）。
⇒ B16 改名后**无法判定它是否被破坏** ⇒ **实际验证覆盖率是 2/3，不是 3/3**。

#### 诊断（2026-09-18，**先分组、后定性**——因为审查正确地指出"2 vs 3 可能只是跨 period"）

**① 不是跨 period。** 夹具是**遗留文件**（15 行，`period_id`/`job_id` 全缺），3 条 usage **同在一个文件内**：

| seq | prompt | completion | cached_tokens |
|---|---|---|---|
| 3 | 1511 | 50 | **`null`** |
| 11 | 2996 | 82 | 256 |
| 12 | 3032 | 117 | 256 |
| 和 | **7539** | **249** | |

组装层报 `6028 / 199` = 2996+3032 / 82+117 ⇒ **恰好丢掉 `seq=3`**。

**② 丢掉的是"事件被拒收"，不是"计量算错"。** 组装层自带拒收账本，直接问它：

```
tape.rejections() → {"counts":{"invalid:assistant/usage":1},
                     "sample":[{"type":"assistant/usage","reason":"invalid","seq":3}],"total":1}
```
⇒ `assembly.js:168` `if (!EF.isValidEvent(e)) { refuse(e,'invalid'); return false; }`

**③ 根因：`cached_tokens` 为 `null`，而校验器只允许 `number`。**

```
adapters/usage.rs:36   pub cached_tokens: Option<u64>,        ← None 是设计内状态（首轮无缓存命中）
run_cycle.rs:596       "cached_tokens": usage.cached_tokens,  ← None ⇒ JSON null
event_family.js:262      cached_tokens: ['number'],           ← 校验器不接受 null
```
**⇒ 每轮第一条 usage 被整条丢弃。** 而 `prove_track.node.js:145-153` **有 `cached == null` 分支** ⇒ **下游本来就懂 null，是校验器比消费者更严。**

**④ 对照：不是 B16 引入的。** 用 `HEAD~1` 的旧资产（git worktree）跑同一夹具 ⇒ **完全相同**：
```
【旧资产】merged:15  节点:14  metering:2   拒收: invalid:assistant/usage @ seq=3
```
⇒ **改动前即如此**；且 `event_family.js` 不在 B16 的 diff 里。

#### 为什么它比"少一次调用"更贵

> **每轮的第一条 usage 必然被丢**（首轮无缓存命中是常态）⇒ **计量系统性地低报，且低报的量随"首轮频率"变化。**
> 而 `pt_replay` 是**唯一盯着这件事的测试**，它长期红着 ⇒ **没人看见**。

**⇒ 与 K-013 同族**（33 条向量无人加载）：**"测试存在" ≠ "测试在跑且在判"。**

**⇒ 修法（未落，待确认语义）**：`cached_tokens` 是 `Option`，`null` 表达"无缓存读数" ⇒ **应在可选字段的类型表里接受 `null`**（若 `event_family.js` 的可选字段一律接受 `null`，则该改动是**一处、且对 11 个事件类型一致**）。
**⚠️ 不接受"改写入端不写该字段"作为默认修法**——那会把"明确的无值"降级为"字段不存在"，正是 K-013 那一族（声明与事实分叉）。

**归属**：Cellrix（事件族校验器 ↔ 计量聚合）。
**现状**：**🔴 未修**（**改动前即红**，非 B16 引入）。**动作位 = B19。**

---

## 14. 操作知识（2026-09-18 新增，踩过才知道）

1. **"先实测"不等于"必须打真上游"。** 为验证 B15/B16 我起过 Tuck（`config.toml` 配了**真实上游 key**）+ Anaphase，**真打了一次 DeepSeek**。
   **实测副作用（已核）**：Mind 未运行 ⇒ `mind degraded at resolve … fallback Noop`、`0 memory nodes` ⇒ **无 L2/L3 写入**；但**写了三处持久化**：
   `.helix/events/run-8bba24c5ee368a4a-p006aac4877000000.events.jsonl`、`Tuck/gateway-audit.jsonl`、`anaphase-helix/session_notes.json`。
   **⇒ 下一次验证"同名输入两轮"时，这三处会带上本次的痕迹。**
   **⇒ 规则**：验证身份/命名类改动时，**优先用临时目录 + 单测**；必须打真栈时，先记下你将要留下的痕迹。

2. **`cargo build` 不会替换正在运行的二进制**：进程在跑 ⇒ 编译产出被丢弃（表现为 cargo 秒级"完成"）。
   **⇒ 判断方法**：看进程启动时间 vs 二进制 mtime（本次实测：进程 `04:04:35` > 二进制 `04:04:16` ⇒ 跑的是新的）。
   **⇒ `up --restart` 前先确认进程已停。**

3. **面板资产是 `include_str!` 烧进二进制的** ⇒ 改 `web/assets/*` 后必须**重编 + 重启**；**验证要 `curl 主页` 再 grep，取 `/assets/...` 会得到假阴性**（本次实测：主页含 `byId[list[i].period_id]`）。

4. **`session_notes.json` 是运行时产物但被 git 跟踪** ⇒ **每次运行都显示为脏**，长期被误当成"人在飞的文件"。
   **⇒ 处置**：加入 `.gitignore` 并 `git rm --cached`（见本轮提交）。**它是纪元脱水简报，不是源码。**

---

## 15. 第九组 K（2026-09-18）｜**B18 第一个必查事实：载波与取用路径**

> 审查指出「"载波走带外"未核 Tuck 能否从 metadata 取到并写入审计链（现有 trace_id 从哪来，是 B18 第一个要确认的事实）」。
> **已核，结论是 B18 比预期小得多，且"不进帧"这条天然满足。**

### K-030 ｜**trace header 有两个名字，只有一个是活的；另一个是死常量**（🟠 已定位未修）

**链路（逐行）**：

```
Anaphase 发:  anaphase-helix/src/adapters/http_reasoning.rs:113
              req = req.header("x-tuck-trace", trace_id)      ← 活的
Tuck 收:      Tuck/crates/tuck-gateway/src/gov.rs:76-81
              headers.get("x-tuck-trace")…unwrap_or("local")   ← 活的（缺省 "local"）
Tuck 写:      Tuck/crates/tuck-gateway/src/ledger.rs:30-37
              record(&p, kind, trace_id, payload) → {"trace_id": trace_id, …}
死常量:       Tuck/crates/tuck-core/src/proxy.rs:49
              pub const TRACE_HEADER: &str = "x-tuck-trace-id";  ← 全仓零引用，且名字多 "-id"
```

**⇒ 两条结论**：
1. **载波是 HTTP header，完全在帧与 PFP 之外** ⇒ **B18 的"载波不得进帧/不得进 PFP"这一条天然满足**，无需新增机制，只需**不改变现状**。
2. **`TRACE_HEADER` 是死常量且名字与实况不符**：一个未来的实现者若照它写，**会送一个没人读的头**，而审计里只会得到 `"local"`。
   ⇒ **与 K-008 同族**（注释/常量自称的事实与实现不符）。

**⇒ 对 B18 的直接含义**：B18 的形状是**极小的**——只需在既有 header 旁**多送一个 `period_id`**，`gov.rs:76` 旁多取一次，`ledger.record` 多一个字段。
**⇒ 且"换键前先想清楚旧键上挂着什么"因此有具体答案**：旧键是 header `x-tuck-trace` 的值（= `job_id`），挂在 **1255 条**审计记录上；**新增字段不会破坏它们，改字段名会。**

**归属**：Tuck ↔ Anaphase（跨来源标识）。
**现状**：**🟠 已定位未修**。**动作位 = B18。**

---

## 16. 第十组 K（2026-09-18）

### K-031 ｜**`allocate_period_id` 对非 16-hex 入参静默产出错误 id**（🔴 未修）

**坑**：`anaphase-helix/src/session_events.rs:56-59`

```rust
let raw = job_id.strip_prefix("run-").unwrap_or(job_id);
let digest: String = raw.chars().filter(|c| c.is_ascii_hexdigit()).collect();
let digest = &digest[..digest.len().min(16)];
format!("run-{digest}-p{unix_secs:010x}{n:06x}")
```

**两处不安全**：
1. **`[..min(16)]` 是字节切片** ⇒ 非 ASCII 字符边界会 **panic**（如 `日本語`）；
2. **过滤非 hex 字符是静默的** ⇒ `"run-abcd&"` ⇒ 摘要变 `abcd`，**与 `run-abcd` 撞同一个前缀**；全非 hex 时得到 **`run--p…`**（空摘要）。

**⇒ 后果**：**`period_id` 以 `job_id` 为前缀**这条性质，**只在入参是 16 位 hex 时成立**。
生产路径恒为 `derive_job_id` 的 16 位 hex（`contract/mod.rs:396`）⇒ **当前不触发**；但**该函数的契约比它的实现窄**。

**归属**：Anaphase 内部。
**现状**：**🔴 未修**（当前不可达，但它是 B18/B20 前置性质的**唯一**依据 —— 不能靠"碰巧不触发"）。**动作位：加断言或改签名**（如收 `[u8;8]` 摘要而非 `&str`）。

### K-032 ｜**`unwrap_or("local")` 让"取不到关联键"变成合法值 ⇒ B18 可能静默失败**（🔴 未修）

**坑**：`Tuck/crates/tuck-gateway/src/gov.rs:76-81`

```rust
let trace_id = headers.get("x-tuck-trace")
    .and_then(|v| v.to_str().ok())
    .unwrap_or("local")          ← 取不到 ⇒ 静默 fallback
```

**⇒ 危险不在"值不对"，在"**审计仍在产生、条数仍在涨，而它已经失去区分能力**"。**
**且 `"local"` 是一个合法的、有意的值**（本地调用）⇒ **它无法与"header 缺失/不匹配"区分**。
（`proxy.rs:49` 的 `x-tuck-trace-id` 死常量正是能让它静默 fallback 的那种错误：名字不匹配 ⇒ 不报错 ⇒ 退化成 `"local"`。）

**⇒ 这是同一个形状第四次出现**：

| # | 位置 | 形态 |
|---|---|---|
| 1 | `run_cycle.rs` K-004 | `.or()` 拿人类摘要顶机器 id |
| 2 | `main.rs` K-027 | 所有 `Err` ⇒ `missing: true` |
| 3 | `main.rs:1206` | `unwrap_or(placeholder)` 当执行命令 |
| 4 | `gov.rs:76` | **`unwrap_or("local")` 当关联键** |

**⇒ 共同形状：默认值把"缺失"伪装成"一个合法的取值"。**

**归属**：Tuck ↔ Anaphase。
**现状**：**🔴 未修**。**动作位 = B18 判据 ⑤**（见下）。

---

## 16.1 第十一组 K（2026-09-18，来自拆分前读源码）

### K-033 ｜**同一个安全语义，同一文件里两个相反的方向 —— 安全方向由"是否配了 pipeline"决定**（🔴 未修）

**坑**：`anaphase-helix/src/run_cycle/mod.rs`，**同一个「safety audit / HITL 出错怎么办」有五个分支，方向不一致**：

| # | 位置 | 出错时 | 方向 | 日志自陈 |
|---|---|---|---|---|
| 1 | `ReflexCheck` soft_reflex `Err`（1240–1243） | `ReflexPassed` | **放行** | `"default allow"` |
| 2 | `Execution` legacy `safety.audit` `Err`（1300–1303） | `Success` | **放行** | `"fallback allow"` |
| 3 | `Execution` legacy HITL `Err`（1310–1313） | `Failure` | **拦** | `"fail-closed"` |
| 4 | `execute_structured` HITL（`_ =>`, 1620–1625） | `Failure` | **拦** | `"HITL blocked"` |
| 5 | `execute_structured` safety.audit（`_ =>`, 1627–1632） | `Failure` | **拦** | `"Safety audit blocked"` |

**⇒ 核心事实（可逐字复核）**：**同一个「safety audit 失败该怎么走」在本文件里有相反的答案** ——
legacy 路径（#2）**放行**，structured 路径（#5）**拦**。

**⇒ 而路由这两个答案的是**（1251）：
```rust
if self.pipeline.is_some() && !self.context.calls.is_empty() { … execute_structured() }
```
**⇒ 即：安全方向取决于「pipeline 有没有配上」，而不是取决于任何安全判断。**

**⇒ 三个次生事实**：
1. **`safety.audit` 抛异常 = 动作被放行**（#2）。这就是"安全子系统只有形状没有实现"的行号级证据。
2. **只有 fail-closed 的那几处日志说了自己 fail-closed**（#3）⇒ 日志本身泄漏了不一致；**方向性的词只出现在一侧**。
3. **#1 与 #2 是同一族的两次**：`Err` 分支同样写作 `warn!(...)` ⇒ 响度相同、**方向相反** ⇒ 单看日志无法判方向（P13）。

**⚠️ 本轮只观测，不修**（拆分规则：该段只移动 + 只加观测）。
**⇒ 归属**：Anaphase。

**⇒ 动作位一（原）：H1 / H5 的实测输入**（见 `审查链存档/索引.md` §4），
修法取决于"反射/审计是求生机制还是安全闸门"的裁定 —— **不得先修后裁**。

**⇒ 动作位二（2026-09-18 追加，**B17 的第一个真实判据**）**：
本坑不是"某一条路径写错了"，而是**整个系统的安全语义取决于一个配置项有没有被填写**：

`config.rs:46` 声明 `pub tuck_endpoint: Option<String>`，`config.rs:310` 的默认值是 **`None`**。
而 legacy / structured 两个路径的相反答案，正是由"pipeline 有没有配上"路由的。

**⇒ 与另两条同族，合起来是三层**：

| 层 | 坑 | 形态 |
|---|---|---|
| 1 | **B14′** | `security_gate` 缺席 与 构造失败 **折叠成同一个 `None`** ⇒ 缺配置与坏配置不可分 |
| 2 | **K-025** | 两条路径对同一情况给出**相反答案** |
| 3 | **本坑 K-033** | 哪条路径生效，**取决于配置项是否被填写** |

**⇒ 共同结论**：**安全等级是一个"未声明的默认值"** —— 它由"有没有配"决定，而缺省为空。
这正是 **B17（治理层可自检）** 的判据：
> 三条前提（Anaphase endpoint / `tuck_endpoint` / Tuck `audit_path`）任一不满足时，
> **必须显式处于「未治理」状态且可查询**；**闸门须测能力而非端口**。

**⇒ K-033 就是该判据缺位时的具体后果**：配置缺失不是报"未治理"，
而是**静默换了一条更松的安全路径**。
**⇒ 故 K-033 挂 B17，不挂空。** 修法（统一方向）仍等 H1/H5；**但"缺配置必须可查询"不依赖任何裁定**，可先做。

**⚠️ 数字更正（在案）**：本坑初稿引用为 `config.rs:304`。**实测为 `config.rs:46`（字段声明）与
`config.rs:310`（默认值 `None`）**；`396` 另有一处 `None`。原数字不重写，在此更正。

---

### K-034 ｜**本地提交 ≠ 同步：78 个提交从未推送，而人类的审查入口是 GitHub**（🔴 未修）

**坑**：五仓在 2026-09-18 的实测：

| 仓 | upstream | **ahead** |
|---|---|---|
| `commonintents/.github` | `origin/main` | **37** |
| `anaphase-helix` | `origin/rs` | **34** |
| `commonintents/BIND-19` | `origin/v2.0-alpha` | **6** |
| `Cellrix` | `origin/rs` | **1** |
| `Tuck` | `origin/rs` | **0** |
| | | **合计 78** |

**⇒ 人类问的是「我在 GitHub 没有看到有更新？提交和同步了？」** —— 而答案曾是"**否**"。

**⇒ 形状不是"忘了敲一条命令"，是**：
**「提交」与「人类可见」被当成了同一件事，而它们之间隔着一个 `push`。**
⇒ 与 K-032 同族：**把"缺失"（未推送）伪装成"一个合法状态"（已提交）**。

**⇒ 判据/动作位**：每轮收尾的完成条件是 **`git fetch --all` 之后 `ahead` 与 `behind` 皆为 0**
（见 2026-09-18 修订）。
**⇒ 禁止强推**：BIND-19 那次远端有你 9-06 的提交、推送被拒，处理是 **fetch → 核对文件重叠 → rebase → 推送**，
**没有强推、没有丢提交**。这个处理是正确样板；**强推会静默覆盖人类的分支，一律禁止**。
**⇒ 归属**：全仓。**现状**：**🟡 已修一次，判据待生效**。

**⚠️ 2026-09-18 修订（原判据有洞）**：`git status -sb` 的 `ahead` 是**对本地缓存的远端引用**算的。
**未 `fetch` 就查 `ahead=0`，可能只是在跟过期缓存比对** —— 那正是本坑的同一形状：
**用一个不能揭示失败的判据宣布成功**（与 K-032、P16 同族）。

**⇒ 修订后的判据（三段，缺一不可）**：
```bash
git fetch --all                       # ① 先让缓存追平远端
git rev-list --left-right --count @{u}...HEAD   # ② ahead 与 behind 皆为 0
git log @{u}..                        # ③ 必须为空
```
**⇒ 并且必须写明范围**：判据只覆盖**本轮实际涉及的仓**，并**逐一列出仓名**；
不写范围就会像「仓数口径」那样漂移（该口径已漂移过 4 次）。

---

### K-035 ｜**模型声明的 impasse 被状态机抹掉 ⇒ 僵局被报成「完成且成功」**（🔴 未修，已定位）

**坑**：`anaphase-helix/src/run_cycle/mod.rs`

**机制（逐字可复核）**：`outcome.impasse` 在**周期结束时按当时的 `condition` 重算**：

```rust
outcome.impasse = undefined_transition || condition == TransitionCondition::Impass;
```

而 `condition` 是**最后一步**产出的条件。要走到 `Perception`（周期结束）**只有一条边**：
`(Reflection, Success) → Perception`。
**⇒ 于是周期结束时 `condition` 恒为 `Success`，`undefined_transition` 恒为 `false`。**

**⇒ 两个后果**：
1. **`impasse` 事实上变成了 `!done` 的同义词** —— 只在「没有转移规则」时为真，
   而它的文档注释写的是「finished via an impasse condition」。
2. **模型回 `{"impasse": true}` 时**：`Reasoning` 返回 `Impass` → 走 `(Reasoning, Impass) → Reflection`
   → `Reflection` 返回 `Success` → 进 `Perception`。此时判 `done = true`，
   `success` 由 `last_verdict`（无工具 ⇒ `condition == Success`）判为 **true**，
   `impasse` 判为 **false**。
   **⇒ 一个「我卡住了」被报成「完成且成功」。**

**⇒ 这是主循环里「把失败伪装成成功」的第三个实例**：

| # | 位置 | 形态 |
|---|---|---|
| 1 | 未定义转移 fallback（已修） | 无规则 ⇒ 报 `done` |
| 2 | `done=false` 与 `success=true` 并存（已修） | 没走通 ⇒ 报成功 |
| 3 | **本坑：声明僵局被 `Success` 覆盖** | **卡住 ⇒ 报完成且成功** |

**⇒ 为什么既有测试抓不到**：
- `an_impasse_goes_to_reflection` 只断言**转移表的边**，其消息却声称"the outcome is marked" ——
  **该主张从未被测试**；
- `an_incomplete_period_is_bounded_and_distinguishable` 确实断言了 `outcome.impasse`，
  但它**走的是未定义转移那条路**（删掉一条边），所以 `impasse` 由 `undefined_transition` 置真，
  **两条路它分不开** ⇒ 修复前后都绿。

**⇒ 严重性**：**B22（僵局的通知不得依赖面板）建在一个可被静默清除的 flag 上。**
通知一条永远不置真的 `impasse`，不是通知。

**⇒ 动作位**：**B22 的前置**。
**⇒ 已有可复现证据**：一个返回 `{"impasse":true}` 的 `ReasoningAdapter`，
断言 `outcome.impasse` —— **修复前红、修复后绿**（变异注入已验证：把表达式改回
`condition == Impass`，该测试立刻变红）。
**⇒ 现状**：**🔴 未修**（本轮只定位与记账；修复待与 B22 一并落，见下）。

---

### 2026-09-18 追加：第三类额度 `fix_window`（**修复不是债**）

**问题**：K-035 与 B22 的实现让 `src/run_cycle/verdict.rs` 新增 102 NCLOC、
`src/run_cycle/mod.rs` +13、`src/main.rs` +4，ratchet 因此变红。
而 CI-6 的 ratchet 是 `min(old, current)`，**不可能上调基准** ⇒ 合法增长只有两个出口：
`[[waiver]]`（人签、owner+due）与 `[[split_window]]`（有上限、指向 ADR）。
**两个都不对**：

| 出口 | 语义 | 承诺 |
|---|---|---|
| `[[waiver]]` | **债** ——「我会还」 | owner + due |
| `[[split_window]]` | **重构额度** ——「结构变了」 | cap + ADR |
| **`[[fix_window]]`** | **修复** ——「它本来就该存在」 | **cap + K-ID** |

**⇒ 把修复记成 waiver，等于宣布「修复是债务」**：它会到期、会被催还，
**而修复是资产增加，不是欠款**。
**⇒ 反过来，修复也不该并入拆分额度**：ADR-0042 里那个「估 60 / 实 X」
是留给「**为什么要拆 2093 行**」的唯一答案；把修复行数并进去，**这个数字永远拿不到**。

**⇒ 三条语义（与另两类对称）**：
1. **不带 `due`** —— 修复不会在某个日期自行消失；**带 `due` 的 fix_window 是错误，不是疏忽**
   （一条带期限的修复就是贴错标签的债）。检查器**直接报错拒绝**。
2. **合法性来自 `k_id`** —— 「变更须引坑 ID」这条规则早已有，这里只是用到行数上。
   **没有 `k_id` 的条目就是伪装的 waiver**，同样报错拒绝。
3. **修复可放开单文件**（与 `split_window` 相反）—— 拆分是搬行、不往单个文件里加行；
   **修复恰恰是往某个文件里加行**，而且它说得出是哪一个。
   **且额度只绑在指名目标上**，不像 `split_window` 那样摊给所有目录。

**⇒ 实测基准（本轮）**：

| 目标 | 前 → 后 | cap | k_id |
|---|---|---|---|
| `src/run_cycle/`（目录） | 1156 → 1275 | 119 | K-035 / B22 |
| `src/run_cycle/mod.rs` | 1142 → 1155 | 13 | K-035 / B22 |
| `src/main.rs` | 936 → 940 | 4 | B22 |

**⇒ 与拆分额度分开记**：`[[split_window]] src/ allowance=60` **原样不动**，
它留给 ADR-0042 的拆分，`估 60 / 实 X` 的比较仍然可做。

**⚠️ 同批留下的一个未决项（本轮审查提出，尚未实现）**：
`verdict.rs` 作为**新文件**按 D6 在出生时播种 ⇒ 单文件放行，
**但它仍计入目录总额** ⇒ **吃掉 102/119 的目录额度**。
**⇒ 新建文件恰恰是「新增观测」的正常手段** ⇒ 新文件越多，目录额度越不够。
**⇒ 候选修法（与 D6 对称）**：新**目录**也在出生时播种；或新文件的出生值不计入目录基线。
**⇒ 状态**：**未实现**，登记待裁定（它会改变「目录总额不可被拆文件规避」这条论证的边界）。

---

## 17.2 第十三组（2026-09-18，审查第 30 轮）

### K-038 ｜**运行时的「0」比 grep 的「0」更危险 —— 它没有磁盘状态可对照**（🟠 已加阳性对照）

**事实**：`safety_gate_tests.rs` 断言「低风险不咨询 HITL」时用了一个计数器，断言 `== 0`。
**而计数器若被接到一处永不执行的位置，它永远报 0，测试永远绿** ——
**「HITL 确实没被调用」与「计数器没在跑」不可分。**

**⇒ 与更早两次同族，且更重**：

| # | 实例 | 该次处置 |
|---|---|---|
| 1 | **P7** 定规矩却无对照 | 补对照 |
| 2 | **C1** grep 零命中，但 grep 错了路径 | 补 `with_tool_command` 正对照 |
| 3 | **本坑**：运行时计数器报 0 | **本次补** |

**⇒ 差别**：grep 的零命中**至少反映当前磁盘状态**；**运行时的 0 是路径给的** ——
路径一变它静默归零，**没有任何东西会响**。

**⇒ 补的两条（已实现并变异验证）**：

| | 内容 | 期望 |
|---|---|---|
| **PC-1** | 让 HITL **真被调用** | 计数器**必须 > 0** |
| **PC-2** | 把 PC-1 的断言跑在**一个故意死掉的计数器**上 | PC-1 **必须红** |

**⇒ PC-2 才是关键**：它证明 PC-1 在查计数器，不是在查别的东西。
**⇒ 且 PC-2 自身也必须可红**：把那个"死"计数器改成活的 ⇒ PC-2 **确实红**（已验）
—— 否则 `catch_unwind` 只是在自证。

### K-039 ｜**「零消费者」这个结论本身需要正对照**（🟡 已核）

**事实**：第 29 轮我声称改了日志文案、并说"没有测试钉住旧串"。
**那次只 grep 了测试，没有枚举真消费者**（面板 / 脚本 / 文档）——**这是同一族的省事**。

**⇒ 本轮补做（全工作区，含面板与文档）**：

| 串 | 命中 |
|---|---|
| `HITL blocked tool` / `Safety audit blocked tool` / `fallback allow` / `Safety audit rejected` / `high-risk action blocked` | **0** |
| **正对照**：`[SafetyGate]`（新前缀） | **5** ✅ |

**⇒ 正对照通过 ⇒ 该零命中为真**，不是"grep 又错了地方"。
**⇒ 结论**：旧文案无消费者，改动无需同步。

### 数字更正（在案）：参数化计入 D8「修复引起」

安全闸的方向被参数化（`OnAuditError`），**参数化是新增抽象，不是纯移动**。
⇒ 已按 **D8 的"修复引起"** 记账，不计入拆分成本。
⇒ 并补了**"参数值 = 移动前现状"的断言**（扫描两个调用点，顺序与取值一起钉住），
变异验证：交换两处方向 ⇒ 该断言红。
⇒ 同时断言 **`OnAuditError` 不得长出 `Default`** —— 一个有默认值的方向，是没人选过的方向。

### CI-7 的设计改为**结构化标签**（采纳 P15，不再解析消息）

**原方案**（解析测试消息文字声称了什么）**不可行**：把自然语言变成可解析的东西是难的那一半，
而人读正是 K-036 失效的那一环。

**⇒ 改为**：测试文件顶部写**结构化标签**，如
```rust
//! guards: impasse-survives-to-outcome
```
**⇒ 判据（P15：标签是索引，判据是闸门）**：CI-7 对每条标签**要求存在一个能使其变红的变异**；
**没有对应变异的标签 = 装饰**，报红。
**⇒ 状态**：**设计已定，未实现。**

### B17 落地记录（本轮实现）

**缺陷本体（四行）**：
```rust
None => json!({ "ok": true, "detail": "not configured" })   // check_endpoint
None => Ok(())                                              // gate_ok
```
**⇒ 缺失的前提与满足的前提给出同一个判决。** 与本账其余各条同族。

**⚠️ 一处过度归因的更正（在案）**：第 29 轮把 `gate_ok` 的 `None => Ok(())` 读成
"与 `unwrap_or("local")` 同类"。**实测不符**：该 pass-through 是**有理由的既定策略**
（"按需驱动: nothing to gate"），**且有测试钉住**。真正的缺口是
**没有任何东西告诉运维"你们在未治理状态下运行"**。
⇒ 故 `gate_ok` **未改**（未治理是否应拒绝启动是策略问题），**补的是可见性**。

**⇒ 三态而非两态**（B17 三条前提中，**只有 `tuck_endpoint` 在本 crate 有配置源**）：
| 状态 | 含义 |
|---|---|
| `ungoverned` | **已知**前提未满足（未配置 / 不可达） |
| `indeterminate` | 已知的都满足，但**有未知的** ⇒ **不能自称已治理** |
| `governed` | 全部已知且满足 |

**⇒ 中间态是关键**：折进 `governed` 是over-claim，折进 `ungoverned` 是对"缺配置源"喊狼。
**⇒ `governed` 在本构建中不可达，这就是诚实答案。**

**⇒ 两条可见通道**：`/v1/health` 的 `governance` 键（**与 `ok` 分开**：
未配置的引擎在旧义上"健康"、在新义上"未治理"，两个都要能同时读到）+ 启动告警。

**⇒ 为什么单独成模块**：比例上限先于我发现了答案 ——
把 ~58 行追加到 198 行的 `health.rs` 是 **60% 增长**，**远超修复允许的十分之一**。
**⇒ 该规则是对的：这是功能，不是修复。功能给一个文件。**

**⇒ 顺序：先量后写**（`src/governance.rs` 56 行出生播种；`health.rs` +1、`lib.rs` +3、
`main.rs` 4→7 并把 B17 加进引用；另加一条 `src/` 的 +40，**使 B17 的新文件不吃掉
ADR-0042 的拆分额度**，"估 60 / 实 X" 的比较仍然可做）。
**⇒ 且我自己的引用规则先拒了第一版**：`B17` 不在 `pits.toml` 里 ——
**未记录的坑就是伪装的 waiver**。

### 待办（第 30 轮裁定顺序的剩余项）

| # | 项 | 状态 |
|---|---|---|
| 6 | `perception` / `memory_retrieval` / `reasoning` / `reflection` 拆分 | ⬜ 未做 |
| 7 | `reflex_check` | ⬜ 等 H5 |
| 8 | ADR 收尾（段数 / 缺陷数 / 修复行数） | ⬜ 未做 |
| — | **CI-7 结构化标签** | ⬜ 设计已定，未实现 |
| — | 其余测试是否存在 K-036 同类"消息撒谎" | ⬜ 未排查 |


## 17.3 第十四组（2026-09-18，审查第 31 轮）

### K-040 ｜**不可达的枚举值＝下一条死代码 ⇒ "不可达"本身必须被钉住**（🟡 已钉，未普遍化）

**事实**：B17 的三态里，**`governed` 在本构建不可达**（三条前提中两条无配置源）。
我上一轮把它当作"诚实答案"就停下了 —— **但诚实之后少做了一件事**。

**⇒ 一个永不出现的枚举值，会让任何以它为条件的代码成为死代码**：
下一个人看到三态，会写 `match { Governed => ..., _ => warn }`，
**那个 arm 永远不执行，且没有任何东西会响。**

**⇒ 这是转移表的第二次**：那里声明了 13 条边，**分母是"可达的"**；
这里声明了 3 个状态，**可达的只有 2 个**。同一种"声明 ≠ 可达"。

**⇒ 已做**（`governance_tests.rs`）：
- `known_preconditions()` / `unknown_preconditions()` **成为函数**（契约事实，不再是 `status` 内部的字面量）；
- 断言 `known.len() + unknown.len() == PRECONDITIONS`；
- **断言 `!unknown.is_empty()`**，失败消息**写明该怎么做**：
  "某个前提拿到了配置源 ⇒ `governed` 可能已可达 ⇒ 回来重新推演三态，
  检查是否有人处理 `governed`，若已可达，那段不再是死代码、需要测试与变异"；
- **再加行为断言**：已知前提全部满足时，`state` 仍**不得**为 `governed`
  （列表断言与行为断言必须同时成立，否则两者在互相掩护）。

**⇒ 变异验证**：把 `unknown_preconditions()` 清空（模拟"某前提拿到配置源"）
⇒ 该断言**红**（另带 3 条同族测试一起红）。
**⇒ 状态**：**已钉住这一处**；**未普遍化**（其余"声明了但不可达"的地方未排查）。

### CI-7 落地：**标签是索引，判据是闸门**（P15）

**设计问题的答案（本轮审查提出）**：标签的变异是人工写还是自动派生？
- **人工写 ⇒ CI-7 只验证"标签存在且有人写了变异"⇒ 又是可达性而非能力**（CI-4 那一族第五次）
- **自动派生 ⇒ 检查器自动改坏符号 ⇒ 断言必须红**，这才是 M9 的自动化

**⇒ 采用的第一版（介于两者之间，且是真正有牙的那一半）**：
**变异作为数据声明（`ci/guards.toml` 的 `from`/`to` 字面替换），由检查器 _执行_ 它**：
1. 应用替换 ⇒ 跑该测试 ⇒ **必须失败**；
2. 还原 ⇒ 再跑 ⇒ **必须通过**（否则"本就红的测试"会被误当成真守卫）。

**⇒ 为什么这算能力而非可达性**：文件里写"有人给它写了变异"只是存在性；
**执行它并要求红，才是能力**。

**⇒ 三条防自证的规则**（都已实现）：
1. `from` 在文件中**必须恰好出现一次**（否则变异有歧义）；
2. `from` **不得只落在注释上**（改不动行为的替换，证明不了什么）；
3. 还原后测试必须**通过**。

**⇒ CI-7 自身的正对照（已验）**：
- 把某个 guard 的 `to` 改成与 `from` 同义（空操作）⇒ **CI-7 报"decoration"并退 1**；
- 标签指向不存在的条目 ⇒ **CI-7 退 1**（"a claim nobody can act on"）。

**⇒ 首轮运行即抓到一个真缺陷（与 K-036 同族）**：
`check_guards.py` 的读取器是**手写的**（本机 Python 3.9，`tomllib` 要 3.11），
**忘了解析转义** ⇒ 三个 `from` 带着字面的反斜杠-n，**匹配 0 次**。
**⇒ 抓到它的是"必须恰好出现一次"这条规则 —— 它为别的理由而存在，却先救了自己。**

**⇒ `[ENG] 已知简化`（写入文件头与本账）**：替换由条目作者选，
所以它证明的是"**这个**改动会让**这个**测试变红"，**不是"任何破坏该不变量的改动都会"**。
**⇒ 升级触发条件**：当某条的 `from` 只与不变量名义相关，或同文件两条无法独立应用时，
**改为符号级变异**（解析该项、破坏其函数体、重跑）。

**⇒ 首批 6 条 guard**：`impasse-survives`(K-035) · `gate-directions-differ`(K-033) ·
`hitl-error-blocks`(K-033) · `governed-is-dead`(B17) · `governance-states`(B17) · `fix-cap-ratio`(B17)。

### 记账更正与单列（第 31 轮 🟠1 / 🟠2）

**🟠1（问，已核）**：B17 的 `src/` +40 **确实在 `[[fix_window]]` 里，不在 `[[split_window]]`**。
⇒ "估 60 / 实 X" **没有被第二次稀释**（第一次是差点把修复行数并进去，见 D8）。

**🟠2**：`main.rs` **每轮都在涨**，且它本身在拆分候选里。**⇒ 单列其累计涨幅**：

| 时间 | main.rs NCLOC | 本轮增量 | 引用 |
|---|---|---|---|
| 拆分前 | 936 | — | — |
| B22 | 940 | +4 | B22 |
| B17 | 943 | +3 | B17 |
| **累计** | **943** | **+7** | 上限（10%）93 |

**⇒ 它是"下一步该拆谁"的信号**：`main.rs` 943 行且**持续上涨**，
而它已在 CI-6 的 8 条 waiver 里。**⇒ 建议：`run_cycle` 四段拆完后，`main.rs` 优先于其余。**

### 下一步顺序（第 31 轮裁定）

| # | 项 | 状态 |
|---|---|---|
| 1 | `governed` 不可达 → 静态断言 | ✅ **已做**（含变异验证） |
| 2 | **CI-7：标签 + 自动派生变异；第一版标 `[ENG]`** | ✅ **已做**（6 条 guard，含自身正对照） |
| 3 | K-036 排查**限 ~40 条**（safety / reflex / execution 相关） | ⬜ 未做 |
| 4 | 剩余 4 段拆分（`perception`/`memory_retrieval`/`reasoning`/`reflection`） | ⬜ **CI-7 之后**才动 |
| 5 | `reflex_check` | ⬜ 等 H5 |
| 6 | ADR 收尾（段数 / 缺陷数 / 修复行数） | ⬜ 未做 |

**⇒ 第 3 项的范围必须写死**：只扫 safety/reflex/execution 相关（约 40 条），
**扫全仓会因噪声被放弃，那样又变成"假装做了"**。


## 17.4 第十五组（2026-09-18，审查第 32 轮）

### K-041 ｜**会改源码的门禁，不得住在读源码的测试套件里**（🔴 已修，方法有据）

**事实**：CI-7 作为 `tests/ci_guards.rs` 跑在 `cargo test` 里。
它为了证明 guard 会红，**要变异 `src/` 再重编** ——
而 `cargo test` **并行跑测试二进制**，于是在它持有变异的那段时间里，
**别的测试可能从被变异过的树重建出来**。

**⇒ 实测到了一次**：全量跑时 `run_cycle_deterministic_replay` 失败（312 passed / 1 failed），
**单独重跑 3 次全过，再跑全量 334/0**。

**⇒ 形状是本账最熟的那一种**：不是"答案错"，是"时好时坏" ——
**而间歇性失败更坏，因为它训练人"重跑直到绿"。**

**⇒ 已修**：`tests/ci_guards.rs` 改为 `#[ignore]`（不是删除 —— 理由要能从套件里被发现），
CI-7 变成**独立门禁** `python3 ci/check_guards.py`；
显式跑法 `cargo test --test ci_guards -- --ignored --test-threads=1`（实测 92s，可用）。
**默认套件恢复为 333 passed / 0 failed。**

**⇒ 升级路径（写进文件头）**：把它改成**在 `git worktree` 的副本上跑** ——
变异副本、在副本里构建与测试、不碰本树。**代价是每次一次完整编译**，
所以暂未做；而那是"必须与其它测试并行跑"时的诚实价格。

### K-042 ｜**CI-7 分不清"断言失败"与"编译失败"**（🔴 已修，且正对照是我自己造的）

**事实（我上轮亲手写下过这条纪律）**：「编译不过的变异不是证据」。
**而我这轮造的机器把编译不过当成证据** —— `returncode != 0` 即算"红"。
**⇒ 这是 CI-4 那一族的第六次：只测"有东西红了"，不测"该红的那个红了"。**

**⇒ 实测（先证明洞存在，再修）**：塞进一条 `to` 为 Python 语法错误的 guard，
**旧版 CI-7 报 `OK 7 guard(s) verified`**。

**⇒ 修法三条，缺一不可**：
1. **构建必须成功** —— `error[E…]` / `could not compile` 是**检查器错误**，不是"测试红"；
2. **必须是 _指名_ 的那个测试报 FAILED**（存在预红测试时，空操作变异也会"红"）；
3. **无变异时同一测试必须绿**（否则它本就不是 guard）。

**⇒ 修完再测**：Python 语法错误 ⇒ **退 3**；Rust 编译错误 ⇒ **退 3**
（且第一版这里**未捕获 GuardError**，以 traceback 退 1 —— 正是"1 与 3 不可混"的又一次）；
空操作 ⇒ **报 decoration 退 1**。

**⇒ 顺带发现**：我最初用"Rust 编译错误"来演示这个洞，**但演示用的却是 Python 语法错误**，
Rust-only 的正则直接放过去了 ⇒ 加了 `py_compile`。**一个例子选错语言，会让检查看起来比实际更窄。**

### CI-7 的成本闸门（审查第 32 轮 🔴三）

**问题**：首批 6 条 × 2 次全量 = 12 次；20+ 条 ⇒ 40+ 次。**越来越慢的检查会被关掉。**

**⇒ 已做两条**：
1. **基线共享** —— 同一测试目标的所有 guard 共跑一次基线，成本从 `2×guards` 降为 `targets + guards`；
2. **成本棘轮** —— `budget_seconds`（默认 240s）写进 `ci/guards.toml`；
   **超时即 FAIL 并要求分层**（`tier = "slow"`）。**不让它悄悄地一轮比一轮慢。**

**⇒ 实测**：7 条 guard，**65s / 240s**。

### 播种必须走带外（审查第 32 轮 🔴二）

**问题**：`min(old, current)` 永不上调 ⇒ **唯一能让基准上涨的路径是"键不存在然后重新播种"**。
自动写回如果自己做这件事，**就是检查器给自己上调基准** ——
第 25 轮那个洞（ratchet 能写自己要检查的值）**换了个门**。

**⇒ 已做**：新增 `[[seed]]`（**人签**，必须 `k_id` 或 `adr` + `ncloc`）。
写回**只能消费签名**：
- 无签名的键 ⇒ **拒写**并退 3（"a seed is a decision"）；
- 现值**大于**签名 ⇒ **拒写**并退 3（自动上调被拒）。

**⇒ 已把三个自动播种出来的键补签**（`verdict.rs` 102 / `safety_gate.rs` 53 / `governance.rs` 60），
并在 `baseline.toml` 里**如实注明它们是"规则之前就被自动播下的"，属于补签而非依规签署**。

**⇒ 正对照（我第一次写错、第二次才对）**：
只删 ratchet 键**不够** —— 因为**我已经给它签了名**，重新播种是被授权的，退 0 才是对的。
必须**同时**删键与删签名 ⇒ 退 3；删键 + 签名低于现值 ⇒ 退 3。**两次现已实测。**

**⇒ 且第一版检查放在写回之后、`return 0` 之下 ⇒ 永不触发的"通过"**（正对照靠什么都不做而通过）。
已改为**写回之前的 pre-pass**：非法签名**必须阻止写回**，而不是与写回并列被报告。

### 自证集修正（审查第 32 轮 🟠1）

**问题**：`unknown_preconditions()` 是自证的 ——
若有人加了第 4 个前提且没进任何列表，`known + unknown == 3` **仍然绿**
⇒ **"我们不知道自己不知道什么"无法被这个函数表达。**

**⇒ 已做**：改从**源码**核对 —— 扫 `config.rs` 的全部 `Option` 字段（实测 21 个），
每个必须落在：治理前提（known ∪ unknown）**或**测试内显式列出的 `NOT_A_PRECONDITION`。
**⇒ 新 `Option` 字段不能悄悄出现，必须被分类，而分类的那一刻就是有人决定"治理是否依赖它"。**

**⇒ 反向断言意外抓到一条真事实**：`anaphase_endpoint` **在 config.rs 里根本没有这个字段**。
⇒ 未知桶里其实是**两种不同的东西**，已记明：
| 名称 | 性质 | 修法 |
|---|---|---|
| `tuck_audit_path` | 有来源，但**在别的器官**（Tuck 的配置） | 跨器官读取 |
| `anaphase_endpoint` | **任何地方都没有来源** —— 不是"此处读不到"，是"未定义" | 字段先存在 |

反向断言只对 **known** 列表生效（对 unknown 生效等于否定其定义）。

### main.rs 需要"新功能该去哪"的判据（审查第 32 轮 🟠2）

**事实**：`main.rs` 每轮都涨（936 → 940 → 943）。**它不是巧合，它是所有新功能的默认落地点。**

**⇒ 判据（采纳，写在此处备查）**：
> **新功能的默认落点不得是 `main.rs`。** 若一个改动只有放进 `main.rs` 才成立，
> 先回答"它属于哪个已有模块"；答不出来就是**缺一个模块**，而不是 `main.rs` 该长大。

**⇒ 与拆分的关系**：**拆 `main.rs` 之前先定这条**，否则拆完还涨。

### 下一步顺序（第 32 轮裁定）

| # | 项 | 状态 |
|---|---|---|
| 1 | CI-7 补 `expect_fail`（指定测试 + 基线必绿 + 排除编译失败） | ✅ 已做 |
| 2 | CI-7 分层 / 只跑指定测试 / 成本棘轮 | ✅ 已做（基线共享 + budget） |
| 3 | 重新播种走带外 | ✅ 已做（`[[seed]]` + pre-pass） |
| 4 | **K-036 排查（限 ~40 条 safety/reflex/execution）** | ⬜ **产物现在才可信** |
| 5 | 剩余 4 段拆分 | ⬜ |


## 17.5 第十六组（2026-09-18，审查第 33 轮）

### K-043 ｜**"再跑一次就绿了"不是验证 —— 一次通过不能证伪间歇性**（🟡 已补实验，结果见下）

**事实**：我发现了一个间歇性失败，**把它命名为"最坏的一种"**，
然后**用一次全绿就把它关掉了**。

**⇒ 而"再跑一次就绿了"恰恰就是我自己描述的那个训练。**

**⇒ 本轮做裁决实验（M8/M9 第四次复用）**：
`CI-7 已移出 cargo test ⇒ 套件内不再有任何变异行为`
⇒ **若仍有间歇失败，归因必错**（那就是另一条 K）。

**⇒ 实验设计（含有效性守卫）**：连跑 10 次全量，并在首尾各记录
`git rev-parse HEAD` 与 `git status --porcelain | wc -l`。
**若首尾不一致 ⇒ 实验无效**（在移动的树上测量）。

**⚠️ 第一次实验我自己作废了**：我在**启动 10 次连跑之后继续编辑工作区**
（改 walker、加 seed、加测试），于是那两次运行（`passed=232 failed=3`）
**测的是一个移动的目标**。
**⇒ 这是同一族的又一次：一个不能揭示自身失效的实验设计。**
**⇒ 处置**：杀掉、先把改动全部落盘并提交（`df537a4`），**在冻结版本上重跑**，
并把"首尾一致性"写进脚本作为守卫。

#### 实验结果（冻结版 `df537a4`，10 次全量）

```
run 1..10 : passed=337 failed=0     ← 10/10 全绿
有效性：rev df537a4 -> df537a4   dirty 0 -> 0   ← 实验期间工作区未变
结论：green=10  red=0
```

**⇒ 归因成立**：CI-7 在套件内**是**那次间歇失败的原因。
**⇒ 且"一个自称确定性重放的测试出现非确定性"这条 K，不成立** ——
`run_cycle_deterministic_replay` 的确定性没有被推翻，**它当时读到的是被变异的源码**。

**⇒ 方法论收获（写进本条）**：
**"我修好了"与"它不再发生"是两件事，而后者需要 N 次样本 + 一个有效性守卫**（rev/dirty 首尾一致）。
**⇒ 一次通过只能证明"存在一个通过的世界"，不能证明"失败的世界已消失"。**

### 审计者本身不受审计（审查第 33 轮 🔴二，实测比其推断更广）

**审查推断**："CI-7 不在 ratchet 里（Python 脚本通常不进 Rust 计数）"。
**⇒ 实测成立，且波及 CI-6**：`iter_sources` 只 yield `.rs`
⇒ **`ci/` 是整个仓库唯一不被计数的目录**，
**CI-7 管着全仓每一个 guard，而它自己无预算、无 ratchet、无豁免 —— CI-6 也一样。**
**⇒ 两个审计者是仅有的两个不被审计的文件。**

**⇒ 已做**：
1. `iter_sources` 增加 `ci/*.py` ⇒ **审计者的行数第一次成为真实数字**：
   `check_line_budget.py` 757（已豁免，与其余 8 条同规格）；
   `check_guards.py` 300，然后在它"不再相信裸非零退出"的那一刻变成 318；
2. **元层级封顶写进两个文件头**：**没有 CI-8**；
   若需要第三个检查器，说明 **CI-7 造错了**，而不是梯子少一级；
3. CI-7 的三条不变量由 **`tests/ci_guards_policy.rs` 的普通测试**检查，
   **不是变异守卫** —— 守它会需要第八级。
   **它的"体量"则进了机器**（CI-6 计数 + 签名种子）。
   **⇒ 一个检查器无法用它审别人的同等严格度审自己而不陷入无穷回归**；
   假装可以，正是审查警告的那件事。

**⇒ 接入的过程本身触发了三道自己的门**（顺序正确）：
- seed 门：**"两个审计者没有签名种子"** ⇒ 逼我签名；
- fix_window：CI-7 从 300 长到 318 ⇒ **它自己报自己**；
- 300 预算：318 > 300 ⇒ 需要 waiver，**而那条 waiver 正是我在种子里写下的预言**
  （"它的下一行需要 waiver 或拆分"）。

### K-044 ｜**一条永远打印不出来的 advisory**（🟡 已修）

**事实**：over-300 的清单**印在 `ok = not blocking` 的提前 `return 0` 之后**
⇒ **只在别的东西已经红了的时候才会出现**。
⇒ 一个 **757 行、超出 300 预算一倍多**的文件，**在干净运行时被报告给谁都不是**。

**⇒ 已修**：把该清单**上提到提前返回之前**。
**⇒ 而这次上提立刻让 CI-7 自己的增长可见** —— 两件事是同一个改动的两端。

### K-045 ｜**"配置键不在任何段内"被静默丢弃**（🟡 已修为错误）

**发现方式**：我为 CI-7 写自检测试，**第一个夹具忘了写 `[[guard]]` 头**，
而**解析器把整份文件接受了**。

**⇒ 根因**：`key = value` 出现在任何段之外时被**静默丢弃** ——
**而该文件的文档字符串明说"malformed is an error, not a skip"**。
**⇒ 危害**：一个整条缺失 `[[guard]]` 头的条目会**消失**，
于是它的 guard 看起来像**"没打标签"**而不是**"写错了"** —— **丢掉的条目与通过的条目不可分。**

**⇒ 已修**：段外的 `key = value` 报错，并补了对应测试。
**⇒ 这是"自检测试先抓到自己"的第二次**（上一次是 `from` 的转义）。

### 成本闸门不得抑制守卫密度（审查第 33 轮 🔴三）

**审查论断**：超时即 FAIL ⇒ 加 guard 会撞预算 ⇒ 下一个人选择**不加 guard**。
**⇒ 成立。** 与"覆盖率红线催生无意义测试"同族。

**⇒ 已改**：超预算 ⇒ **WARN 并仍然通过**（提示分层），
**FAIL 只保留给真正错的东西**：decoration guard / 预红测试 / 编译不过的变异。
**⇒ 判据**：**守卫密度比墙钟更值钱。**

### 提交信息必须配检查（审查第 33 轮 🟠3，Beyoncé Rule）

**已做两件**：
1. **全量 grep 五仓全部提交消息**：shell 残渣 **0 处**；
   **正对照**：同一 grep 找到 `error` **19 处**、`K-041/K-042` **2 处**
   ⇒ **零命中为真**（不是"grep 又错了地方"）。
2. **机制化**：`helix-mind/tools/hooks/commit-msg` 增加 **gate 0**，
   拒绝两类残渣：shell 报错串（`command not found` / `bash: ` / `return: can only` …），
   与**孤立反斜杠**。

**⚠️ 这条规则的第一版瞄错了形状**：它只匹配**独占一行的反斜杠**，
而实际残骸是**行内的**（`CI-7 ran inside \ and mutates src/` —— 四个词被吃掉的位置）。
**⇒ 是正对照抓到的**（PC-1 反而通过）。改成"两侧为空白/行首行尾的反斜杠"，
**并回放 40 条真实历史消息验证假阳性 = 0**。

### 仍未做（本轮如实欠着）

| 项 | 状态 |
|---|---|
| **UNDEFINED 第三类**（`anaphase_endpoint` 任何地方都无字段 ⇒ 不可能"已满足"⇒ 若它影响安全路径，`governed` 的分子是错的） | ⬜ 未做 |
| **补签的一次性迁移窗口**（补签若无边界 ⇒"人签"约束被稀释） | ⬜ 未做 |
| K-036 排查（~40 条 safety/reflex/execution） | ⬜ 未做（管子已接好） |


## 17.6 第十七组（2026-09-18，审查第 34 轮）

### K-046 ｜**幽灵前提：一个用来检测"看不见"的机制，自己看不见被什么锁住**（🔴 已修）

**事实**：`anaphase_endpoint` 原在 `unknown` 桶里 ⇒ `unknown` 非空 ⇒ `governed` 不可达。
**⇒ 而这个"不可达"的理由，可能只是一个遗留命名，不是架构事实。**

**⇒ 实测**：该名字在本仓**无处定义** —— 无字段、无读者、无写者。
最近的真实存在是 `cap_http_port`：**一个带默认值的 `u16`，不可能"未满足"**。
⇒ 若 B17 指的是它，它**不是前提**；若指别的，**那个东西不存在**。
**而本仓没有任何东西能区分这两种读法。**
**⇒ 所以它不能被当作"未知的前提"报告** —— 在输出里，"未知"与"未定义"不可区分，
下一个读到 `indeterminate` 的人会以为系统在诚实地报告不确定性，
**实际它可能只是在报告一个拼写错误。**

**⇒ 四类而非三类**：
| 类 | 例 | 性质 |
|---|---|---|
| **readable** | `tuck_endpoint` | 本仓可读 |
| **unlocated** | `tuck_audit_path` | **已定义，在别的器官** |
| **undefined** | `anaphase_endpoint` | **B17 文本自身的缺陷** |
| unmet | — | 已读但未满足 |

**⇒ `PRECONDITIONS` 从 3 更正为 2** —— 把幽灵计入总数，
等于让总数描述一份**不存在的清单**，并让 `preconditions_known` 变成**虚构的分数**。

**⇒ 按审查要求重新推演三态**：`governed` **仍然不可达**，
**且现在是一个事实**（`tuck_audit_path` 定义在别的器官，本构建读不到）。
**⇒ 这才是想要的结果：同样的结论，但不再建立在一个可能的笔误上。**
⇒ 新增测试把"幽灵未解决"钉住，**有人定义或删除它的那天必须变红**。

### K-047 ｜**有效性守卫是最后一个"声明冒充机制"**（🟡 已有正对照）

**审查指出**：我连跑 10 次所依赖的有效性守卫（`rev`/`dirty`）**自己没有对照** ⇒
**"dirty 0 -> 0"只是另一句声明。**

**⇒ 已做（并已提交，不再是临时脚本）**：`ci/n_runs.sh` 增加 `--selftest-guard`：
建一个 scratch 仓库 → **故意弄脏** → **要求守卫看见**。
**⇒ 且脚本在脏树上直接拒绝启动** —— 那正是能抓住第一次无效实验的检查。

**⇒ N=10 的依据写进脚本而非口头**：观测失败率约 1/3 ⇒ 10 次全绿在"什么都没变"的假设下
概率 `(2/3)^10 ≈ 1.7%`；**3 次是 30%，不算证据**。
**⇒ 未声明依据的 N，与本账其它任何未声明的数字是同一个缺陷。**

**⇒ 机制记为并行竞态（不只是"读到变异源码"）**，并推广为通用约束：
> **任何在运行时读文件、或依赖构建产物的测试，在并行套件内都与任何写树者竞态。**

### K-048 ｜**成本 WARN 需要消费者**（🟡 已修）

**审查指出**：把 FAIL 改成 WARN 是对的，但**警告模式会精准复现 K-029（有账本没人读）**。
**⇒ 已做**：CI-7 输出 `CI7-SUMMARY {…}` 机器可读行 + 写 `target/ci7-report.json`
（`over_budget` 是显式布尔量，**不是让读者从两个数字自己推断** ——
推断一次错了就会不再信任它）。

### 术语：阳性/阴性对照的定义写死（🟠5）

**审查指出**：我用"正对照"描述**回放 40 条历史消息查假阳性** —— 那是**阴性对照**。
**⇒ 搞反的代价**：把阴性对照当阳性对照，会让一个"永远绿"的检查看起来有效。
**⇒ 已写进 `ci/n_runs.sh` 头部**：
- **阳性对照** = 构造一个**应该失败**的情形，并要求它失败。不失败 ⇒ 该检查没在检查。
- **阴性对照** = 拿**已知正确**的输入跑，要求它通过。它防的是误报。

### hook 的定位写死（🟠6）

**审查指出**：`--no-verify` 可绕过、换机器不生效、网页提交不跑。
**⇒ 已写进 `commit-msg` 文件头**：它是「**帮我别犯这个错**」，不是「**保证这个错不进门**」。
**⇒ 并特别写明**：**它守的恰恰是"提交消息"——最容易被当成证据的东西**。
若被误当成保证，就是第四例"声明冒充机制"。

### K-049 ｜**种子机制缺一条"合法上调"的路**（🟡 已补）

**问题（用出来的，不是想出来的）**：`min(old, current)` 保住了自动路径的单向性，
**但也没留下任何被认可的上调方式**；而 `fix_window` 的 10% 比例上限，
让**小文件的下限只有 5 行**。
**⇒ 一个 56 行模块的正确性修复，塞不进 5 行** ——
而假装塞得进，只会把作者推向一个**为了满足预算而更差的设计**。

**⇒ 已补**：**签名的种子可以"重新取"一个已存在的键**（带 K-ID，写在 `baseline.toml`），
**每一次重取都会按名字打印出来**。
**⇒ 保护不是"上调不可能"，而是"没有任何自动路径能上调"。**

### 🟠7 Python 与 Rust 的体量标准

**已确认**：`.py` **已计入 CI-6**（`iter_sources` 增加 `ci/*.py`，见 17.5）。
**⇒ 但 300 是按 Rust 标定的**；Python 无类型声明、无 trait impl，合理体量不同。
**⇒ 已标 `[ENG]` 的简化，写明 300 对 `.py` 是沿用而非标定，待重新标定。**

### 仍未做

| 项 | 状态 |
|---|---|
| K-036 排查（~40 条 safety/reflex/execution） | ⬜ 未做（本轮被前五项占满） |


## 17.7 第十八组（2026-09-18，审查第 35 轮）

### K-050 ｜**canary 只证明了它跑的那条路 —— 分子对了，分母错了**（🔴 已修，数字已在案更正）

**审查提问**：CI-6 的 canary 是 `ci/canary/ncloc_known.rs`，**只验证 Rust 路径**；
而 CI-7 是 Python。**CI-6 的注释识别处理 `#` 吗？**

**⇒ 实测：不处理。** 计数器只认 `//` 与 `/* */` ⇒ **Python 的 `#` 注释全被算成代码**：

| 文件 | 记在种子里的值 | 真值 | 虚高 |
|---|---|---|---|
| `ci/check_guards.py` | 343 | **311 → 316**（修完引用感知后） | 32 |
| `ci/check_line_budget.py` | 811 | **667 → 761** | 144 |

**⇒ 结论（"CI-7 超出 300 预算"）存活，数字不存活。**
**⇒ 已在案更正**（种子理由里写明原值、真值与原因）——
**一个保留了错数的追认，比错数本身更坏。**

**⇒ canary 为什么没抓到**：它证明了计数器**在它测的那条路上**是对的，
而计数走的是**另一条路**。**与 9084/3499 同族：分子对了，分母（语种适用性）错了。**

**⇒ 已做**：
1. `ncloc(text, lang)`；
2. **Python canary** `ci/canary/ncloc_known.py`（按构造已知：40 代码 + 1 行内注释行 +
   1 纯字符串行 + 3 多行 docstring = **45**；15 条 `#` 不计）。
   **同一夹具按 Rust 规则读是 65** —— 那个差就是当初的错，测试把两个数都钉住；
3. **引用感知的行判定**：第一版用 `line.split("#", 1)[0]`，
   **在有代码前缀时是对的、在"整行只有一个含 `#` 的字符串"时是错的** ——
   这正是 Rust canary 早已为 `//` 钉住的那个陷阱；
4. **跨行状态**：docstring 已声明算代码，而多行 docstring 的**续行**既无 `#` 也无三引号
   ⇒ 无状态扫描会把它们当空行丢掉。**单元用例抓到了。**

**⇒ 顺带（首次扫描 `ci/` 才暴露的假阳性）**：计数器**把自己标成了 DATA-ONLY** ——
标记串 `//! DATA-ONLY` 出现在**它自己的定义里** ⇒ 然后它因"17 条分支"判自己失败。
**⇒ 标记是 Rust 内部文档属性，Python 文件不可能携带它；且它必须位于行首。**
新增 `declares_data_only(text, lang)` 同时要求两条。
**这与"匹配不到的键"同形：一个由"提及"而非"声明"满足的检查。**
（`src/config.rs` 仍被正确识别为 DATA-ONLY，已测。）

### B17 原文已改（修订写进权威本身）

**审查论断**：K-046 说「2 条已定义 + 1 条幽灵」，**B17 原文说三条** ⇒ **两份权威、数字不同**；
而 B17 的用途正是让不可见变可见，**它的前提清单就是它自检的对象**
⇒ 清单错了而规范不改，**下一个照 B17 实现的人会把幽灵前提重新引入**。
**⇒ 这就是 K-029 的形状：账本有人记、规范没人改 ⇒ 账本永远只是账本。**

**⇒ 已做**：`CI-144_决策索引.md` 的 B17 行 —
**原文保留并划线**，紧接一段**修订段**（形态同 ADR-0041 §七），写明修订后的两条前提、
理由、可复核的证据出处，以及"为什么不只记在坑记录里"。

### K-051 ｜**不可达状态必须带 unblock，否则会被当成死代码删掉**（🟡 已修）

**审查论断**：`unlocated` 混淆两件事 ——
**已知在别处但未读（可读而未读，待办）** 与 **真的不知道在哪（盲区）**。
两者都输出 `indeterminate` ⇒ **B17 永远说"我不知道" ⇒ 等于没做。**
**⇒ 而 `governed` 不可达的理由是"跨器官读取未做"，不是架构上不可能。**

**⇒ 后果**：下一个人看到 `Governed => …` 这个永不执行的 arm，
**会当作死代码删掉 —— 而它恰恰是跨器官读取落地那天该活过来的 arm。**
**把"尚未"压成"永不"，与把"未知"压成"已满足"是同一个缺陷。**

**⇒ 已做**：`unblock_for_governed() -> Option<&'static str>`，
**不可达时有值、值必须指向一个动作**（不是复述条件），且**暴露在 `/v1/health` 的载荷里**。
测试**双向**绑定：不可达 ⇔ 字段非空。
**⇒ 规则（推广）**：**每个"不可达 / 未定位"状态必须带一个 unblock 字段指向动作位；
字段为空不得通过。**

### 仍未做（本轮如实欠着）

| # | 项 | 状态 |
|---|---|---|
| 3 | **六类额度的"什么时候用哪个"决策表 + 误用检测** | ⬜ 未做 |
| 5 | 重取次数计入 ADR 收尾（否则"估 60 / 实 X"被污染） | ⬜ 未做 |
| 6 | **K-036 扫描：先全扫登记，再统一修** | ⬜ 未做 |

**⇒ 第 3 项的形状（记下以免丢失）**：`waiver`(due) / `split_window`(cap+ADR) /
`fix_window`(cap+K-ID) / `seed` 人签 / **重取** / **迁移窗口** 六类并存
⇒ **一个被拒就换一类再试 ⇒ 绕过比遵守容易。**
**⇒ 判据**：误用必须退 3（用 `fix_window` 做重构 / 用 `waiver` 做修复），
否则第 28 轮那句"**修复不是债**"会被稀释成"**修复只要换个类别就能拿到 due**"。

**⇒ 第 6 项的形状（审查建议，采纳）**：**两阶段** ——
① **只登记不改码**，扫完 ~40 条；② **统一修法 + 一次性额度**。
否则每修一条走一遍带外流程，**成本会逼你放弃后半程**。


## 17.8 第十九组（2026-09-18，审查第 36 轮）

### K-052 ｜**签名管住了"谁改"，没管住"值对不对"**（🔴 已修，机制化）

**审查论断**：`811` 那个数是**人签进种子的** —— 而签它的时候，**计数器正在把 `#` 注释算成代码**。
**⇒ 签名机制（第 32 轮）保证的是"没有自动路径能上调"，不保证"值是对的"。**
而**种子的值唯一来源就是计数器** ⇒ **计数器错了，人签的就是错的**。

**⇒ 更重的推论**：**canary 只覆盖了 Rust，错出在另一条路上，
是"人工扫 `ci/`"才发现的。下次不会有人扫。**

**⇒ 已做**：
1. **`COUNTER_VERSION`**（当前 2，注释里列出 1→2 改了什么）；
2. **每个 `[[seed]]` 记录 `counter = N`**；不匹配 ⇒ **退 3**，理由明说
   "这个值是对**另一套规则**的正确测量"；
3. **`ratchet.gen.toml` 也记录 `counter = N`**；
4. **版本变更 ⇒ 所有旧基准失效，重新推导，并大声说明**（本轮重推 **57** 条），
   **且这一次跳过增长判定**。
   **⇒ 理由**：**一把尺子量出的界，不是另一把尺子下该守的界**；
   而静默跨尺子比较，会让**真实回归藏进度量变化里**，或让**度量变化记到下一个编辑者头上**。
5. **版本缺失（`None`）也算过期** —— 一把不说自己是谁量的尺子，不能用来约束今天的测量。**未知从严格。**

**⇒ 为什么必须大声**：本轮 `ci/` 目录 1057 → 1266 的增长里，**大部分是度量变化**（`.sh` 首次被计入）
**而非代码增长**。比例上限（10%）无从分辨二者 ⇒ **只能靠"换尺子 ⇒ 重推基准"这条规则**。

### K-053 ｜**语言集合必须封闭 —— 未知扩展名报错，不静默套用**（🔴 已修）

**审查论断**：`ncloc(text, lang)` 的 `lang` 从扩展名来 ⇒ **未知扩展名默认走哪套规则？**
默认 Rust ⇒ `.js` 的 `/* */` 被算成代码、`.toml` 全成代码。
**⇒ 与 P6 同族：未知应缺省到严，不是缺省到"挑一套"。**

**⇒ 已做**：`LANG_BY_EXT`（白名单）+ `DATA_EXT`（已声明非源码）+ **扫描 `src/`、`ci/` 下的未知扩展名 ⇒ 退 3**。

**⇒ 首跑即抓出两个真情况**：
1. **`ci/n_runs.sh`** —— **它是源码，而 walker 一直静默跳过它**（只找 `.rs` 与 `ci/*.py`）
   ⇒ 新增 `hash` 语言（`#` 注释、引用感知、docstring 跨行状态），**Python 与 shell 共用**
   ⇒ **一个 canary 覆盖两者**（被测的是计数器，不是语言）；
2. **`src/.DS_Store`** —— macOS 垃圾文件，**被 git 追踪着** ⇒ 已删除并加 `.gitignore`。

### K-054 ｜**`ci/` 需要"工具档"，不是"人签豁免"**（🔴 已修）

**审查论断**：你把"标记串出现在自己定义里"当 bug 修了，**但真问题在它后面**：
**DATA-ONLY 是 Rust 的 `//!` 属性，Python 不可能携带** ⇒ 这两个文件**无法申请 DATA-ONLY**；
而它们真值 761 / 316，**都超 300**（且 300 对 `.py` 是我自己标的"沿用非标定"）。
**⇒ 现状是两个管着全生态的文件处于"永久违规 + 靠人签 waiver 活着"。**
**⇒ 这不是自证问题，是分类缺失。豁免带 due，分类不带。**

**⇒ 已做**：新增 `[tooling]`（`target = "ci/"`，`budget = 900`，指向 **ADR-0042 D9**），
**两条一直到期的人签 waiver 被删除**，由分类取代。

**⇒ 判据（写进 baseline.toml 与 ADR）**：
> **工具档的准入判据是"体量可归因于它执行的规则数"。**
> 因其它原因长大的检查器是要拆的检查器 —— **那时重取它的种子，而不是加宽它的档。**

**⇒ 分类一落地就报出**：`ci/check_line_budget.py` **901 vs 它自己的 900**。
**⇒ 保留这个信号，没有把预算调成 902** —— 诚实的解法是拆分，
而它属于 ADR-0042 剩余工作，不该藏在一个为了通过而选的数字后面。

### 旧基准签发的额度已回溯（审查第 36 轮 🟠四）

**修正的涟漪**：811→761 使基准下降 144 ⇒ `fix_window` 的 cap（10% × 基准）同步下降
⇒ 基于旧基准签发的额度需重算；**waiver 理由里引用的是错数字 ⇒ 应重签（不是延期）**。

**⇒ 已做**：随版本变更一次性重推 57 条基准；
**两条检查器 waiver 直接删除**（由 `[tooling]` 分类取代，不是延期）；
**`ci/` 的 fix_window 在基准重取后已不覆盖任何增长 ⇒ 删除**（留着就是过期额度）；
两个检查器的 **seed 理由里写明原值、真值、原因**。

### 仍未做

| # | 项 | 状态 |
|---|---|---|
| 5 | **六类决策表 + 误用退 3 + 重取带预期终态体量** | ⬜ 未做 |
| 6 | **K-036 两阶段扫描** | ⬜ 未做 |

**⇒ 第 5 项的形状（只落地了一条，且我差点记错）**：

**⚠️ 更正（同轮）**：我先在账与提交信息里写了"已加一条误用检测：同一目标不得同时挂
`waiver` 与 `fix_window`"。**它没有落地；而实现之后发现它本身就是错的检查** ——
`waiver` 管的是 **300 行预算**、`fix_window` 管的是 **ratchet**，**两者是不同的规则**，
同时持有是**正确记账**。它命中的两条（`src/main.rs`、`src/run_cycle/mod.rs`）**都是合法的**。
**⇒ 一个在正确输入上开火的检查，只会训练人忽略它。**

**⇒ 真正可查且真正错的是：豁免了一个不需要豁免的文件** ——
`waiver` 的目标**在预算之内** ⇒ **它什么都没豁免，却读起来像"已获批的例外"，
并占着一个"这个文件是已知问题"的位置**。
既有的 stale 检查只抓"目标已不存在"，这条抓"目标还在、但已不需要豁免"。
**⇒ 对 8 条真实 waiver 无假阳性**；首跑命中的是**一个测试夹具**（100 行文件上的 waiver），
**夹具已改，而不是把检查放宽。**

**⇒ 剩余**：六类的"什么时候用哪个"决策表写成表；
**重取必须带 `expected_final`，超过即需 ADR**
（否则重取会变成"设计期的无限额度" —— `ci/check_line_budget.py` 一个工作单元内已被重取 **4** 次，
`src/governance.rs` **3** 次 ⇒ **抖动本身就是发现**）。
**剩余**：六类的"什么时候用哪个"决策表写成表；**重取必须带 `expected_final`，超过即需 ADR**
（否则重取会变成"设计期的无限额度"—— `governance.rs` 一个工作单元内已被重取 **3** 次：56→60→74→88）。


## 17.9 第二十组（2026-09-18，审查第 37 轮：改用实证与现成工具）

### K-055 ｜**拆分优先级按行数排是错的 —— 本仓 git 历史直接证伪**（🔴 已实测，优先级反转）

**审查翻转**：前三十六轮用"自洽论证"建议机制，本轮改用**实证结论**：
> **行数与缺陷密度相关性很弱**；更有预测力的是 **churn（变更频率）**与"被多少 commit 碰过"。
> 一个 1500 行但半年没人碰的文件，**比一个 300 行每周都改的文件更不需要拆**。

**⇒ 用本仓自己的历史实测 `git log --format= --name-only -200`**：

| 文件 | churn | NCLOC | 在 CI-6 的 waiver（按行数）名单上？ |
|---|---|---|---|
| `src/main.rs` | **46** | 943 | ✅ |
| `src/run_cycle.rs`（拆分前） | **35** | — | — |
| **`src/config.rs`** | **27** | — | ❌ **完全不在名单上** |
| `src/session_events.rs`（拆分前） | 20 | — | ❌ |
| `src/lib.rs` | 19 | — | ❌ |
| `src/adapters/mod.rs` | 18 | — | ❌ |
| **`src/bind.rs`** | **1** | 302 | ✅ **仅因行数在名单上** |
| **`src/rails.rs`** | **2** | 370 | ✅ **仅因行数在名单上** |

**⇒ 结论（不是论证，是数据）**：
- **`bind.rs`（302 行）与 `rails.rs`（370 行）是全仓 churn 最低的两个文件，却排在拆分名单上**；
- **`config.rs`（改过 27 次）根本不在名单上**。

**⇒ 优先级反转（与第 31 轮 🟠2 的建议相反）**：
> 第 31 轮说"`run_cycle` 四段拆完后，`main.rs` 优先于其余"。
> **churn 数据说 `main.rs` 是 churn 第一（46）且最大（943）⇒ 它该是第一个，不是最后一个。**

**⇒ 处置**：**拆分优先级改为按 churn 排，行数只作为"是否超过硬阈值"的判据，不作为排序依据。**
**⇒ 但"按 `HelixState` 拆"那个依据仍然成立**（5 个改变的理由 = 单一职责），**只是排序依据换了。**

### K-056 ｜**"编译不过的变异不算证据"我两轮前已修 —— 本条声明已过期**（🟡 已复核）

**审查声明**："你的 CI-7 把编译失败当成了有效变异。"
**⇒ 实测（本轮复核，非转述）**：**该洞已在第 32 轮修掉**。
塞一条 Rust 编译不过的变异 ⇒ **`CHECKER ERROR: the mutated tree does not COMPILE … A mutation that does not compile is not evidence`，退出码 3**。
**⇒ 3 = 拒绝，不是接受。**（`BUILD_ERROR_RE` 于 183 行；另有 `syntax_ok()` 覆盖 Python 路径。）

**⇒ 但审查的机制性论点成立且重要**：`cargo-mutants` **从第一天就把"编译不过"归为 `unviable`，不算数** ——
**这类判据不该由每个团队各自重新发现。** 我踩的坑它早就分类好了。

### 工具可用性：本轮**装不上**（实测，不是推断）

| 检查 | 结果 |
|---|---|
| `command -v cargo-mutants` | **未安装** |
| `curl https://crates.io/api/v1/crates/cargo-mutants` | **HTTP 403** |
| `.cargo/config.toml` / `vendor/` | **均无** |
| `~/.cargo/registry/cache` 内 `cargo-mutants` | **无** |

**⇒ 无网络、无镜像、无 vendor、无缓存 ⇒ 本环境无法安装 `cargo-mutants`（同样适用于 `cargo-deny` / `cargo-udeps`）。**
**⇒ 所以"最该立刻做的一件事"本轮做不了 —— 如实记账，不假装做了。**

**⇒ 一旦环境允许，命令原样记下**：
```bash
cargo install cargo-mutants
cargo mutants --all-features          # 看 missed 的数目
```
**⇒ 判据（采纳）**：`missed` 必须为 0 ⇒ 这就是"每条 guard 必须能变红"的工业等价物。

**⇒ 且有一条必须先回答的实测问题（审查自己也指出）**：
`cargo-mutants` 在 343 个测试上跑一次要多久、**会不会因并行竞态误报（K-041 那个问题）** ——
**K-041 已证明：会改源码、会重编的工具，在并行套件内会与读树者竞态。** 这是要测的，不是假设的。

### 采纳 / 部分采纳 / 已具备

| 审查建议 | 本仓现状 |
|---|---|
| **工具优先，不手写** | **采纳**。但本轮受网络阻断（见上）。**"先查有没有现成的"这一条已记为新判据** |
| **`#[expect]` 语义：抑制未被使用就该报错** | **已具备，不是待做** —— `STALE_EXEMPTION`（目标被改名/删除 ⇒ 报错）+ 本轮的 **idle-waiver**（豁免了个不需要豁免的文件 ⇒ 退 3）。**审查说"我用的是 owner+due 而不是这个"——两者我都有** |
| **六类额度砍到两类** | **部分采纳**：`split_window`/`fix_window`/`seed`/重取 **确实都是为"让合法增长能发生"而加的**，而成熟实践只有一句"**ratchet 值通过 code review 改，脚本只拒绝自动上调**" ⇒ **`[[seed]]` 人签已经够了**。**⇒ 待做：合并其余四类（第 5 项）** |
| **不要写自审框架；不该配 guard，该"别审"** | **部分采纳**。CI-7 的不变量**已经是普通测试**（`ci_guards_policy.rs`），**其体量由 CI-6 管**。**"封顶"与"别审"在现状上同义**；差别只在措辞 |
| **CI-1 用 cargo-deny + ArchUnit 式架构测试** | **采纳，但装不上**（同上）。思想可直接抄：B0 矩阵 = **一条架构测试**，不是文档 + 自写脚本 |

### 对新判据的采纳（写进工作方式）

1. **先查现成工具**，有就用，不自己造；
2. **引用实证与实践**，不用自洽论证；
3. **问"怎么做"，就给命令与工具名。**

**⇒ 本条最重的自省**：**这三十几轮里有相当一部分自审机制，是别人已经造好、
并且早已解决了我正在踩的那个坑的东西。**


## 17.10 第二十一组（2026-09-18，审查第 38 轮：离线工具与"别闭门造车"）

### 先记一条工作方式（本轮采纳且已生效）

**审查第 38 轮的自我更正**：上一轮给的工具清单**需要网络，而本环境没有**。
**⇒ 正确记账不是"我们不知道有现成的"，而是**：
> **「已评估，当前环境不可用，故自研；条件满足后迁移」** —— 这是 ADR 该记的一条。

**⇒ 并纠正我自己的一次探针错误**：我用 `cargo metadata --version` 测可用性，
**它没有那个 flag ⇒ 探针无效，而不是工具不可用**。
重新实测：**`cargo metadata` 可用**（23 条依赖）；`cargo clippy` / `cargo fmt` / `cargo tree` **均可用**。
**⇒ 同一族：一个无效的探针会把"可用"报成"不可用"。**

### 离线工具实测（本轮，非转述）

| 工具 | 状态 |
|---|---|
| `cargo metadata` | ✅ **可用**（CI-1 依赖边的内置来源） |
| `cargo clippy` / `cargo fmt` / `cargo tree` | ✅ 可用 |
| `cargo-mutants` / `cargo-deny` / `cargo-udeps` | ❌ **结构性不可用**（crates.io 403、无 `.cargo/config.toml`、无 vendor、无缓存） |

### K-057 ｜**DATA-ONLY 用分支数豁免，却从不看 churn**（🔴 已修，实证在案）

**审查论断**：`config.rs`（**churn 27**）在名单**之外**，因为它被标 DATA-ONLY（**豁免，不带 due**）；
而 **DATA-ONLY 的判据是"分支 ≤10"，与 churn 完全无关**。
**⇒ 一个被改过 27 次的文件，装着决定安全语义的默认值（K-033），却被永久豁免、无人审查。**

**⇒ 已修**：**churn > 15 ⇒ 豁免必须重新陈述**（写出"因何而热"+ 复核期限）。
**⇒ 首跑即命中 `src/config.rs`（churn 27），已具名原因**：
> 它仍是 declarations-only（7 分支），**但配置截面在持续增长**（新端点、新默认值）。
> **而那正是 K-033 的病灶所在。**

**⇒ 这不是推翻 DATA-ONLY**（7 分支就是 7 分支），**是补上它没看的那一个维度** ——
**与"400 行预算缺一个分类"是同一件事。**

### K-058 ｜**离线变异扫描器：它在找到任何东西之前，先在自己身上找到两个 bug**（🟡 已建，未跑完）

**审查论断（Beyoncé Rule）**：`cargo-mutants` 装不进 CI ⇒ **在别的机器跑一次的数第二天就过期**；
**正确做法是把手写的离线变异器做进 CI**。这个论点成立，**已建** `ci/scan_mutants.py`。

**⇒ 而它先抓到了自己的两个 bug（都是跑出来的，不是想出来的）**：
1. **它变异了测试代码** —— 文件过滤只认 `*_tests.rs`，**不认行内 `#[cfg(test)]` 模块**
   ⇒ 第一份样本把 `src/hitl.rs` 自己测试里的 `assert_eq!(deny…, false)` 改成了 `true`。
   **变异一个测试，不能证明任何测试是否注意到任何事。**
2. **它没能挺过一次 kill** —— 恢复写在 `finally` 里，**而 SIGTERM 不跑 `finally`**
   ⇒ 被杀死的扫描**在树里留下了变异**；
   **而补上的"启动时恢复"自己也是坏的** —— 它把备份前缀拼在相对路径上，**什么都没恢复**。
   **⇒ 一条无法恢复的恢复路径。**

**⇒ 第二个 bug 是被"一边跑扫描一边跑全量测试"抓到的 —— K-041 当场重演**：
**5 个 `run_cycle` 测试失败，纯粹因为后台扫描器当时正持有一个变异。**

**⇒ 而且扫描本身没跑完 —— 所以本轮不报 `missed` 数，因为没有那个数。**
**⇒ 已知成本（实测）**：`cargo test --lib` ≈ 17s、全量 ≈ 35s ⇒ **普查要跑数小时** ⇒ 只能抽样，
**而抽样量必须随结果一起打印**（未声明的 N 是本账一直在抓的缺陷）。

### 🟠 三：churn 要看"改了什么" —— 已实测，结论支持"拆 main.rs 前先补落点判据"

**审查论断**：46 次改动只告诉你"它老被碰"；**真正决定怎么拆的是改的类型。**

**⇒ 实测 `git log --oneline -46 -- src/main.rs`**：绝大多数是 `feat(...)`（session-events / health /
chat / O-1 / Engram / ADR-0017 / ADR-0018 …）。
**⇒ 即"接线新功能"，也就是落点问题** ⇒ **审查 🟠四① 成立**：
> **"新功能的默认落点不得是 `main.rs`"这条判据必须先真正生效，否则拆完 46 次会重新长出来。**

**⚠️ 该判据目前只是"已记录"，没有任何机制** —— **本轮如实记账，未实现。**

### ✅ 已采纳 / 部分采纳

| 审查建议 | 处置 |
|---|---|
| `cargo metadata` → CI-1 依赖边 | **确认可用**；**CI-1 仍未实现**（欠着） |
| DATA-ONLY 加 churn 判据 | ✅ **已做** |
| CI-7 扫描模式 → 顺带自动完成 K-036 | ✅ **已建**（`ci/scan_mutants.py`），**未跑完** |
| 拆 main.rs 前先看改了什么 + 落点判据生效 | ✅ **已实测**；**判据未机制化** ⇒ 拆 main.rs 暂缓 |
| 额度合并 + `expected_final` | ⬜ 未做 |


## 17. 新增候选原则（本链产出）

### P14（候选）：**校验器不得严于消费者**
> **若一个消费者能处理某取值，而校验器将其拒绝，则被拒的数据会以"从未发生过"的形式消失。**

**证据**：K-029 —— `event_family.js:262` 的 `cached_tokens: ['number']` 拒收 `null`，而 `prove_track.node.js:145-153` **有 `cached == null` 分支**。
**⇒ 判据**：**校验器的类型表必须是消费者实际接受集合的子集**；每一条可选字段的类型，都应能指回明确的消费点。
**⇒ 状态**：**候选**（需阳性对照：找出至少一处"校验器比消费者宽松"的反例，说明该原则不是单向过严的借口）。

### P16（候选）：**完成判据必须是「外部可观测的事实」，不是「本地已做的动作」**
> **若一个动作的效果要经由另一侧才能被看见，那么"我做完了"不能作为完成判据 —— 判据必须是"那一侧看见了"。**

**证据（目前仅一条）**：K-034 —— 78 个提交"已提交"（本地事实）而 GitHub 上零可见（外部事实）；
**"工作区干净"在 78 个提交未推送时仍然为真**，故它是一个**不能揭示该失败的判据**。

**⇒ 判据**：每条收尾动作都要能指回一个**接收方侧**的观测点（`ahead == 0` / 对端 `ref` 已更新 / 响应字段已出现）。
**⇒ 与 P13 的分工**：P13 问「出错时它响不响」；P16 问「响了，但**谁听见**」。
**⇒ 状态**：**候选，证据不足**（仅 1 条 ⇒ 不足以支撑一条原则；需第二条独立实例，否则应并入 P13 而非独立成条）。

---

## 17.1 第十二组 K（2026-09-18，来自审查第 29 轮）

### K-036 ｜**测试的说明文字声称的保护，与实际断言不符 ⇒ 那句说明在撒谎**（🟠 已定位，未普遍排查）

**实例**：`anaphase-helix/src/run_cycle/tests.rs`

```rust
/// The impasse path specifically, because it is the one the early rounds kept
/// asking about and because nothing else walks it.
#[test]
fn an_impasse_goes_to_reflection() {
    assert_eq!(body.transitions.get(&(S::Reasoning, Condition::Impass)), Some(&S::Reflection),
        "an impasse must reach Reflection, where the outcome is marked");   // ← 这句话
}
```

**它断言的是转移表里的那条边。消息却声称「where the outcome is marked」——
而"outcome 被标记"这一条从未被断言。** 那句声称是**假的**（见 K-035：outcome 恰恰没被标记）。

**⇒ 危害不是"少测了一条"，是**：**断言与说明不一致时，读者会按说明相信覆盖已存在，
于是不去补测**。这比没有测试更坏——没有测试至少看起来没有。

**⚠️ 归属更正（在案）**：本轮审查把它记为「**K-013 的第 8 次出现**」。
**实测不符**：K-013 的具体内容是「**测试向量集存在，但没有任何测试加载它**」，
与本坑是**同族但不同条**。`CI-144_合拢坑记录.md` 自己在别处已用「**与 K-013 同族**」的措辞
（"测试存在" ≠ "测试在跑且在判"）。**⇒ 故本坑另立 K-036，并注明族属，不计入 K-013 的次数。**

**⇒ 建议的机制（审查提出，采纳为候选）——「断言与消息的一致性」**：

> **任何测试，若其名/消息声称保护 X，则改坏 X 必须使该测试变红。**

**⇒ 判据用裁决实验**：构造「X 变坏」的变异 ⇒ 消息声称 X 的测试**若仍绿 ⇒ 它是装饰**。
**⇒ 与已有的 `every_returned_condition_has_a_rule` 同法**（该测试已用静态扫描抓出过一次漏边）。
**⇒ 状态**：**候选 CI 类（CI-7）**，未实现。**障碍**：需先把"消息声称什么"变成可解析的东西，
否则只能靠人读——而人读正是本坑失效的那一环。

**⇒ 现状**：**🟠 已定位未修**。**已修的部分**：K-035 那条测试的消息已改为
"an impasse must reach Reflection"（只说它真的断言了的东西），并补了
`a_declared_impasse_survives_to_the_outcome` 断言 outcome。
**未做**：**没有排查其余测试是否存在同类撒谎**（审查亦自陈"未验证是否普遍"）。

### K-037 ｜**A 类决策位的答案可能早已存在于某处 ⇒ 用最贵的通道解最便宜的问题**（🟡 已修订判据）

**事实**：**H4①②** 的答案是你自己在 **2026-09-06** 写进 `BIND-19` README 的一个提交：

> 仓库双身份：本分支（v2.0-alpha）= **Rust 参考实现**；协议规范正文在 `main` 分支
> （v1.0.0-RFC-4，spec-only，tag `v1.0.0-RFC-4`）。**规范与实现分离，各自独立演进。**

而 A 类索引把 H4 悬挂了十几轮。**代价不是"没找到"，是"没同步"**：
本仓的远端跟踪引用停在 **08-29**，该提交在 **09-06**，本轮 fetch 才看到（K-034）。

**⇒ 判据（采纳）**：**任何 A 类决策位在提出之前，先执行一次"该答案是否已在某处存在"的检索**
（`git fetch --all` → 全仓 README / spec / ADR → 决策索引自身的历史）。
**⇒ 理由**：否则是在用**最贵的通道（人类裁决）**去解决一个**最便宜的问题（没 fetch）**。

### H4 的现状（回答第 29 轮的提问）

| 子项 | 状态 |
|---|---|
| **H4①** 工作副本 | ✅ **已关**（`docs readme` 提交 + 决策索引已记"降为工作副本约定确认"） |
| **H4②** `spec/` 正文 vs `src/` 实现谁是线格式权威 | ✅ **已关**：README 明写**规范在 `main`（tag `v1.0.0-RFC-4`）、实现是参考**；且实测 `refs/heads/main` 与 `refs/tags/v1.0.0-RFC-4^{}` **同指 `5d39b963`** |
| **H4③** K-015 四方不一致（清单 `cf143000` vs 规范表/README `cf14 00 00`）谁对 | 🔴 **仍开**。README 那一条只说了"规范与实现分离"，**没有回答值本身谁对**。第三种可能（`cf14 00 00` 是另一条向量的值被抄错位置）**亦仍未排除** |

**⇒ 故 H4 不是"已关"，是 H4① ② 已关、**H4③ 仍开**。**裁定前仍不得据实测改规范**（原判据不变）。

### 新目录播种 —— 第 29 轮的表态与折中（**未实现**）

**审查表态**：**倾向不做**（即不给新目录无限额度）。
理由：目录级 ratchet 的唯一作用是防止"**拆文件**变成**新增总量**"；
**新目录一出生就播种 ⇒ 它从第一天起就有自己的额度，而那正是 D6 之前那个洞。**

**⇒ 但纯钉死也错**：一个新建目录被钉在出生值，等于**禁止它成长**。

**⇒ 折中（采纳为方案，待实现）**：
> **新目录的播种值 = `min(所有子文件出生值之和)`**，
> **且不允许单独上调，只允许随子文件一起下降。**

**⇒ 性质**：它既不是"无限额度"（有上界=子和），也不是"永久禁锢"（随子文件降而降）。
**⇒ 状态**：**未实现**。落地时须同时给出**变异证据**（把 `min(子和)` 换成 `max` 或 `∞` ⇒ 必须红）。

---

## 17.11 第二十二组（2026-09-20，模式权威：报告的模式必须是执行的那个）

### K-059 ｜**报告的模式与执行模式可以不一致 —— 三个模式参数里两个从执行路径不可达**（🔴 已修，含变异证据）

**发现方式**：排查一句"注释自白"。

`helix-mind-retrieval/src/lib.rs` 的 `stage_local_dominant` 里写着：

> `Skilled` is the *semantically correct* parameter set here: this stage is the
> focused local pass, and `negotiate_mode` has not run yet (it is called after
> this stage).

**⇒ 那句话本身就是根因的自述**：因为**模式协商发生在检索之后**，这一层拿不到模式，
只能硬编码 `Skilled`。

**⇒ 后果三层，一层比一层重**：

1. **三个模式参数里两个从执行路径不可达。** `SaCoreParams::for_mode` 有三个分支
   （Skilled α 0.5 / 软边关；Anchor α 0.7 / 软边 ×0.8；Imagination α 0.9 / 软边 ×0.95），
   而查询路径只到达第一支 ⇒ `alpha_anchor`、`alpha_imagination`、`decay_imagination`
   是**没有消费者的配置项**。
2. **报告的模式是假的。** `effective_mode: Imagination` 报给身体，而 stage 1 实际用
   α=0.5、软边关闭跑的。**报告与执行不一致，而报告的那个是谎。**
3. **退化结果会伪造一个模式。** `start_ids` 为空时**硬编码返回 `Anchor`** ——
   请求 Skilled 却被告知 Anchor。**数字就在手里，报了个别的。**

**⇒ 危害**：这不是"少一个功能"，是**证轨可以记录一个从未发生的决策**。
`MindProvenance`（suggested / effective / negotiation）若建在这个基础上，
它记录的将是**一个标签**，而不是**一次执行**。

**⇒ 已修（三处）**：

1. `negotiate_mode` **提到检索之前**。它读的是函数顶端取的 `current_impasse`，
   所以上提是**纯重构、逐字不改既有输出** —— 可验证，不是"应该没问题"。
2. stage 1 **按模式分发到各自的 traverse**（`skilled_retrieve` / `anchor_retrieve` /
   `imagination_retrieve`）。查证三者真实差异：`skilled_traverse` 与 `anchor_traverse`
   **除注释外逐字相同**（都调 `sa_core_diffusion(.., None)`），
   `imagination_traverse` 是唯一有实质差异的（按温度放松相对门 τ）。
   ⇒ **模式的全部语义就是三个数**（α / 软边衰减 / 相对门），而这三个数早已在
   `for_mode` 里编码完毕。
3. 退化结果**如实报告协商出的模式与理由**。

**⇒ 变异证据**：把 `for_mode(mode, ..)` 改回 `for_mode(CognitiveMode::Skilled, ..)`，
`the_reported_mode_is_the_mode_that_diffuses` **必须红**。实测红，报
`Anchor ... must reach through; got [<仅种子节点>]` —— 正是旧行为。
杠杆是**软边**：`decay_factor` 乘在软边权重上，Skilled 的
`SOFT_EDGES_DISABLED = 0.0` 把它归零 ⇒ 仅靠软边可达的节点对 Skilled 不可达。
**⇒ 独立佐证**：存储层既有测试 `skilled_mode_disables_soft_edges_entirely` 单独立住了
"Skilled 关软边"这一半，两半合起来才闭环。

**⇒ 同族第二例（身体侧的自我授权）**：`anaphase-helix/src/adapters/mind.rs` 的

```rust
allow_imagination: suggested_mode == CognitiveMode::Imagination
```

**身体自己建议、又自己批准** —— 一个门，由请求者向自己签发，永远拒绝不了
（VISION 原则 3：身体可建议，不决策）。而且它**反转了**：`suggested_mode` 按查询
**长度**回退，于是

| 查询 | 身体建议 | 旧授权 | 正确 |
|---|---|---|---|
| 短 +「探索」 | Skilled（按长度） | **false** ✗ | true |
| 长、无探索词 | Imagination（按长度） | **true** ✗ | false |

**人类明说"探索"，反而是唯一拿不到想象的情况。** 而**同一份 `explore_keywords`
早已被 `derive_budget_tier` 信任** —— 授权该来自人类自己的措辞，身体只转达。

**⇒ 已修**：把请求构造抽成纯函数 `build_query_request`。
**抽出来才使"接线"可测** —— adapter 需要活的 gRPC 通道，内联字面量只能靠读、
不能靠跑，这正是那句自我签发能活下来的原因。变异回旧表达式 ⇒ **两个方向都红**。

**⇒ 本次最该记住的一条**：`long_query()` 这个既有测试夹具**含「未知」**，
而「未知」正是一个 `explore_keywords` ⇒ 它**合法地**授权了想象。
**测试第一版假设它不授权 ⇒ 断言错了，不是代码错了。** 是断言被事实纠正。

**⇒ 未结（如实欠着）**：**梯子没有按模式封顶。** `negotiate_mode` 说
`Survival mode: only Skilled available`，而 stage 4/5 照样用 Anchor / Imagination 参数。
**Mind 的决定被自己的梯子违反。** 修法是把梯子按模式封顶（Skilled 不升级），
但那是关于"生存是否该省"的语义决定，未决。

**⇒ 未结（同族，已记录未修）**：`derive_budget_tier` **先查长度、后查探索词**，
所以 `「头脑风暴」`（4 字 ≤ `short_query`）拿不到 `ExogenousRequired`。
已写进测试断言如实记录，未静默带过。

### K-060 ｜**本地指针与台账同号不同坑 —— "可核对"这一层是断的**（⬜ 未修）

**发现方式**：为 K-059 登记新坑时，想按规矩取"台账的下一个空号"。

**⇒ 实测**：

| ID | `anaphase-helix/ci/pits.toml` | 本台账 |
|---|---|---|
| K-044 | 安全门在生产路径上没有门 | 一条永远打印不出来的 advisory |
| K-045 | 身体丢掉了线上已有的事实 | "配置键不在任何段内"被静默丢弃 |

**同号，不同坑。** 且本地指针**只到 K-045**，而本台账已到 **K-058**。

**⇒ 危害**：`pits.toml` 头部写着「the cap may only cite an id that appears here」
以及「the order must be read off git」。**存在性检查在本地自洽**，
但"这个 id 指的就是台账里那条坑"这一层**断了** ——
于是 `fix_window` 引用 `K-044` / `K-045` 时，**它引用的证据在台账里指向另一条坑**。
**⇒ 与 K-042 同族：检查存在，但检查的不是它声称的东西。**

**⇒ 未修**：需要一次 ID 归并（本地指针重指台账，或明确声明两个命名空间并加前缀）。
**⇒ 在此之前**，新坑一律取**台账**的空号（本次因此取 K-059，而非 K-046 —— K-046 在台账里已被「幽灵前提」占用）。

---

## 17.12 第二十三组（2026-09-20，缺省分支长扫：Tuck/crates + FlowModus 全量）

> 补 `CI-144_默认放行分类表.md` §5.1 自己声明的缺口（「本表只扫了 `anaphase-helix/src/`；若 Tuck（边缘物理闸）有同类分支，本表结论不适用于它」）。
> 方法沿用原表：**定向 grep，非穷尽**；计数口径沿用 §2（**禁止合并计数**）。**Tuck 29 行 + FlowModus 42 行**，已并入原表而非另立文件（避免两份清单）。
> 计数：**Tuck** 生效 13 / 其中安全判定 9 / **fail-open 4（T-1 T-7 T-9 T-10）** / 未生效 14 / 未定 2 / 依赖降级 2。
> **FlowModus** 生效 31 / 安全判定 14 / **fail-open 10（F-1 F-5 F-7 F-8 F-11 F-14 F-15 F-21 F-24 F-31）** / 未生效 10 / 未定 1 / 依赖降级 9。
> **⇒「Tuck 有 N 处 fail-open」这种说法同样作废**——按口径说话。

### K-061 ｜**准入闸门：实现、测试、写进示例配置，而没有任何装配者安装它**（🔴 已定位）

`with_access` 调用者仅 `tuck-gateway/tests/access_gate_test.rs:26,140`；`serve_gateway`（`tuck/src/main.rs:101-155`）只调 `with_upstream_key/with_upstreams/with_chain`；**`TuckConfig` 八个字段（`server/gateway/security/audit/credential/hsm/hot_reload/log`）无 `access`**，且 Tuck 无 `deny_unknown_fields` ⇒ `config.example.toml` 整段 `[access]`（含 `default_action="deny"`）**被静默丢弃**。
**⇒ 分子对了，分母错了**：`PLAN.md:96` 记「准入闸门全部交付（…pipeline 接线…）✅」，其验收证据是 `--all-features` 全量测试——而那个测试**自己调用 `with_access`**；ADR-0005 自己的追踪表 T1–T9 **全 ⬜**。

### K-062 ｜**「取不到规则」在同一仓有相反两个答案，弱的那条是默认路径**（🔴 已定位）

`main.rs:147-148`（空 = 无规则，放行）↔ `reload.rs:16-17`（"The gate never degrades to 'no rules', because that would silently disable content governance **while looking healthy**"）。**K-025 的形状，落在 Tuck。**

### K-063 ｜**治理判据只吃 `content` 的字符串形态**（🔴 已定位）

`gov.rs:146` 对非数组 `messages` **整段跳过治理循环**；`gov.rs:149-152` 对多模态 `content`（OpenAI 视觉形态，数组）取 `as_str()` 得 `None` → `unwrap_or_default()` ⇒ **检测跑在空串上**。不是漏报，是**判据形态与协议形态不一致**。

### K-064 ｜**`security.fail_closed` 默认 true，而没有任何读取者**（🟡 已定位）

K-047「声明冒充机制」在 Tuck 的实例：操作者把它改成 true 或 false **都不产生任何行为差异**。

### K-065 ｜**篡改证据链只有写入者，没有校验者**（🔴 已定位）

`verify_chain`/`verify_anchors`/`open_with_anchor` 生产无调用者（`main.rs:142` 用普通 `open`）；`/v1/audit` 直读文件且坏行 `else { continue }`（**静默消失**）；`ledger.rs:40` `let _ = chain.append(..)` **丢弃 append 失败**。README:60「Ed25519-anchored so a full rewrite of the chain is detectable」**在运行时没有机制**。

### K-066 ｜**最高风险级有一个 bit 的无条件放行，而模块自述「Never default-pass」**（🔴 已复核）

`lib.rs:408-411`「Any error … returns Reject. **Never default-pass.**」与 `:364 catastrophic_override: Decision::HardOverridePass` + `:430-431` 的早退并存；`tuck/src/main.rs:59` 用的正是 `SecurityPolicy::default()`。
**⇒ 已独立复核（逐行）**：策略本身默认是严的（`catastrophic: Reject`），**但帧头里的一个 bit（`override_flag() & 0b1`）把最坏等级翻成直通**。注释自称 *"non-negotiable"* ⇒ **是故意的设计，不是疏忽**。
**⇒ 定性风险不在「有没有 fail-open」，而在风险等级与 override 位都由被检帧自述。**
**⇒ 与原则 6「固定路径无分支」冲突**：这是一个由线上比特控制的分支，而它控制的是"最危险的帧走不走"。

### K-067 ｜**FlowModus「缺 billing ⇒ 判免费」与同仓自己的教义冲突**（🔴 已定位）

`anti_corruption.rs:124-126` 原文：「Present but malformed ⇒ an error, **never a silent skip**: the free tier is enforced on the parsed declaration, so a rate card that failed to parse must not read as free.」
而生效路径 `registry.rs:55` `is_free_supplier(...).unwrap_or(true)` 把**缺失**的 billing 读作免费；`models: []` 又因 `all()` 空真读作免费。**free/paid 物理隔离门的判据自相矛盾。**

### K-068 ｜**「未知名 → 报错」与「未知名 → 静默改走默认」在同一处并存**（🟠 已定位）

Rust：`router.rs:157-159 manual→Err` 对 `:147-151 group→auto`。
Python：`lifecycle.py:146-149 _manual_decision→raise` 对 `:158-159 _group_decision→_auto_decision`（**组名被整体丢弃**，命名路由组完全无效）。**K-025 形状第三例。**

### K-069 ｜**两个「校验」CLI 只打印、恒返回 0**（🔴 已定位）

`cli.rs:81` `let _ = (verify_registry, PROTOCOL_ROOT_PUBLIC_KEY_BYTES);` —— **明确把校验函数变成未使用量**；`supplier_cmd.rs:165-185 cmd_test` 恒 `Ok(())`。
**⇒ 任何以 `flowmodus verify` / `supplier test` 为门禁的自动化恒过。**

### K-070 ｜**Layer 4 的「硬边界」在默认配置下是直通**（🔴 已定位）

健康过滤（TERMINAL 跳过）**只写在 `priority_groups` 非空的分支里**，而 `BiasConfig::default().priority_groups` 为空且两个装配点（`serve_cmd.rs:117`、`cli.rs:172`）都用 default ⇒ `health.rs:10-11` 的「Layer 4 skips TERMINAL suppliers entirely」**在默认部署下不成立**；`require_verified_supplier`/`require_regions`/`require_modalities` **三个声明字段零消费者**，Python 侧 `layer4_filter.py:111-114` 更是字面 `pass`。

### 规则 2（职责决定 fail 方向）接触 Tuck 的结论 —— **必须分两条轴**

**轴一「判不出」：存活，证据充分。** magic 错 `lib.rs:84`、保留位非零 `:88`、帧过短 `:450`、PFP 解析失败 `:456`、SAP 非法 `anaphase_bridge.rs:306`、PFP 非法 `:263`、HITL 超时 `hitl.rs:212` —— **全部 Reject**。
**轴二「已判」：规则 2 不覆盖这一轴，而 Tuck 在这一轴上有一个活的、最高风险级的无条件放行（K-066）。**

**⇒ 直接回答「Tuck 里有没有『职责是决定能不能执行』却缺省放行的组件」**：
- **有，但不是「判不出」那一类**：边缘闸 `decide()` 在 T-1 上是**活的无条件放行（已判）**。
- **准入闸 `AccessTable` 设计上 fail-closed**（`access.rs:59-61`、`:173-183`），**但在任何一个仓内定义的构建组合下都没有活着的执行**（K-061）。
- **FlowModus 里存在对规则 2 的正面违反三处**：`registry.rs:55`（职责是 free/paid 隔离，缺价格时答"允许"）、`layer2_registry.rs:28-32`（职责是挑合格候选，无声明时答"全部"）、`layer4_filter` 硬边界（约束未设时各自跳过）。

**⇒ 未核（沿用原表 §5.4 的 P7 未取得）**：方法为定向 grep 非穷尽；**未编译任何特性组合**验证；部署二进制的特性集是**推断而非反汇编取证**；FlowModus 当时**无进程运行**，其"生效"为入口可达性而非实测。

### 17.12.1 勘误与补充（并行精读返回后，**以本节计数为准**）

**A. 结构性活性证明（重新框定了 Tuck）**：`tuck-gateway` **不依赖 `tuck-core`**（其 `Cargo.toml` 无该依赖）。shipped 二进制里 `tuck-core` 的消费者只有 `tuck/src/main.rs:16`（`TuckConfig, decide_from_bytes, SecurityPolicy`）与 `logging.rs:24`。⇒ **其余全部 `tuck-core` 模块被编译但无运行时调用者**：`hitl`/`catastrophic`/`injection`/`outbound`/`proxy`/`credential`/`file_store`/`hsm`/`audit`/`audit_store`/`audit_query`/`tamper`/`mind_bridge`/`tentacle_bridge`/`sap`/`metrics`/`status`/`hot_reload`。它们仍是 `pub` 库 API（供仓外消费者），故记「未生效（无生产调用者）」而非删除。

**B. 修正后的计数（**取代 17.12 的 Tuck 行**；FlowModus 不变）**

| 器官 | 当前生效 | 属安全判定 | **fail-open** | 未生效 | 未定 | 依赖降级 | 只做记录 | 边界缺陷（非放行） |
|---|---|---|---|---|---|---|---|---|
| **Tuck/crates/**（43 行） | **23** | **20** | **8**（T-1 T-7 T-9 T-10 T-34 T-35 T-37 T-38） | **17** | **3** | **4** | **8** | **1**（T-36） |
| **FlowModus/**（42 行） | 31 | 19 | 10 | 10 | 1 | 9 | 5 | 0 |

> **T-36 明确不计入 fail-open** —— 它是 fail-closed（崩溃），不是默认放行；把它算进「N 处 fail-open」正是原表 §0 规则 1 要禁止的混淆。

### K-071 ｜**一个响应体就能让边缘闸的病历表中毒**（🔴 已定位；**最高严重度**）

`tuck-gateway/src/redact.rs:113` 的 `&rest[start..min(start+4,len)]` **未做字符边界检查**：上游响应体含字面 `P_` 后接多字节 UTF-8（`"P_中"`、`"P_。"`、`"P_😀"`）即 panic。而 demap 在**持有全局会话表 Mutex** 时执行（`state.rs:191-195` `.expect("table lock")`）⇒ debug 下锁中毒，**此后每个请求都 panic**（单请求触发的**持续性** DoS）；release profile `panic="abort"` ⇒ 进程直接终止。
**⇒ 这不是 fail-open，是 fail-suicide** —— 它的价值在于证明「边缘物理闸」的**可用性边界没有任何兜底**。按规则 1，**不得计入 fail-open 计数**。

### K-072 ｜**占位符宽度契约没有单一定义处 ⇒ 静默错实体替换**（🔴 已定位）

生成端 `redact.rs:65` 允许 `P_100`（**3 位**十六进制），消费端 `:113-114` **硬编码 4 字节**。同一会话内实体数 ≥257（`seq≥256`）时：`P_100` 被当作 `P_10` 解析成**另一个真实实体（seq 16）**，且原占位符尾字符 `0` 留在正文里。
**⇒ 而 `demap_miss` 不增**（因为 `"P_10"` 解析"成功"）⇒ **错误不会被任何计数器看见**。发生位置是模块自称 *"the highest-sensitivity asset in the ecosystem"* 的映射表。

### K-073 ｜**被删掉的账本「校验通过」——读侧、写侧、验侧三处都缺一环**（🔴 已定位）

- 验侧：`tuck-audit/src/lib.rs:349-360` 把 `NotFound` 当「空链，有效」，注释逐字 *"A missing chain is an empty chain: valid, zero records."*
- 写侧：`open():181-196` 用 `scan_tail` **不校验哈希**即恢复尾状态并**继续追加到被篡改的链上**（`main.rs:142` 生效）；`set_permissions(0600)` 失败被 `let _ =` 丢弃（`:193`）
- 读侧：`audit_api.rs:61-93` `read_to_string` 失败 → HTTP **200** `{"entries":[],"count":0}`，与「无记录」不可区分；坏行 `else { continue }` 静默消失
- 另：`ledger.rs:40` `let _ = chain.append(..)` 丢弃 append 失败
**⇒ 而 README:60 仍宣称「Ed25519-anchored so a full rewrite of the chain is detectable」。**

### K-074 ｜**同一个 `compile` 里，「规则不可用」有两个相反答案**（🟠 已定位）

`policy.rs:96-99`：`kind="regex"` 缺 `pattern` ⇒ **`Err`（大声失败）**；
`policy.rs:103-110`：`kind="dict"` 缺 `words` ⇒ `.unwrap_or_default()` 得到**空词表** ⇒ **编译通过、永不命中**。
**⇒ 一条写错的 dict 规则会安静地不存在。K-025 形状，这次在同一个函数内部。**
另有 `policy.rs:153-154`：entropy 规则未写 `min_len`/`min_entropy` 时静默取 16 / 3.5 bits-per-char。

### K-075 ｜**整个 `[security]` 段是死开关（K-064 的扩展）**（🔴 已定位）

不只 `fail_closed` 一个：`default_risk_level` / `require_pfp` / `require_sap_for_high_risk` / `max_audit_entries` 在 `config.rs` 之外**零引用**；`TuckConfig` 的 `audit`/`credential`/`hsm`/`hot_reload` 段在 `tuck/src` **从不被读**；且 `TUCK_SECURITY__FAIL_CLOSED=yes` 解析成 **`false`** 而非报错。
**⇒ 运维以为在调一台仪表，其实仪表没接线。**

**⇒ 本轮结论不变**：规则 2 在 Tuck 的**「判不出」轴完好**（新证：`hitl.rs:211-213` 超时确为 Reject；`credential.rs`/`file_store.rs` 每种查找失败都是 `Err`；`proxy.rs:188-203` 对 `Decision` 穷尽匹配**无 `_ =>`**；`outbound.rs:205 _ => None` 拒绝注入）；**「已判」轴上有 T-1 的单比特无条件放行**。真正的 Tuck 缺口集中在**装配**（K-061）、**配置**（K-064/K-075）、**规则加载**（K-062/K-074）、**判据形态**（K-063）、**账本**（K-073）、**边界缺陷**（K-071/K-072）。
**⇒ 仍未核**：`hot_reload.rs`/`health.rs`/`metrics.rs`/`status.rs` 未并入（按结构性结论它们同样无生产调用者 ⇒ 只能记「未生效」，**不改变 fail-open = 8**）；未编译任何特性组合；K-071 的 panic 与 K-072 的错实体**未实跑**（由逐字读码推出：`"P_中"` 字节数 5，`min(0+4,5)=4`，`&s[0..4]` 必切在 `中`(字节 2..5) 内部）。

### 17.12.2 PART 3 收尾（T-44..T-66 全部 ❌未生效；**Tuck 最终计数以此为准**）

**最终计数（取代前两版）**：

| 器官 | 当前生效 | 属安全判定 | **fail-open 且生效** | 未生效 | 未定 | 依赖降级 | 只做记录 | 边界缺陷 |
|---|---|---|---|---|---|---|---|---|
| **Tuck/crates/**（66 行） | **23** | **33** | **8**（T-1 T-7 T-9 T-10 T-34 T-35 T-37 T-38）——**未变** | **39** | **4** | **8** | **12** | **1**（T-36） |
| **FlowModus/**（42 行） | 31 | 19 | 10 | 10 | 1 | 9 | 5 | 0 |

> **Tuck 安全判定共 33 处,仅 13 处生效,20 处未生效。** ⇒ **「实现完整 ≠ 装配生效」的量化形态:Tuck 的安全判定代码里有 20/33 是死的。**

**⇒ 而本组给出一句决定性的判断:**
> **未生效的那一整串 fail-open(T-44..T-53 + T-6/T-7)今天都不可达,但都是 `pub` API 与已验收的 ADR 实现 —— 接线一次,这条清单同时生效。**

### K-076 ｜**「没有 SAP 就是通过」是 Tuck 的明文缺省**（🔴 已定位）

`sap.rs:534-536` `let Some(sap) = sap else { return SapDecision::Pass; }`,注释逐字「No SAP — PFP Pass is final」;而 SAP 在 Anaphase 侧**本就可选**(`anaphase_bridge.rs:71`)。
**⇒ 原表 #1 对「PFP optional」判为「未定(规范内部矛盾)」;在 Tuck,同一个问题的答案是「已定的放行」。**

### K-077 ｜**签名校验器没人装 ⇒ SAP 签名默认不校验**（🔴 已定位）

`sap.rs:552-559` 只在 `verifier` 为 `Some` 时校验;而 `TuckSecurityGate::new` 置 `signature_verifier: None`,`with_signature_verifier` **零调用者**。**与 K-061 同形:机制写完了,装配没写。**

### K-078 ｜**重放保护可被「首帧」与 LRU 淘汰绕过**（🟠 已定位）

`sap.rs:169-172` 首帧 `/* any seq is valid */`;`:457-465` LRU 淘汰会忘掉该 source 的 last-seq ⇒ **同一 source 的重放重新被接受**。**两条路径都不需要攻击者做任何特殊构造。**

### K-079 ｜**密钥轮换告警是注释**（🟡 已定位）

`sap.rs:261-265` `needs_key_rotation()` 的函数体**只有说明文字** —— 无错误、无告警、无日志;`SapError::KeyRotationNeeded` **全仓从未构造**。K-047「声明冒充机制」第四例(在 K-064/K-075 之后)。

### K-080 ｜**「两个空串相等」被当作完整性已验证**（🔴 已定位；**缺省即通过的最纯粹形态**）

`tentacle_bridge.rs:374` `let integrity_verified = request.integrity_hash == request.computed_hash;`
**⇒ 两个缺省空串相等 ⇒ `verified = true`。** 模块内**无任何非空/hex 校验**。

### K-081 ｜**名为 sandbox 的结构允许一切**（🔴 已定位）

`tentacle_bridge.rs:464-472` 只有两条窄拒绝规则 ⇒ `execute=true` 单独、或 `network=["*"]` 单独,**都落到 `(Pass, None)`**;
`tentacle_bridge.rs:640-649` 的 `filesystem_allowed: true` **无条件**(无视 `involves_filesystem_write`),`allowed_network` 留空而字段文档自述 **「empty = all allowed」⇒ 无限制出网**。

### K-082 ｜**插件审计账本把所有拒绝都记成 Pass**（🔴 已定位）

`tentacle_bridge.rs:527-534` 审计条目**硬编码 `Decision::Pass`**,真实判定被塞进 `identity_label` 参数走私,而注释声称那是 source 字段。
**⇒ 与 T-19/T-21/T-31/T-56 合看:Tuck 的账本层在写侧丢错误、在记侧记错结论、在验侧把「文件被删」当「有效空链」—— 三条腿都不落地,而 README:60 仍宣称可检测全量重写。**

**最终回答「Tuck 里有没有『职责是决定能不能执行』却缺省放行的组件」:有,且分两类。**
- **生效的一类只有一个 bit**:T-1（`lib.rs:430-431` + `:364`）
- **未生效的一类有一整串**:SAP 缺失即 Pass(T-44)、HardOverride 跳过全部 SAP 检查(T-45)、无校验器即不校验签名(T-46)、插件权限落网即 Pass(T-51)、空完整性哈希即「已验证」(T-50)、沙箱默认放开文件系统与全网(T-53)、准入闸门从不安装(T-6)、内容治理零规则(T-7)

**⇒ 正面例(该给的信用)**:这四个文件里**没有任何超时缺省为放行**;`proxy.rs:188-203` 与 `sap.rs:524-531` 均对 `Decision` **穷尽匹配、无 `_ =>`**;proxy 头解析失败一律传播 `Err`;非测试代码仅 3 处 `expect(` 且可证不会失败。

### 17.12.3 Tuck 收官（PART 5，K-083…K-086）＋ 打包部署事实

**⚠️ 「生效」的口径更正（本组最重要）**：审计核实 **①`Dockerfile:33` 是 `cargo build --release --workspace`，无 `--features gateway`**（而 `crates/tuck/Cargo.toml: default = []`）⇒ 镜像里没有网关代码；**②`Dockerfile:58` 只复制 `config.example.toml`，而 `:71` 的 CMD 指向从未进入镜像的 `/etc/tuck/config.toml`** ⇒ `TuckConfig::load` 返 `Err`、`main` 的 `?` 传播、非零退出、systemd 崩溃循环；**③`:64-65` `HEALTHCHECK tuck --version` 恒通过**（它不需要配置）。
**⇒ 所以「当前生效」= 在 README 那个构建下生效（本机 debug，pid 1171），不是"在生产里生效"。** 且 `panic="abort"` 只属于 release ⇒ **同一棵树，两种执行语义**。

**最终计数（Tuck 90 行）**：生效 **26** / 属安全判定 **34（仅 14 生效）** / **fail-open 8（四版未变）** / 未生效 **60** / 未定 4 / 依赖降级 19 / 只做记录 19 / 边界缺陷 1。
**⇒ Tuck 安全判定代码 34 处里 20 处是死的** ⇒「实现完整 ≠ 装配生效」的量化形态。

**最终回答**：**没有任何「职责是决定能不能执行」的组件通过 catch-all、`Err(_)` 或缺失凭据路径缺省放行** —— tuck-core 里每个闸形 match 都穷尽且方向正确。**真正会放行的是「缺失缺省」(absence default)**：(1) `rules_path=""` ⇒ 零规则 ⇒ 每条消息 `Pass`；(2) `upstream_key=""` ⇒ 转发调用方自己的 `Authorization`；(3) `ledger.rs:40 let _ = chain.append(...)` 且 **`record() -> ()` 使「写失败即阻断动作」在类型层面不可能**；外加一个 bit 的 K-066 与一个响应体即杀进程的 K-071。

### K-083 ｜**把「没有证据」报告成「完美」**（🔴 已定位）

`health.rs:190-196,241-247,275-279,293-297`：总查询/校验/决策数为 0 时上报 **Healthy** 且 `pass_rate = 100.0`、`credential_success_rate = 100.0`；配合 `:324-349` 指标行缺失即读作 0 ⇒ **指标管线断掉反而更像一切正常**。

### K-084 ｜**同一秒内的策略修改被判为「已是最新」**（🟠 已定位）

`hot_reload.rs:183-192` 用 `as_secs()` 比 mtime，同秒编辑返回 `UpToDate` ⇒ **静默继续用陈旧策略**；模块自己的测试靠 `sleep(1s)` 绕过。另：`:201-213` 的三把顺序写锁与注释「Atomic swap」不符（可产生撕裂视图）；`:263` 丢弃 `reload()` 结果。

### K-085 ｜**半个配置文件是仪表盘装饰**（🔴 已定位）

不只 `[security]`（K-075）：`[audit]`/`[credential]`/`[hsm]`/`[hot_reload]`/`[metrics]` 在发货二进制里**零消费者**；`config.rs:20` 文档承诺 `TUCK_CONFIG` 而 `load()` **从不读它**；文档指向的 `TUCK_CREDENTIAL__MASTER_KEY`/`TUCK_HSM__*` 无人读（真实 store 读 `TUCK_MASTER_KEY`）；**`validate()` 从不检查 `[gateway]`**（`:211-273` 只查 server/security/audit/credential/log）⇒ `upstream_key=""`、`rules_path=""` **都能通过校验**。

### K-086 ｜**被打包的部署什么都不跑，而它的健康检查恒绿**（🔴 已定位）

见本组开头。**「未运行的网关」+「恒绿的健康检查」+「README 描述的构建与 Dockerfile 的构建不是同一个」，三者叠加：任何只看健康检查与文档的人都会以为这道门在守。**

### 17.13 Cellrix 组（K-087…K-101；**原报编号 K-076…K-090 与 Tuck 组撞车，此处重编**）

> Cellrix = **整个生态的主要展示窗口**（非仅证轨）。方法同为定向 grep，**非穷尽**。
> **计数口径**：当前生效 **26** · 属**安全判定 9**（C-3 C-4 C-5 C-6 C-7 C-8 C-16 C-18 C-20 C-22）· 未生效 1（C-27）· 未定 3 · 依赖降级 11 · **已 fail-safe 2**（C-14 C-15）。
> **⇒「Cellrix 有 N 处 fail-open」一律作废。** 真正该翻的 9 处形状一致：**把"判不出"渲染成正常值，方向向上。**

**Q2 终局答案：主窗口显示不出五类结果。** ① 状态词表是**闭集四值** `ok/fail/pending/done`（`prove_track.data.js:41-44`），**无 error、无 unknown**；`fail: { t: 'failure' }` 把**被拒=失败**；`isVerdictMet(p) ? 'ok' : 'fail'`（`render.js:213`）把 `false` 与 `undefined` 压成同值。② **`E-*`/`W-*` 零命中**；错误类型全是 `String`、无 `code` 字段；退出码只有 0 与隐式 1。③ **未知渲染成正常值**（逐字见 K-087/K-088/K-089）。④ 证据链部分存在，但 `n.source || 'a session'` 是**无痕默认**。

### K-087 ｜**未知 status 渲染成「本来就正常完成」**（🔴 已定位）

`prove_track.view.js:106` `var st = STATUS[it.status] || STATUS.done;` ⇒ 灰 `done`；`:268` **只渲染映射后的值，原始值人类看不到**；测试只喂 `status:'done'`，从不测未知。

### K-088 ｜**被拒/拦截类决策在网页 Ledger 渲染成绿色「正常」**（🔴 已定位）

`cockpit.js:35` 的 `else → 'e-ok'` 吃下 `?`/空/`block`(小写)/**未知码** ⇒ 全绿。**全 `web/assets` 无 block/hold/redact/deny 的渲染分支。**

### K-089 ｜**是否放行是二值坍缩，且两个视图给出相反的谎**（🔴 已定位）

`render.js:213` 把**未知并入失败**；`session_list.js:335` `c.passed === false ? 'FAIL' : 'PASS'` 把**缺失并入通过**。**同一事实、两个界面、两种谎** —— 对主窗口而言这是最贵的失效方式。

### K-090 ｜**血缘遍历的键不一致**（🟠 已定位）

消费者按 `period_id` 查，默认路径喂 `job_id`（`script.html:75,77`）⇒ 命中不到；而**测试恰好断言 `job_id` 查不到**（`chain_merge_test.js:184-186`），生产却把它当正常路径，失败又被 `script.html:78` 静默吞成单段窗口。`chainJobIds` 已实现且有测试，**生产调用点为零**。

### K-091 ｜**生态事实有 4 个来源，最权威的那个被读了然后扔掉**（🔴 已定位）

`AgentSnapshot.ecosystem: Vec<GloveInfo>`（`run_cycle/mod.rs:85`）**每 2 秒逐字节透传到浏览器**（`routes.rs:35-36` 无字段裁剪），而 Cellrix **全仓零处消费**（`gloves`/`GloveInfo` 在 `web/src`/`ui/src`/`transport/src`/`protocol/src`/`cli/src` 全零命中）。浏览器生态灯改用**写在函数体里的 6 个字面端口**（`routes.rs:262-321`），而紧邻注释 `:257-259` 自称 *"not hardcoded guesses"*。
**⇒ 4 个来源互不引用**：① Anaphase `GloveInfo`（最权威，**无人看**）② Cellrix 字面端口探测（弱，**屏幕上那个**）③ Cellrix CLI `health_check`（**仅启动 stdout**）④ `transport/src/health.rs`（**有 `Unknown`、严判 —— 零消费者**）。

### K-092 ｜**`probe` 以「body 是不是以 `{` 开头」代替健康判定**（🔴 已定位）

`web/src/http.rs:45-52`：`if body.trim_start().starts_with('{') { Ok(()) }`。Anaphase 自身不健康时 `/v1/health` **仍返回 JSON** ⇒ **判为健康**。
**⇒ 实测后果：同一屏、同一 2 秒周期内，CLI 打印 `anaphase: ❌ self-check failed`，而浏览器同时亮绿灯。** 而 `unhealthy_names` 唯一调用者 `server.rs:140` 落进 `println!`，**永不进浏览器**。

### K-093 ｜**所有探测失败折叠成单一 `"starting"`**（🟠 已定位）

`routes.rs:269-274` `Err(_) => "starting"` ⇒ 4xx/5xx/超时/非 JSON/连接中断**全部不可分**，而 `tokens.html:236` 对 `"starting"` 确有黄灯 ⇒ **「服务正在起来」与「服务永久坏了」同色**。

### K-094 ｜**注释在撒谎（本仓第 3 例）**（🟡 已定位）

`routes.rs:257-259` 注释自称 *"not hardcoded guesses"*，其下方就是 6 个字面端口。同族：`cockpit.js:29-30`（称数据源是 Tuck 审计链，实为 Anaphase 快照）、`cli/src/main.rs:98-99`（称"没有凭据时会显示网关错误"，而红分支不可达）。

### K-095 ｜**TUI 证轨把「网关没答」渲染成绿灯「0 entries」+ 安抚文案**（🔴 已定位；**本组最高价值**）

`ui/src/widgets/prove_track.rs:84-90` 的红分支 `if let Some(e) = &state.error` **不可达** —— `ProveTrackViewState.error` 全仓**只有被赋 `None`**（`app/state.rs:123`、`prove_track.rs:62`），取数失败被静默吞掉（`app.rs:127` `Err(_) => { /* transient — keep polling */ }`）。于是授权失败/500/超时/解析失败与干净网关**都是绿灯 `0 entries`**，还附 `prove_track.rs:111` 的 *"no imprints yet — run a governed call"*。
**⇒ 把「我没读到」说成「还没发生」** —— 比网页版更坏，因为网页只是不显示，TUI **主动安抚**。

### K-096 ｜**空快照渲染成完全正常的 DRIVE 驾驶舱**（🟠 已定位）

`anaphase_client.rs:436-441` `None => Ok(AgentSnapshot::empty())`，而 `protocol/src/anaphase.rs:1244` 的空快照**默认 `mode: Drive`**（青色正常态）⇒ 取数失败照常画出 `[DRIVE]` + `"ledger: (empty — no calls executed yet)"`，**无任何"数据缺失"标记**。

### K-097 ｜**失败动作以正常样式显示**（🟠 已定位）

`input_box.rs:110-120` 只有 `starts_with("✗")` 才用 `style_alert()`，而 `handler.rs:355` 构造的是 `"(action failed: {error})"` —— **不以 `✗` 开头** ⇒ 走正常 reasoning-indigo。

### K-098 ｜**缺 `action_id` 的按钮真的发出 id `"unknown"`**（🔴 已定位；**安全判定**）

`handler.rs:320-324` `.unwrap_or("unknown")` ⇒ `:338-344` 当作真实 id 送进 `ActionRequest`。**「决定能不能执行」位置上的未知被当成可执行值** ⇒ 按 Rule 2 应 fail-closed。

### K-099 ｜**陈旧的正常画面优先于错误**（🔴 已定位）

`app.rs:339-345`：`state.error`（含 `"Connection lost: No heartbeat…"`）**只在 `snapshot` 为 `None` 时渲染** ⇒ 只要还挂着旧快照，错误完全不可见。另有 `app.rs:216-227` 的 `StreamError` 处理**没有 else** —— 错误文本里没有单引号就**既不显示也不置状态**。

### K-100 ｜**未知「枚举字符串」不产生 Unknown**（🟠 已定位）

TUI 的 Unknown **只在 `NodeType` 一维存在**（靠 `#[serde(other)]`，见下）；`InteractionMode`/`VerdictStatus`/`LedgerEntry` **都没有** ⇒ 未识别值在反序列化时失败、更新被丢弃（`app.rs:94`）⇒ **屏幕上留着上一个快照（陈旧 = 看起来正常）**。

### K-101 ｜**安全相关的拒绝无留痕**（🟡 已定位）

`transport/src/uds/server.rs:103-108`：对端 UID 校验失败 ⇒ `continue`，**无事件、无审计、无日志、无 TUI 通知**（方向本身 fail-closed，**正确**；问题是"有人被拒"这件事不可观测）。`uds.rs:208` 在 `target_ns` 匹配不到任何客户端时仍返回 `Success{"focus swapped"}`。

**✅ Cellrix 的正面例（该给的信用，且是本组最重要的一句话）**：`ui/src/widgets/fallback.rs:17-32`（`NodeType::Unknown` ⇒ `"⚠️ Unknown Node Type"` + alert 边框 + **原始 JSON**）与 `web/assets/wayout.js:80-93`（未登记码 ⇒ `kind:'error'` + 保留原始 detail + `unclassified:true`，`wayout_test.js:157-161` 断言）证明：
> **「让未知显露」在这个生态里已经做到过两次。所以这不是能力缺口，是选择。**

### K-102 ｜**锁钉点落后于修复 —— 一个假绿灯**（🟡 **已修**）

`anaphase-helix/Cargo.lock:2271` 把 dev 依赖 `tuck-core` 钉在 `#0001dde5492…`，而 Tuck 侧刚提交了 `c8d6d90`（脱敏占位符的字符边界与宽度修复）⇒ **`tests/tuck_gate.rs` 编译并运行的是未修复的 tuck-core，而本地显示绿灯。**
**⇒ 判据更正（本次实测得出，重要）**：只比对「远端分支 vs 本地 HEAD」**不充分** —— **锁文件会覆盖分支**。实测远端 `origin/rs` 已等于本地 HEAD，而 lock 仍是旧 SHA ⇒ **只跑 `ls-remote` 会得到虚假的安心**。**正确判据是把 lock 钉住的 SHA 与修复提交比对。**
**⇒ 已修**：`cargo update -p tuck-core --precise c8d6d905`（不连带升级其他依赖，输出自证 "105 unchanged dependencies"）；`cargo test --test tuck_gate` ⇒ **3 passed / 0 failed**（对含修复的修订编译）。提交 `anaphase-helix 8489ad4`。
**⇒ 附带未解决**：git 依赖用**分支名而非不可变引用**，是一处「未声明的可变性来源」，与「确定性优先」冲突；本提交选择「钉 SHA 但刷新到最新」而非改依赖形态。

### K-103 ｜**解码失败即静默丢弃（同调用点的第四个站点）**（⬜ **未修**）

`crates/tuck-gateway/src/gov.rs` 同一调用点：`std::str::from_utf8(&chunk).unwrap_or_default()` —— **任何切在多字节字符内部的 chunk 被静默丢弃**。K-071/K-072 的修复**消除了 panic，但没有消除这个数据丢失**；它不属于「宽度」根因（是「解码失败」根因），故**未并入 K-071/K-072 那一批**。
**⇒ 按加固后的通则（同根因修复：定义处必须统一；未一起修的站点必须显式登记为一个坑）—— 本条即该登记。**

### 17.14 扩展性面批（K-104 一族）

### K-104 ｜**`[large]` 守卫只覆盖 8/14 个仓**（🟡 **已修**）

`install-hooks.sh` 的 `REPOS` 是**手维护列表**:注释写 "five repos",实际列 8 个,而工作区有 **13 仓 + 1 嵌套仓(`commonintents/.github`)** ⇒ **6 个仓完全没有 hook**(Helix-MCP-Learner / lodestone-md / lodestone-spec / lumtract / phyt-DNA / commonintents/.github)⇒ ADR 首行闸门与 `[large]` 闸门在那六个仓都不跑。**它们看起来被覆盖,是因为这个脚本存在;没有任何东西说明它覆盖哪些仓。**
**⇒ 教训不是"补名字"**:一个写在「为了不让手维护被遗忘」的脚本里的手维护列表,**是同一个缺陷上抬了一层**。
**⇒ 已修**:列全 14 个仓;**接线前只读验证安全**(Helix-MCP-Learner 5/5 ADR 通过,其余 ADR 数为 0);**正向验证闸门确实会跑**(假 ADR 被 REJECT 并引用解析器);`--check` 现 14/14 ok。提交 `helix-mind 57a0335`。

### K-104b ｜**守卫的范围注释与正则不符**（🟡 **已修，但见 K-104c**）

`commit-msg` 注释写「**Source**, assets and ADRs keep the tight limit」,而正则是 `^(web/assets/|web/src/|docs/decisions/)` ⇒ **`src/` 与 `crates/` 不在内**。后果:一个 978 行的 `src/` 改动静默通过,而一个 51 行的 ADR 会被拦。
**⇒ 正则故意不改**:它自己的上一段已警告扩范围的代价(*"Three consecutive [large] markers is how a gate becomes a ritual, and a ritual is a gate nobody reads"*)。要更宽,诚实形式是**对任意路径设比值上限**,而不是加长前缀列表 —— 那是决定,不是清理。提交 `helix-mind c9cab5b`。

### K-104c ｜**同一处注释两次写错，只有表驱动断言抓到第二次**（🔴 **已修；本族最有价值**）

K-104b 的第一次改写声称「They are **two Cellrix trees plus the ADRs, and nothing else**」—— **这句仍然错**:numstat 路径是**仓相对的**,前缀**与仓无关**,且 `docs/decisions/` 在 **10 个仓**里都存在。
**⇒ 抓到它的是「把注释变成表驱动断言」那一步**:断言对旧注释 RED(`注释里没有机器可读的 @scope 行`、`与仓无关`),加 `@scope:` 行后 GREEN。**正则仍一字未动。** 提交 `helix-mind b1b7d7d`。
**⇒ 这是「覆盖范围必须与声明范围一致」目前最干净的实例:同一处注释写错两次,而只有断言抓到第二次。**
**⇒ 巨人路径**:架构约束要**写成测试跑,不要写成文档**(ArchUnit / dependency-cruiser 的形态)。**文档会漂移,断言不会。**

### K-105 ｜**单调性反转（第三次同族）**（🟡 **已修**）

`HelixECO-Glove/core/src/reviewer.rs:348` R005:同一函数内两条分支严重度相反 —— `risk_level` **为空** ⇒ `Warning`(轻);`"UNKNOWN"`(**明确声明不知道**)⇒ `Error`(重到被拒)。
**⇒ 系统惩罚诚实声明,奖励沉默。最便宜的攻击因此不是伪造危险值,而是把字段剥掉** —— 剥掉 ⇒ `Warning` ⇒ 被接受且无痕。且它按激励**摧毁**了 `FieldProvenance`(服务器会学会留空而不是声明 unknown)。
**⇒ 已修**(与 K-067 的 R005、`risk_rating` 合起来是第三次同族):阶梯 `Accepted(0) < Quarantine(1) < Rejected(2)`;`unknown` ⇒ **Quarantine**(`staging/`,复用既有待人工区,**不新造目录、不删除**);沉默 ⇒ **Rejected**(`rejected/`);注册表外值 ⇒ `Error` 不变。
**⇒ 四条验收判据全给**:①隔离而非被拒 RED→GREEN;②比值判据做成**顾问式仪器、故意不接线**(行为未变:`GUARDED` 与 HEAD 逐字相同、非注释改动 0 行);③往注册表加值 ⇒ R005 自动接受且 **reviewer.rs sha256 前后一致**(派生是真的);④空字段不轻于 unknown RED→GREEN。变异证据齐全,**未用 `git reset --hard`**。
**⇒ 副产品:码注册表第一次被真正消费**(build.rs 构建期解析 §1.2,不可达则构建**大声失败**、不静默回退);新增 4 个码 `W-RISK-UNKNOWN`/`E-RISK-MISSING`/`E-RISK-ILLEGAL`/`W-RISK-RESERVED`。提交 `HelixECO-Glove 48a5db5` · `commonintents/.github 88169b9`。

---

### 17.15 「仪器失效」族总表（**四例同形，已升格为通则候选 C**）

| 次 | 判据 | 实际被消费的是 |
|---|---|---|
| 1 | `cargo metadata` 的 crate 依赖 | 模块边（ADR D11 实测） |
| 2 | CI-7 的 diff 模式 | 系统全量状态（全量 369s/240s 跑不动） |
| 3 | L2 用回声返回值推断 | 真实输出 |
| 4 | `git ls-remote` 比对分支尖端 | **`Cargo.lock` 钉住的 SHA** |

**⇒ 第 4 条最干净:此处有三个 SHA —— 本地 HEAD / 远端分支尖端 / lock 钉点;构建消费第三个,而拟判据比对了前两个 ⇒ 虚假安心**(远端已含修复,而构建用的是旧钉点)。修好后 `tuck_gate` 才对含修复的修订跑出 3 passed / 0 failed。提交 `anaphase-helix 8489ad4`。
**⇒ 通则候选(C)**:**判据必须指向「实际被消费的那个对象」,不得指向其代理。** 已进 `CI-144_DNA修订提案.md` 第一批。

---

## 17.16 交接复核批（K-106）

### K-106 ｜**契约把「Anaphase `GateVerdict`」写成了「Tuck `GateVerdict`」——两个器官的类型被并成一个名字**（🟡 **已订正**）

**发现于**：2026-09-21 接手第一小时（复核 I7 的承重引用时）。

**事实（已一手核实，逐字）**：

| 器官 | 实际类型 | 定义位置 | 变体（逐字） | 有 `Err`／「判不出」变体？ |
|---|---|---|---|---|
| **anaphase-helix** | `GateVerdict` | `src/security.rs:58`（另一处 `src/run_cycle/safety_gate.rs:52`） | `Pass` / `Reject` / `NeedHumanConfirm` / `HardOverride` | **无** |
| **Tuck** | **`GateDecision`** | `crates/tuck-core/src/anaphase_bridge.rs:153` | `Pass` / `Reject` / `HitlRequired` / `HardOverride` | **无** |

- **`grep -rn 'GateVerdict' Tuck` ⇒ 0 命中**（全仓，含 `crates`）。Tuck 里没有这个名字。
- **两侧「无 `Err` 变体」这个实质结论都成立** —— 所以这是一处**归属／命名错误**，**不是**一个假事实。这也是它能活下来的原因：每处单独读都"说得通"。

**⇒ 它自己就是通则候选(C)的实例**：判据指向了一个**记错的名字**（代理），而真正被消费的对象是**两个不同器官里的两个不同类型**。与 §17.15 四例同形。

**为什么危险**：`CI-144_默认放行分类表.md` §5.1 **自己写明**「本表只扫了 `anaphase-helix/src/`」「若 Tuck（边缘物理闸）有同类分支，**本表结论不适用于它**」。把 Tuck 写成 `GateVerdict` 的持有者，等于**让一张明示不覆盖 Tuck 的表，看起来覆盖了 Tuck**；而契约 §8 的 L2 又要以这张表为依据。**一处记错的名字，足以让"未扫"看起来像"已扫"。**

**订正（纯事实，未改任何机制／未动 DNA）**：

| # | 文件 | 原文 | 订正为 |
|---|---|---|---|
| 1 | `CI-144_跨链生命周期与反馈契约.md:420` | 「**Tuck 侧**"闸门不存在"…而 `GateVerdict` 无 `Err` 变体」 | 两侧分别指名：Anaphase `GateVerdict` / Tuck `GateDecision`，各附 `file:line` |
| 2 | `CI-144_跨链生命周期与反馈契约.md:168`（§8 L2） | `GateVerdict` 未写器官名 | 补「**Anaphase**」；注明 Tuck 侧同形但名不同，并保留该表自身的适用边界 |
| 3 | `CI-144_默认放行分类表.md:115`（§5.3） | 只写 `GateVerdict` | 补明**这是 `anaphase-helix` 的类型**，非 Tuck 的 |
| 4 | `HANDOFF-0920.md:49` | 「**Tuck `GateVerdict`**」 | 同 1，并直接标注原文有误、指向本坑 |

**⇒ 教训（并入候选 B「覆盖范围 = 声明范围」）**：**引用一张表之前，先读它自己的"未扫"清单。** 一张表的价值有一半在它**声明不覆盖什么** —— 把不覆盖的对象写进引用里，等于把边界擦掉。

**未做（不宣称）**：本次**只订正归属，未核** Tuck 侧是否存在「闸门缺席 ⇒ 全放行」的同类**行为**（分类表 §5.1 留下的开放问题）。**不得据本坑的订正就认为 Tuck 侧已被扫过。**

---

### K-107 ｜**S-1 的选项本身是个假二分：规范强制"无条件放行"，而那个 bit 由未签名字节里的另一个 bit 规范化地取消**（🟡 **依据已钉死；待人类裁**）

**触发**：回答 S-1 的规范层问题（"实现为可授权机制 / 确认不该存在"）时发现**两个选项都不成立**，需先修正问题。

**事实（全部一手，本仓/本生态内自带）**：

| # | 事实 | 出处 |
|---|---|---|
| 1 | 无条件放行**不是代码的默认值，是规范要求**：「`HARD_OVERRIDE` ＝ 灾难场景下**无条件放行，优先级高于任何本地策略**」 | `BIND-19/docs/spec/pfp-xcf14.md:152` |
| 2 | 且规范**明文宣称不可协商**：「此规则是协议的**不可协商部分**」 | 同上 `:154` |
| 3 | 因此 `Tuck/crates/tuck-core/src/lib.rs:430-431` 是**规范的忠实实现**，不是实现者的选择 | 已逐字读 |
| 4 | **PFP 的 4 个字节没有任何密码学保护**；SAP 的签名**只覆盖 PAH（传感器读数）** ⇒ `Risk-Level` / `Override-Flag` / `Replay-Enable` **全在签名之外，帧构造方（AI）可无签名构造** | **K-012**（规范原文逐条） |
| 5 | **同一个未签名的字节里**，`Replay-Enable=0` ⇒ 有效风险被强制为 `MEDIUM` ⇒「**永远无法触发 CATASTROPHIC 硬覆盖**」 | `sap-xcf14.md:195-197`；K-012 连带 |
| 6 | 规范**已自带"可扩展"通道**：SAP 自称「**渐进生长** | 演进层……**未来可扩展新的安全证明机制**」 | `sap-xcf14.md:29` |

**⇒ 结构性事实**：**降级位与放行位在同一个未签名的字节里。** 帧的构造方同时拥有
「把紧急请求规范化地取消」（事实 5）与「把任意帧自述为紧急并放行」（事实 4）两种能力。
两者的共同前提是**事实 4**，而不是"默认值选松了"。

**⇒ 对"成熟实践 / 前沿"的检索结论（已排除了不可用来源）**：
- 检索到的紧急访问一致形态（break-glass / two-person rule / 工业 bypass 管理）在**每一条**上都要求
  **授权 + 可审计 + 与它所旁路的东西相互独立**。**没有一个成熟形态让"被放行的东西自述自己该被放行"。**
- **诚实边界**：IEC 61511-1:2016、CWE-636、以及在检索中出现的 Rockwell / GAO 文档，本次
  **均未取到权威全文** ⇒ 按 §6.6 的既有纪律，**不进入依据**。上述形态是从多个二手入口**一致收敛**得出的，
  **不是**权威条文引用。

**⇒ 所以真正要裁的不是"要不要无条件放行"，而是**：

| 选项 | 含意 | 与已有规范的关系 |
|---|---|---|
| **A（建议）** | **保留**规范的"不可协商"语义，但要求 `Override-Flag` **来自一个可鉴权的来源**（把覆盖范围扩展到 PFP 的判定位 —— 规范**已预留**该演进通道，事实 6） | **不改** `pfp:154`；改的是"谁来断言"
| **B** | 认定"自述即可无条件放行"不该存在 ⇒ 不是"取消放行"，而是**把这条 bit 判为不可信输入并拒收** | 与 `pfp:154` 的**字面**冲突 ⇒ **需先做规范裁决，不是代码裁决** |
| **C** | 保持现状（今天的状态） | 也就是**"自述"＋"未签名"** —— 即事实 4 |

**⇒ 对 §11.4 推进顺序的一处修正**：原建议把 **S-1 排在最后**。按事实 4/6，**S-2（无 SAP 即放行）
是 S-1 鉴权故事的前提**（没有 SAP 就没有可鉴权的来源）⇒ **S-2 的观察期数据应先行**，S-1 不必然最后。

**本次改动**：**零**。本节只钉依据与改动问题，**未动 DNA、未动代码、未动任何闸门**。

---

