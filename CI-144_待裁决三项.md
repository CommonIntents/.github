# CI-144 待裁决三项（**已裁定** 2026-09-18）

> ## 裁定（各一行）
> - **H5 = 锁上**（`soft_reflex` 返回 `Err` ⇒ **fail-closed**），**且必须配门铃**（B22）
> - **H2b = 刻着**（`safety_rules` **静态**），删掉与实现不符的注释
> - **H1 = 放行机制保留，改为白名单功能，归 Tuck（门房）**，分级降级，且必须可管理
>
> **⇒ 第②层「可信任」达成。** 下面保留原始证据链（"现状（实测）"部分**不动**）——
> 它是裁定所依据的东西，删掉就等于把裁定变成没有依据的结论。
>
> **⚠️ 本文件是第三次"把决定搬进人真正会读的文档"**：前两次是 B17 前提清单、幽灵前提。
> 每次都得到同一条发现：**账本不搬进规范，就等于没人读的账本。**

> 2026-09-18 ｜ **这是全项目唯一的瓶颈。** 拆分管线不需要人、工具链该收尾，
> 而过去五十轮里唯一被反复推迟、却最便宜的东西就是这三行。
>
> **本文件只做一件事：让"五分钟"真的是五分钟。**
> 每项给：**现状（实测）→ 真正在问什么 → 每一答的后果 → 落点**。
> **不替你选。** 这三项都是**威胁模型与产品定位**决定，只有你能定。

---

## H5 —— `reflex.rs` 是求生机制还是安全闸门？

### 现状（实测，`reflex.rs` 全文只有 22 行）

| 成员 | 行为 |
|---|---|
| `hard_reflex(action) -> bool` | **deny-list 子串匹配，只能拦、不能放** —— `!any(action.contains(rule))`。**它的形状是闸门。** |
| `soft_reflex(fear, ctx) -> Result<f64>` | 叫恐惧模型拿 `p_death`；调用方在 `p_death > threshold` 时拦 |
| `soft_reflex` 的 **`Err` 分支** | **放行**（`ReflexCheck` 臂：`Err(e) => { warn!(...default allow); Ok(ReflexPassed) }`）—— **它的形状是求生。** |

**⚠️ 所以"二选一"问错了**：**同一个 struct 里同时有闸门和求生。**
`hard` 是闸门（永远只能拦），`soft` 的出错路径是求生（坏了就放行）。
**这就是 H5 五十轮答不下来的原因 —— 它拿一个是非题问一个非是非的东西。**

### 真正在问的（**一行**）

> **`soft_reflex` 返回 `Err` 时（恐惧模型不可用），应当放行还是拦下？**

这是唯一的开放点：`hard` 已经只能拦，`soft` 的成功路径有阈值把关，**只有 `Err` 的方向没定**。

### 每一答的后果

| 答 | 后果 |
|---|---|
| **放行**（= 求生机制） | 现状即正确。**K-007 #3 归「未接线」，B5 撤销**，替代 R1。`Err` 分支的 `warn!` 文案应改成明确表态（现在写的是 `default allow`，方向已对，但没说清为什么） |
| **拦下**（= 安全闸门） | **B5 原样恢复**；`ReflexCheck` 臂的 `Err` 分支改为 `ReflexBlocked`；**且必须在 `warn!` 里写明这是 fail-closed**（P13：放行与拦下的日志响度相同、方向相反时，读者无法判方向） |

### 落点

`src/run_cycle/reflex_check.rs`（**尚未拆分**；`ReflexCheck` 臂 29 NCLOC，在 `mod.rs`）。
**⇒ 这一行答完即放行 `reflex_check` 段的拆分。**

---

## H2b —— `safety_rules` 应当静态还是运行时可更新？

### 现状（实测，关键）

```rust
// src/reflex.rs:7
pub safety_rules: Vec<String>, // Safety constraints loaded from L2
```

**而全仓只有一处生产赋值**：

```rust
// src/main.rs:742
safety_rules: vec!["rm -rf /".to_string(), "shutdown".to_string()],
```

**⇒ 三件事实**：
1. **注释说"loaded from L2"（从记忆加载）—— 而没有加载器。** `config.rs` 里没有它，没有读取点，没有写入点；
2. **它是 `main.rs` 里的字面量** ⇒ 属于本链已记录的**第五类格子：非 CI-144 的跨器官硬编码契约**；
3. **它现在是静态的**（编译进二进制）。

### 真正在问的（**一行**）

> **`safety_rules` 是"源码即边界"（静态），还是运行时可从 L2（记忆）更新？**

**⇒ 这一条的分量在 K-033**：若可运行时更新，**被记忆污染的 `safety_rules` 可以把这个 deny-list 卸掉** ——
那是 H1 之外**唯一能绕过其余全部措施的单点**（第 29 轮原话）。

### 每一答的后果

| 答 | 后果 |
|---|---|
| **静态** | ✅ **1 行修好**：删掉 `reflex.rs:7` 的 `loaded from L2` 注释（它现在与实现不符）。规则就是代码，边界清楚 |
| **运行时** | **需要实现 L2 加载器**（目前不存在）；**同时必须回答"谁能写 L2"（= H2）**，否则引入的正是 K-033 那个洞 |

**⇒ 注意**：**现在就"静态"是零成本的**，因为**当前行为已经是静态**，只是注释在撒谎。

---

## H1 —— 紧急处置动作集中，哪些是 veto 型（延续现状），哪些需要"放行"？

### 现状（实测）

- `hard_reflex` 是 **veto-only**（只能拦）；
- **没有任何"放行"机制存在** —— 没有白名单、没有带外触发器；
- 而 PFP 里有一个 **`Override-Flag`（`pfp-xcf14.md` 3.7，字节 3 位 1）**，
  第 152 行的字面定义是 **"优先级高于任何本地策略"**（= 放行型，第 29 轮读原文坐实）。

### 真正在问的（**一行**）

> **哪些紧急动作必须是 veto 型（只能拦），哪些需要"放行"型（能越过本地策略）？**

**⇒ 若答案是"全部 veto 型"** ⇒ `Override-Flag` 是**不必要的字段**，应从 PFP 移出或改为带外（= 规范修订）；
**⇒ 若确有放行型** ⇒ 落**白名单**（**一个尚未被命名的功能**），**不是 PFP 的一个 bit**。

**⚠️ 且所选机制必须说明其可信来源** —— 否则与 `Replay-Enable` 同构
（一个无签名字段降级另一个；见 `CI-144_更正史.md §6.7.1`）。

---

## 这三行解锁什么

| 答完 | 解锁 |
|---|---|
| **H1 + H2b + H5** | **第②层「可信任」** —— README 的三层状态从 ① 升到 ② |
| **H5** | `reflex_check` 段拆分（现等它） |
| **H2b** | `reflex.rs` 的注释与实现对齐（1 行） |
| **H1** | `Override-Flag` 的去留 + 白名单功能是否立项 |

**⇒ 剩余 3 项（H2 谁能写记忆 / H3 缺陷纳入范围 / H4③ K-015 谁对）不阻塞第②层**，
可以在第②层达成后按自己的节奏答。

---

## 附：「免确认特权像订阅一样可收」这套提议的审查（2026-09-18）

> 提议来自第 50 轮：**免确认 = 一种特权，应当与订阅同构** ——
> ①可见 ②一键可撤销 ③默认过期（不是默认永久），配 TTL + 全局 kill switch。
> **方向我认同，但有两条事实修正、一条顺序修正，且它要求的四个数不是我能定的。**

### ✅ 我认同的（四条，都可测）

1. **顺序不可颠倒：可见 → 可撤销 → 会过期。**
   **"忘了自己订过它，取消按钮再显眼也没用"** —— 这条在本链已有对应物：
   `grandfathered` 种子的迁移窗口、waiver 的 `due`。**它们都是"会过期"那一档，而"可见"那一档刚补上**（`CI-144_未结清单.md`）。
2. **对称判据 M ≤ N**（撤销步数 ≤ 授权步数）—— **可测，且直接命中一个已知反模式**：
   **任何需要"改配置 + 重启 + 找对文件"的撤销路径都违反它。**
3. **走了免确认通道的事件必须留标记。** —— 这是 P13/P16：**事件流里"免确认"与"已确认"长得一样，本身就是一次静默**。
4. **"别把 TTL 设太短"的警告**：太短 ⇒ 人被续订烦死 ⇒ **他会要求永久授权 ⇒ 又回到订阅陷阱，而且这次是他自己要求的。**
   **⇒ 这与 400 行那条是同一条教训：未标定的数字会反过来变成约束。**

### 🔴 事实修正一：AWS 那条引用被说满了（已联网核实）

提议称「AWS STS 临时凭证 vs 永久 access key ⇒ **长期凭证是审计对象，不是默认形态**」。
**AWS 自己的文档不是这么说的。** 检索到的官方页里有一节标题就是
**「有些情况无法使用临时凭证，而您可能需要使用长期凭证」**
（[AWS 安全最佳实践](https://docs.aws.amazon.com/zh_cn/IAM/latest/UserGuide/securing_access-keys.html)）。
**⇒ AWS 的立场是"优先临时、确有例外、长期的要轮换与保护"，不是"长期不是默认形态"。**
**⇒ 原则（优先可过期）成立；那个干净的两分法不成立。**

### 🔴 事实修正二：无人值守时，"显式续订"可能永远不发生

提议的"默认过期"假定**有一个订阅者会来续**。**而这个系统是无人值守的 agent。**
⇒ **默认过期可能静默地把一个能力关掉，而且没有人在场发现。**
**⇒ 这正是订阅陷阱的镜像，而提议只警告了"TTL 太短"，没警告"没人续"。**
**⇒ 判据：过期必须是响的**（K-038 / PC-1 那一族：**一个没人看见的过期不是信号**）——
到期与未续订必须产生与"面板是否打开"无关的可见事实。

### 🔴 顺序修正：先settle 不一致，再加生命周期

**现在有三个组件在回答"检查器不可用时怎么办"，而答案互相矛盾**（实测）：

| 组件 | 方向 |
|---|---|
| `SecurityGate` 的 `HardOverride` | **放行**（`permits()` 含它）—— 且注释写 "audited" |
| `HITL` | **拦**（`hitl.rs` 三处 fail-closed） |
| `soft_reflex` 的 `Err` | **放行**（`default allow`） |

**⇒ 在一致的基线上加 TTL/撤销模型，是把不一致乘上去；先加模型，等于把 K-033 复制三份。**

### 🔴 顺带把 H1 缩小了（读 PFP 3.7 原文）

`pfp-xcf14.md` 3.7 明写：

> `HARD_OVERRIDE` = 硬覆盖（**灾难场景下无条件放行，优先级高于任何本地策略**）
> **CATASTROPHIC 硬覆盖规则**：……接收端**必须**在物理层优先响应……**此规则是协议的不可协商部分。**

**⇒ H1 不是完全开放的**：**冻结规范已经强制了一条"放行型"路径**（CATASTROPHIC + HARD_OVERRIDE），
且称其不可协商。**⇒ 所以 H1 真正在问的是**：

> **在规范已强制的那个灾难场景之外，还有哪些动作需要"放行"型？**

**⇒ 这比"哪些是 veto 型、哪些是放行型"便宜得多，且不与冻结层冲突。**

### ✍️ 提议要我定的四个数：**不是我的**（都是策略）

| # | 问题 | 为什么是人的 |
|---|---|---|
| 1 | 撤销必须"一键、立即"？ | 威胁模型 |
| 2 | 默认 TTL 取值 | **未标定**；提议自己也说要先"只记录不生效"跑观察期 |
| 3 | 查 Tuck 失败 ⇒ 额度算作已用完（fail-closed）？ | 与 H5 同一类：出错方向是策略 |
| 4 | 要不要全局 kill switch？ | 产品定位 |

**⇒ 其中第 2 条我同意"先只记录不生效"** —— 那正是本链一直在做的：**先测，再定数。**
