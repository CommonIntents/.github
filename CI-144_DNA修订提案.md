# CI-144 DNA 修订提案（**需人类批准；本文件不改 DNA.md 一字**）

> 建立：2026-09-20 ｜ 依据：`CI-144_跨链生命周期与反馈契约.md` §10/§11/§12 · `CI-144_合拢坑记录.md` K-059…K-104 · `CI-144_默认放行分类表.md`
> **性质**：提案。`RNA.md` 第 3 条铁律「**禁止修宪 —— 不得修改 DNA.md（可提议，人类拥有最终决策权）**」在七仓重复（helix-mind / lodestone-md / phyt-DNA / Helix-MCP-Learner / helix-tentacle / Tuck / anaphase），`Cellrix/docs/DNA.md:110` 另有「AI 不得修改 DNA.md（宪法）」。
> **⇒ 本文件是提案，DNA.md 一字未动**（`commonintents/.github/DNA.md` sha256 前 32 位 `19c4acd9fcb3794ff7c221f08fe7bc24`）。
> **⚠️ 2026-09-21 更新**：该提案 **A/C/B 已获人类批准并落地**（D/E 仍观察）⇒ **上述 sha256 已失效**。
> 当前 DNA.md 为 **v1.1**，sha256 前 32 位 **`16991bc7b7648b4e45d5236875514fc3`**。裁决见 `docs/ADR-0001-DNA修订-A-C采纳-B单列.md`。

---

## 0. 分批评据：**证据密度**，不是措辞

一条通则该不该进宪法，看它**已经独立出现过几次**。措辞漂亮而只有一例的，是观察；反复出现且跨器官的，才是原则。

| 候选 | 独立实例 | 判据 |
|---|---|---|
| **A. 未知必须显式表示；危险的是「缺失缺省」** | **7** | ✅ **第一批** |
| **B. 机制的覆盖范围必须与其声明的范围一致** | **6** | ✅ **第一批** |
| **C. 判据必须指向「实际被消费的那个对象」，不得指向其代理** | **4** | ✅ **第一批** |
| D. 同根因的修复：定义处必须先统一（一处定义） | **1 案 / 3 站点** | ⏸ 第二批（观察） |
| E. 单调性：不确定性上升，不得导致更宽松的处置 | **2** | ⏸ 第二批（观察） |

**⇒ 本提案只请批 A/B/C。** D/E 的证据密度不足，**登记为观察项**，等它们各自再有实例再提 —— **否则宪法会变成一张写着"我们当时的想法"的纸**。

---

## 1. 第一批（请批）

### A. 未知必须显式表示；危险的是「缺失缺省」

**拟补入 `DNA.md` 原则 4「向后兼容是硬约束」的「工程映射」行**（不新增公理 —— 原则 4 已要求「忽略未知字段」，缺的只是机制）：

> **未知必须在类型或字段级显式可表示，不得用默认值承载。**
> **未知必须在每一跳保留其显式表示（不得被折叠为已知值或默认值）。接收方闭集枚举必须拒绝未知值，且该拒绝必须可观测。**
> **危险的通常不是「错误缺省」，而是「缺失缺省」：空配置、空凭据、以及让「写失败则阻断」在类型层面不可表达的函数签名。**

**七个实例（全部实测，不是推断）**：

| # | 位置 | 形态 |
|---|---|---|
| 1 | `BudgetSnapshot.remaining: Option<u64>` | `None` 显示"未知"而非 0 |
| 2 | `Allow` 强制带 `grant_id` | 无凭据写不出 Allow |
| 3 | `RiskLevel` 缺 `Unknown` ⇒ 认不出落 `Low` | 认不出 ⇒ 权限**更宽** |
| 4 | `rules_path = ""` ⇒ 零规则 ⇒ 每条消息 `Pass` | **缺失即放行** |
| 5 | `upstream_key = ""` ⇒ 转发调用方自己的 `Authorization` | **缺失即转发凭证** |
| 6 | `ledger.rs:40 let _ = chain.append(..)` 且 `record() -> ()` | **「写失败即阻断」在类型层面不可能** |
| 7 | R005：空 ⇒ `Warning`，`"UNKNOWN"` ⇒ `Error` | **反向激励，奖励沉默** |

**外部旁证（已一手核实）**：[RFC 9413](https://www.rfc-editor.org/rfc/rfc9413.txt) §2.2（742 行已取到，章节号逐条核过）：
*"A well-designed extensibility mechanism establishes clear rules for the handling of elements like new messages or parameters… New messages or parameters thereby become **entirely expected**."*
—— **一个新值"比缺席更不被期待"，与 `entirely expected` 恰好相反。**
**⇒ 但结论挂在内部证据上（上面七个实例），RFC 只是旁证。** 理由：把成立的结论挂在单一外部引用上，一旦引用有误，结论会被一并质疑。

**⚠️ 已消歧（否则这条会被实现成 fail-open）**：**「透传」= 保留显式表示，不是让未知通过。** 接收方仍**必须拒绝**，且拒绝**必须可观测**。

---

### B. 机制的覆盖范围必须与其声明的范围一致

**拟立为第七条原则**（或并入原则 3「决策先于变更」的工程映射 —— 归属待裁）：

> **任何机制（闸门、守卫、检查、注册表、合法集）的覆盖范围，必须与其声明的范围一致。**
> **不一致处必须显式登记为一个坑，不得留给「还没排期」。**

**六个实例**：

| # | 机制 | 声称覆盖 | 实际覆盖 |
|---|---|---|---|
| 1 | `cargo metadata` | 模块边 | crate 级 23 条（ADR D11 实测） |
| 2 | CI-7 全量 | 系统状态 | 369s/240s 跑不动 ⇒ 只剩 diff |
| 3 | L2 观测推断 | 真实输出结构 | 回声脚本 |
| 4 | `ls-remote` vs lock 钉点 | **实际被消费的修订** | 分支尖端 |
| 5 | K-104(b) `GUARDED` 正则 | 「Source, assets and ADRs」 | 两次 Cellrix 树 + ADR 前缀（**仓无关**） |
| 6 | K-104(c) 同一注释的**第二次**改写 | 「两次 Cellrix 树，不含别的」 | 同上，**又错一次** |

**⇒ 第 5、6 条是同一处注释**两次写错，而**只有表驱动断言抓到了第二次** —— 这正是"声明会漂移、断言不会"的实证。

**为什么单列**：B 与 A 不同 —— A 说"该怎么表示"，B 说"你声称覆盖了什么，就真的覆盖什么"。**B 的失效是静默的**：机制存在 ⇒ 看起来有保护 ⇒ **没人再去查它覆盖到哪**。

---

### C. 判据必须指向「实际被消费的那个对象」，不得指向其代理

**拟补入原则 1「spec 正文是唯一源真相」的工程映射**（同族：都会"看起来在查，其实查了别的"）：

> **判据必须指向实际被消费的那个对象；指向其代理的判据，是假绿灯的来源。**

**四个实例**：

| # | 判据 | 实际被消费的是 |
|---|---|---|
| 1 | `cargo metadata` 的 crate 依赖 | 模块边 |
| 2 | CI-7 的 diff 模式 | 系统全量状态 |
| 3 | L2 用回声返回值推断 | 真实输出 |
| 4 | `git ls-remote` 比对分支尖端 | **`Cargo.lock` 钉住的 SHA** |

**⇒ 第 4 条是最干净的：此处有三个 SHA** —— 本地 HEAD、远端分支尖端、**lock 钉点**；**构建消费的是第三个**，而拟判据比对了前两个 ⇒ **虚假安心**（远端已含修复，而构建用的是旧钉点）。**修好之后，`tuck_gate` 才对含修复的修订跑出 3 passed。**

---

## 2. 第二批（观察，暂不提）

| 候选 | 现有实例 | 何时再提 |
|---|---|---|
| **D. 同根因的修复：定义处必须先统一；未一起修的站点必须显式登记** | 1 案 / 3 站点（脱敏占位符宽度：生成端、`demap`、SSE carry） | 再有 1–2 个独立案件 |
| **E. 单调性：不确定性上升，不得导致更宽松的处置** | 2（`risk_rating` 认不出⇒`Low`；R005 空 vs unknown） | 再有 1–2 个独立实例 |

**⇒ D 的原表述（「要么一起做，要么都不做」）已撤回**：它与本项目已撤回的「净增 ≤ 0 ⇒ 免检」同形（**不可满足 ⇒ 不是门禁是禁令**）。加固版以"统一定义处"为可判动作，才可立。

---

## 3. 与既有宪法的关系（**不新增悬空引用**）

| 拟议 | 落点 | 理由 |
|---|---|---|
| **A** | 原则 4「向后兼容是硬约束」的**工程映射** | 原则 4 **已**要求「忽略未知字段」；缺的是机制，不是公理 |
| **C** | 原则 1「spec 正文是唯一源真相」的**工程映射** | 同族：都关乎"查的是不是那个东西" |
| **B** | **待裁**：单列第七条，或并入原则 3 | **归属是真问题，我不替你定**：单列会改变"六条公理"这个数字本身 |

**⇒ 三处都不写"将来可能如此"的条款。留位置，不留悬空引用。**

---

## 4. 请裁

| # | 问题 | 我的建议 |
|---|---|---|
| **Q1** | A/B/C 是否采纳？可逐条 | **A、C 采纳**（落点是既有原则的工程映射，不改公理数）；**B 采纳但落点待你定** |
| **Q2** | **B 单列第七条，还是并入原则 3？** | 单列更清楚（它覆盖闸门/守卫/注册表/合法集四类，跨原则）；但**改公理数本身是宪法级动作，须你定** |
| **Q3** | D/E 是否同意"先观察不提"？ | **建议同意。** 证据密度不足时立原则，会让宪法记下"我们当时的想法"而非"反复发生的规律" |
| **Q4** | 落成方式：一次改 `commonintents/.github/DNA.md`，还是各仓 `docs/DNA.md` 各自改？ | **协议家族层改一次**（三条都关于协议家族），各仓引用不复制 —— 理由：唯一事实来源 |

---

## 5. 引用核实状态（不可省略）

| 项 | 状态 |
|---|---|
| **RFC 9413** | ✅ **一手取到原文**（`rfc-editor.org/rfc/rfc9413.txt`，742 行），§2.2 引文为原文，章节号逐条核过 |
| **实例 1–7、B 六例、C 四例** | ✅ 均来自本会话的实测与逐字读码，各自有 file:line 或变异证据，可回到 `CI-144_合拢坑记录.md` 对应 K 条 |
| `DNA.md` 未改动 | ~~✅ `git status` 为空；sha256 前 32 位 `19c4acd9fcb3794ff7c221f08fe7bc24`（可复核）~~ **⚠️ 已于 2026-09-21 失效**：A/C/B 获批落地，DNA.md 现为 v1.1，sha256 前 32 位 `16991bc7b7648b4e45d5236875514fc3`（见 `docs/ADR-0001-DNA修订-A-C采纳-B单列.md`）。**此行的历史值为当时实测，保留不改** |
| **未核** | 本提案**未**引用 CWE-636、IEC 61511、ArchUnit（那三项在本会话中始终未核实，故不进入本提案的依据） |
