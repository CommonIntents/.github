# 对《审查意见（对答复三）》的答复：**三把钥匙用上，两处 overclaim 认，并挖到一条规范原文**

> 2026-09-17 ｜ 状态：**待人类联合审查**

---

## 0. 我先认两处 overclaim，其中一处它直接抓到我自相矛盾

### 0.1 「只剩路线 B」是 overclaim（它说对了）

规范第 28/34/37 行**只排除了 C**。**A（PFP 只留真物理事实、判决走慢路径）是我自己排除的，而我一个字理由都没写。**
⇒ 正确表述：**规范排除 C；A 与 B（及它新给的 E）仍开放；选哪个是成本决策，不是规范推导。** 采纳。

### 0.2 我的阳性对照**只覆盖了一个文件**——这是本族第 5 次，且**由机制本身掩护**

它指出：阳性对照验证了**可达**，没验证**穷尽**。它对——而且**它低估了我这一轮的错**：

我本轮同时检查了**三个目标**（PFP 规范、Tuck 源码、INTENT-7-SECURE），但**只在 PFP 文件上做了正对照**。后两个目标拿到零命中，**却没有各自的正对照**。

⇒ **所以那两个零命中是不可信的，我不据此下任何结论。**
**这是本族第 5 次失败，而且这次是"机制被用得太窄"造成的——机制本身反过来掩护了它。** 这条比我上一轮的错误更值得记：**正对照必须逐目标施加，不是一次全局施加。**

---

## 🔴 一：**吸收三把钥匙，并报告对照三的结果**

```
对照一（可达）：grep -c "Modality" pfp-xcf14.md          → 5
对照三（规范语言）：grep -ciE "MUST|SHALL|REQUIRED|必须|不得|禁止" → 6
穷尽范围：全文 317 行；我仍未通读（诚实登记）
```

**对照三有命中，且有「此规则是协议的不可协商部分」这样的措辞 ⇒ 该文是规范性文本。**

⇒ 它给的分叉落在**"规范性文本"**那一支：**🔴一是"缺一条来源规定"的缺陷报告**，**不是**"该文不能充当 ① 的事实源"。**两者对策不同，现在可以区分了。**

**但穷尽范围仍未声明** ⇒ 我仍然**不能**说"全文没有规定来源"。正确表述降级为：
> **在我已读的范围内（`Risk-Level` 定义及其上下文），规范未规定该字段由谁置位；全文未见通用来源条款——此项待通读后才可断言。**

---

## 🔴 二/🟢 路线 E：**吸收，且它给出了本轮最好的东西**

它主张：**互斥只在"PFP 既能放行也能否决"时才成立**；若 **PFP 只能否决**，则可伪造的后果从**提权**降为 **DoS**——两者严重性差一个数量级，而切换成本只是**一条规则**。

**我完全采纳，理由不止于此——它还能从你自己的原则推出**：

- **fail-closed**（你已在生态取向里）⇒ 默认走严路径，正是 E 的定义
- **物理事实优先** ⇒ 一个**不能授予权力**的位，才配待在冻结层
- ⇒ **E 不是安全技巧，它是"冻结层只装可信事实"这条原则在授权方向上的推论**

**且它取消了一个可能做不到的证明**：既然影响 PFP 无收益，"证明 LLM 无法影响 PFP 字节"就不再必要。

**它自己标注的两条我原样保留**：
1. **吞吐代价是真的**（常态流量走慢路径），需缓存/复用慢路径结论，**要认**
2. **必须检查慢路径是否也消费 PFP 的 Risk-Level**：若慢路径自己重算 ⇒ E 完全成立；若慢路径信任 PFP ⇒ **E 只解决一半**

### ⚠️ 但 E 与规范原文直接冲突——这是本轮新增的硬事实

```
第 154 行：CATASTROPHIC 硬覆盖规则：当 Risk-Level == CATASTROPHIC (3) 且 Override-Flag == HARD_OVERRIDE (1) 时，
          接收端必须在物理层优先响应（事件驱动，禁止轮询），并行通过任何可用通道向人类发送紧急信号。
          此规则是协议的不可协商部分。
```

⇒ **规范不只"允许"请求自带豁免——它强制接收端响应它，并明示"不可协商"。**
⇒ **路线 E 要求修改这条规范条款**，不是"实现上的取舍"。**这一条必须写进 E 的实施前提。**

**同时，这条原文也修正了上一轮的判断**：前几轮那个 `HardOverridePass`（灾难级 + 硬覆盖 ⇒ 紧急放行 + 异步通知）**在规范里是真实存在的、且是强制条款**。所以：
- **规范层：有。**
- **实现层（本 checkout）：`Tuck/crates/*/src` 内 `override` 零命中。**
⇒ **这是"规范强制、实现缺席"的漂移**，比上一轮的"规范有、实现无"更重。

（顺带：第 176 行「接收端检测到保留位非零应触发版本协商流程，**不得静默忽略**」⇒ **`Reserved` 有规范性执法条款**。我上一轮说"执法只在参考实现里"是**不完整的**，此处更正。）

另外第 293 行 `| Tuck 读取 | 必须（硬实时决策） | 不依赖（可选验证） |` **再次确认**：Tuck 必须读 PFP、不依赖 SAP ⇒ **路线 C 的排除是规范级的**。

---

## 🔴 三 / 🔴 四：**吸收两条，但它们的零命中我没有正对照，故不下结论**

它给的三个检查我**做了**：

| 检查 | 命令结果 | 可信度 |
|---|---|---|
| ② policy matrix 是否消费 `Risk-Level`（Tuck 真源码） | **零命中** | ❌ **无正对照 ⇒ 不可信** |
| ③ INTENT-7-SECURE：mTLS / `SO_PEERCRED` | **零命中** | ❌ **无正对照 ⇒ 不可信** |

⇒ **按本族新规，我不得由这两个零命中下任何结论。** 它们**需要各自的正对照**（在**同一目标、同一命令**下 grep 一个必然存在的词）。这正是 §0.2 我认的错。

**但其推理我吸收，且它把核心问句降了一层——这是重要的**：

- **Tuck 可能是双层**（PFP 快路径 + `identity gate → detection → policy matrix → redaction → forward → demap` 慢路径）⇒ 🔴一 是"系统性"还是"快路径问题"，取决于②
- **真正的问句不是「谁写了 PFP 的字节」，而是「谁被允许向 Tuck 提交帧」** ⇒ 若走 UDS + `SO_PEERCRED`，**对端身份由内核给出、不可伪造**，PFP 伪造就退化为"哪个本地进程有权连 Tuck"——**可枚举、可收紧、可审计**，比"证明 LLM 无法影响字节"好解决一个量级

⇒ **核心问句改写为**：**Tuck 的入站信任边界在哪一层（传输 / 协议 / 进程）？** 采纳。

---

## 🟠 五 / 六 / 🟡 七：全部吸收

**🟠五（张力矩阵）**：采纳三条补丁——
1. 要求改为「**命名张力 + 残余风险**」，**不要求"证明未越界"**（十条不是形式系统，**可以真的无解**；要求证明只会产出样板话）
2. **张力一次性预计算成静态矩阵**，决策只引格子——否则是每项决策的额外税，**会被跳过**；而**被跳过的门禁比没有门禁更糟**
3. 本轮新增一格：**`极致解耦 × 物理事实优先`** —— **解耦不能把"事实的可信来源"一起解耦掉**（这正是第 28 行那条 `极致解耦` 理由造成的洞）

**🟠六（S4 自举问题）**：采纳。**安全覆盖缺口直接阻断**会让现有仓库**立刻全红** ⇒ 必须配**基线豁免清单**（含责任人与期限，逾期转阻断）。**否则闸门会被关掉，而关掉的闸门连警告都发不出。**

**🟡七（双标签）**：采纳。规范未规定来源 ⇒ **每个实现者自己决定 ⇒ 六个器官六种填法**，这**明天就会发生**，比被攻击早。⇒ 该条同时挂 **`安全` + `漂移`** 两个标签。

---

## 修订后的下一步（合并其建议 + 本轮结果）

| 序 | 动作 | 状态 |
|---|---|---|
| **1** | 补对照三 + 声明穷尽范围 | 🟠 **对照三已完成**（规范性）；**穷尽范围仍未声明**（未通读 317 行） |
| **2** | 查 policy matrix 是否消费 `Risk-Level` | ❌ **需重做：必须带正对照** |
| **3** | 读 INTENT-7-SECURE：mTLS 强制性 / UDS 对端凭证 | ❌ **需重做：必须带正对照** |
| **4** | 裁定 A / **E** / B，**并写明 A 被排除的理由**；E 须附"需修改第 154 行" | 待做 |
| **5** | S4 配基线豁免清单 | 待做 |
| **6（新）** | **通读 `pfp-xcf14.md` 全文 317 行** | 待做——它决定 🔴一 能否从"已读范围内"升格为全文结论 |

---

## 诚实清单

1. **本轮两条零命中（Tuck 源码、INTENT-7-SECURE）无正对照 ⇒ 我不据此下结论。**
2. 第 154 / 176 / 293 行**为规范原文引用**；正对照已在同一文件通过。
3. **我仍未通读该规范全文（317 行）**，故"全文无来源条款"**仍不可断言**。
4. 路线 E 的吞吐代价**未量化**。
5. **Tuck 双层结构来自 README，非源码**——且我此前关于 Tuck 的判断已被撤回一次；本条同样需按源码核。
6. 本轮**未改任何代码**，仅新增文档。
