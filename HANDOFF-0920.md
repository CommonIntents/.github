# HANDOFF — 2026-09-20 ｜ 跨链生命周期、反馈与扩展性

> **性质**：交接件。覆盖 2026-09-20 一日内跨仓的工作与**待裁决项**。
> **前序**：`helix-mind/docs/helixECO/HANDOFF-0916.md`（**09-16** 两条主线，已提交 `94bad00`）。本件不重复它。
> **本件不替代**：`CI-144_未结清单.md`（B 类）、`CI-144_默认放行分类表.md`（缺省方向）、`CI-144_合并坑记录.md`（K-001…K-105）。

---

## 0. 一句话

**九轮审计收敛成一句结构事实：这条链的每一处保证都是由「缺席」维持的，而缺席是看不见的。**
本日的工作是**把"有没有"变成"可判"** —— 不是把"应该"写得更清楚。

---

## 1. 已交付（全部已推送，`ahead` 全 0）

| 仓 | 提交 | 内容 |
|---|---|---|
| `commonintents/.github` | `54c03d8` | 跨链契约 + 码注册表（新） |
| | `44bb899` | 坑号归并 K-083…K-101 |
| | `fe5bddf` | 契约 §12 跨仓未知透传 |
| | `88169b9` | 注册表 §1.2 RiskLevel 词表 + 4 个码 |
| | `8a43129` | 契约 §13 整链安全与鲁棒 |
| | `2d1df69` | K-104 一族 + K-105 + 仪器失效族总表 |
| | `3a49894` | **DNA 修订提案**（第一批 A/B/C） |
| `Helix-MCP-Learner` | `3ac856b` | `RiskLevel::Unknown` + 补 MCP 声明字段 + 字段级出处 |
| `Tuck` | `c8d6d90` | 脱敏占位符字符边界 + 宽度契约（同根因三处） |
| `anaphase-helix` | `8489ad4` | `tuck-core` 锁钉点前移到修复提交（消一个假绿灯） |
| `helix-mind` | `57a0335` | hooks 接线补齐 14 个仓 |
| | `c9cab5b` / `b1b7d7d` | 守卫范围注释说真话 + 表驱动范围断言（K-104b/c） |
| | `94bad00` | 09-16 两份在飞文件（原 Human confirmation 队列） |
| `helix-tentacle` | `2554ebf` | README 构建指令纠错 + 两条 09-16 审计行 |
| `HelixECO-Glove` | `48a5db5` | **R005 单调性反转修复 + 合法集改由注册表派生** |

**五份审计**（上游链 / 下游链 / Tuck·FlowModus / Cellrix / 各仓缺省分支长扫）**全部只读收尾**，产出 **K-059…K-105**。
**`DNA.md` 在 09-20 当日一字未动**（sha256 前 32 位 `19c4acd9fcb3794ff7c221f08fe7bc24`，当事人可复核）。
**⚠️ 2026-09-21 接手后状态变更**：§3 的 **Q1–Q4 已获人类裁决并落地**（A/C 采纳、**B 单列第七条**、D/E 观察、落协议家族层一次）⇒ `DNA.md` 现为 **v1.1**，sha256 前 32 位 **`16991bc7b7648b4e45d5236875514fc3`**。裁决见 `commonintents/.github/docs/ADR-0001-DNA修订-A-C采纳-B单列.md`；**公理数 6 → 7**。

---

## 2. 在飞（**接手第一件事**）

**I5/I6/I7 三条不变量断言** —— 已派出执行方，brief 载于本次会话；**它尚未开始改文件**（派发后核验：`HelixECO-Glove` / `helix-mind` / `commonintents` 三仓当时全干净）。

| id | 不变量 | 落点 |
|---|---|---|
| **I5** | 覆盖范围 = 声明范围 | 推广 `commit-msg.scope.sh` 那类断言到其余声明 |
| **I6** | 缺失不得比显式未知更宽松（单调性） | R005 侧已修，要的是**能抓到回归**的断言 |
| **I7** | 「闸门在不在」必须可区分于「闸门放行」 | 本日新发现：**Anaphase `GateVerdict`**（`src/security.rs:58`）与 **Tuck `GateDecision`**（`crates/tuck-core/src/anaphase_bridge.rs:153`）**均无 `Err`/「判不出」变体** ⇒ 若"判不出"不可表达，**那本身就是发现**，要登记而非发明 API。**⚠️ 原文写作「Tuck `GateVerdict`」是错的 —— Tuck 里 `GateVerdict` 0 命中；见 K-106** |

**硬约束（brief 已钉死）**：不新建检查器、不碰 `ci/`、不改任何闸门行为（`GUARDED` 正则一字不动）、**不用 `git reset --hard`**、每条必须证明"能红"、已被既有断言覆盖的就引用而不是复制。

**⇒ 接手动作：收它的报告 → 复核 → 提交 → 并入台账。** 若它中途消失，先 `git status` 扫全部 14 个仓（**中断会改文件的子代理会跨多仓留下残留 —— 本日已发生过一次，跨 5 个仓**）。

---

## 3. 待裁决（**权限边界，不是技术问题**）

> **✅ 2026-09-21 接手后已全部裁决（Q1–Q7）** —— 裁决与落地见下表，原始文案保留不改。
> 落地记录：`docs/ADR-0001-DNA修订-A-C采纳-B单列.md`（Q1–Q4）· **K-107**（Q5 的 S-1 问题修正）· **K-108**（约束来源订正 + 流程冲突）。

| # | 事项 | 我方建议 | 为何需要你 | **裁决结果** |
|---|---|---|---|---|
| **Q1** | DNA 提案 A/B/C 是否采纳（可逐条） | **A、C 采纳**（落既有原则的工程映射，不改公理数） | 宪法级 | ✅ **A、C 采纳**（落原则 1 / 原则 4） |
| **Q2** | **B 单列第七条，还是并入原则 3？** | 单列更清楚（跨闸门/守卫/注册表/合法集四类） | **改公理数本身是宪法级动作** | ✅ **单列第七条** ⇒ **公理数 6 → 7**，DNA 升 v1.1 |
| **Q3** | D/E 是否同意"先观察不提"（证据密度 1 案 3 站点 / 2 例） | 建议同意 | 立原则的判据是证据密度 | ✅ **同意，先观察不提** |
| **Q4** | 落点：协议家族层一次改，还是各仓各自改 | 协议家族层改一次 | 唯一事实来源 | ✅ **协议家族层改一次** |
| **Q5** | **§11 四项"改安全默认"**：S-1 Tuck 单比特无条件放行 · S-2 无 SAP 即放行 · S-3 两个空串相等即"完整性已验证" · S-4 账本把所有拒绝记成 Pass | **S-3/S-4 零代价先做；S-2 带观察期；S-1 先答规范问题** | 分类表 §0 规则 1：翻 fail-closed 必须先落「安全判定」类并由人类指出保护的外部动作 | ✅ **S-3/S-4 直接做；S-2 带观察期（shadow 只记不拦）；S-1 先答规范问题** ⚠️ **S-1 的问题本身经复核为假二分，已修正为「谁来断言紧急」——见 K-107，仍待你裁** |
| **Q6** | **K-104(b)**：`GUARDED` 只守 `web/assets\|web/src\|docs/decisions`，而注释曾声称按 "Source" 划 | **正则不改**；要更宽就改成对任意路径设**比值上限** | 改的是"什么会被拦" | ✅ **正则不改**；要更宽走任意路径比值上限 |
| **Q7** | **I2/I3/I4 的三处 Cellrix 未修违反**（未知码渲染成绿 `e-ok` · `0 block` 与"闸门从未运行"不可分 · 4 个来源互不引用） | 需要动窗口渲染语义 | 策略面 | ✅ **维持等策略面裁决**，本轮不动 |

**⇒ DNA 提案的四条依据与全部待裁项文案在 `CI-144_DNA修订提案.md` §4。**
**⇒ 另有一项在接手时才浮现、原清单没有的**：**S-1 的规范问题被证为假二分**（K-107）；**修宪的约束来源被记错、并掩盖了铁律 8 的提交权限冲突**（K-108）—— **后者连带接手方自身的一笔流程违规，已主动登记。**

### 3.1 当前人类队列（2026-09-21 新增；**每项必须有 owner 与 due**）

> **为什么单列**：技术侧已多次出现"一切就绪、只等一句话"。**没有 due 的待裁项会沉淀为永久阻塞** ——
> 与"恒红的门禁"同族（红灯即仪式）。**owner 一栏只有人类能填**，其余是建议，非指派。

| # | 事项 | 卡住什么 | owner | due（建议） | 不做会怎样 |
|---|---|---|---|---|---|
| **H1** | `EXCLUDE` 6 行（`commonintents/{BIND-19,CAPABILITY-13,INTENT-7,INTENT-7-SECURE,PFP-xCF14,SAP-xCF14}`）是否装钩子 | 6 个仓的 ADR 首行闸门与 `[large]` 闸门**不跑** | 人类 | 2026-09-28 | 排除名单长期停在 `pending-human` ⇒ 事实上等于永久豁免（断言仍绿，因为"待批"是合法状态） |
| **H2** | K-112：Tuck 侧 gate 缺席的 **record-only** 记录 | Tuck 侧"没有闸门"仍不可观测 | 人类（**已降级：不需裁决，只需点头**） | 2026-09-28 | 与 H1 同：可修项停在"还没排期" |
| **H3** | `ci-pending-K112.patch`：`ci/pits.toml` 收录 K-112 + `fix_window` cap 14→22 | `tests/ci_line_budget.rs` 两条红 | 人类（`ci/` 保留区） | 2026-09-21 | anaphase 套件保持 2 红 ⇒ 下次真实回归藏在里面看不见 |
| **H4** | `DNA.md` v1.1（原则 7 已写入、原则 5/6 编号已核非空洞）+ `ADR-0001-DNA修订-A-C采纳-B单列.md` **未提交** | 宪法级改动随时可能被下一次清理抹掉（本批已发生过一次 `reset --hard`） | 人类 | **立即**（提交或 `git stash`） | 与 `ci-pending-K112.patch` 同：未提交的成果等于没有 |
| **H5** | §11 四项安全判定（S-1 经复核已是假二分，见 K-107） | 长期 `blocked` | 人类 | — | 已知，且已明示为权限边界 |
| **H6** | `rejected/` 是否补**门铃 + 复审入口** | I6 只修了一半：沉默改判 `rejected/` 后，若该目录无通知即"换了个房间的静默" | 人类 | 2026-09-28 | 证据黑洞 + DoS 向量 |
| **H7** | **`EXCLUDE` 的 `pending-human` 行数必须被报告，且随轮次单调不增**（§15.4，report-only） | 否则是"有字段无消费者"：`status` 存在，却无任何断言消费它 ⇒ 可无限期待批而套件恒绿 | 人类（可与 H1 同批） | 2026-09-28 | 与"只有计数、没有消费者的仪表"同族 |
| **H8** | **K-113 举证导出完整性**：负 span 已复现并落断言；**悬空引用载体待定位**；修改导出行为属策略 | 一条带**负跨度**的文档会以"完整举证"的形态被引用 | 人类 | 2026-09-28 | 举证的可对外复核性退化 |

**⇒ 建议一次性批量裁决 H1–H6**：它们各自的判据都已备好，逐条等待只是把同一个瓶颈重复六次。

---

## 3.2 I7 的两条**边界**（登记，不宣称）

I7 在 Anaphase 侧已落地并全绿，但它**不是**"闸门一定可观测"的保证，只是"在环未满时，缺席有一份记录"：

| # | 边界 | 实测 |
|---|---|---|
| **B1** | `events_cap = 1024`（`knowledge_base/fixture-codex.json:14`）。环满后 `emit_event` **静默拒录**，只累加 `dropped`；该计数经 `GET /v1/agent/events` 暴露 | `src/events.rs:56-59`；`src/main.rs:112` |
| **B2** | `SecurityGate` 只暴露 `check` ⇒ **"装了却从未被调用"与"被调用且放行"仍不可分** | `src/security.rs`；presence 记录只能证明"对象存在" |

**⇒ 两条同属"沉默"族**：B1 使记录在负载下消失，B2 使"存在但无效"不可见。
**⇒ 若 `dropped > 0` 本身不告警，则"丢失"也是静默的 —— 这与"凭证被吃掉"是同一个可观测性缺口。**

---

## 4. 本日确立的四条通则（**提案，非生效**）

| 批 | 通则 | 证据密度 |
|---|---|---|
| **A** | **未知必须显式表示；危险的是「缺失缺省」**（「透传」= 保留显式表示，**不是让未知通过**） | **7** |
| **B** | **机制的覆盖范围必须与其声明的范围一致** | **6** |
| **C** | **判据必须指向「实际被消费的那个对象」，不得指向其代理** | **4** |
| D（观察） | 同根因的修复：定义处必须先统一；未一起修的站点必须显式登记 | 1 案 / 3 站点 |
| E（观察） | 单调性：不确定性上升，不得导致更宽松的处置 | 2 |

**外部旁证只有一个**（已一手取到原文，章节号逐条核过）：[RFC 9413](https://www.rfc-editor.org/rfc/rfc9413.txt) §2.2（742 行）· §5.1；[RFC 9170](https://www.rfc-editor.org/rfc/rfc9170.txt) §2.3 / §3.1 / §3.3 / §4.2 / §4.4（923 行）。
**⇒ 结论一律挂在内部证据上，RFC 是旁证不是承重墙。**
**⚠️ 历史引用纠错**：`wire image` 在 **9413 与 9170 里均为 0 次**（该引用作废）；`ossification` 9413 为 0、9170 为 2；正确出处是 **9170 §2.3 与 §3.3**，不是 9413。

---

## 5. 我在本日犯过并已记录的错（**接手时别重犯**）

| 错 | 教训 |
|---|---|
| 编辑了错误的 proto 消息并据此声称修好 | 已在 K-045 记录：**该改动保留但不得作为修复被引用** |
| 用 `\| tail` 读测试输出，管道退出码掩盖了 FAILED | **差点报了一个绿的假数字。** 读 `PIPESTATUS` 或看显式 `test result` 行 |
| 中断普查后只查了一个仓就说"树干净" | 残留**跨 5 个仓**。**中断会改文件的子代理后，扫它 brief 覆盖的每一个仓** |
| 探针清理用 `git reset --hard`，抹掉自己未暂存的编辑 | **`--hard` 不是探针清理工具**；用 worktree / `git stash` / 只删探针文件 |
| `cd ..` 路径算错，两条 commit 在错误目录执行、报 "nothing to commit" | **用绝对路径**；并且**读输出**才发现，而不是假设成功 |
| 一处守卫范围注释**写错两次** | 抓到第二次的不是人眼，是**表驱动断言**。**文档会漂移，断言不会** |

---

## 6. 未核（**不宣称**）

1. **五份审计的方法均为定向 grep，非穷尽** ⇒ 按 P7，**"穷尽"钥匙未取得**。
2. **未编译任何特性组合**；部署二进制的特性集是**推断，未反汇编取证**。
3. **Tuck 的 19 个非决策模块未逐行读**（只做定向 grep）。
4. **未被实测**：K-071 的 panic、K-072 的错实体替换、K-050 的空串相等（均由逐字读码推出，虽有精确推导）。
5. **FlowModus 无进程运行** ⇒ 其"生效"为入口可达性，非实测。
6. **三项外部引用始终未核**：CWE-636、IEC 61511-1:2016（二手）、ArchUnit / dependency-cruiser（教程与报道） ⇒ **不进入任何依据**。
7. **`lumtract/README.md` 在飞未提交**（+342/−170 的排版规范化，**有意的工作，未删**）；`git status` 显示它 modified。

---

## 7. 接手者的第一小时

```
1. git status  扫全部 14 个仓（找残留在飞改动）
2. 收 I5/I6/I7 执行方的报告 → 复核"能红" → 提交 → 并入台账
3. 若它消失了：重派，brief 用本次会话里那一份（含四条硬约束）
4. 把 §3 的 Q1–Q7 提到人类面前 —— 它们全部是权限边界，不是技术问题
```

**⇒ 不要在没有 §3 裁决的情况下"接线"任何一段不存在的链条** —— 审计已证：Tuck 安全判定代码 **34 处里 20 处是死的，而它们都是 `pub` API 与已验收的 ADR 实现 —— 接线一次，那一整串 fail-open 同时生效。**
