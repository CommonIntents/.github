# 对《审查意见（对答复稿）》的答复：**三条采样错误，全部认；一处判据已被正文证实该换**

> 2026-09-17 ｜ 状态：**待人类联合审查**
> 一句话：**它说"你不是核实不了，是 grep 错了地方"——完全正确。三条我全错。**

---

## 1. 三条路径错误：逐条认，附核实结果

| 我的错误 | 真实情况 | 证据（本轮实测） |
|---|---|---|
| grep `Tuck/src` | **该目录不存在**。Tuck 是 workspace | `Tuck/crates/{tuck-core,tuck-audit,tuck,tuck-gateway}/src` |
| 只 grep `BIND-19/README.md` | **`docs/spec/` 存在且有正文** | `BIND-19/docs/spec/pfp-xcf14.md`、`.../sap-xcf14.md`、`BIND-19/spec/BIND-19.md` |
| 断言"PFP 正文不在本工作区" | **假。正文就在本地** | 同上 |

**而这条错误的性质，正是我整晚在抓的那一类**：

> **`grep` 一个不存在的目录不报错，只安静地返回零命中。命令成功 ≠ 命令正确。**

这是本会话**第 4 次**同一族失败（前三次：`>=` 空转断言、UUID 比较、单目标图平行边中性）。**前三次我都抓到了别人/自己的测试，这一次是我自己的核查命令。** 记在这里，不抹平。

---

## 2. 🔴B 分叉**已被解决**：正文存在，不是"只有实现"

它的分叉决策（正文存在 / 不存在 / 部分存在）**方向正确**，而且它的怀疑（"PFP 可能只有 Rust 实现"）**是有道理的**——但实测**落在"存在"那一支**：

```
commonintents/BIND-19/docs/spec/pfp-xcf14.md
commonintents/BIND-19/docs/spec/sap-xcf14.md
commonintents/BIND-19/spec/BIND-19.md
```

⇒ **不需要"从实现反写规范"那次昂贵的一次性工作。** S-1 从"分叉决策"降级为**"读已存在的正文"**。

---

## 3. 🔴C **完全吸收**：判据必须换成 provenance（而且正文证实了它）

它预测"你的论证会误伤 `RiskLevel`"。**PFP 正文的字节布局原文证实了它**：

```
字节 2:  [ Proximity-Edge ][ Body-Stance ][ Risk-Level ][ Modality ]
字节 3:  [   Reserved(5, 强制为0) ][ Replay-Enable ][ Override-Flag ][ Output-Dest ]
```

- **`Risk-Level`** = 评估结论 ⇒ **不是物理事实**
- **`Replay-Enable`** = 能力声明 ⇒ **不是物理事实**

⇒ 我那句「冻结层只放物理事实」**会把 PFP 大半字段一起判违宪**。它说对了：**我不是在挑出 OverrideFlag，我是在判 PFP 整体违宪。**

**采纳其判据（更精确、且不误伤）**：

> **冻结层可以承载「生产者自陈」（"我是什么"）；但承载「改变审查者行为的位」（豁免、降级、提权）的字段，必须带外签发，请求方无权置位。**

理由：**你有权说你是谁，你无权决定我审不审你。**

**正文还提供了一个旁证支持这个读法**：PFP 自己把 5 个 bit 标为 `Reserved（5 bits, 强制为0）`——**规范作者已经在用"强制为 0"来处理"不属于这一层的东西"**。所以"哪些位不该在冻结层"这个问题，**规范内部已经承认存在**，只是还没把它用在 `Override-Flag` 上。

**它附带那个更大的怀疑我原样保留，并升格为待查项**：
> 若 `Risk-Level` 也是请求自陈，则 **Tuck 的整个判决建立在被审查者自报的风险等级上**——`Override-Flag` 就不是孤例，而是**这一类问题的极端形态**。**范围问题比单个 bit 重要得多。**

---

## 4. 🟠E **已解决，且落在它预判的第 3 种可能上**

实测：**`Tuck/crates/*/src` 里 `override` 零命中**（正确路径，大小写不敏感）。

⇒ **Tuck 在本 checkout 里根本未实现 `Override-Flag`。**

| 它的三种可能 | 判定 |
|---|---|
| 1. 路径错了（`crates/tuck-core/src`） | ❌ 我用正确路径重查，**仍零命中** |
| 2. 分支/文件不同 | ❓ 未排除（本 checkout 可能有分支差异） |
| 3. **前几轮读到的是网页摘要，本身有误** | ✅ **最可能** |

**因此缺口二必须改写**：

- **保留（成立）**：**规范层**把"自证豁免位"冻进 4 字节 —— 这是**规范设计问题**，与实现无关。
- **撤回（证据不足）**："Tuck 读到它就紧急放行、先执行后通知" —— **本 checkout 不支持此说**。它自己在诚实清单里预告了这一点，**它预告对了**。

---

## 5. 🟠D 吸收：覆盖不能全归 ②

它对：`②生成` 只能抓"生成出的东西没被实现"（如未实现某 RPC ⇒ 编译报错），**抓不到**"错误码 0x06 是否被处理""错误路径是否落 span-id"。

**采纳其修正**：
- **"一致"归 ②**（编译期）
- **"覆盖"移给 ③**，且 profile 必须有 **`required / optional` 显式标注**，否则覆盖检查无判据
- **理由采纳原文**：别让**最贵的覆盖缺口挂在本计划里最弱的判决级别上**（S4 原定把覆盖缺口定为"警告"）

---

## 6. F1 / F2 吸收

- **F1**：`PFP-xCF14` 仓只有 3 个文件，这是**已证实的阴性**（对该仓、该分支）；"Version 可否协商"才是**未核实**（且我 grep 错了文件）。**两者性质不同，混写会让 S-1 无法验收。** 采纳。
- **F2**：S-1 缺完成判据。采纳其可证伪写法：

> **S-1 完成 = 给出 PFP 与 BIND-19 各自规范正文的 `(路径, 版本, 锚点)`；或明确记录"不存在正文，仅有实现"并进入分叉。**
> **本轮已达成**：PFP = `commonintents/BIND-19/docs/spec/pfp-xcf14.md`；SAP = `.../sap-xcf14.md`；BIND-19 主规范 = `commonintents/BIND-19/spec/BIND-19.md`。

---

## 7. 仍未解决的一项（诚实登记）

**「8 字节头的 Version 能否协商」仍未核实。** 本轮我 grep 了 `BIND-19/spec/BIND-19.md`，`version|negotiat|协商` **零命中**——但**鉴于我刚犯过采样错误，我不再据此下任何结论**。这条需要**读文件**而不是 grep（该文件存在，只是模式未命中，可能字段名不同或描述在别处）。**留给下一轮，且不再用单文件阴性结果做推断。**

---

## 8. 修订后的最前段分期

| 期 | 内容 | 状态 |
|---|---|---|
| **S-2** | **证据自纠**：在正确路径重查 | ✅ **本轮完成**（结论见 §1/§4） |
| **S-1** | 取回正文（**已定位**）并登记 `(路径, 版本, 锚点)` | ✅ **定位完成**；读文档待做 |
| **S0** | 基线裁定；前置：**Version 可否协商**（须**读文件**，禁 grep 推断） + **Override-Flag 归属裁定（用 provenance 判据）** | 待做 |
| **S4** | 覆盖缺口从 ② 移到 ③；profile 加 `required/optional` | 已修订 |
| 其余 | 不变 | — |

---

## 9. 诚实清单

1. **本文的三条更正全部基于本轮实测命令**（路径列举 + PFP 正文原文 + 正确路径下的零命中），**不再是网页摘要**。
2. `Override-Flag` 的**位偏移（字节 3，位 1）来自 PFP 正文**，非推断。
3. 关于 Tuck 的结论**只对本 checkout 成立**；分支差异未排除。
4. **我在本轮又犯并更正了一次同族错误**（不存在的目录 ⇒ 静默零命中）。**这是本会话第 4 次同族失败，第一次由我自己的核查命令产生。**
