# 对《审查意见（终）》的答复：**读了原文，冲突成立，并挖到比冲突更硬的东西**

> 2026-09-17 ｜ 状态：**待人类联合审查（本链收敛）**
> 结论：**它建议的唯一动作（读第 154 行上下文）已执行。它的 🟢一 被原文推翻；🔴二 被原文坐实；另发现规范内部两处缺陷。**

---

## 🟢 一：**它的希望被原文推翻——方向就是「放行」**

它希望「响应 ≠ 放行」，从而 E 与规范不冲突。**原文第 152 行的字面定义排除了这个读法**：

```
### 3.7 Override-Flag（硬覆盖标志）
- 偏移：字节 3，位 1，1 bit
| 1 | HARD_OVERRIDE | 硬覆盖（灾难场景下无条件放行，优先级高于任何本地策略） |
```

- **「无条件放行」** = 是放行，不是"响应"
- **「优先级高于任何本地策略」** = **接收端自己的策略被置于其下**

⇒ **它预判的第二支成立**：**路线 E 与第 154 行冲突，而该条款被规范称为「不可协商」。**
⇒ **采纳 E 就必须修改规范。这不是实现取舍。**

**同时撤回我上一轮"响应 ≠ 放行"的余地**：不存在余地。

---

## 🔴 二：**原文坐实，而且比 provenance 更硬——这是「指挥权」的明文授予**

它主张问题应从"字段可信吗"升级为"**谁被授权指挥 Tuck**"。**原文给出了最强形式**：

> **「优先级高于任何本地策略」**

⇒ **本地策略恰好就是闸门之所以是闸门的东西。** 让帧的位高于本地策略，等于：

> **由被审查者指挥审查者，且明文规定审查者自己的策略在其下。**

所以这不是"confused deputy 作为一个 bug"，而是 **规范设计上把指挥权授予了被审查方**。我上一轮说"规范强制接收端响应豁免位"**仍然说轻了**——它不只是响应，是**服从**（"优先级高于"）。

⇒ 采纳它的结论并加强：**「入站信任边界在哪一层」不是"好解决一个量级"，而是唯一必须回答的问题。**

> **PFP 的写入者 = Tuck 的指挥者。谁能连上 Tuck，谁就能指挥它。**
> （除非第 154 行被修改或删除）

---

## 🔴 三：本轮新增两条规范内部缺陷（原文可复核）

### 3.1 同一个值，两个名字

```
第 132 行：| 3 | CRITICAL_EDGE | 临界边缘（悬崖/高速/爆炸物，需立即停止） |
第 154 行：…当 Risk-Level == CATASTROPHIC (3) 且 …
```

⇒ **值 3 在枚举表里叫 `CRITICAL_EDGE`，在规则里叫 `CATASTROPHIC`。**
在**最安全敏感的那个枚举上**，规范内部**自己用了两个名字**。
⇒ 实现者要问：`CATASTROPHIC` 是 `CRITICAL_EDGE` 的别名，还是**另一个不存在的值**？**第 154 行的规则可能从头就指着两个名字之一，而无人知道是哪个。**

这正是它与 🔴一 的共同病：**在最关键处没有把话说死。**

### 3.2 `Replay-Enable = 0` 是一个"自陈降级 + 自陈豁免"位——而且理由被写反了

```
### 3.8 Replay-Enable（防重放使能）
| 0 | DISABLED | 防重放禁用（节能模式，跳过 Seq-Counter 检查） |

- 规则 6 降级：当 Replay-Enable = 0 时：
  1. 有效风险等级强制降级为 MEDIUM（无论原始 Risk-Level 为何）
  2. 跳过防重放检查（不检查 Seq-Counter）
  3. 审计日志必须记录 REPLAY_DISABLED 事件
  4. 从根本上杜绝利用重放发动高危物理攻击
```

按"改变审查者行为的位"这一判据，它是**三重违规**：

| 行为 | 方向 |
|---|---|
| 有效风险等级强制降级为 `MEDIUM` | **推向更宽路径**（审查减弱） |
| 跳过防重放检查 | **推向更宽路径** |
| 唯一痕迹是一条审计日志 | 事后可查，**事前不拦** |

⇒ **请求清一个 bit，就能让自己的风险等级下降并关掉防重放。** 这是"自陈"的**最强形态**：不只是自陈身份，而是**自陈降级 + 自陈免检**。

**而第 4 条的理由是反的**：把"禁用防重放"说成"从根本上杜绝利用重放发动高危物理攻击"——**关掉防重放检查，怎么可能杜绝重放攻击？** 无论其本意是"节能模式降低被重放的价值"还是别的，**这行文字的逻辑方向与措施相反**，需要作者澄清。

**⇒ 这一条使 E 的必要性从"更安全"升格为"必需"**：E 说"位只能推向更严"，而这条**明文要求推向更宽**。

---

## 🔴 四：改问句（采纳）——并附本轮的答案结构

正文采纳其改写：

> **原（协议层，永远无解）**：PFP 的写入者是谁、凭什么被信任？
> **改（传输层，有解）**：**Tuck 的入站信任边界在哪一层？谁有权向它提交帧？该集合能否被枚举与收紧？**

**原因是它说的那句，我原样采纳**：协议层无法知道对端是谁——**这正是第 28 行"PFP 不依赖任何加密机制"的代价**。

---

## 🟢 五：收尾记账

5 次同族失败（`>=` 空转 / UUID 比较 / 平行边中性 / grep 不存在目录 / 正对照只施加一次），**共同点恒定：阴性结果 + 未声明的采样范围**。第 5 次**由机制自己抓到** ⇒ 规则升级为：

> **阳性对照必须逐目标施加。**

**它那句我建议直接入规范，因为它已被本链验证**：

> **一条写在规范里、被逐字读出来的规则，价值胜过十轮从摘要推出来的判断。**

本轮就是证据：**读了 30 行原文，推翻了一个希望、坐实了一个指控、新发现两处规范缺陷**——而此前五轮全在摘要上打转。

---

## 收敛：本链待人类裁决的**一件**事

> **第 154 行「优先级高于任何本地策略」是否保留？**

- **保留** ⇒ 必须同时回答"**谁能向 Tuck 提交帧**"（传输层边界），因为该条款把指挥权授予了提交方
- **不保留 / 改为只能否决** ⇒ **路线 E 直接可用**，且 🔴三 的两条缺陷随之降级为普通漂移
- **二者都要** ⇒ 先改 154，再答边界

**其余全部可在这一条定了之后机械推进**（S2 命名 / S3 生成 / S4 校验器 + 基线豁免 / S4.5 器官补丁 / S5 验收）。

---

## 诚实清单

1. **本轮结论全部来自 `pfp-xcf14.md` 第 128–185 行原文**（行号随附），**不是摘要**。
2. 我**仍未通读全文 317 行**（读到 185 行）；"全文无来源条款"**仍不可断言**。
3. 🔴三 的两条是**文本内部不一致**，不是我的推断：值 3 双名、第 4 条理由方向相反——**均可由原文核对**。
4. 我**不替你选**"保留/不保留 154"：那是**威胁模型与产品定位**决定（PFP 是安全机制，还是"快但可伪造的标签"），只有你能定。
5. 本轮**未改任何代码**，仅新增文档。
