# CI-144 决策索引（唯一入口）

> 建立：2026-09-17 ｜ 用途：**回答"现在该做什么"**，而不是增加论述。
> 起因：本目录文档已达 17 份而代码未改一行。审查指出——**文档增长远快于代码，而文档不会被执行、其错误也不会被发现**；若不加以约束，它会成为**第 18 个器官**，且长在最难维护的"之间"。
> 因此本文件是**收敛器**：每份文档必须在下面被归类，且归类只有三种。

---

## 0. 归类规则（本目录的硬约束）

| 类 | 含义 | 要求 |
|---|---|---|
| **A. 待决策** | 有人类决定才能推进 | 必须写明**决定的形式**（能被回答的句子） |
| **B. 待执行** | 无需决策，可直接做 | 必须写明**完成判据** |
| **C. 背景** | 已完成的记录，不再驱动动作 | 只作追溯，不进决策链 |

**判据**：做不到 A/B 的文档，**降级为 C**。**不予保留"既不决策也无动作"的中间态。**

---

## 1. A 类：待人类决策（**只有 4 条，且形式已修正**）

### A1 ｜紧急处置的真实需求，枚举成**动作集**是哪些？

**为什么是这个问题（而不是"第 154 行保留与否"）**：
「保留与否」**没有可回答的形式**——① `CATASTROPHIC` 与 `CRITICAL_EDGE` 是否同一值**至今未定**，若前者不存在则 154 是**死条款**，删它零成本；② 删也不行，因为紧急处置（停机/撤离/断电）的需求是**真的**。
⇒ **真正的决定不是留/删，而是"用哪种机制承载那个真实需求"。**

**必须先枚举**：反射已证实是 **veto-only**（`reflex.rs:13-15`），不需要任何放行权。所以若存在需要"放行"的紧急动作，它**不是反射**，而是一个**尚未被命名的功能**（候选例：立即外呼求助、强制上报）。**它的正确落点是白名单里的一项，不是 PFP 里的一个 bit。**

### A2 ｜`safety_rules` 是**静态**还是**运行时可更新**？

- 静态 ⇒ 写入者 = 源码仓库，边界清晰
- 运行时可更新 ⇒ 进入 A3

### A3 ｜**L2 就是记忆层吗？**

**这是比"谁能写"更根本的一支。** 若 L2 是记忆层，则 `safety_rules` **天然与不可信内容共处一个存储** ⇒ **不是权限问题，是可信边界画错了地方**：*把基因锁的规则，和可被污染的内容，放在同一个存储里。*
⇒ 修法随之变成**物理分开**（规则带外/签名；内容走 L2），**而不是加写入校验**。

### A4 ｜机制选择：**E1**（闸门单向 + 白名单）还是 **E2**（带外签发 + reason）？

| | 含义 | 代价 |
|---|---|---|
| **E1** ✅建议 | PFP **只能否决**；紧急动作走**白名单**；154 **自动作废**（无需保留也无需删除，只需一条"不再要求放行位"的条款） | 常态流量走慢路径（需缓存）；反复触发 ⇒ DoS ⇒ **必须配冷却/re-arm** |
| **E2** | 保留放行语义，改为**带外签发 + reason** | **需要证明"LLM 无法影响帧字节"——在一个 LLM 驱动的躯体里极难给出** |
| E3 | 154 是死条款 ⇒ 直接清除 + changelog | 若 A1 枚举出真实需求，则 E3 不完整 |

**E1 的决定性理由**：**它是唯一不需要那个极难证明的选项。**

---

## 2. B 类：待执行（无需决策，附完成判据）

| # | 动作 | 完成判据 | 归属 |
|---|---|---|---|
| **B1** | 补 **SAP 28B 全文阅读**（Seq-Counter + PAH-Hash + 64-bit signature）——**它才是可能被设计成"签名覆盖 PFP"的那一层** | 给出"PFP 是否被 SAP 覆盖"的**规范级**答案 | CI-144 |
| **B2** | 通读 **PFP 317 行**（现读约 30 行） | "全文无来源规定"可断言或推翻 | CI-144 |
| **B3** | 通读 **BIND-19 `spec/` 正文** | 给出帧头 Version 语义（可否协商），带**阳性对照** | CI-144 |
| **B4** | **`is_high_risk` 改判据维度**：从**命令名**改为**能力声明**（Tentacle 契约已有 `identity_labels` 形状） | 分类依据不再是字符串；`sh -c`/`python -c`/`env`/`xargs` 不再漏报 | Anaphase |
| **B5** | `hard_reflex` 改 **fail-open → allow-list** | 未知动作默认**拒绝** | Anaphase |
| **B6** | `HardOverride` 补 **reason + 带外触发条件**（保留能力，约束触发） | `HardOverride` 与 `Pass` **不再同路径** | Anaphase |
| **B7** | 复核闸门 `None` / `PermissiveGate` 缺省语义 | 可选缺省到**严**，不是到"无闸" | Anaphase |
| **B8** | S2 命名对齐（**Cellrix 27 → Anaphase 8 → Mind 2 → FlowModus 1**） | 旧名命中 **37 → 0** | 全生态 |
| **B9** | S3 事实源单一化 + 生成 + **golden 对照** | 副本数 2 → 1；生成物与手写物**字节/行为等价** | CI-144 |
| **B10** | S4 校验器 + **基线豁免清单**（含责任人与期限） | 人为植入漂移 ⇒ 变红；清单内豁免、逾期转阻断 | CI-144 |
| **B11** | S4.5 器官侧补丁：复合游标 `(period, seq)` + 物化快照 | S5③ 可过 | Anaphase + Cellrix |

**新增机制（本链产出，建议入家族规则）**：
- **阴性结论必须带阳性对照，且逐目标施加**
- **单引一条原则的论证不予通过**（须命名张力 + 残余风险）
- **变更 ① 必须引用坑 ID**

---

## 3. C 类：背景（已完成，不驱动动作）

| 文档 | 性质 |
|---|---|
| `CI-144_合拢坑记录.md` | K-001～K-006，其中 K-004/K-005 已修 |
| `CI-144_之间的形式_计划与审查.md` | S-1～S6 分期（已并入本索引 B 类） |
| `CI-144_之间_审查答复.md` ~ `答复六` | 六轮审查与被审查的往返记录 |

**它们不再各自驱动动作**；动作已全部收进 §1/§2。**保留作追溯，不进决策链。**

---

## 4. 必须记住的边界（防止误以为收尾）

> **本链是「决策链收敛」，不是「缺陷清点完成」。**

| 未读面 | 状态 |
|---|---|
| PFP 规范 | 317 行，读约 30 行 |
| **SAP 28B** | **全文未读** |
| BIND-19 `spec/` | 未通读 |
| `reflex.rs` 其它反射路径 | 未查 |
| `hitl.rs` 全文 | 未读 |
| `security.rs` 全文 | 未读 |

**下一个读规范的人可能在第 200 行发现第 4 处缺陷。** 在 B1–B3 完成前，**不得宣称"规范已核清"**。

---

## 5. 本索引自身的检验

按 §0 的规则，本文件必须做到 A/B：
- **A 类**：4 条，每条都是**可回答的句子**（是/否、或枚举）
- **B 类**：11 条，每条都有**完成判据**
- **C 类**：明确降级，不再驱动动作

**若本文件将来也变成"既不决策也无动作"，它应被降级为 C——包括它自己。**

---

## 6. 对《收敛纪要》的四条更正（作用于动作，不只是措辞）

### 6.1 ⚠️ 纪要 §1-#11「`Override-Flag` 规范有、实现无」**不准确**，且会做错动作

**实测**（答复六，源码逐字）：

| 层 | 事实 |
|---|---|
| 规范 | 有，且是**规范性强制**条款（`pfp-xcf14.md:152/154`） |
| **Anaphase** | **有** —— `security.rs:45-52` `GateVerdict::HardOverride`，`permits()` = `Pass \| HardOverride` |
| **Tuck** | **无** —— `Tuck/crates/*/src` 内 `override` 零命中（正确路径，已核） |

⇒ **"实现无"是错的：能力在 Anaphase 存在，只是 Tuck 没有。**
**这条会做错动作**：若按"声明有 / 实现无"去写 ③ 的第一个 fixture，**它会在 Anaphase 上失败**。
**正确表述**：*同一语义，Anaphase 用「闸门自判」实现、Tuck 无实现、规范用「请求自陈」表达——三者互不一致* ⇒ **fixture 必须钉住具名的消费端**，不能只写"实现无"。

### 6.2 纪要 §3-O4 的行数**需更正**（数字要对）

PFP 规范**317 行**；**我完整读的是第 128–185 行**（其余为 grep 命中行）。
⇒ 未通读约 **259 行**（不是 287——那按"只读 30 行"算的）。
⇒ 并且**"全文无来源规定"仍是条件性结论**，P7 的"穷尽"这把钥匙**尚未取得**。

### 6.3 纪要 §1-#3 的证据等级**应下调**

「改名 **+ 重构**（不是改名）」来自外部摘要，**我未核实**。
我**实测过的**只有**旧名残留计数**（Cellrix 27 / Anaphase 8 / Mind 2 / FlowModus 1 / Tuck 0 / tentacle 0）。
⇒ 该条应标为 `摘要（未核）`，与 #6–#8 的 `源码逐字` **不同级**。

### 6.4 纪要 §4-第 3 步「打 `v0.9-draft`」**缺执行目标**

**CI-144 作为家族没有单一仓库**（6 个协议各自独立仓 + `.github` 组织仓）。
⇒ tag 与 changelog **必须落在 `commonintents/.github`**（本目录），否则该步无处执行。
本文件所在位置即目标。

---

## 7. 更正后，待你裁决仍是**四项**（形式不变）

| # | 问题 | 输入 |
|---|---|---|
| **O1** | 紧急处置的真实动作集枚举 | 一张表 |
| **O2** | `safety_rules` 静态否 / L2 是否记忆层 | 读 `lifecycle.rs` 与 L2 定义 |
| **O3** | **E1**（单向闸门 + 白名单）还是 E2（带外签发） | O1 的结果 |
| **O4** | SAP 28B 全文 + PFP 未通读约 259 行 | 一次通读 |

**我此前记的"两件"已按审查修正为这两组**；**其余全部机械可执行**（见 §2 B1–B11）。

