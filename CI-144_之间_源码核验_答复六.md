# 对《反射 / 白名单 / HARD_OVERRIDE 源码核验》的答复：**四条全部逐字证实，并多挖一条**

> 2026-09-17 ｜ 状态：**待人类联合审查（本链收敛）**
> 方法：它自陈"来自 GitHub 摘要片段"，故按本链新规**先核本地源码再吸收**。

---

## 0. 核验方法（含正对照）

```
正对照：ls src/*.rs | wc -l  → 18        ← 路径与命令有效
文件：src/reflex.rs、src/security.rs、src/hitl.rs 均存在
```

---

## 1. 四条断言：**全部逐字证实**

| # | 它的引用 | 源码原文 | 判定 |
|---|---|---|---|
| ① | `hard_reflex` 黑名单 + fail-open | `reflex.rs:13-15`：`pub fn hard_reflex(&self, action: &str) -> bool { !self.safety_rules.iter().any(\|rule\| action.contains(rule)) }` | ✅ **逐字一致** |
| ② | `HardOverride` 与 `Pass` 同路径、无 reason | `security.rs:45-46`：`/// Emergency pass (audited). Proceeds on the same execution path as Pass.` + `HardOverride,`；`permits()`（:51-53）= `matches!(self, Self::Pass \| Self::HardOverride)` | ✅ **逐字一致** |
| ③ | `None` = 零成本默认 ⇒ 无闸 | `security.rs:13-15`（模块文档）：`None` gate = legacy behavior; **the pipeline runs exactly as before (110-test baseline untouched)**. Pass / HardOverride proceed; Reject / HitlRequired block... | ✅ **逐字一致** |
| ④ | `PermissiveGate` 恒 `Pass` | `security.rs:75-82`：`async fn check(...) -> GateVerdict { GateVerdict::Pass }` | ✅ **逐字一致** |
| ⑤ | HITL 靠字符串分类 | `hitl.rs:35-43`：`pub fn is_high_risk(command: &str) -> bool` + 子串词表 `WRITE`/`NETWORK` | ✅ **逐字一致** |

**⇒ 它这一轮的准确率是 100%**（与前几轮它自陈"来自摘要"的部分不同）。**这一条值得记：它这轮的引用全部可复核，而它诚实标注了来源等级。**

---

## 2. 🔴 我多挖到的一条（它没提，且踩在你的原则红线上）

```rust
// hitl.rs:35-43
pub fn is_high_risk(command: &str) -> bool {
    const WRITE: &[&str] = &["rm","mv","cp","mkdir","touch","truncate","dd","shred","write","delete","remove","unlink"];
    const NETWORK: &[&str] = &["curl","wget","nc","ncat","ssh","scp","sftp","http","https","fetch","post","send"];
    ...
}
```

**两张安全判定词表是内联 `const` 数组，不是配置来源。**

⇒ **这直接违反你的 `0硬编码`** —— 而且违反在**"哪些动作算高风险"这个最安全敏感的判定上**。
⇒ 并且它是**子串匹配**：`"rm"` 会命中 `"confirm"`（**误报**），而 `"rm "`、`"RM"`、`"rm.exe"`、`"r\\m"`、`"r\"\"m"` 全部**绕过**（**漏报**）。

**与它 §2 的发现合起来，两个闸门的薄弱点是同构的**：

| 闸门 | 判定方式 | 默认 | 违规原则 |
|---|---|---|---|
| `hard_reflex` | 子串黑名单（规则来自 L2） | **fail-open** | 默认方向 |
| `is_high_risk`（HITL 前置） | 子串黑名单（**内联硬编码**） | fail-closed（但分类可绕） | **`0硬编码`** |

⇒ **`0硬编码` 被违反的地方，正好是安全判定。** 这一条建议并入它的第 8 节清单。

---

## 3. 它的核心结论：**吸收**

### 3.1 反射是 veto-only ⇒ 不需要 `HARD_OVERRIDE`（它 §1，成立）

源码证实：`hard_reflex` 返回 `bool` 且只用于阻断；`soft_reflex` 返回 `p_death`，是**评估**。**两层都没有"执行"语义。**

⇒ 采纳：**`CATASTROPHIC + HARD_OVERRIDE` 不是反射的通道。反射从来不需要通行证。**
⇒ 这也**终止了我此前那半条假设**（"灾难性请求 = 条件反射"不成立）。

### 3.2 规范与实现**各错各的**（它 §4，成立且重要）

| | 谁产生"无条件放行" | 可被不可信内容影响？ |
|---|---|---|
| **规范**（PFP `Override-Flag`） | **请求携带的位** | ✅ 可（自陈豁免） |
| **实现**（`GateVerdict::HardOverride`） | **闸门自己判决** | ❌ 不可 |

⇒ **同一个语义，两层建立在相反的信任模型上。两者都不对，而且互相矛盾。**
这比我上几轮说的"一致地错"更准确：**不是一致，是各错各的。**

### 3.3 双层可选 = 双层 fail-open（它 §5，成立）

规范侧 PFP `optional` + 实现侧 `None`/`PermissiveGate` 缺省 `Pass` ⇒ **两层同时缺省 = 完全无防护，且不报错。**

⇒ 采纳其判据：**"可选"本身合理（极致解耦、零成本接入）；错的是缺省方向——可选应缺省到严，不是缺省到无。**

---

## 4. 白名单思维：采纳，并采纳其"两张表"的切分

| | 作用 | 位置 |
|---|---|---|
| **A. 动作白名单** | 哪些**外部动作**可执行 | Anaphase 反射/审核层 |
| **B. 紧急动作集** | 灾难时可做什么 | Tuck / PFP 层 |

**关键联动（采纳其原文）**：

> **若 A 是白名单（默认拒绝），则"紧急动作"只要在 A 的白名单里就自动通过 ⇒ `HARD_OVERRIDE` 立刻失去存在理由。**

⇒ **白名单思维一次性解决两张表，并让那个"不可协商"的放行位变得多余。**
⇒ **这是"E 不牺牲第 154 行的真实需求、只牺牲它错误的表达方式"的代码级证据。**

**其代价我也接受并写明**：反复触发 ⇒ **DoS（系统停在停机态）** ⇒ **必须配冷却 / re-arm（触发后需人类或带外信号复位）**。
⇒ **"可检测可恢复的 DoS，换掉不可恢复的提权"——这笔交易划算，但要写下来，不能默认忽略。**

---

## 5. 修订后的清单（采纳，并把第 6 条提到第一）

| # | 事项 | 归属 |
|---|---|---|
| **1** | **查 L2 的写入权限**（谁能在运行时改 `safety_rules`） | **Anaphase，优先级最高** |
| **2** | `hard_reflex` 改 **fail-open → allow-list**（默认拒绝） | Anaphase |
| **3** | `HardOverride` 补 **reason + 带外触发条件**（保留能力，约束触发） | Anaphase |
| **4** | `Override-Flag` **移出 PFP 或改为带外** | CI-144 规范 |
| **5** | PFP 缺省行为**显式定义为"走慢路径"** | CI-144 规范 |
| **6** | 复核闸门 `None` / `PermissiveGate` 的缺省语义（可选应缺省到严） | Anaphase + 规范 |
| **7（新增）** | **`is_high_risk` 的词表出配置**（修 `0硬编码`）+ 换掉子串匹配 | Anaphase |

**第 1 条排第一的理由采纳其原文**：**若 `safety_rules` 可被不可信内容影响，则前面各条全部可被绕过——改规则比改动作更容易。**

---

## 6. 本链收敛：待人类裁决的**两件**（而非一件）

| # | 决定 | 为什么只有你能定 |
|---|---|---|
| **1** | **第 154 行「优先级高于任何本地策略」是否保留** | 它是**威胁模型与产品定位**决定：PFP 是安全机制，还是"快但可伪造的标签" |
| **2** | **L2 的 `safety_rules` 谁能写** | 它决定反射是不是可被记忆污染卸掉；**是第 1 条之外唯一能绕过全部其余措施的单点** |

**其余全部可在两条定了之后机械推进。**

---

## 7. 诚实清单

1. **本轮四条断言已逐字核到本地源码并附行号**，正对照通过；**不再是摘要**。
2. 我**未读** `reflex.rs` / `security.rs` / `hitl.rs` 全文（只读了相关行及上下文）；**是否存在其它反射或闸门路径未查**。
3. `action` 的来源（是否为 LLM 原始输出）**仍未确认**——它与它都只是从 `&str` 类型推断。**若已结构化，`hard_reflex` 那条的强度需下调。**
4. **L2 是什么层、谁能写：未查**（已列为第 1 优先项）。
5. 本轮**未改任何代码**，仅新增文档。
