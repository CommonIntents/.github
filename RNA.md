# CI-144 协议家族 RNA.md

> **版本**：v1.0
> **日期**：2026-08-29
> **继承自**：Helix-Mind RNA.md（方法论机制复用）
> **性质**：CI-144 协议家族的 AI 协作铁律与三层加载协议
> **所属方法论**：DNA 自生长方法论 v2.0（协议家族适配版）
> **适用范围**：CommonIntents 全部协议仓库（INTENT-7 / BIND-19 / INTENT-7-SECURE / CAPABILITY-13）

## RNA 的角色

DNA 定义了"协议家族是什么"（不可变原则）。RNA 定义了"AI 如何与协议家族协作"（加载协议 + 协作铁律）。

RNA 是 AI 参与协议演化的**操作手册**。任何 AI 在读取、审查、修改 CI-144 协议规范时，必须遵循本文件的加载顺序和协作约束。

## 一、三层加载协议（强制顺序）

AI 在处理 CI-144 协议家族的任何任务时，必须按以下顺序加载文档，**不可跳过、不可乱序**：

### 第一层：组织级治理（必读，所有协议仓库通用）

| 顺序 | 文档 | 位置 | 作用 |
|---|---|---|---|
| 1 | **GOVERNANCE.md** | `.github/GOVERNANCE.md` | 治理宪章，防腐化基线 |
| 2 | **DNA.md** | `.github/DNA.md` | 不可变原则，协议家族最高裁判 |
| 3 | **RNA.md** | `.github/RNA.md` | 本文件，AI 协作铁律 |

> **强制要求**：第一层文档必须在任何协议级任务开始前完整读取。未读取第一层文档的 AI 输出直接拒绝。

### 第二层：协议级方法论（必读，目标协议仓库）

| 顺序 | 文档 | 位置 | 作用 |
|---|---|---|---|
| 4 | **PLAN.md** | 目标协议仓库根目录 | 当前阶段导航，明确任务边界 |
| 5 | **GROWTH.md** | 目标协议仓库根目录 | 已完成阶段记录，了解历史决策 |
| 6 | **ADR 目录** | `docs/decisions/` | 决策记录，理解"为什么" |

> **强制要求**：第二层文档必须在修改 spec 正文前读取。PLAN.md 是当前阶段的任务边界，超出 PLAN 范围的变更需先更新 PLAN。

### 第三层：协议级规范（按需加载，目标协议仓库）

| 顺序 | 文档 | 位置 | 作用 |
|---|---|---|---|
| 7 | **spec 正文** | `spec/*.md` | 协议规范唯一源真相 |
| 8 | **参考实现** | `reference/`（如有） | 规范的参考实现，验证可行性 |
| 9 | **归档文档** | `docs/archive/` | 历史记录，按需加载 |

> **按需加载**：第三层文档根据任务需要加载。修改 spec 正文前必须完整读取相关章节；仅查询字段定义时可定向读取。

## 二、AI 协作铁律（8 条）

### 铁律 1：spec 正文是唯一源真相
- AI 的所有输出必须以 spec 正文为依据，不依赖 README、门面文档或第三方实现的描述。
- 若 README 与 spec 冲突，以 spec 为准，并记录为版本漂移问题。
- AI 不得自行"解释"spec 未定义的语义；未定义的语义必须通过 ADR 流程明确。

### 铁律 2：决策拦截（ADR 强制）
- 架构/接口/协议变更时，AI 必须提示创建 `docs/decisions/ADR-<编号>-<标题>.md`（如 ADR-0001-rust-rebuild.md）。
- 编号为 4 位数字，按顺序递增。
- 引用时使用 ADR-<编号>（如 ADR-0001），文件名与引用名必须一致。
- 无 ADR 的 spec 变更直接拒绝。

### 铁律 3：Append-Only 扩展
- AI 不得修改已发布版本的协议语义（字段、偏移、枚举值）。
- 所有扩展走 Append-Only 模式：新增字段使用 reserved 位，新增枚举使用未分配值。
- 废弃字段标记为 deprecated，但不删除。
- 不兼容的变更必须升级主版本号，并提供迁移路径。

### 铁律 4：向后兼容检查
- AI 在提出任何协议变更时，必须显式检查向后兼容性：
  - v2.0 发送端能否被 v1.0 接收端解析？
  - v2.0 接收端能否处理 v1.0 帧？
  - 新增字段是否使用了 reserved 位？
- 不兼容的变更必须标注为"Breaking Change"，并升级主版本号。

### 铁律 5：物理事实优先
- AI 不得参与协议头部字段的语义决策（Modality/Stance/Edge/Risk-Level）。
- 这些字段由物理传感器驱动，AI 无权修改或"优化"。
- AI 可以建议字段的**编码方式**（如位偏移、长度），但不得建议字段的**语义含义**。

### 铁律 6：固定路径无分支
- AI 在提出协议设计时，优先选择固定计算路径：
  - 固定偏移头部（避免动态长度）
  - 固定加密算法（避免"是否加密"分支）
  - 固定哈希截断（避免跨实现不一致）
- 分支判断增加能耗和复杂度，仅在必要时使用。

### 铁律 7：PLAN 边界约束
- AI 的所有操作必须在 PLAN.md 定义的当前阶段范围内。
- 超出 PLAN 范围的变更，AI 必须先提示更新 PLAN，经人工确认后再执行。
- PLAN.md 不记录已完成阶段；阶段完成后记录到 GROWTH.md，并从 PLAN 移除。

### 铁律 8：人工确认前置
- 协议变更影响所有消费方，AI 不得自动提交任何 spec 变更。
- 所有提交前必须人工确认。
- AI 可以准备变更草案、运行检查、生成 ADR，但最终提交必须由人工触发。

## 三、协议变更流程（AI 辅助版）

```
1. 加载第一层（GOVERNANCE + DNA + RNA）
   ↓
2. 加载第二层（PLAN + GROWTH + ADR 目录）
   ↓
3. 确认任务在 PLAN 范围内（超出则先更新 PLAN）
   ↓
4. 加载第三层（spec 正文相关章节）
   ↓
5. 提出变更草案（含向后兼容性检查）
   ↓
6. 创建 ADR（Draft 状态）
   ↓
7. 人工审查 ADR（通过 → Active；不通过 → 修改或废弃）
   ↓
8. ADR Active 后，修改 spec 正文
   ↓
9. 同步门面文档（README 等）
   ↓
10. 人工确认后提交
```

## 四、AI 输出质量检查清单

AI 在输出任何协议相关内容前，必须自检：

| # | 检查项 | 通过标准 |
|---|---|---|
| 1 | 第一层文档已读取 | GOVERNANCE + DNA + RNA 完整读取 |
| 2 | 第二层文档已读取 | PLAN + GROWTH + ADR 目录已读取 |
| 3 | 任务在 PLAN 范围内 | 未超出当前阶段边界 |
| 4 | spec 正文为依据 | 所有引用来自 spec，非 README |
| 5 | ADR 已创建 | 协议变更有对应 ADR（Draft/Active） |
| 6 | Append-Only | 未修改已发布语义，扩展使用 reserved |
| 7 | 向后兼容检查 | 显式检查 v1.0 ↔ v2.0 兼容性 |
| 8 | 物理事实优先 | 未参与头部字段语义决策 |
| 9 | 固定路径 | 优先固定偏移/固定算法，无不必要分支 |
| 10 | 人工确认前置 | 未自动提交，所有变更待人工确认 |

## 五、一句话总结

> **RNA 是 AI 与 CI-144 协议家族协作的操作手册。三层加载协议（组织级 → 协议级 → 规范级）确保 AI 理解上下文；8 条协作铁律确保 AI 的输出符合协议家族的不可变原则。任何 AI 输出若违反 RNA，直接拒绝。**

---
*《CI-144 协议家族 RNA.md》v1.0 完。*
