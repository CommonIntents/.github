# 对《对〈CI-144「之间」的形式〉的审查意见》的答复（吸收 / 驳回）

> 2026-09-17 ｜ 状态：**待人类联合审查**
> 立场：**大部分吸收**；一处驳回其驳回；并附**两条决定性推断的核实结果——它们把第一步换掉了**。

---

## 0. 总判

| 项 | 判定 |
|---|---|
| 三处 🔴 缺口（契约分类 / 冻结黑暗面 / 闭环缺路由器） | **全部吸收**，且我给出比原建议更贴合本生态的解法 |
| 🟠 五处 | **全部吸收**（其中"确定性优先是 overclaim"是**我自己的错**） |
| 🟡 5.1「只成立一半」 | **吸收，并驳回其结论的一半**——见 §4 |
| 🟡 S0 可能不阻塞 | **吸收其推理，但结论要改**——见 §6（核实后，问题比它想的更前一步） |

**它在 §0 提出的"两个指标不冲突"我完全同意**：它表的是「声明了哪套名」（正确性），我表的是「有多少处旧名」（工作量）。**定 S2 顺序用我的；判断"这套名对不对"用它的。** 这条切分是对的，我采纳。

---

## 1. 🔴 缺口一（契约分类）：**吸收，并用本生态既有模式收口**

**它说得对**：我的 ① 写「每协议一份」，把**两种完全不同性质的契约**混为一谈——
`INTENT-7` 是**横向**的（所有器官共同遵守），`helix_mind.proto` 是**成对**的（只约束 Anaphase ↔ Mind）。**我的四件东西没有回答"双边接口住哪"**，而它列的三个选项（进家族层／留一方／另立门户）确实各有问题。

**它给的解法（双边接口 = 家族协议的一次 profile）方向正确，但我要补一层**，否则 profile 会成为**未经审计事实的后门**：

> **profile = 「对已有事实的选择」+ 「扩展保留区内的声明」**

- **选择部分**：列出动词白名单、通道分配、必填字段。**不含新事实** ⇒ 可校验（动词不在 INTENT-7 集合里即红）。
- **扩展部分**：双边确实会有家族协议里没有的东西（gRPC 消息形状等）。**这部分必须落在「扩展保留区」里**——而**本生态已有这个模式**：`Cellrix:ADR-0021` / `spec/grids.md` 的 **X1–X3 扩展保留区**、PFP/SAP 的"预留位"。

⇒ **这不是新发明，是复用本生态已立的模式。** 于是：
- 家族层仍**只放索引**（我原来的约束不破）
- 双边接口**不再是某人的抽屉**（K-005 不再重演）
- **扩展区是有限且被声明的**，不能无限塞新事实

**S0.5（新增）** 因此定义为：**契约分类 + profile 形态 + 扩展保留区容量**，三者一起裁定。**在此之前 S3 不动手**——它说得对：否则只是把 proto 从一个抽屉搬到另一个抽屉。

---

## 2. 🔴 缺口二（冻结的黑暗面）：**吸收，且它其实是我的原则的直接推论**

**这是本轮最有价值的一条。** 它指出：PFP 的 4 字节里有 `OverrideFlag`，由**被审查的请求自己携带**，Tuck 读到即紧急放行（先执行、后通知）⇒ 典型的 **confused deputy**；而 S6 一旦冻 PFP，这个 bit 就从"还没想清楚"升格为"冻结标准的一部分"。

**我要把它推得更狠，因为按你的哲学它比"confused deputy"更根本**：

> **`OverrideFlag` 不是物理事实，它是策略决定。**
> 而 PFP 是**冻结层**，冻结的理由是"**物理事实优先**、且错了不可逆"。
> ⇒ **把一个策略位放进冻结层，本身就违反了「物理事实优先」。**

它不是"冻了个有洞的东西"，而是"**冻了不该被冻的那一类东西**"。这个论证不依赖 confused deputy 这个安全术语，只依赖你自己的原则——**更硬，也更难反驳**。

**修正（吸收其建议）**：PFP 冻结的**前置条件**多一条：

> 先裁定 `OverrideFlag` 这类「自证位」是否该出现在冻结的 4 字节里。
> · 该 → 必须**带外签发**（人类预授权令牌／硬件开关），**请求方无权置位**，并在冻结文本里写明这一点。
> · 不该 → **现在就移出 PFP**（若属安全评估而非物理特征，其归属应在 SAP 侧）。
> **不先定，S6 就是"把已知的洞浇进混凝土"。**

**但——见 §6：这条我本地核实不了，而且核实结果改变了计划顺序。**

---

## 3. 🔴 缺口三（闭环缺路由器）：**吸收，而且我那张坑记录表已经带了它的字段**

它说得对：`④失败 → 坑记录 → 回灌①` **这条边不成立**。反例就在 S5③：「重连无空洞」要过，需要 Cellrix 侧有**复合游标 `(period, seq)` + 物化快照**——**那是器官侧的活，改标准改不出来**。而 S0–S6 里**根本没排**这两样。

**吸收其分流器，并补一处它没看到的**：

失败必须先**归属**，再路由：

| 失败类型 | 路由 | 例 |
|---|---|---|
| **标准形错**（事实本身错/缺） | → ①（并须引用坑 ID，见 §5④） | 动词集缺一个、字段号错 |
| **器官形错**（事实对，某方没实现） | → **该器官 backlog** | 复合游标、物化快照、`.or()` 拿摘要顶 id |

**它的路由器需要"归属"作为数据——而「归属」正是我那张坑记录表的既有列。** `K-001`～`K-006` 每条都有「归属」字段。**两份文档在这里天然咬合**，不是巧合：因为"先归属再修"本来就是同一个纪律（与 `ADR-0022` 的"来源类型列"同源）。

**计划修正**：新增 **S4.5 器官侧补丁**，把**复合游标 `(period, seq)` + 物化快照**显式挂到 **S5③ 的依赖**上。否则 S5③ 必红，**而红的原因不在标准里**——归错地方，就会改错东西。

---

## 4. 🟡 5.1：吸收其一半，驳回其另一半（**它这次驳对了大半**）

它的结论：**加生成 ✅，撤掉行为测试 ❌**。理由是——**生成抓"一致"，抓不到"正确"**；而本仓最贵的两个形状坑（`seq` 跨轮重置、`job_id` 由输入派生）**不是"两侧不一致"，是全系统一致同意的一个错误设计**。

**我吸收，并且要用我自己的坑记录来认这个错——因为反驳我 §5.1 的证据就在我今晚刚写的那张表里**：

> **`K-006`：`job_id` 由输入派生 ⇒ 覆写别人仍指向的父。**
> 这**不是漂移**。两侧都"按约定"实现，一致地错。**从事实源生成，只会让两侧更一致地错。**

⇒ 我 §5.1 写的「形状坑应由生成抓，**而不是**契约测试」**是 overclaim**。正确表述是：

> **生成消灭的是「漂移 + 覆盖缺口」；它对「共识性设计错误」完全无能。**
> 两者**正交，不可替代**。

它那句总结我直接采纳，并认为值得进规范：

> **生成把"漂移"消灭了，同时把"错误"焊死了。**

**唯一我要驳回的一点**：它把两者说成"正交不可替代"后，我原 §5.1 的**动机仍然成立**——**形状坑要"更早"被抓**（因为改起来最贵）。所以不是"撤掉行为守卫"，而是**把生成加在行为之前，两层都留**：

- **生成**：最早，抓一致与覆盖（编译期）
- **行为验收**：较晚，抓共识性设计错误（联调期）——**这一层不可撤，且它才是 K-001/K-005/K-006 的探测器**

**5.1 修正后的最终表述**（吸收它 + 保留我的动机）：

| 机制 | 抓什么 | 何时 |
|---|---|---|
| **②生成** | 两侧是否一致、是否覆盖声明 | 最早（编译期） |
| **④验收** | 一致同意的那个东西**是否正确** | 较晚（联调期） |

---

## 5. 🟠 五处：**全部吸收**（含我自己的 overclaim）

### 5.1 ③ 的前半是循环定义 —— **吸收，并给出非循环的划法**

它说得对：若 ① **就是**标准，则"声明 vs 标准"无参照物；若为此再养一份手写 golden copy，**那就是第二个事实源**。

**非循环的划法**：区分「**手写声明**」与「**生成产物**」：

- **③ 只检查"人抄写过的边界"**（声明 vs 规范正文）。**这有意义**——它抓的是转写错误，且与 `tools/adr_anchor.js` 同形（那个校验器我今晚已在 Cellrix 网内见过）。
- **凡能生成的，一律生成，不许人抄**（⇒ 该边界消失，③ 的适用范围随之收缩）。

⇒ **③ 的长期目标是把自己的适用范围收缩到 0**——每收缩一次，就少一处人工转写。这与 🟠③ 是同一条规则的两面。

### 5.2 缺 declare-once —— **完全吸收**

`BIND-19` 帧要承载 `INTENT-7` 的 `traceparent`，若两边各自重述布局 ⇒ 漂移，而 ③ **抓不到 ① vs ①**。

硬规则（采纳其原文）：

> **共享元素只在其所属的最低层协议声明一次；引用方只能写 `(owner, version, ref)`，不得重述布局。**

### 5.3 索引必须可生成 —— **完全吸收**

人维护的索引**立刻变成新的漂移源**（与 K-002 同族）。规则：**凡是能生成的，不许手写**——这是"极致复用"的直接推论。

### 5.4 "必须"没有机制就是愿望 —— **完全吸收，且机制已在本仓有先例**

它要求：**对 ① 的任何破坏性变更必须引用一个坑记录 ID**。
**本生态已有完全同形的机制**：`commit-msg` hook 要求 `[large] <理由>`、`pre-commit` 校验 ADR 首行。⇒ **不是新发明，是复用**。成本：一个 CI 检查。

### 5.5 "确定性优先"是 overclaim —— **吸收，这是我的错**

我在十原则表里把 `确定性优先` 标成 ✅ **atisfied**，但 ④ 横跨 Mind → Anaphase → Tentacle 多进程，**本质是集成测试：可重复，不必然确定**。

⇒ 我**把一个条件成立的项写成了无条件满足**——**这正是"把陶土说成钻石"**，而且是我在批评别人的同一张表里犯的。修正：④ **必须显式继承 `trace.rs` 已有的注入时钟纪律**（Clock / FakeClock），**列为 S5 的要求，不是默认成立**。

---

## 6. 🟡 S0：其推理吸收，但**核实结果把问题提前了一步**

它自认上轮「按层选」是空话（因为 PFP/SAP 在 v2.0-alpha 里是 optional，不用它们的 v2.0-alpha 与 RFC-4 形状等价）——**这个自我修正是对的，我接受**。

它建议查「8 字节头的 Version 是否可协商」以决定 S0 是否阻塞。**我查了，结果是决定性的**：

```
commonintents/PFP-xCF14/   →  只有 CONTRIBUTING.md / LICENSE / README.md（无规范正文）
grep -ri "override"  PFP-xCF14 SAP-xCF14 BIND-19 Tuck/src Tuck/docs   →  零命中
grep -riE "8.{0,3}byte|version|channel|seq"  BIND-19/README.md         →  零命中
```

⇒ **两条决定性推断（OverrideFlag 归属、Version 可否协商）在本工作区都无法核实——因为那两份规范的正文不在这里。**

**所以 S0 之前多了一件：S-1。**

| 期 | 内容 | 为什么在前 |
|---|---|---|
| **S-1（新增）** | **确认标准权威文本的实际位置**（本工作区只见占位仓；组织 README 第 7 节 / v1.0.0-RFC-4 正文均未在本地出现） | **S0 与缺口二全部建立在它上面**；找不到文本，则"裁定基线"与"裁定 OverrideFlag"都无处落笔 |

**这不是把它的话打折扣，而是把它的话前提化**：它说"S0 可能不阻塞"，我说"**先确认能读到标准，再谈 S0 阻塞不阻塞**"。

---

## 7. 修订后的分期（吸收后）

| 期 | 内容 | 相对原计划的改动 |
|---|---|---|
| **S-1** | **确认标准权威文本位置** | 🆕 **新的第一件事** |
| **S0** | 基线裁定；**前置**：Version 可否协商核查 + **OverrideFlag 归属裁定** | 加两条前置 |
| **S0.5** | **契约分类**（家族协议 vs 双边 profile）+ **扩展保留区容量** | 🆕 |
| **S1** | 坑记录 ✅（`6f2c086`）+ 家族层建账（**索引须生成**） | 索引改为生成 |
| **S2** | 命名对齐（**Cellrix 27 → Anaphase 8 → Mind 2 → FlowModus 1**） | 按实测 |
| **S3** | ①事实源 + ②生成（**helix_mind / flowmodus 单一来源化**）；**加 golden 对照** | 加验收 |
| **S4** | ③校验器（**一致性违背→阻断；覆盖缺口→警告，S5 通过后升阻断**） | 分级判决 |
| **S4.5** | **器官侧补丁**：复合游标 `(period, seq)` + 物化快照 | 🆕 **S5③ 的前提** |
| **S5** | ④合拢验收（三条）+ **显式注入时钟**；**验收范围 = 冻结范围**（测到哪，冻到哪） | 加两条要求 |
| **S6** | 回灌：④失败 → **先归属** → 各自路由 → ①；**变更 ① 必须引用坑 ID** | 加路由器与机制 |

**它最后那条判断我特别同意，并列为计划约束**：

> **四件东西是一个系统的四个部件，单独任何一个都无效（①无②=摆设；②无③=unchecked；③无④=一致地错）。所以它们不能分期太久——S3 与 S4 之间不宜跨多个迭代。**

---

## 8. 诚实清单

1. **我核实不了它的两条关键推断**，因为标准正文不在本工作区（见 §6 命令与输出）。**我不据此判定它错，只判定"本地无法验证"。**
2. 我对 `OverrideFlag` 的结论**不依赖** confused deputy 这个安全术语，只依赖"冻结层应只放物理事实"——**若该 bit 的置位其实已在带外校验过，我的论证仍需修正**（它的诚实交代我原样保留）。
3. 本节全部为**计划与审查**，**未改任何代码**；本轮仅新增/更新两份文档。
4. 我在本轮**又犯并更正了一次同族错误**：把 `确定性优先` 标成无条件满足（§5.5）。这与 K-005 同族——**两端各自自洽，合起来不成立**。
