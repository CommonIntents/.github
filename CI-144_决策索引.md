# CI-144 决策索引（唯一入口）

> 建立：2026-09-17 ｜ **第九版修订：2026-09-17（B2 完成：PFP 全文读完，新增 K-013～K-017）**
> 用途：**回答"现在该做什么"**，而不是增加论述。
> 起因：本目录文档已达 17 份而代码未改一行。审查指出——**文档增长远快于代码**；若不加以约束，它会成为**第 18 个器官**。
> **本版自我更正**：六轮审查往返中，本文件与 HANDOFF 又增加了 5 次提交、**代码 0 行**。**审查本身也开始变成器官。**
> ⇒ **本版是收敛终点：此后不再有"审查轮"，只有 A 类答复与 B 类提交。**

---

## 0. 先钉住两件事（否则后面每个数字都会漂）

### 0.1 规范读哪一份（**本版新增，因为此前每次读都隐含选了一份却没声明**）

**工作区里有同一 remote 的两份 BIND-19 检出**：

```
/Users/jason/Doubao/chats/Jasonmilk/BIND-19               HEAD=4321c09  branch=v2.0-alpha  tag=(无)
/Users/jason/Doubao/chats/Jasonmilk/commonintents/BIND-19 HEAD=bbc35c6  branch=v2.0-alpha  tag=v2.0-rc.1
origin 两者相同：https://github.com/CommonIntents/BIND-19.git
```

**判据在规范自己身上**（两处，源码逐字）：

```
SAP-xCF14 §10「权威来源声明」: 本规范的唯一权威来源在 CommonIntents/BIND-19/docs/spec/sap-xcf14.md
PFP/SAP 页眉「权威来源」    : github.com/CommonIntents/BIND-19/blob/v2.0-rc.1/docs/spec/...
                            「对应 BIND-19 版本」: v2.0-rc.1
```

**⇒ 本索引及一切行号判断，一律以 `commonintents/BIND-19/docs/spec/` 为准。**
**理由（可复核）**：该目录带 `v2.0-rc.1` tag；根目录那份无 tag，且缺少 `commonintents` 那份独有的 CC BY-ND 版权行。

| 文件 | 根 BIND-19 | **commonintents/BIND-19（准）** | 差异 |
|---|---|---|---|
| `docs/spec/pfp-xcf14.md` | 316 行 | **317 行** | 仅版权行 |
| `docs/spec/sap-xcf14.md` | 261 行 | **262 行** | 仅版权行 |
| `spec/BIND-19.md` | 195 行 | 195 行 | 仅版权行 |

> **索引 §6.2 的「PFP 317 行」因此复核通过**——它一直指的是 `commonintents` 那份。
> **本版之前，"317"是个未声明范围的数字；现在它有了来源。**

### 0.2 仓数与清单（此前写"14 个独立 git 仓"，实测 **20**）

```
find . -name .git -maxdepth 3 | wc -l   →  20
```

- **commonintents（7）**：`.github` / `BIND-19` / `CAPABILITY-13` / `INTENT-7` / `INTENT-7-SECURE` / `PFP-xCF14` / `SAP-xCF14`
- **器官（13）**：`BIND-19`(根) / `Cellrix` / `FlowModus` / `Helix-MCP-Learner` / `HelixECO-Glove` / `Tuck` / `anaphase-helix` / `helix-mind` / `helix-tentacle` / `lodestone-md` / `lodestone-spec` / `lumtract` / `phyt-DNA`

> **注**：`commonintents/PFP-xCF14` 与 `commonintents/SAP-xCF14` **只有 README + CONTRIBUTING，无 `spec/`**（实测）。规范正文住 BIND-19。⇒ **"规范在哪个仓"这个早先悬着的问题，答案是 BIND-19。**

---

## 0.3 归类规则（硬约束）

| 类 | 含义 | 要求 |
|---|---|---|
| **A. 待决策** | 有人类决定才能推进 | 必须写明**决定的形式**；**且必须"一行可答"**（答复函数式：对任意输入只需一条规则，而非对每条输入各答一行） |
| **B. 待执行** | 无需决策，可直接做 | 必须写明**完成判据**，且判据须**可证伪** |
| **C. 背景** | 已完成的记录 | 只作追溯；**含"不做，因为 X"的显式条目** |

---

## 1. A 类：待人类决策（**5 个决策位，按依赖排序**）

| 序 | # | 问题（一行可答） | 依赖 | 若答 X 则… |
|---|---|---|---|---|
| 1 | **H4** | **两份 BIND-19 检出中，哪一个是本家族的工作副本？**（内容判据已由 §0.1 给出，此项只需确认工作约定） | — | 答"commonintents 那份" ⇒ B12 快照有唯一目标 |
| 2 | **H5** | **`reflex.rs` 是求生机制，还是安全闸门？** | — | 答"求生" ⇒ 反射 fail-open **保持**，K-007 #3 归「未接线」，B5 撤销、替代 R1；答"闸门" ⇒ B5 原样恢复 |
| 3 | **H1** | **紧急处置动作集中，哪些是 veto 型（延续现状即可），哪些需要"放行"？** | — | 放行型 ⇒ 落白名单（**一个尚未被命名的功能**），不是 PFP 的一个 bit。**⚠️ 本版追加：所选机制必须说明其可信来源**——否则会与 `Replay-Enable` 同构（一个无签名字段降级另一个），见 §6.7.1 |
| 4 | **H2** | **谁能写记忆？写入是否经过与 `suggested_actions` 相同的审查？** | — | 若写入无审查 ⇒ 被守卫的与被执行的是两批数据，中间隔一次记忆 |
| 5 | **H2b** | **`safety_rules` 应当静态（源码即边界）还是运行时可更新？** | **← H5** | 答"静态" ⇒ 须修 `reflex.rs:7` 的 `loaded from L2` 注释（与实现不符） |
| 6 | **H3** | **B1–B3 通读后新发现的缺陷，纳入本次冻结 / 仅安全类纳入 / 全部推迟？** | **← B1–B3** | 一行规则，非逐条答 |

### H1 的背景（原 A1/A4，已并入）

反射已证实是 **veto-only**（`reflex.rs:13-15` 只返回 bool）；且 `hard_reflex` 的实际检查面是 `suggested_actions`，**不是**执行路径（见 K-009）。
⇒ **任何需要"放行"的紧急动作都不是反射，而是一个尚未被命名的功能。**

### H5 / H1 之下的机制选择（原 A4：E1 vs E2）——**已裁，附条件**

> **裁定：暂定 E1（PFP 只能否决 + 紧急动作走白名单；PFP 154 的放行位不再被要求）。**
>
> **理由（第七版重写）**：不是"E2 不可行"（那是第三轮被撤回的越界结论），而是——
> **已实证：被守卫的数据与被执行的数据不是同一批**（K-009）。任何依赖"输入可信"的机制，在已实证的那条路上都不安全。**E1 正好不依赖输入可信。**
>
> **升级条件**：若「`execute_calls` → PFP 帧构造」这一段被证明与 LLM 文本隔离，**可重开 E2**。
> ⇒ **该路径至今未读**，故 E1 为**审慎默认**，非定论。

**E3（直接清除 154）** 仍不作选：若 H1 枚举出真实需求，E3 不完整。

---

## 2. B 类：待执行（无需决策，附**可证伪**判据）

| # | 动作 | 完成判据（可证伪） | 归属 |
|---|---|---|---|
| **B0** | **建实现矩阵**：每个协议一张表，谁 MUST/SHOULD/MAY/**N-A** 实现哪一层。**列含「权威路径」「免疫归谁」** | 每个协议 × 每个消费端都有格子；**空格必须显式写 N-A**，不得留白 | CI-144 |
| **B0′** | **枚举「默认放行」分支，产出分类表（不是翻转清单）** | 表含四列：**位置 / 语义 / 当前是否生效 / 归类**。四类 = 安全判定（须 fail-closed）/ 依赖降级（保持 fail-open）/ 求生机制（待 H5）/ **未生效**（4a 未接线、4b 不可达） | Anaphase |
| ~~B1~~ | **✅ 已完成（2026-09-17）** —— 通读 SAP 全文 262 行。**规范级答案见 §6.7** | 判据达成：答案由规范原文给出，非推断 | CI-144 |
| ~~B2~~ | **✅ 已完成（2026-09-17）** —— PFP 317 行**全文**读完（此前只读 128–185）。**"全文无来源规定"可断言**；但**新发现 5 处规范内部缺陷**（K-013～K-017） | 判据达成：逐段读完 + 用规范自带伪代码重编码 5/5 向量通过（**阳性对照**） | CI-144 |
| **B3** | 通读 **BIND-19 `spec/BIND-19.md`**（195 行） | 给出帧头 Version 语义（可否协商）。带阳性对照 | CI-144 |
| **B4** | **`is_high_risk` 改判据维度**：从**命令名**改为**能力声明** | 变异证明：`sh -c` / `python -c` / `env` / `xargs` **注入后必须被列高风险**（当前实现全部漏报，见 `hitl.rs:82-92` 单测词表）。**注：`check_approval(&c.tool, &[])` 的"空切片丢上下文"是本项的症状，不独立立条** | Anaphase |
| ~~B5~~ | **撤销**。理由：K-009 + K-011 ⇒ 反射不覆盖执行路径，改 allow-list 不保护任何东西。**替代为 R1** | — | — |
| **B6** | `HardOverride` 补 **reason + 带外触发条件** | 变异证明：**无带外触发源时 `HardOverride` 不可达** | Anaphase |
| **B7** | **闸门缺省复盘**：`security_gate: None` 与 `PermissiveGate` | 变异证明：**不配闸门 ⇒ 高风险动作必须被拦**；配 `PermissiveGate` 后通过。**注：`PermissiveGate` 生产零实例化（全仓仅 `security.rs` 自测引用）** | Anaphase |
| **B8** | S2 命名对齐（**Cellrix 27 → Anaphase 8 → Mind 2 → FlowModus 1**） | 旧名命中 **37 → 0** | 全生态 |
| **B9** | S3 事实源单一化 + 生成 + **golden 对照** | 副本数 2 → 1；生成物与手写物**字节/行为等价** | CI-144 |
| **B10** | S4 校验器 + 基线豁免清单（含责任人与期限）。**← B0** | 人为植入漂移 ⇒ 变红；清单内豁免、逾期转阻断。**无 B0 则无可比对对象** | CI-144 |
| **B11** | 器官侧补丁：复合游标 `(period, seq)` + 物化快照 | S5③ 可过 | Anaphase + Cellrix |
| **B12** | **打 `v0.9-draft` + changelog（落 `commonintents/.github`）+ 生成式家族快照** | 快照为 CI-144 各成员仓的 `{路径, remote, 分支, commit, tag, spec 版本串}` 清单，**由命令生成、可重跑字节一致、禁止手写**。**← H4** | CI-144 |
| **B13** | **约束"回落路径的命令来源"（两个旋钮，主次分明）** | **主 `tool_command`**（`Option<String>`，生产无调用者、无配置键，但有完整执行接线 + `tests/integration_test.rs:193` 证明其活）；**次 `execution_placeholder`**（默认字面量 `"echo"`，无校验）。判据：**任何"命令"值不得无条件进入 `tool.execute(command, ...)`** | Anaphase |
| **B14′** | **`security_gate` 缺席的两种原因（未配置 / 构造失败）当前折叠成同一个 `None`** | 判据：**"闸门缺席"必须可区分于"闸门通过"**；构造失败不得静默降级为未配置。**同族阳性对照：Tuck 已就 `None ≠ 空表` 立过条款，anaphase 侧复发且无人写条款** | Anaphase |
| **R1** | **`ReflexArc` 归位（三条）**：① 模块头标注**「当前未接入执行路径 / 占位」**；② `safety_rules` 改名以表达自保语义；③ 修 `reflex.rs:7` 的 `loaded from L2` 注释 | 三条均落地；②③ 有变异证明（改回旧名/旧注释 ⇒ 校验变红） | Anaphase |
| **B15** | **器官侧坑账本**：`anaphase-helix` 需要与 `CI-144_合拢坑记录.md` 同构的账本（坑 → 证据 → 归属 → 现状） | K-007～K-010 在 `.github` 侧只留指针行；器官侧每条有可复核证据。**关联 `合拢坑记录 §5` 的规则 1 边界裁定** | Anaphase |

**新增机制（本链产出，建议入家族规则）**：
- **阴性结论必须带阳性对照，且逐目标施加**（本链 6 次同族失败，第 6 次由机制自己抓出）
- **单引一条原则的论证不予通过**（须命名张力 + 残余风险）
- **变更 ① 必须引用坑 ID**
- **计数必须带口径**（"生效 / 未生效 / 未读"三分，禁止合并计数）

---

## 3. C 类：背景（含"不做，因为 X"）

| 条目 | 性质 |
|---|---|
| `CI-144_合拢坑记录.md` | K-001～K-012；**K 类唯一账本**（其中 K-007～K-010 为器官内部，见该文 §5 的裁定请求） |
| `CI-144_之间的形式_计划与审查.md` | S-1～S6 分期（已并入 §2） |
| `CI-144_之间_审查答复.md` ~ `答复六` + `HANDOFF_审查意见_第四~六轮.md` | **六轮往返记录。本版起停止新增轮次** |
| **F（原 H6）：默认路径的闸门责任归谁** | **不做，因为** B13 若钳住两旋钮 ⇒ 回落路径无可配置命令 ⇒ 本问题无对象。仅当 B13 保留旋钮时重提 |

---

## 4. 必须记住的边界（防止误以为收尾）

> **本链是「决策链收敛」，不是「缺陷清点完成」。**

| 未读面 | 状态 |
|---|---|
| **SAP 262 行** | ✅ **全文已读**（B1，2026-09-17）⇒ 结论见 §6.7 |
| **PFP 317 行** | ✅ **全文已读**（B2，2026-09-17）⇒ 结论见 §6.8 |
| `BIND-19/spec/BIND-19.md` 195 行 | 未通读（B3） |
| `commonintents/BIND-19/src/pfp.rs` | **部分已读**（解码/规则6/reserved 相关行）；`src/sap.rs` **未读** |
| **PFP "optional" 的真实语义** | ✅ **已读（B2）** ⇒ **规范自身三处不一致**，见 §6.8.1 |
| `tests/test_vectors/ci-144-v2.0-test-vectors.json` | ✅ 33 条已逐条核（B2）；**无任何测试加载它** ⇒ K-013 |
| `hitl.rs` | 仅读 `:35` 签名与 `:82-92` 单测 |
| `security.rs` | 已通读（108 行） |
| `reflex.rs` | 已通读（23 行） |

**下一个读规范的人可能在第 200 行发现第 4 处缺陷。**

### 4.1 ⚠️ 计数口径（**本版新增：防止"3"这个数字带着假出门**）

> **B0′ 的产出必须区分：当前生效 / 未生效 / 未读。三者禁止合并计数。**

| # | 位置 | 语义 | 当前是否生效 | 归类 |
|---|---|---|---|---|
| 1 | PFP optional（`pfp:1.2`）**↔** PFP 必需（`sap:6`） | **规范内部矛盾**（见 §6.8.1） | **未定** | 待规范裁定 |
| 2 | `pipeline.security_gate: None` | 缺省=无守卫 | ✅ **生效**（生产从不构造闸门） | 安全判定 |
| 3 | `hard_reflex` deny-list | 缺省=无守卫 | ⚠️ **未接线**（守 `echo` 参数） | 4a |
| 4 | `soft_reflex` Err ⇒ 放行 | 失败=放行 | ✅ **生效** | **待 H5** |
| 5 | `safety.audit` Err ⇒ `Ok(Success)` | 失败=放行 | ❌ **4b 不可达**（唯一 impl 恒 `Ok(true)`） | 4b |

> **⇒ 当前生效的 fail-open = 2 处（#2 / #4），未生效 = 2 处（#3 / #5）。**
> **#1 已从「未读」升级为「规范内部矛盾」**（B2 完成）⇒ **它不再是采样缺口，是一条待裁定的规范缺陷（K-014 同族）。**
> 本计数**仍不写总数**：K-013（向量未被加载）未修之前，"生效"的判定缺可执行判据。

---

## 5. 本索引自身的检验

- **A 类**：**5 个决策位**（H1–H5，含 H2b 计 6 行 5 位），每条**一行可答**
- **B 类**：**13 条未完成**（B0 / B0′ / B3–B4 / B6–B15 / R1；**B1✅ B2✅ B5 撤销**），每条有**可证伪**判据
- **C 类**：明确降级，含一条**"不做，因为 X"**

**已完成闭环**：**B1**（§6.7）、**B2**（§6.8）⇒ 本索引**连续两次因完成而减少待办**。

**若本文件将来也变成"既不决策也无动作"，它应被降级为 C——包括它自己。**

---

## 6. 对《收敛纪要》的更正（**含 B1/B2 带来的四条新更正**）

### 6.1 ⚠️「`Override-Flag` 规范有、实现无」**不准确**（原文保留）

| 层 | 事实 |
|---|---|
| 规范 | 有，且是**规范性强制**条款（`pfp-xcf14.md:152/154`） |
| **Anaphase** | **有** —— `security.rs:45-52` `GateVerdict::HardOverride`，`permits()` = `Pass \| HardOverride` |
| **Tuck** | **无** —— `Tuck/crates/*/src` 内 `override` 零命中 |

⇒ **"实现无"是错的：能力在 Anaphase 存在，只是 Tuck 没有。**
⇒ **正确表述**：同一语义，Anaphase 用「闸门自判」、Tuck 无实现、规范用「请求自陈」——**三者互不一致** ⇒ **fixture 必须钉住具名的消费端**。

### 6.2 PFP 行数**复核通过**（本版更新为可复核）

PFP 规范 **317 行**（`commonintents/BIND-19/docs/spec/pfp-xcf14.md`，见 §0.1）；完整读的是 **128–185**，未通读约 **259 行**。
⇒ **"全文无来源规定"仍是条件性结论**，P7 的"穷尽"钥匙**尚未取得**。

### 6.3 「改名 + 重构」的证据等级**应下调**（原文保留）

该条来自外部摘要，**未核实**。实测过的只有**旧名残留计数**（Cellrix 27 / Anaphase 8 / Mind 2 / FlowModus 1 / Tuck 0 / tentacle 0）。
⇒ 标为 `摘要（未核）`，与源码逐字**不同级**。

### 6.4 打版**缺执行目标**（原文保留，已升级为 B12）

CI-144 作为家族没有单一仓库 ⇒ tag 与 changelog **必须落在 `commonintents/.github`**。
**本版补充**：**家族快照 ≠ 本仓的版本 tag**，它是一组 `(repo, commit, …)` 清单，且**必须生成**（P8）。**← H4**

### 6.5 🆕 本版新增：`docs/spec/` 有两个检出（见 §0.1）

**此前每次判行号都隐含选了一份却没声明**——同一族病（未声明采样范围）。
⇒ 现在判据来自规范自身（"唯一权威来源"条款 + `v2.0-rc.1` tag），**不再依赖人的约定**。

### 6.6 🆕 本版新增：`commonintents/PFP-xCF14` / `SAP-xCF14` **没有 `spec/`**

只有 `README.md` + `CONTRIBUTING.md`。**规范正文住 BIND-19** ⇒ `CI-144_合拢坑记录.md` §2 末尾"其 ❓ 之一已解"应据此收紧：**"在组织内"成立，但"规范在 PFP 仓"不成立。**

---

### 6.7 ✅ **B1 完成：SAP 不覆盖 PFP。PFP 四个字节没有任何密码学保护。**

**读法（阳性对照齐）**：SAP 全文 262 行逐行读完；并核 PFP 317 行中全部含「签名 / 防伪 / 篡改 / 覆盖」的行（`grep -n "签名\|防伪\|篡改\|覆盖"` ⇒ 命中 6 行，**无一条是 PFP 自身的签名机制**）。

| 层 | 是否被签名覆盖 | 规范原文 |
|---|---|---|
| **PAH**（物理上下文＝传感器读数） | ✅ 覆盖（112-bit 哈希 + 64-bit 截断签名） | `sap:3.6` "物理上下文数据：传感器读数…的序列化字节" |
| **PFP 的 4 个字节** | ❌ **不覆盖** | `sap:1.2` 栈：`[BIND-19 8B] + [PFP 4B] + [SAP 28B(可选)] + [Payload]`；`sap:8` "Tuck 读取：不依赖（可选验证）" |
| BIND-19 头 | ❌ 不覆盖 | 同上 |

⇒ **SAP 的签名锁的是 PAH，而 PAH 由传感器数据哈希生成。PFP 的 `Risk-Level` / `Override-Flag` / `Replay-Enable` 全部在 SAP 之外。**

**⇒ PFP 关于自身可信性的唯一依据是设计哲学里的一句声明**：

```
pfp:1.1  「物理事实优先 | 所有字段由传感器驱动生成，AI 无权修改」
```
**⇒ 这是一句规范声明，不是一个机制。** 而帧由 AI 构造 ⇒ **"AI 无权修改"在帧构造处没有被任何东西强制。**

**⇒ 由此，原 A4 的「E2 需要证明 LLM 无法影响帧字节」现在有答案了**：
**该证明的前提为假。** PFP 的字节由帧构造方决定，**SAP 从不覆盖它** ⇒ **E2 的重开条件（"帧构造路径与 LLM 文本隔离"）不是未读，是已被规范判定为不存在。**
（仍保留第三轮的方法论教训：当时从"反射输入被污染"跳到这里是越界；**现在这一步是由规范原文走完的，不是推断。**）

#### 6.7.1 连带发现：SAP 规则 6 + PFP `Override-Flag` 构成**自我降级链**

```
pfp:3.8 / sap:6
  当 PFP.Replay-Enable == 0：
     有效风险强制 = MEDIUM        （无论 PFP.Risk-Level 原始值为何）
     永远无法触发 CATASTROPHIC 硬覆盖
     伪代码原文：effective_risk = RiskLevel::Medium;  // 后续决策仅使用 effective_risk，丢弃原始高优先级
```

**⇒ 两条原文合起来**：

> **紧急处置唯一的机制是 `Risk-Level == CATASTROPHIC && Override-Flag == HARD_OVERRIDE`（`pfp:3.7`，自称"不可协商部分"）。
> 而这个机制被**同一个未被签名的 4 字节里**的 `Replay-Enable` 位降级——规范化、有伪代码、无需绕过。**

⇒ **这不是"缺陷"，是"机制被另一个同源字段消解"。** 且防重放的本意（抗重放攻击）与它的实现（丢弃 Risk-Level）方向相反：**为了防重放，牺牲了紧急放行。**

**⇒ 对 H1 的直接影响（本版新增）**：
H1 不能只枚举动作集，还必须裁 —— **"紧急处置依靠 PFP 的哪个字段"**。若答案是 `Override-Flag`，则**该字段当前无任何防伪**，H1 的答案会落在一个可被无签名构造的位上。
**⇒ H1 的回答形式增加一句**：所选机制必须能说明**它的可信来源**；否则它与 `Replay-Enable` 同构（**一个字段降级另一个字段**）。

**归属**：CI-144（默认放行/降级策略，跨 PFP ↔ SAP 两个规范）。
**现状**：🔴 **规范层已坐实，代码层未核**（`commonintents/BIND-19/src/pfp.rs` / `sap.rs` 是否实现规则 6、是否校验覆盖范围 —— **未读**）。

### 6.8 ✅ **B2 完成：PFP 全文读完。"全文无来源规定"可断言——但 PFP 自身有 5 处规范内部缺陷。**

**读法**：1–59 / 60–143 / 144–185 / 186–317 **逐段读完**（非 grep 命中行）。
**阳性对照**：用规范 `pfp:5` 自带的解码伪代码，把 5 条 `pfp_codec` 向量**逐条重编码**，**5/5 与 `expected` 一致** ⇒ 伪代码可执行、向量与伪代码自洽。
**⇒ 正因对照做了，才能把"向量写错"与"声称与实现分叉"分开。**

#### 6.8.1 PFP 是否 optional —— **规范自身三处不一致**（B0′ #1 的答案）

| 处 | 原文 | 含义 |
|---|---|---|
| `pfp:1.2` | `[BIND-19 8B] + [PFP 4B (optional)] + [SAP 28B (optional)] + [Payload]` | **PFP 可选** |
| `sap:1.2` | 同栈，但 `sap:1.3` 说「SAP 依赖 PFP 存在（**SAP 不能单独出现**）」 | **PFP 是 SAP 的前提** |
| `sap:1.2` 末句 | 「Tuck 硬实时决策**仅依赖 PFP**」 | **PFP 必需** |

⇒ **"optional" 只在栈图上，正文与消费方语义都要求它存在。**
**⇒ 缺 PFP 的帧怎么处理（拒绝 / 按最严风险 / 降级）——三处都没写。** ⇒ **这是 B0′ #1 格的真实内容，不是采样缺口。**

#### 6.8.2 新发现 5 处（详见 `CI-144_合拢坑记录.md §7`）

| K | 内容 | 级别 |
|---|---|---|
| **K-013** | **33 条测试向量 / 8 分类存在，但无任何测试加载它**（`grep` 零命中）⇒ **互操作性层的地基缺失** | 🔴 |
| **K-014** | **Reserved 非零的处理有三处互斥规定**：`pfp:3.9`=触发协商 / `pfp:5`=返回 `Err` / 参考实现=解析后 `Ok` 另提供 `has_unknown_reserved()`；**且 `pfp:7` 向量表自己给了 `cf14 ff 0f`（reserved≠0）** | 🔴 |
| **K-015** | `pfp-all_zero` **自称"全零"但编码是 `cf143000`**（`body_stance=Unknown`）；规范表与 README 又写 `cf14 00 00` ⇒ **四处声明互不相同** | 🟠 |
| **K-016** | `pfp:6` 「约 3 个 CPU 周期（**~0.3 ps**）」⇒ **单位错约 3000 倍**（3 周期 @3GHz ≈ 1 ns）；**而这一层自称冻结不朽** | 🔴 |
| **K-017** | `rule6_downgrade` + `catastrophic_detection` 共 6 条向量，**无一条同时具备 `Risk=CATASTROPHIC` 与 `Replay=0`** ⇒ **系统性避开 K-012 那条降级链的唯一触发格** | 🟠 |

**⇒ 与 K-012 的合并结论**：**PFP 自称冻结，却在冻结层里留下了三处内部矛盾（K-014 / K-015 / K-016）**，而它的安全证明层（SAP）**不覆盖它**。
**⇒ 对 B12（打版）的直接含义**：**PFP 若要修，必须走 PFP-xCF15（新版本），不得原地改** —— 这个约束本身也是待裁定项。

---

## 7. 撤回留痕（**不静默重写**）

| 撤回项 | 原表述 | 撤回理由 |
|---|---|---|
| C1 | `with_tool_command` 零调用点 | **只 grep 了 `src/`**；`tests/integration_test.rs:207` 有调用。失效的是**范围**，不是 grep 模式（阳性对照 `with_pipeline` 命中） |
| C2 | `tool_command` 恒为 `None` | 由 C1 推出，随 C1 作废 |
| C4 | `command` 不由 `action_str` 派生 | **表述错**：命令来自 `tool_command`，`action_str` 是同一调用的实参 |
| — | K-008「拼接比逐个动作更松」 | **证据不足**。并集不删除已有子串 ⇒ 不可能更松。**保留两条**：无逐动作归因；注释自称 `O(1) hash matching` 实为线性 |
| — | "K-007 扩到五处" | **计数口径错**（把 4b 与未读项并进主计数）。已改为 §4.1 三分口径 |
| — | "删除 `execution_placeholder`" | **成本高于收益**：会使"回落路径"概念消失，牵连向后兼容测试 |
| — | "E2 当前不可行" | **越界**：从"反射输入被污染"跳到了"帧字节不可隔离"，跨了一整层 |
| — | "H4 两个副本未定正统、tag 已分叉" | **本版推翻**：两仓 HEAD 同属 `v2.0-alpha` 链，差异仅版权行 + 一个 README 提交；规范自己声明了权威来源。⇒ H4 降为"工作副本约定确认" |
