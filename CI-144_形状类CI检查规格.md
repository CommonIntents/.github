# CI-144 形状类 CI 检查规格（v0.4 双落地版）

> 建立：2026-09-18 ｜ 状态：**CI-4 与 CI-6 均已落地**，其余 Proposed
> 用途：**形状错了不可逆 ⇒ 进 CI；内容错了能改 ⇒ 不进。**
> **本版与 v0.3 的差别：CI-6 已实现并验证（含 DATA-ONLY 验证器）；CI-4 首批豁免已交付。**

---

## 0. 立规（先于所有检查）

| 类别 | 进 CI？ |
|---|---|
| **形状类**（已闭合、机械、错了不可逆） | ✅ |
| **存在性类**（已声明、可能未实现） | ✅（**必须先配基线豁免清单**） |
| **意图类**（H1–H5 等未裁定） | ❌ |
| **策略类**（会持续演化） | ❌ |

> **一个还没裁定的东西进了 CI，就从"待定"变成"正统"，而且有红绿灯替它撑腰。**

---

## 1. 通用机制

### 1.1 三态

`PASS` / `FAIL` / **`AMBIGUOUS`**（声明自身矛盾 ⇒ 不判，计入待裁定清单）。

### 1.2 退出码

| 码 | 含义 |
|---|---|
| 0 | 全 PASS |
| 1 | 有 FAIL ⇒ 阻断 |
| 2 | AMBIGUOUS 超阈值 ⇒ 阻断 |
| **3** | **检查器自身错误 ⇒ 阻断，绝不静默返回 0** |

> 第 3 条对应本链第 5 个同形状：**默认值把「缺失」伪装成「一个合法的取值」**（`.or()` 顶 id · `Err ⇒ missing:true` · `unwrap_or(placeholder)` · `unwrap_or("local")` · 现在加上"工具缺失 ⇒ 退出 0"）。

### 1.3 声明源必须可生成（P8）

数字**不写进配置**：`2093` 这类是派生事实，写了就腐烂。

### 1.4 承重变异（每条检查上线前必须有一条"区分真的在查 X 与只是在跑"的变异）

### 1.5 本地可跑：`cargo test` 与 CI 同一条命令

### 1.6 误报纪律：记录"被标无用/被绕过"的次数，>10% 观察 · >25% 关停

---

## 2. 豁免：**唯一一套、带外、带期限**

`ci/baseline.toml`（与 CI-1/CI-4/CI-5/CI-6 共用）。

| 硬规则 |
|---|
| `owner` + `due` 必填 |
| **到期 ⇒ 自动失效，检查重新变红**（不自动续期） |
| 豁免总数**只减不增** |

> **已撤回「文件头自注解」方案**——那是 PFP `Override-Flag` 的复刻（被审查者自签通行证），违反 P4。
> **文件不该能给自己开豁免；豁免只能从外面给，且必须带期限。**

---

## 2.1 🆕 P15（候选）：**允许自证，但自证必须可验证**

> **任何一个"我自己声明我属于例外"的机制，必须同时存在一个不依赖该声明的客观判据；判据缺失 ⇒ 不得上线。**
>
> **标签是索引，判据是闸门——两者不能是同一个东西。**

**本链的三处落地（这就是它从候选走到原则的证据）**：

| 自证形式 | 客观判据（不依赖该声明） |
|---|---|
| `//! DATA-ONLY` | **分支预算 ≤10**（`config.rs` 实测 7 条生产段分支） |
| `//! vector-category: X` | **同一文件必须出现清单路径** ⇒ 才是加载器而不是注释 |
| 文件头契约摘要 | **不计入 NCLOC** ⇒ 写摘要"免费"，与控体量同向 |

**⇒ 与"文件头自注解豁免"的关键差别不在写在哪个位置，在有没有第三方能核的判据。**
**⇒ 状态：候选**（需阳性对照：找出至少一处"自证存在但判据也确实独立"的反例，或证明该原则不要求判据与被证项同源）。

---

## 2.2 due 交叉核对（2026-09-18，实测）

| 日期 | CI-4（BIND-19） | CI-6（Anaphase） | 合计 |
|---|---|---|---|
| 2026-10-02 | — | 1 | 1 |
| **2026-10-16** | **8** | 2 | **10（峰值）** |
| 2026-10-30 | 8 | 1 | 9 |
| 2026-11-13 | 3 | 2 | 5 |
| 2026-11-27 | 4 | 1 | 5 |
| 2026-12-11 | 5 | 1 | 6 |

**⇒ 两仓闸门互相独立**（各自的 `cargo test` 各自阻断）⇒ **不会"某天同时关掉一个闸门"**。
**⇒ 但峰值（10-16 / 10-30 各 8 条）的真实风险不是"同一天"，是"那天要交付的东西没人验证过做得到"。**
**⇒ 可行性预验已制度化，且**第一次执行就救了一个日期**：
- `pfp_codec` 5 条当天交付（`src/pfp.rs` 已有实现）⇒ **证明"接线"很便宜**；
- **但那只证明了接线便宜，不证明其它批次**——所以 `sap_codec`+`pah_signature` 同批（10-16）在到期前先核：
  - `sap_codec`：纯编解码、实现已有 ⇒ **交付**（豁免删除）；
  - `pah_signature`：`src/sap.rs` **只有字段拷贝，无 64 位截断、无验证** ⇒ **从零实现** ⇒ **重排到 2026-11-27**，理由写进豁免单。
**⇒ 结论：一个 due 在被依赖之前必须先被核过；核一次的成本是一个 grep，不核的成本是关闸。**

---

## 3. 六条检查

### ✅ CI-4｜测试向量加载 —— **已落地（2026-09-18）**

#### ⚠️ 三层语义：CI-4 证明的是哪一层（**必须先读**）

| 层 | 问的是 | CI-4 现在 |
|---|---|---|
| 1 **可达** | 向量被读到了吗 | ✅ |
| 2 **能力** | 它跑出来对吗（**vs golden**） | ✅（M8 落地后） |
| 3 **合规** | 它与**规范**一致吗 | ❌ **且现在不可做** |

**⇒ 第 3 层不可做，因为规范自己四处矛盾**（K-014 `Reserved` 三处互斥 · K-015 四方声明各异）——**没有可作为基准的规范值。**
**⇒ 因此必须写死一句**：**CI-4 不证明实现与规范一致。**
**⇒ 豁免的关闭条件同步**：每还清一批，只关掉 **第 1 + 第 2 层**，**不得读作"规范已一致"**。否则 28 条还清之后会产生一个假的"合规"印象。

**实现**：`commonintents/BIND-19/tests/vector_manifest.rs` + `commonintents/BIND-19/ci/baseline.toml`
**运行**：`cargo test --test vector_manifest`（与 CI 同一条命令）
**对应**：K-013

**双向规则**：向量有而测试未引用 ⇒ FAIL；测试引用了不存在的 ⇒ FAIL。
**范围**：只扫 `tests/`。**`examples/` 故意排除**——生成器就在那里，算进去等于"因为写了所以通过"，而本检查要抓的恰恰是"没人读"。

**实测判据（全部已跑）**：

| # | 变异 | 结果 |
|---|---|---|
| M1 | 删掉一条豁免（该向量未被引用） | ✅ FAIL，**点名该向量** |
| M2 | 正常态 | ✅ PASS（2 passed） |
| **M3 ★** | **往清单加一条向量** | ✅ **FAIL** —— 承重：没有它只会验"测试能跑"，不会验"向量被用" |
| M4 | `CI_TODAY` 推过 `due` | ✅ FAIL，**点名 33 条全部过期** |
| M5 | 测试引用不存在的 id | ✅ FAIL，**点名幻影** |

**当前状态**：**23 条豁免**（两批共 10 条已交付删除），**按 category 分批**：

| 批次 | category | 条数 | due | 依据 |
|---|---|---|---|---|
| ✅ 已交付 | `pfp_codec` | 5 | — | `src/pfp.rs` 已有编解码与单测 ⇒ **可证真而非承诺** |
| ✅ **已交付** | `sap_codec` | 5 | — | 纯编解码，`SapHeader` 已有实现 |
| ⏭ **已重排** | `pah_signature` | 3 | **2026-11-27** | **预验发现：`src/sap.rs` 有 `pah_signature` 字段但无截断、无验证（规范 `sap:3.7`）⇒ 从零实现，不是补测试** |
| 2 | `replay_protection` · `rule6_downgrade` | 8 | 2026-10-30 | |
| 3 | `catastrophic_detection` | 3 | 2026-11-13 | |
| 4 | `key_rotation` | 4 | 2026-11-27 | |
| 5 | `frame_codec` | 5 | 2026-12-11 | |

**⚠️ 分批的理由（已实测）**：33 条**不共享加载器**——`tests/` 中**零 parse 调用** ⇒ **8 个 category 是 8 个独立实现**。共用一个 due 会在某天早晨把整仓变红，结果是**闸门被关掉而不是被满足**。**能达成的期限才是期限。**

**加载器的"引用"口径（重要）**：加载器从清单读 id，**不在源码里写 id**。硬要求写 id 会逼加载器**重抄向量**——正是本检查要消灭的"手工同步副本"。
⇒ 允许测试声明 `//! vector-category: <name>`，**但该声明被验证而非被信任**：同一文件必须同时出现清单路径。
**⇒ 两个变异证明不可空转**：移除清单路径 ⇒ 红；删掉加载器 ⇒ 红。

**⇒ 检查器自身的两个缺陷（用它时才发现，读它看不出来）**：
1. 豁免匹配最初按"从 id 前缀猜的 category"组装键，而真实 category 是 `catastrophic_detection` 而 id 是 `catastrophic-…` ⇒ **全部豁免静默失效**；
2. tokenizer 把 `catastrophic-` 当成 id —— **它自己源码里 `c == '-'` 那段被自己扫到**。
> **过严的检查会被忽略，而被忽略的检查正是这一族要命名的那件事（§1.6）。**

---

### CI-6｜行数预算（NCLOC）

**口径**：**NCLOC**（除空行与全部注释，含 `///` `//!`）· 测试与 `.md` 不计 · 内联 `#[cfg(test)]` **第一版计入**（精确排除需括号配对，脚本错会**静默少算**）。

**工具**：`tokei`（已装 v15.0.0；正确处理字符串内的 `//`——自写脚本会踩这个坑）。

#### 阈值：**已实测锁定 = 300 NCLOC**

**实测依据（2026-09-18，`anaphase-helix`）**：

| 文件 | NCLOC | 处置 |
|---|---|---|
| `run_cycle.rs` | **1506** | 超限 ⇒ 豁免（拆分方案 = `ADR-0042`） |
| `main.rs` | 913 | 超限 ⇒ 豁免 |
| `contract/mod.rs` | 598 | 超限 ⇒ 豁免 |
| `bin/up.rs` | 569 | 超限 ⇒ 豁免 |
| `adapters/http_reasoning.rs` | 449 | 超限 ⇒ 豁免 |
| `rails.rs` | 369 | ✅ 合规 |
| `adapters/mind.rs` | 363 | ✅ |
| `config.rs` | 350 | ✅（**DATA-ONLY**，见下） |
| `bind.rs` | 301 | ⚠️ **卡线**（1 行余量） |
| `criteria/mod.rs` | 291 | ✅ |
| `session_events/identity.rs` | 168 | ✅（拆后最大逻辑文件） |

**标定值**：`session_events` 拆后最大逻辑文件 = **168 NCLOC** ⇒ `×1.3 = 218`。
**⇒ 未采用 218，采用 300**，理由三条：
1. 实测中位数落在 **250–370**（`rails` 369 / `mind` 363 / `config` 350）；
2. **300 一行不多杀**（291 / 269 / 261 均活），只把 **5 个真问题**逼上豁免台（是带期限的债，不是永久违规名单）；
3. 168 这个基准来自**一个被强行拆过的模块**，拿它的最大值当"自然上限"高估了约束力。

**⚠️ 已知风险**：`bind.rs`（301）离阈值 1 行 ⇒ **下一行代码就会超**。这不是缺陷，是预算在起作用的正常表现。

#### 三档分类

| 档 | 判据 | 规则 |
|---|---|---|
| 逻辑文件 | 有分支判断 | 阈值硬线 |
| **DATA-ONLY** | 字段/常量/fixture | 豁免，文件头标 `//! DATA-ONLY` |
| **GENERATED** | 生成物 | 豁免 + 禁止手改 |

#### 🆕 `config.rs` 定性：**DATA-ONLY（已核）**

**实测**：551 物理行 / **350 NCLOC** / **7 条分支语句**；结构为 `struct` 字段声明 + `impl Default` 的字段赋值 + `default_*()` 常量函数。
**⇒ 判为 DATA-ONLY，标 `//! DATA-ONLY`，不拆。**
**理由**：拆一个 7 条分支的字段表只会制造"配置在东、默认值在西"的漂移面（K-002 同族）。

#### 防钻空子

| 漏洞 | 对策 |
|---|---|
| 压成长行降行数 | 行长 >120 单独计数 |
| 拆太散用文件数换行数 | 单目录 ≤12 文件 |

#### 判据

| # | 用例 | 期望 |
|---|---|---|
| M1 | 超限且不加豁免 | FAIL(1) |
| M2 | 正常 | PASS(0) |
| **M3 ★** | **加 100 行注释** | **仍 PASS** —— 承重：证明"排除注释"真的生效 |
| M4 | `tokei` 缺失/输入不可读 | **退出 3，绝不静默 0** |

**状态**：✅ **已落地（2026-09-18）**
**实现**：`anaphase-helix/ci/check_line_budget.py` + `ci/baseline.toml`（豁免，**人签**）+ `ci/ratchet.gen.toml`（ratchet，**派生、脚本写**）；由 `tests/ci_line_budget.rs` 接入 `cargo test`（**包装层显式区分退出 1 与 3**，否则"检查器没跑"会被读成"没发现问题"）。

**已跑的变异**：

| # | 变异 | 结果 |
|---|---|---|
| M1 | 删掉一条豁免 | ✅ FAIL，点名 |
| M2 | 正常 | ✅ PASS |
| **M3 ★** | **给 `bind.rs` 加 100 行注释** | ✅ **仍 PASS** —— 承重：证明注释真的被排除 |
| M4 | 豁免缺 `owner` | ✅ **退出 3**（不是 0） |
| M5 | 给 DATA-ONLY 文件塞 12 条分支 | ✅ FAIL，点名该标记 |
| M6 | **损坏豁免键** | ✅ FAIL，点名该文件 —— **对应 CI-4 初版那个"33 条静默失效"** |
| M7 | 豁免过期 | ✅ FAIL，**且经 `cargo test` 也红**（证明包装层不是装饰） |

**计数器已与 `tokei` 逐文件对账**（`config` 350/350 · `identity` 168/168 · `criteria` 291/291）⇒ 自写脚本不是"未验证的解析器"。

**行长（120）默认 warn、不改退出码**：实测 50 处超长，多为长字符串字面量；硬阻断会造出"被绕过而不是被遵守"的检查（§1.6）。`--line-mode fail` 可在需要时收紧。

#### 🆕 ratchet 的形态（v0.4 新增，解决两个必答）

**问：基准怎么更新？** 只有一种答案是对的，另两种各有一个坏处：

| 形态 | 坏处 |
|---|---|
| 自动取当前值 | 文件变长基准跟着长 ⇒ **永远 PASS（失效）** |
| 手写固定值 | 文件变小后仍允许长回去 ⇒ **单向性被破坏，且手写即腐烂** |
| **`min(旧基准, 当前值)` + 脚本写回** | ✅ **唯一保持"只减不增"的形态**。已实现并实测：压缩 `trace.rs` ⇒ 基准 **261 → 258**；加回 ⇒ **失败**，基准**不回涨** |

**⇒ 且 ratchet 住在 `ci/ratchet.gen.toml`（标记 GENERATED）**，与 `baseline.toml`（豁免，人签）**分开**：
**ratchet 是派生的测量，不是豁免** ⇒ 检查器可以写它；**豁免必须人签** ⇒ 检查器不许写。**P4 仍然成立。**

**问：拆分成新文件会绕开 ratchet 吗？** 会 —— 新文件没有基准。**⇒ 已加目录级：每个目录的总 NOCL 也入 ratchet。**
**实测**：把一个文件拆成两半（总量不变）⇒ **通过**；往新文件灌 50 行 ⇒ **红**（`src/ 5004 -> 5054`）。
**⇒ 绕过方式变成"整目录不许变长"，而整目录变长就是真变长。**

**⇒ 新文件的规则（本条在 `run_cycle` 拆分**之前**定，正是为此）**：新文件**首次出现即被播种为当前值**（一出生就钉死，不许长）；**若这批新文件来自拆分，目录总量不变 ⇒ 目录级放行**。

**DATA-ONLY 验证器（本版新增）**：标了 `//! DATA-ONLY` 的文件**必须通过分支预算（10）**，否则 FAIL 并点名。
**⇒ 这是与"文件头自注解豁免"的关键差别**：`config.rs` 实测生产段 **7 条分支** ⇒ `7 < 10` 成立 ⇒ 标记是**可被第三方核对的主张**，不是自签通行证。

---

### CI-2｜命名残留

规则：非豁免路径命中旧名 ⇒ FAIL；豁免路径 ⇒ PASS；映射引用不存在的名 ⇒ **AMBIGUOUS**。
**M3 ★**：把映射表的 `new` 改成不存在的名 ⇒ 必须 AMBIGUOUS，不是 PASS。
**优先级**（实测）：Cellrix 27 → Anaphase 8 → Mind 2 → FlowModus 1。
**状态**：🟠 需先定豁免路径清单（历史白皮书里的旧名是**真实史料**，改了就是重写历史）。

---

### CI-3｜帧字节 golden（PFP 4B / SAP 28B）

规则：布局变化 ⇒ FAIL；值变化布局不变 ⇒ **AMBIGUOUS**；golden 缺失 ⇒ 退出 3。
**⚠️ 前置**：K-014（`Reserved` 三处互斥）、K-015（`pfp-all_zero` 四处声明互不相同）**未裁定** ⇒ **只能跑布局，不能跑值**，否则把规范自己的矛盾判成实现的错。
**状态**：🔴 部分可上线。**B18 判据④ 依赖它。**

---

### CI-1｜依赖边（Bazel visibility 等价物）

输入 `cargo metadata --format-version 1 --all-features`（**`--all-features` 必带**，否则 feature-gated 边漏检）；
声明源 `ci/deps-allowlist.toml`，**`default = "deny"`**。
**M3 ★**：从声明表删掉一条真实存在的边 ⇒ FAIL。
**状态**：🔴 需先补全 B0 矩阵（20 仓已扫 3 仓）⇒ **第一版必须 `--report-only`，首版大量 AMBIGUOUS 是预期不是故障。**

---

### CI-5｜单仓副本（One-Version Rule 小规模版）

规则：同一 remote 检出到两路径 ⇒ FAIL。
**M3 ★**：删掉某目录的 `.git` ⇒ **退出 3，不是 PASS**（`git config --get` 在非 git 目录**返回非零但不报错**——"命令成功 ≠ 命令正确"）。
**状态**：🟠 需先入基线豁免（`commonintents/BIND-19` 与根 `BIND-19` 是已知违规）。

---

## 4. 落地顺序

```
✅ CI-4（已落地）
   ↓
CI-6（阈值已实测，只差检查器）
   ↓
CI-2 → CI-3（布局）→ CI-1 → CI-5
```

> **先上一条真能阻断的，比上六条半通的更有价值——因为第一条会证明这套机制是活的。而 CI-4 已经证明了。**

---

## 5. 诚实清单

1. **阈值标定的样本量 = 1**（`session_events` 一个模块）。若其它已拆模块落点明显不同，按实测重定。
2. **注释占比 15–25% 是估计**，未逐文件实测；300 这个数因此是"实测中位数 + 判断"的合成，标 `[ENG]` 的部分在此。
3. **内联 `#[cfg(test)]` 第一版计入**，是已知简化，未实现精确排除。
4. **行长 120 / 目录 12 文件** 是建议值，**非既有规定**。
5. **CI-6 / CI-1 / CI-2 / CI-3 / CI-5 均未实现**；本文件对它们只是规格。
6. **`bind.rs` 301 卡线**（余量 1 行）——下一个人加一行就会超，这是预算在工作，不是缺陷。
7. **刻意不进 CI**：H1–H5 意图类 · 策略类 · 规范内部矛盾该修哪边 —— 它们留在文档里当**问句**，不当红绿灯。

---

## 6. 拆分额度（split_window）—— 与豁免的区别（2026-09-18 实测追加）

**实测**：`src/criteria/mod.rs` 291 NOCL → 拆成 3 文件 = **295 NOCL**（+4）。
**原因**：`mod.rs` 新增 `pub use` 行（原文件没有）⇒ **部分是永久增量**。

**⇒ 目录级 ratchet 会在拆分第一天变红，而红因是"检查器在正常工作"。**
**⇒ 两种错误处置**（report-only / 临时豁免）**都在教"重构＝关闸门"** ⇒ 采用**提前授权的额度**。

| | 豁免（waiver） | 额度（split_window） |
|---|---|---|
| 含义 | **允许超线** | **承认重构会改总量** |
| 上限 | 无（就是豁免） | **有（allowance）** |
| 到期 | 自动失效 | 自动失效 |
| 存放 | `baseline.toml`（人签） | `baseline.toml`（同文件，**但不是豁免类**） |

**三条语义（均已实测）**：
1. **只放开目录聚合**，单文件 ratchet 仍红（+30 行：目录放行、文件红）；
2. **超额仍红**（+70 行 > 60 ⇒ `src/ 5004 -> 5074 (with a +60 split allowance)`）；
3. **窗口内目录基准不写回**（否则额度被逐次吃掉），**窗口关闭后重新播种**。

**⇒ 新文件例外（D6）**：新文件一出生钉死；**但拆分窗口内出生的文件可随同额度移动一次**（否则第二天补模块头就红）。

---

## 7. A 类进展：H4 + H7 合并（降维）

**H7 已并入 H4 作为第 ③ 问** —— 两者是同一类问题（**谁是权威**）：

| | |
|---|---|
| H4① | 两份 BIND-19 检出，哪一个是工作副本 |
| H4② | 该仓内部，`spec/` 正文与 `src/` 谁定线格式 |
| H4③ | K-015 四方不一致（清单 `cf143000` vs 规范表/README `cf14 00 00`），谁对 |

**⇒ A 类从 6 个决策位降到 5 个，且最关键的那项一次答完。**
**⇒ 提醒（写给下一个会话）**：B 类里不依赖人类的部分已基本做完；**剩余每一项都在等 H1–H5**。
