# CI-144 / Anaphase「默认放行」分类表（B0′）

> 建立：2026-09-17 ｜ 对象：**B0′**
> 用途：**枚举所有"默认放行 / 缺省通过 / 失败继续"的分支，并分类——不是翻转清单。**
> 依据：`CI-144_决策索引.md §2 B0′`；刹车来自 P6（可选应缺省到严）与 Axiom C（优雅降级，`anaphase-helix/README.md:424`）。

---

## 0. 两条硬规则（本表的存在理由）

> **规则 1（分类，不是翻转）**：**本表的产出是分类，不是动作。**
> 任何"翻成 fail-closed"的提议，**必须先落进「安全判定」类，并指出它保护的是哪个外部动作**。
> **理由**：本仓 `fail-open` 至少 10 处明写，其中多数是**依赖降级**——翻它等于"记忆不可用即停机"，直接违反 Axiom C。

> **规则 2（判据：职责决定 fail 方向）**：**一个组件判不出时，应回到它职责的"默认答案"。**

| 职责 | 判不出时的答案 | 已实测的实例 |
|---|---|---|
| **决定"能不能执行"** | **不能**（fail-closed） | ✅ 结构化路径的 HITL（`run_cycle.rs:1561-1566`，`_ =>` 即阻断） |
| **只做记录 / 给意见** | ⚠️ 取决于它是否被当成决策者 | ⚠️ `safety.audit`（`run_cycle.rs:1247-1250`）**被当成决策者，却 fail-open** |
| **提供依赖** | **无依赖**（fail-open） | ✅ 记忆解析（`adapters/mod.rs:141-152`）、配置容错（`config.rs:360`） |
| **维持自身存活** | **继续存活**（fail-open） | ⚠️ 待 H5 定性（`run_cycle.rs:1181-1183`） |

> **规则 2 的状态**：**候选原则 P13，尚未取得"穷尽"钥匙**（见 `CI-144_更正史.md`）。
> **它的阳性对照是它自己的反例**：`security_gate` 职责同样是"决定能不能"，却缺省放行（#2）⇒ **判据不错，是没人遵守它。**

---

## 1. 分类表（**计数口径见 §2**）

| # | 位置 | 触发条件 | 失败/缺省时 | 当前生效 | 归类 |
|---|---|---|---|---|---|
| **1** | PFP optional（`pfp:1.2` ↔ `sap:6`） | 帧内无 PFP | **规范三处不一致，未定义** | **未定** | 待规范裁定（`更正史 §6.8.1`） |
| **2** | `pipeline/mod.rs:97,118` `security_gate: None` | 生产从不构造闸门（`main.rs:694` 无 `with_security_gate`） | **跳过闸门检查**，直达执行 | ✅ **生效** | **安全判定** |
| **3** | `reflex.rs:14` `hard_reflex` deny-list | 动作不含任一规则子串 | **放行** | ⚠️ **未接线**（守的是 `echo` 参数，见 K-009/K-011/K-021） | **未生效 4a** |
| **4** | `run_cycle.rs:1181-1183` `soft_reflex` `Err` | 恐惧预测失败 | `ReflexPassed`（原文 `default allow`） | ✅ **生效** | **待 H5**（求生 / 安全） |
| **5** | `run_cycle.rs:1247-1250` `safety.audit` `Err` | 审计返回 `Err` | `Ok(Success)`（原文 `fallback allow`） | ❌ **未生效 4b**（唯一 impl 恒 `Ok(true)`） | **4b 不可达** |
| **6** | `run_cycle.rs:1223-1227` 记忆检索 `Err` | Mind 不可达 | `Ok(Success)`，继续下一状态 | ✅ **生效** | **依赖降级** |
| **7** | `run_cycle.rs:1046-1050` 推理 `Err` | LLM 失败 | `Ok(Impass)` ⇒ **流向 `Reflection`**（`run_cycle.rs:354`）并置 `outcome.impasse`（`:667`） | ✅ **生效** | **依赖降级**（见下方 #7 的更正） |
| **8** | `pipeline/mod.rs:352-362` `resolve_pipeline` | endpoint 空 / 连接失败 | 返回 `None` ⇒ 回落路径 | ✅ **生效** | **依赖降级** + **与 #2 复合**（见 §3） |
| **9** | `config.rs:143` / `config.rs:360` | 配置未启用 / 环境变量未知 | 忽略、不影响循环 | ✅ **生效** | **依赖降级** |
| **10** | `adapters/mod.rs:141-152` 记忆适配器解析 | endpoint 空 / 连接失败 | `NoopMemoryAdapter` + warn | ✅ **生效** | **依赖降级** |
| **11** | `run_cycle.rs:1561-1566` 结构化 HITL `_ =>` | `Err` 或 `Ok(false)` | **`Ok(Failure)`——阻断** | ✅ **生效** | **安全判定（已 fail-closed）** |

---

## 2. ⚠️ 计数口径（**禁止合并计数**）

| 口径 | 数值 |
|---|---|
| **当前生效** | **8 处**（#2 #4 #6 #7 #8 #9 #10 #11） |
| **其中属「安全判定」** | **2 处**（#2 fail-open ❌ · #11 fail-closed ✅） |
| **未生效** | **2 处**（#3 未接线 4a · #5 不可达 4b） |
| **未定** | **1 处**（#1 规范内部矛盾） |
| **依赖降级（依 Axiom C 应保持 fail-open）** | **4 处**（#6 #8 #9 #10） |

> **⇒ 真正"应当翻"的，在本次扫描范围内只有 1 处：#2。**
> **⇒ "Anaphase 有 N 处 fail-open"这种说法一律作废**——按口径说话。

---

## 3. 🔴 本次扫描的两个产出（它们不是登记，是新发现）

### K-025 ｜**同一个"闸门判不出"的问题，两条路径给出相反答案，而弱的那条是默认路径**

| 路径 | 守卫 | 判不出时 | 位置 |
|---|---|---|---|
| **结构化路径**（有 pipeline） | `hitl.check_approval` | **阻断**（`_ =>` ⇒ `Failure`） | `run_cycle.rs:1561-1566` |
| **回落路径**（无 pipeline，**默认**） | `hitl.check_approval` | **`Err` ⇒ 阻断**（自陈 fail-closed） | `run_cycle.rs:1254-1258` |
| | `safety.audit` | **`Err` ⇒ 放行**（自陈 `fallback allow`） | `run_cycle.rs:1247-1250` |

**⇒ 同一个作者、相邻两个 `match`、相反的两个选择**（这是 `更正史` 里记的"每处单独决定过"的实证）。
**⇒ 而结构化路径的统一 `_ =>` 阻断是更强的语义**：它连 `Err` 也当阻断。
**⇒ 两条路径不一致的后果**：**默认路径（无 pipeline）的守卫比结构化路径弱**，而默认路径恰好是 `resolve_pipeline` 失败时的落点（#8）。

### K-026 ｜**`is_high_risk` 不看参数，而它同时是"低风险直通"与"高风险放行"的判据**

**证据（源码逐字）**：

```rust
hitl.rs:35   pub fn is_high_risk(command: &str) -> bool          ← 只吃一个字符串
hitl.rs:66-71  pub fn check_approval(&self, command: &str, args: &[String]) -> Result<bool,String> {
                   if !Self::is_high_risk(command) {
                       return Ok(true);                       ← 低风险 = 不经人类，直接放行
                   }
                   (self.approver)(command, args)              ← 仅高风险才用 args
               }
```

**⇒ 两条推论**：
1. **`args` 只在"已判定为高风险"之后才被使用** ⇒ **`&[]`（结构化路径传空切片）不是丢上下文，是因为这个函数本来就用不到**。⇒ 这**印证**了此前把 `&[]` 降为 B4「症状」的裁定。
2. **`is_high_risk` 是双重关键**：它既决定"要不要问人"，也（经由 `?`）决定"能不能不问就执行"。**它判错的代价不是漏问一次，是直接放行**（如 `sh -c` / `python -c` / `env` / `xargs` 按 `hitl.rs:82-92` 的既有单测均**不在高风险词表**）⇒ **这正是 B4 的完整代价，此前只写成"漏报"。**

---

## 4. 明确**不**翻的（"不做，因为 X"）

| 位置 | 不翻的理由 |
|---|---|
| #6 记忆检索失败 | 翻了就是"记忆不可用 ⇒ 停机"，违反 Axiom C；且它保护的不是外部动作 |
| #7 推理失败 | **不翻，因为它不是 fail-open**：它不继续执行，而是转入 `Reflection`（`run_cycle.rs:354`）并置 `outcome.impasse`（`:667`）。<br>**⚠️ 更正**：本表草稿曾写"中止本轮、方向已正确"——**两处都不准确**（`Impass` 不是终止态）。<br>**⚠️ 且有一个未核**：**"标记 `impasse` 等于已上报人类"未经核实**——`hitl.check_approval` 只在 `Execution` 被调用；`impasse` 的去向（是否触发 HITL 或 CAPABILITY-13 挑战-响应）**未核** ⇒ 列入 §5 |
| #8 `resolve_pipeline` | 依赖解析，同 #6；**但它的复合后果见 §3 K-025** |
| #9 配置容错 | 未知环境变量忽略是配置层常态 |
| #10 记忆适配器解析 | 同上（`DNA 铁律 6` 的原文就是"所有依赖必须有降级策略"） |
| #3 `hard_reflex` | **不翻，因为不该改它**：它未接线（R1）。改一个不生效的 deny-list 是装修一间不在承重墙上的房间 |
| #5 `safety.audit` `Err` | **暂不翻**：4b 不可达；**但接真实 adapter 前必须先定调**（B14′ 关联） |

---

## 5. 未核（不宣称）

1. **本表只扫了 `anaphase-helix/src/`**（两链范围内）。**未扫**：`Tuck/crates`、`Cellrix`、`FlowModus`、`helix-tentacle`、`helix-mind`。
   ⇒ **若 Tuck（边缘物理闸）有同类分支，本表结论不适用于它。**
2. **#1（PFP optional）**：规范内部矛盾（`更正史 §6.8.1`），未裁定 ⇒ **计数口径因此仍带一个"未定"。**
3. **`GateVerdict` 无 `Err` 变体** ⇒ 闸门**不能表达"判不出"**；**构造失败被折叠进 `None`**（B14′）。**本表未把"闸门不存在"与"闸门判不出"分开计数**，因为后者无法表达。
   ⇒ **⚠️ 此处的 `GateVerdict` 是 `anaphase-helix` 的类型**（`src/security.rs:57-74`），**不是 Tuck 的** —— Tuck 侧同形类型名 **`GateDecision`**（`crates/tuck-core/src/anaphase_bridge.rs:151-162`），且在按 §5.1 本表未扫的范围内。归属订正见 **K-106**。
   ⇒ **🔴 且「构造失败被折叠进 `None`」一句已被独立复核推翻（K-109）**：生产路径上**根本不构造闸门**（`pipeline/mod.rs:113-118` 硬编码 `None`；`with_security_gate` 仅被 `tests/` 调用），故**不存在**"构造失败⇒`None`"这一折叠。**该句的支撑只是一句注释。**
   ⇒ 实际状态更严重也更简单：`None` 的含义是「**从未装配**」，而它对**低风险**工具静默放行（`pipeline/mod.rs:165-212`）⇒ 那一半情形下**"闸门在不在"仍不可判**。
4. **扫描方法为定向 grep（`fail-open` / `Err(e) =>` / `unwrap_or`），非穷尽** ⇒ **不得声称"已枚举全部"。** 按 P7，本表的"穷尽"钥匙**未取得**。
5. **`impasse` 的去向未核**：#7 的推理失败会置 `outcome.impasse`（`run_cycle.rs:667`），但**它是否触发 `hitl`/CAPABILITY-13 的挑战-响应，未查**。⇒ **"推理失败后人类会不会被告知"这个问题，本表答不了。**
