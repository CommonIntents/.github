# CI-144 协议家族 DNA.md
> © 2026 CommonIntents Organization · Apache 2.0

> **版本**：v1.2
> **日期**：2026-08-29（v1.0）｜ 2026-09-21（v1.1：新增原则 7；补原则 1 / 原则 4 的工程映射）｜ 2026-09-23（v1.2：锚定 phyt-DNA v1.0 —— 补方法论归属；**公理内容未变**）
> **继承自**：Helix-Mind DNA.md（**机制模板**——五件套流转 / 归档 / ADR 两态）
> **性质**：CI-144 协议家族的不可变原则与自生长流程
> **所属方法论**：**phyt-DNA v1.0**（方法论锚点项目 `https://github.com/Jasonmilk/phyt-DNA`）
> **适用范围**：CommonIntents 全部协议仓库（INTENT-7 / BIND-19 / INTENT-7-SECURE / CAPABILITY-13）

## 协议家族的自白

Helix 项目的 DNA 定义了代码项目的"如何生长"。CI-144 协议家族的 DNA 定义了协议规范的"如何演化"。

协议与代码不同：
- **代码**可以快速迭代，错误可以在下一个版本修复。
- **协议**一旦发布，就会被多个消费方实现，错误的修复成本极高。
- 因此，协议的 DNA 更强调**向后兼容**、**决策可追溯**、**变更有门槛**。

修改协议 DNA 等于修改协议家族的身份，旧身份的信用不会转移。这不是社会规则，是生态事实。

**本文件是所有 CI-144 协议变更的最高裁判。** 任何 RFC/PR 若与 DNA 冲突，以 DNA 为准。

## 一、不可变原则（7 条公理）

以下 7 条原则是 CI-144 协议家族所有设计决策的最终依据。

### 原则 1：spec 正文是唯一源真相
协议的语义、字段、偏移、枚举值，全部以 `spec/` 目录下的规范正文为准。README、门面文档、第三方实现都必须对齐 spec，防版本漂移。

> **工程映射**：每次发布前执行 spec ↔ README 对齐检查；消费方实现以 spec 为契约，不依赖 README 的描述。
> **工程映射（判据必须指向被消费的对象）**：**判据必须指向"实际被消费的那个对象"，不得指向其代理。**
> 反例（已实测）：比对 `git ls-remote` 的分支尖端 ≠ 构建实际使用的对象 —— 构建消费的是 `Cargo.lock` 钉住的 SHA，
> 前两者都指向了代理，于是"远端已含修复"给出了**虚假安心**。同类四例见 `CI-144_合拢坑记录.md` §17.15。

### 原则 2：Append-Only 扩展，禁止静默修改
已发布版本的协议语义（v1.0.0-RFC-4 及以后）不可静默修改。所有扩展走 Append-Only 模式：
- 新增字段使用 reserved 预留位
- 新增枚举值使用未分配值
- 新增帧类型使用未分配类型号
- 废弃字段标记为 deprecated，但不删除（防消费方解析错误）

> **工程映射**：PAL 第 15 位 Reserved 强制为 0，未来扩展走 RFC/ADR 流程；BIND-19 帧类型 0x07 需在 v2.0-alpha.1 前确认未被占用。

### 原则 3：决策先于变更（ADR 强制）
任何协议变更（字段新增、语义修改、帧类型分配）必须先有 ADR（D 层冻结），再改 spec 正文（A 层），最后同步门面（N 层）。无 ADR 的变更直接拒绝。

> **工程映射**：ADR 存放于各协议仓库的 `docs/decisions/` 目录；两态（Draft/Active），Active 后不可覆写，仅可 Superseded。

### 原则 4：向后兼容是硬约束
新版本协议必须能与旧版本共存：
- v2.0 发送端必须能被 v1.0 接收端解析（忽略未知字段）
- v2.0 接收端必须能处理 v1.0 帧（按旧逻辑处理）
- 不兼容的变更必须升级主版本号（v1.x → v2.0），并提供迁移路径

> **工程映射**：BIND-19 新增 PAL-Present 标志位，v1.0 接收端忽略；PAL-Version 字段支持版本协商；Reserved 位非零触发版本协商流程。
> **工程映射（未知必须显式表示）**：**未知必须显式表示；危险的是「缺失缺省」。**
> 「透传」的含意是**保留显式表示**（保留原码 + 记录来源），**不是让未知通过**。
> 已实测反例：`RiskLevel::Unknown` 曾被判 `Error`、而**字段缺失**反被判 `Warning` —— 系统惩罚诚实声明、奖励沉默，
> 于是最便宜的攻击不是伪造危险值，而是**把字段剥掉**。同类七例见 `CI-144_DNA修订提案.md` A。

### 原则 5：物理事实优先，AI 语义不参与协议决策
协议字段的语义由物理事实（传感器读数、硬件状态、网络条件）驱动，不由 AI 的语义推理或网络信任决定。AI 无权修改协议头部字段。

> **工程映射**：PAL 头部字段（Modality/Stance/Edge/Risk-Level）由传感器驱动生成；PAH（Physical-Context-Hash）由传感器数据哈希锁定，防 AI 伪造。

### 原则 6：极致节能，固定路径无分支
协议设计优先选择固定计算路径，避免分支判断：
- 固定偏移头部（PAL 24 字节 = 3×64-bit 对齐 SIMD）
- 固定加密算法（AES-GCM，无"是否加密"分支）
- 固定哈希截断（SHA-256 前 64 位，跨实现一致）
- 明文元数据不解密（Tuck 只读 PAL 头部，不解析载荷）

> **工程映射**：Tuck 硬实时决策路径零分支；固定路径可利用硬件加速流水线；分支判断打断流水线，产生额外能耗。

### 原则 7：机制的覆盖范围必须与其声明的范围一致
一个机制声明自己覆盖什么，就必须**实际**覆盖什么。声明与机制不一致时，**声明是错的**（除非改的是机制本身，那需要另一次决策）——因为**读者依据声明作判断，而声明不会被任何东西自动检查**。
- 手维护的仓列表 ≠ 守卫实际覆盖的仓
- 注释里写的范围 ≠ 正则实际匹配的范围
- 注册表的词表 ≠ 消费方各自硬编码的合法集

> **工程映射**：每处声明覆盖的机制，必须有**断言**核对"实际覆盖 = 声明覆盖"，不一致 ⇒ 红。
> 先例（已落地）：`commit-msg.scope.sh` + `guarded_paths.tsv`（表驱动，逐行核对注释声明与正则实际行为）。
> 反面实例（已实测）：同一处守卫范围注释**写错两次**，抓住第二次的不是人眼，是**表驱动断言** ——
> **文档会漂移，断言不会。** 同类六例见 `CI-144_DNA修订提案.md` B。

## 二、分层自纠偏系统（N/D/A 三层）

| 层级 | 名称 | 形式 | 作用 |
|---|---|---|---|
| **N 层** | 叙事层（愿景） | README / VISION / GOVERNANCE | 定义"协议家族应该是什么样"的顶层叙事。所有决策的最终判断依据 |
| **D 层** | 决策层（ADR） | `docs/decisions/ADR-XXXX-*.md` | 记录每一次协议变更的"为什么"和"放弃了什么" |
| **A 层** | 架构层（spec + 参考实现） | `spec/`、`reference/` | 协议规范的最终形态。spec 正文是真理来源的最终载体 |

## 三、文档生态 SOP（DNA v2.0 协议家族适配版）

| 文档 | 职责 | 规则 |
|---|---|---|
| **GOVERNANCE.md** | 组织级治理宪章 | 防腐化基线，变更走 RFC / 审查流程 |
| **DNA.md** | 组织级不可变原则 | 本文件，协议家族最高裁判 |
| **RNA.md** | 组织级 AI 协作铁律 | 三层加载协议 + AI 协作约束 |
| **PLAN.md** | 协议级当前阶段导航 | ≤150 行，超出触发历史迁移；仅含当前阶段 + 下一阶段预览 |
| **GROWTH.md** | 协议级已完成阶段生长记录 | ≤3 条，超则归档至 `docs/archive/growth/` |
| **ADR** | 协议级决策记录 | 两态（Draft/Active），Active 后不可覆写，仅可 Superseded |
| **spec/** | 协议级规范正文 | 唯一源真相，Append-Only 扩展 |
| **归档** | 历史记录 | 随仓库版本化，永不删除 |

> **关键规则**：
> - 阶段完成并记录到 GROWTH.md 后，从 PLAN.md 移除该阶段内容
> - 归档文件随仓库版本化（不忽略）
> - 提交前必须人工确认
> - 协议级 ADR 编号为 4 位数字（`ADR-NNNN-*.md`），**每仓自增**：下一个号 = **本仓** `docs/decisions/` 里的最大号 + 1，**不需要也不得为挑号去扫描其他仓**
> - **跨仓引用一律仓名限定**（`anaphase:ADR-0042` ≠ `helix-mind:ADR-0042`）—— 裸编号在跨仓语境下必然歧义
> - **已分配的编号不回改**（重写历史 + 打断全部交叉引用）；冲突只在**新**分配时避免
> - ⚠️ **本条原文只写「按顺序递增」，未说明是"每仓"还是"全生态"** —— 两种读法各被采纳过，
>   结果 `ADR-0042` 在 2026-09-17 与 2026-09-18 被 `helix-mind` 与 `anaphase-helix` **同时持有**。
>   **歧义写在规则里，就会在实现里分叉。** 改动理由的完整证据见 `anaphase-helix:docs/decisions/README.md` 的编号说明。

## 四、防腐化铁律（5 条）

| # | 铁律 | 说明 |
|---|---|---|
| 1 | **版本以 spec 正文为源真相** | README/门面标注必须对齐，防版本漂移 |
| 2 | **协议语义冻结不可静默修改** | 扩展走 Append-Only / reserved 预留；不兼容变更升级主版本号 |
| 3 | **变更先 ADR（D 层冻结）→ 改 spec → 同步门面** | 决策先于代码/规范 |
| 4 | **生长记录保留近 3 条，超则归档** | 历史永不删除，但按需加载 |
| 5 | **提交前必须人工确认** | 无自动提交；协议变更影响所有消费方，必须人工审查 |

## 五、与 Helix-Mind DNA 的关系

| 维度 | Helix-Mind DNA | CI-144 协议家族 DNA |
|---|---|---|
| **哲学核心** | 记忆不可篡改、相态河流、意志优先 | spec 源真相、Append-Only、向后兼容、物理事实优先 |
| **N 层** | VISION.md（记忆中枢定位） | GOVERNANCE.md + README（协议家族定位） |
| **D 层** | ADR-0001~0021 | ADR-0001 起（各协议仓库独立编号） |
| **方法论机制** | PLAN/GROWTH/ADR 流转 | **完全复用 phyt-DNA v1.0 模板**（`template/DNA.md`：工程映射表 + 五条通用防腐化铁律） |
| **归档规则** | 历史入仓，不忽略 | **完全复用** |
| **演化节奏** | 快速迭代（每周/每月） | 缓慢演化（每季度/每年），变更门槛更高 |

**关键原则**：方法论机制（五件套流转、归档规则、ADR 两态）**完全复用 phyt-DNA v1.0 模板**。哲学内容（**7 条原则**）独立编写，适配协议家族的特点（向后兼容、Append-Only、spec 源真相）。

> **⇒ 归属订正（2026-09-23，v1.2）**：本文件此前写「所属方法论：DNA 自生长方法论 v2.0（协议家族适配版）」——
> 那是**把 Helix-Mind 那份 DNA 文档的版本号误当成了方法论版本号**。两者的版本号不在同一层：
> `Helix-Mind DNA v2.0` 是**一份文档**的版本，`phyt-DNA v1.0` 是**方法论**的版本。
> ⇒ v1.2 起锚定 **phyt-DNA v1.0**，Helix-Mind 只作为**机制模板**被引用。**公理内容未变**（仍是 7 条）。
>
> **⇒ 且"采用方法论"与"被其它项目采用"是两件事，本文件同时成立**：
> - **内部自生长力**：CI-144 是 phyt-DNA 的**采用者** ⇒ 它有资格在自己的实践里催生机制改进（那些改进再走 phyt-DNA 的审查流程）；
> - **外部生长力**：CI-144 是**被消费的协议** ⇒ 它还要等其它项目**测试并复用**，才谈得上把某项实践升格为方法论。

## 六、一句话总结

> **CI-144 协议家族 DNA.md 是规范的基因锁。它**按 phyt-DNA v1.0 方法论生长**（机制完全复用其模板），但 7 条原则独立编写（spec 源真相、Append-Only、向后兼容、物理事实优先、极致节能、**机制覆盖范围＝声明范围**）。任何协议变更必须与这 7 条原则对齐，修改 DNA 等于修改身份，旧身份信用不转移。**

---

## 七、版本历史

| 版本 | 日期 | 变更 | 依据 |
|---|---|---|---|
| v1.0 | 2026-08-29 | 6 条原则成立 | —— |
| **v1.1** | **2026-09-21** | **新增原则 7**（机制覆盖范围＝声明范围）；原则 1 补入"判据指向被消费对象"；原则 4 补入"未知必须显式表示" | `docs/ADR-0001-DNA修订-A-C采纳-B单列.md`；证据见 `CI-144_DNA修订提案.md` |
| **v1.2** | **2026-09-23** | **锚定 phyt-DNA v1.0**（补方法论归属）；订正"所属方法论"一行 —— 原写「DNA 自生长方法论 v2.0」是把 Helix-Mind **文档版本**当成了**方法论版本**。**公理内容未变；公理数仍为 7** | 人类裁定（2026-09-23）：CI-144 作为 phyt-DNA 的**采用者**锚定之；Helix-Mind 退为**机制模板**引用 |

> **注 1**：v1.1 **改动了公理数（6 → 7）** ⇒ 按本文件"修改 DNA 等于修改身份"的声明，**旧身份的信用不转移**。
> **注 2**：v1.2 **不动任何公理**，只修方法论归属与生态对齐表述 ⇒ 不构成身份变更。

---
*《CI-144 协议家族 DNA.md》v1.2 完。*
