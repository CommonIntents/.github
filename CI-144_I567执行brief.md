# I5 / I6 / I7 执行 brief（重建版）

> **性质**：执行指令。重建自 `CI-144_跨链生命周期与反馈契约.md` §13.3 / §13.4。
> **重建原因**：原 brief「载于执行方会话」，该会话未留存。本文件为**重建**，非原文。
> **重建人**：接手方（2026-09-21）。**依据只有契约 §13.3/§13.4 与四条硬约束**，未添加新要求。

---

## 0. 一句话

把 §4.2 的 I5/I6/I7 三条不变量**从文字变成可跑的断言**，落进**已有的**测试套件与 shell 断言里。
**不是**新建检查器，**不是**修业务逻辑，**不是**改闸门行为。

---

## 1. 三条不变量（契约 §13.3 原文）

| id | 不变量 | 断言形态（**判据必须指向实际被消费的对象**） | 现已违反的实例 |
|---|---|---|---|
| **I5** | **覆盖范围 = 声明范围** | 对每一处声明覆盖的机制，断言其**实际**覆盖；不一致 ⇒ 红 | `cargo metadata` / CI-7 / L2 / `ls-remote` / `GUARDED` 正则 / **同一注释两次**（B 的 6 例） |
| **I6** | **缺失不得比显式未知更宽松**（单调性） | 注入空字段与显式 `unknown` ⇒ 断言前者**不轻于**后者 | R005 曾反向（已修，K-105） |
| **I7** | **「闸门在不在」必须可区分于「闸门放行」** | 断言零拒绝时，能区分"没有门"与"门开了但没事" | Tuck `0 block` vs `SecurityPolicy::default()` 无装配（K-061） |

---

## 2. 硬约束（**违反任何一条即视为未完成**）

1. **不新建检查器** —— 不新建脚本类型、不新增"框架"。
2. **不新建目录**。
3. **不碰 `ci/`** —— 冻结令（契约 §13.4）。
4. **不改任何闸门行为** —— `commit-msg` 里的 `GUARDED=` 正则**一字不动**；非注释改动必须为 0 行。
5. **不用 `git reset --hard`** —— 探针清理用 worktree / `git stash` / 只删探针文件。
6. **每条断言必须证明"能红"** —— 给出 RED→GREEN 的变异证据（改坏 ⇒ 断言红；改回 ⇒ 绿）。
7. **已被既有断言覆盖的，引用它，不要复制它。**
8. **绝对路径**。读输出（含退出码 / `test result` 行），**不要用 `| tail` 掩盖退出码**。
9. **收尾扫全部改动过的仓**：`git status --porcelain`，确认无探针残留。

---

## 3. 落点（契约 §13.4 指定的既有形态）

| 形态 | 位置 | 说明 |
|---|---|---|
| 各仓自己的测试套件 | `HelixECO-Glove/core/tests/r005_risk_disposition.rs`（**已存在**） | I6 落这里 |
| `tools/hooks/` 的 shell 断言 | `helix-mind/tools/hooks/`（**已存在**） | I5 落这里 |
| 既有 runner | `helix-mind/tools/hooks/diff-ratio.acceptance.sh`（**已存在**，其步骤 E 已在跑 `commit-msg.scope.sh`） | 新断言应被已有 runner 驱动，**不新造 runner** |

**先例**：K-104c 的 `commit-msg.scope.sh` + `guarded_paths.tsv`（表驱动，`claim/repo/path/basis` 四列）**就是 I5 该有的形状**。
⇒ I5 是**把这个形状推广到其余声明**，不是另起一套。

---

## 4. ⚠️ 已知引用错误（**执行时按订正后的名字写**）

本 brief 交接时发现：`GateVerdict` 的归属被写错。

| 器官 | 实际类型 | 位置 |
|---|---|---|
| **anaphase-helix** | `GateVerdict` | `src/security.rs:58`；另有一处 `src/run_cycle/safety_gate.rs:52` |
| **Tuck** | **`GateDecision`** | `crates/tuck-core/src/anaphase_bridge.rs:153` |

**Tuck 里没有 `GateVerdict`**（全仓 0 命中）。`CI-144_默认放行分类表.md` §5.1 已自我声明**只扫了 `anaphase-helix/src/`**、**结论不适用于 Tuck**。

⇒ **两个类型的共同点已复核**：都是 4 变体，**都没有 `Err`/"判不出"变体**：
- `GateVerdict`：`Pass` / `Reject` / `NeedHumanConfirm` / `HardOverride`
- `GateDecision`：`Pass` / `Reject` / `HitlRequired` / `HardOverride`

⇒ **I7 要登记的是"无法表达"，按上表分别指名到器官。** 已登记为 **K-106**。
⇒ **I7 不得发明 API**：不新增变体、不改 `GateVerdict`/`GateDecision` 的定义（那是行为改动，需人类裁决）。

---

## 5. 交付物

1. **改动清单**：每个仓 → 文件 → 断言行 → 为什么它对应 I5/I6/I7 中哪一条。
2. **"能红"证据**：每条断言的 RED→GREEN 变异证据（改坏什么、红在哪一行、改回后绿）。
3. **`GUARDED` 未变证据**：`commit-msg` 的 `GUARDED=` 行与 HEAD 逐字相同；非注释改动 0 行。
4. **收尾 `git status`**：所有动过的仓，逐仓 `--porcelain`。
5. **未做/做不到的**：若某条断言无法在不违反硬约束的前提下落地，**明说做不到及原因**，不要凑数。

---

## 6. 边界（不宣称）

- 本 brief 是**重建版**，可能与原 brief 措辞不同；**执行依据是契约 §13.3/§13.4 原文**（已在上文第 1 节逐字引用）。
- I7 **只能登记"不可表达"**，**不得**顺带接线任何一段不存在的链条 —— Tuck 安全判定代码 **34 处里 20 处是死的**，接线一次那一整串 fail-open 同时生效（HANDOFF-0920 §7）。
- I2/I3/I4 的 **Cellrix 三处未修违反**属**策略面**（动窗口渲染语义），**不在本 brief 范围**。
