# CI-144 / 跨链「生命周期与反馈」契约

> 建立：2026-09-20 ｜ 对象：**整条链**（Tuck → FlowModus → Anaphase → Tentacle/手套/学习器 → Helix-Mind）
> 用途：**规定"失败 / 错误 / 被拒 / 通过"如何被表达、透传与反馈**，使链条**可扩展、向前兼容**。
> 依据：`CI-144_决策索引.md`；`CI-144_默认放行分类表.md`（**本文件不替代它**）；RFC 9413 / RFC 9170（核实状态见 §8）。

---

## 0. 本文件不做什么（先说边界）

1. **不替代 `CI-144_默认放行分类表.md`。** 那份表枚举的是**"判不出时倒向哪边"**（缺省方向），
   本文件规定的是**"结果如何被表达与传递"**（反馈形状）。**两者是正交的两个问题**，
   接口在 §7：缺省方向是本契约里"未知"那一类的**取值来源**。
2. **不发明新分类。** 分类表已有的 `职责决定 fail 方向` 与 `计数口径` 纪律，本文件**原样沿用**（极致复用）。
3. **不重复发明码。** 各器官已有的码（CI 退出码、协议码等）**并入**统一注册表，不另起一套（见 §3）。

---

## 1. 为什么"扩展性"要求先钉死错误处理（本契约的立足点）

> **RFC 9413 §2.2 Extensibility**（原文，2023-06）：
> *"A well-designed extensibility mechanism establishes **clear rules for the handling of elements
> like new messages or parameters**. This depends on **specifying the handling of malformed or illegal
> inputs** so that implementations behave consistently in all cases that might affect interoperation.
> New messages or parameters thereby become **entirely expected**. If extension mechanisms **and error
> handling** are designed and implemented correctly, new protocol features can be deployed with confidence…
> In contrast, **relying on implementations to consistently handle unexpected input is not a good
> strategy for extensibility.**"*

**⇒ 推论（本契约的全部理由）**：**错误处理不是"扩展性之外再加的日志"，它就是扩展性机制本身。**
所以"具备扩展性和未来兼容性"这条要求，首先要求的是**把非法/未知输入的处理规则写死**——
否则每一个新码在新旧器官之间都会变成一个**未定义行为**。

---

## 2. 五类结果（**不可折叠**）

| 类 | 语义 | 判据 |
|---|---|---|
| **通过 Pass** | 完成，且**用户/调用方拿到了它要的东西** | 以**交付物**为准，不以"某一步返回了 Ok"为准 |
| **被拒 Rejected** | **策略**决定不许。**不是异常** | 有明确的策略来源与凭据 |
| **失败 Failed** | 尝试了，没成功 | 有尝试记录 |
| **错误 Error** | 契约/系统层错（含畸形输入、未知码） | 有码 |
| **未知 Unknown** | **判不出** | **独立类别，不是任何一类的默认值** |

**⇒ 三条硬约束：**

1. **"通过"不得由中间步骤的 `Ok` 推出。** 已实测的反例：`answer.delivered` 检查的是**工具返回值**，
   不是"用户是否拿到答案" ⇒ **50 段经历中 9 条工具轮被误判 `Met`**。⇒ 判据是**交付物**。
2. **被拒不得报成失败，失败不得报成被拒。** 已有同形先例：`AuthorityUnavailable` 不得复用 `PolicyDenied`。
3. **未知必须有类型级表示，不得用默认值承载。** 同形先例三处：`BudgetSnapshot.remaining: Option<u64>`
   的 `None` ≠ 0；`Allow` 强制带 `grant_id`；`RiskLevel` 缺一个 `Unknown`（见 §7 的落地项）。

---

## 3. 码注册表（**唯一事实来源**）

### 3.1 形态（复用 Lodestone §11 的四列）

| code | level | 含义 | **确定性后果** | 归属 |
|---|---|---|---|---|

- **第四列是"反馈"的实体。** 给人看的文字是**渲染**；机器要的是**"接下来会发生什么"**。
  一个只有前三列的码，等于一条没有结论的记录。
- **第五列 `归属` 是新增的**：跨五仓时，"这个码是谁的定义权"必须可查，否则就是 K-002 双副本在码层的复现。
- **注册表只有一份** ⇒ **实例在 `CI-144_码注册表.md`**（本文件只规定**形态**，不列码）。各器官的码是该表的子集；新增码必须先登记后使用。
- **⚠️ 该表已清点出一个必须先解决的问题**：这个生态的"码"有三个**互不相同**的命名空间
  （**诊断码** / **退出码** / **标识码**），混用它们是同词两义的第 4 例。
  **⇒ 标识码（如 `BIND-19`、`CAPABILITY-13`）不得出现在"结果"位置上** —— 它是索引，不是判据。

### 3.2 命名与扩展位（向前兼容的形状）

- **保留段**：`X-` 前缀为**保留码**，**永不被赋予语义**（用途见 §5）。
- **未知码必须可被"原样携带"**：注册表类型必须能表示"我在本版本不认识这个码"，而不是丢弃或报错。
- **不在本版本定义的一律不定义**：不写"将来可能这样"的悬空引用（**留位置，不留悬空引用**）。

---

## 4. 未知必须透传（D3）与不变量（D5）

### 4.1 未知透传

> **RFC 9170 §3.1 Dependency Is Better**：以 SMTP 头字段为类比——
> *"An SMTP implementation therefore needs to be able to **both process header fields that it
> understands and ignore those that it does not**."*

**规则**：每一跳遇到不认识的码 ⇒ **保留原码 + 记录来源**，继续走。
**不得转成"通过"，不得丢弃，不得折叠进相邻类别。**

**⇒ 且 §3.1 的关键是"make the handling of it critical"**：未知处理路径**必须在关键路径上**。
可选的东西会腐烂——这正是本生态"声明了但没有入口"链条的形成机制。

### 4.2 不变量（**先立基准，再改行为**）

> **RFC 9170 §4.2 Invariants**：*"Documenting aspects of the protocol that cannot or will not change
> as extensions or new versions are added can be a useful exercise."*（引 RFC 5704 §2.2 的定义；
> 先例：QUIC-INVARIANTS、TLS 1.3 §9.3）

**候选不变量（每条都有已实测的坑做依据）：**

| # | 不变量 | 依据（已实测） |
|---|---|---|
| **I1** | 追踪标识全程不变 | `x-tuck-trace` 已真接线，可做锚 |
| **I2** | **未知码永不被转成"通过"** | 本契约 §4.1 |
| **I3** | **被拒永不被报成通过** | 9 条工具轮误判 `Met` |
| **I4** | 一个事实只有一个来源 | K-002 同族 |

**⇒ 没有不变量，"未来兼容"就没有基准**——因为无法区分"扩展"与"破坏"。

---

## 5. Grease：把"向前兼容"从声明变成**可证伪的属性**

> **RFC 9170 §3.3 Falsifying Active Use**：
> *"greasing is used to refer to **any attempt to exercise extension points without changing endpoint
> behavior** other than to encourage participants to tolerate new or varying values… an implementation
> that is **regularly exposed to unknown values is less likely to be intolerant of new values** when
> they appear."*

**做法**：周期性地在所有 hop 注入**保留的**未知码 / 未知枚举值 / 未知字段，断言每一跳
**保留或忽略**它，且**绝不**把它变成"通过"（即断言 I2）。

**⇒ 这条直接复用已有的守卫/变异机制**（极致复用），零新工具，且它不依赖任何链路的接通状态——
**可以在链路还断着的时候先建起来**，将来接线时用它验收。

---

## 6. 反馈的可见性与受众分层

> **RFC 9170 §4.4 Effective Feedback**：
> *"The **visibility of errors is critical**… **Ignoring errors could allow problems to become
> entrenched.**"* ／ *"It might also be helpful to **disable automatic error recovery methods during
> development**."* ／ *"**An automated feedback system for capturing failures is therefore necessary
> for failures to be detected.**"* ／
> *"end users, developers, and network operations each have different requirements."*

**⇒ 三条落地：**

1. **每个码进证轨 + 可计数。** 「Ignoring errors could allow problems to become entrenched」
   正是"声明了但没有入口"链条的形成机制。
2. **开发期关闭自动恢复。** 具体：关掉有界重试与 fail-open 回落，**让失败可见**。
   理由：RFC 明说，且本生态的失败**现在正好被重试与 fail-open 盖住**。
3. **一个事实，按需渲染给三种受众**：人看`含义`，机器看`确定性后果`，运维看`计数`。
   同一份注册表，不复制 —— 这是「唯一事实来源」+「按需渲染」。

---

## 7. 与「默认放行分类表」的接口（缺省方向 → 未知类）

分类表的 **Rule 2「职责决定 fail 方向」**（判不出时回到本职责的默认答案）**就是本契约 §2 里
`未知` 那一类的取值来源**。对应关系：

| 职责 | 判不出时的答案（分类表 Rule 2） | 在本契约中归入 |
|---|---|---|
| 决定"能不能执行" | **不能** | `未知` ⇒ **被拒**（fail-closed） |
| 只做记录 / 给意见 | 取决于它是否被当成决策者 | `未知` ⇒ 若被当决策者则**被拒**，否则**通过** |
| 提供依赖 | **无依赖** | `未知` ⇒ **失败**（fail-open 的降级） |
| 维持自身存活 | **继续存活** | `未知` ⇒ **通过** |

**⚠️ 状态**：Rule 2 在分类表里自标为**候选原则 P13，尚未取得"穷尽"钥匙**。
本契约沿用它的**形状**，但**不宣称它已穷尽**——这正是它自己的未核项之一。

**⇒ 本契约的第一批落地项（按依赖排序，与既有清单合并记账，不新开口径）：**

| # | 项 | 位置 |
|---|---|---|
| **L1** | `RiskLevel` 增加 `Unknown`（与补声明字段**必须同一片**，不可拆 —— 单独加会把 fail-open 推成 DoS） | 学习器 |
| **L2** | `GateVerdict` **无 `Err` 变体** ⇒ 闸门**不能表达"判不出"**，构造失败被折叠进 `None`。⇒ §2 的 `未知` 类在闸门处**无法表达** | 分类表 §5.3 |
| **L3** | 码注册表落盘（§3），各器官现有码并入 | 本文件 |
| **L4** | Grease 探针（§5） | 本文件 |

---

## 8. 引用核实状态（**不可省略**）

| 引用 | 状态 |
|---|---|
| **RFC 9413**《Maintaining Robust Protocols》IAB, 2023-06 | ✅ **已取到原文**（`rfc-editor.org/rfc/rfc9413.txt`，742 行）。§2.2 / §2.3 / §4 / §4.1 / §5 / §5.1 / §5.2 章节号**逐条核对存在**；§1 引文为原文 |
| **RFC 9170**《Long-Term Viability of Protocol Extension Mechanisms》IAB, 2021-12 | ✅ **已取到原文**（923 行）。§2.3 / §3 / §3.1 / §3.2 / §3.3 / §3.5 / §4.1 / §4.2 / §4.4 章节号**逐条核对存在**；§3.1 / §3.3 / §4.2 / §4.4 引文为原文 |
| ⚠️ **历史引用纠错** | `wire image` 在 **9413 与 9170 里均为 0 次**（该引用作废）；`ossification` 9413 为 **0**、9170 为 **2**；`middlebox` 9413 为 **0**、9170 为 **7**。**正确出处是 9170 §2.3 与 §3.3**，不是 9413 |
| **RFC 5704 §2.2**（不变量定义） | ✅ 作为 **RFC 9170 §4.2 的引文**读到；**未读 5704 原文** |
| **QUIC-INVARIANTS / TLS 1.3 §9.3** | ⚠️ 作为 9170 §4.2 举的先例读到，**未读这两份原文** |
| **§7 的 P13 状态** | ✅ 引自分类表自身（自标"尚未取得穷尽钥匙"） |
| **§2 的 9 条误判实测** | ✅ 引自 `helixECO/ECOSYSTEM.md` v1.97（50 段经历中 9 条工具轮，8×`web_search`、1×`calc`） |

---

## 9. 未核（不宣称）

1. **Tuck / FlowModus 尚未按分类表口径扫过。** 分类表 §5.1 明确只扫了 `anaphase-helix/src/`，
   并警告"若 Tuck（边缘物理闸）有同类分支，本表结论不适用于它"。**本文件的跨链主张因此尚未成立**，
   扫描进行中，结果落回分类表而非本文件（避免两份清单 —— 与 K-025 的教训同向）。
2. **Cellrix / Tentacle / Helix-Mind 的缺省分支未扫。**
3. **码注册表已落盘**（`CI-144_码注册表.md`），但**只并入了一份外部协议的 14 个诊断码与 CI 退出码**；
   **各器官自有错误类型尚未枚举** ⇒ 「唯一事实来源」目前**仍是目标，不是事实**。
   另：该表清点出的**三个命名空间混用问题**（诊断码/退出码/标识码）**只有纪律，没有机器检查** ⇒ 违反不会被拦下。
4. **§5 的 Grease 探针未实现** ⇒ "向前兼容"目前是**声明**，不是**可证伪的属性**。
5. **本契约未取得任何"穷尽"钥匙**：§2 的五类是否覆盖全部结果，
   §4.2 的四条不变量是否完整，**均未证明**。按 P7，不得声称已穷尽。

---

## 10. 提案：补充 DNA「原则 4」的工程映射（**需人类批准，本文件不得代改**）

> ⚠️ **依据 `commonintents/.github:RNA.md` / 各仓 `RNA.md` 第 3 条铁律：「禁止修宪 —— 不得修改 DNA.md（可提议，人类拥有最终决策权）」。**
> `Cellrix/docs/DNA.md:110` 亦明写「AI 不得修改 DNA.md（宪法）」。
> **⇒ 本节是提案，不是变更。DNA.md 一字未动。**

### 10.1 提案的定性：**不是加新公理，是补一条缺的工程映射**

`commonintents/.github/DNA.md` 的「一、不可变原则（6 条公理）」**已有**：

> **原则 4：向后兼容是硬约束** … v2.0 发送端必须能被 v1.0 接收端解析（**忽略未知字段**）…
> **工程映射**：BIND-19 新增 PAL-Present 标志位，v1.0 接收端忽略；PAL-Version 字段支持版本协商；Reserved 位非零触发版本协商流程。

**⇒ 原则 4 已经要求"忽略未知字段"与"版本协商"。** 而它的**工程映射是一个设计（PAL 位），不是一项检查** ——
**没有任何机制验证"接收端真的忽略了未知字段"**。

**⇒ 所以缺口不是公理，是机制。** 这与本生态反复出现的形状一致：**声明了，但没有入口 / 没有仪器。**

### 10.2 提案内容（拟补入原则 4 的「工程映射」行）

> **工程映射（拟增补）**：
> 1. **未知必须显式可表示**：任何跨器官的结果类型必须能表示"本版本不认识这个码"，
>    **不得用默认值承载未知**；默认值承载未知时须标明它倒向哪一边
>    （**向上/放行 = 缺陷；向下/收紧 = 须留痕**）。
> 2. **未知必须透传**：每一跳遇到不认识的码 ⇒ 保留原码 + 记录来源，**不得转成"通过"、不得丢弃**。
> 3. **必须周期性证伪**：以**保留码**（`X-` 段）注入未知值，断言每一跳保留或忽略它、
>    且**绝不**把它变成"通过"。**不注入 ⇒ "向后兼容"是声明，不是属性。**
> 4. **不变量须成文**：至少钉死 —— 追踪标识全程不变 · 未知码永不转成"通过" · 被拒永不报成通过 · 一个事实一个来源。

### 10.3 依据

| 提案点 | 依据 |
|---|---|
| 未知显式可表示 | 本生态已有三处同形纪律（`AuthorityUnavailable` ≠ `PolicyDenied`；`remaining: None` ≠ 0；`Allow` 须带 `grant_id`）；**另有 CI 规格 §1.2 自记的 5 个实例** |
| 未知透传 | **RFC 9170 §3.1**（SMTP 头字段："process what it understands and ignore those it does not"） |
| 周期性证伪（grease） | **RFC 9170 §3.3 Falsifying Active Use** |
| 不变量须成文 | **RFC 9170 §4.2 Invariants**（引 RFC 5704 §2.2；先例 QUIC-INVARIANTS / TLS 1.3 §9.3） |
| "扩展性即错误处理" | **RFC 9413 §2.2**（原文见本文件 §1） |

### 10.4 为什么这条提案值当（一句）

> **原则 4 现在是"我们设计上支持向后兼容"；补上工程映射后，它变成"我们能证明某个具体版本真的忽略未知字段"。**
> 差别与 §4 里"黑名单是归纳的保证 / 白名单是演绎的保证"同族：**是性质差别，不是程度差别。**

### 10.5 待人类裁决

| # | 问题 | 建议 |
|---|---|---|
| **Q-DNA** | 是否采纳 §10.2 的四点补入原则 4 的工程映射？ | **建议采纳。** 它不新增公理、不改语义，只把已有要求从"设计"升级为"可证伪" |
| **Q-DNA-2** | 若采纳，是补入 `commonintents/.github/DNA.md`（协议家族层），还是各仓 `docs/DNA.md` 各自补？ | **协议家族层补一次**（原则 4 本身就在那一层），各仓引用不复制 |

---

## 11. 「安全判定」类提案：Tuck 的四处缺省方向（**待人类裁决，未实施**）

> ⚠️ 依据 `CI-144_默认放行分类表.md` **§0 规则 1**：
> *"本表的产出是分类，不是动作。**任何『翻成 fail-closed』的提议，必须先落进「安全判定」类，并指出它保护的是哪个外部动作。**"*
> **本节即为那一步。四项均已落 `安全判定` 类，并各自点名它保护的外部动作。DNA / 代码一字未动。**

### 11.1 为什么这四项要一起裁决

`Tuck/crates` 的扫描给出一个量化事实：**安全判定共 33 处，仅 13 处生效 —— 20 处是死的**；而其中一整串 fail-open **今天不可达，但都是 `pub` API 与已验收的 ADR 实现**。
**⇒ 接线一次，这条清单同时生效。** 所以"先接线再修"是危险顺序：接通会把它们一起激活，而没有仪器能看见。

### 11.2 逐项

| # | 位置 | **保护的外部动作** | 现状 | 拟议 | 代价（不粉饰） |
|---|---|---|---|---|---|
| **S-1** `K-066` | `tuck-core/src/lib.rs:364`（`catastrophic_override: HardOverridePass`）+ `:430-431` | **把"最危险级别的帧"投递给下游物理执行体**（Tuck 是边缘物理闸，其后是不可回退的动作） | 默认策略严（`catastrophic: Reject`），但**帧头一个 bit** 把它翻成无条件直通；`tuck/src/main.rs:59` 用的正是该默认 | **默认改为不直通**：`HardOverride` 位不再单独构成放行理由；若确需放行，须由**带凭据的显式策略**授予，而非帧自述 | 消防通道的"无条件"语义消失。**但这正是 BIND-19 灾难放行条款的现状：它从未被实现为可授权机制，只被实现成一个 bit。** ⇒ 真正的代价是"需要补一个授权机制"，不是"失去能力" |
| **S-2** `K-076` | `tuck-core/src/sap.rs:534-536` | **放行一个未经重放/签名校验的帧** | `let Some(sap) = sap else { return SapDecision::Pass; }`，注释「No SAP — PFP Pass is final」；而 **SAP 在 Anaphase 侧本就可选** ⇒ **省略 SAP 即是绕过校验的默认路径** | **`None` 不得等于 `Pass`**：改为 `SapDecision::Unknown`（新增变体），由调用者按职责决定 —— 而调用者职责是"决定能不能执行" ⇒ **不得执行** | 未升级的 Anaphase 会被拒。**⇒ 这是可测的：先 shadow（只记不拦）取真实拒绝率，再决定切换时点**（复用 Q3 既有观察期思路，不新造流程） |
| **S-3** `K-080` | `tuck-gateway/src/tentacle_bridge.rs:374` | **放行一个完整性未知的插件** | `integrity_verified = integrity_hash == computed_hash` ⇒ **两个缺省空串相等 ⇒ `verified = true`**；模块内无任何非空/hex 校验 | **空 ≠ 已验证**：`hash` 缺失或非 hex 时，`integrity_verified` 必须是 `Unknown`／`false`，**不得为 `true`** | 缺 hash 的历史插件会被标"未验证"。**⇒ 这本来就是事实**——今天是**用一个比较运算把"没数据"说成"数据一致"** |
| **S-4** `K-082` | `tuck-gateway/src/tentacle_bridge.rs:527-534` | **账本的证据价值**（人类据以追责/复盘） | 审计条目**硬编码 `Decision::Pass`**，真实判定被塞进 `identity_label` 参数走私，注释却称那是 source 字段 | **记账写真实判定**；`identity_label` 回归其字段本义 | 无功能代价。**已写入的历史行按 Append-Only 不动，由读者按新语义解读**（与 `is_period_id` 的既有处置同向） |

### 11.3 四项的共同形状（一句话）

> **它们都不是"判不出时选错了方向"，而是"把'没有数据'当成了一个数据值"。**
> `"" == ""` 是 `S-3` 的形态，`None => Pass` 是 `S-2` 的形态，`bit=1 => 直通` 是 `S-1` 的形态，`硬编码 Pass` 是 `S-4` 的形态。
> **⇒ 所以 §10.2 拟补入 DNA 原则 4 的第一条（"未知必须显式可表示"）就是这四项的统一修法。**

### 11.4 建议的推进顺序（若采纳）

1. **S-3 与 S-4 先做** —— 零行为代价、纯记账/判据修正，且**不依赖任何装配**
2. **S-2 次之**，但**必须带观察期**（先只记录不拦截），因为它是唯一会让在跑系统产生真实拒绝的一项
3. **S-1 最后**，因为它需要先回答一个问题：**BIND-19 的灾难放行条款，是要"实现一个可授权的机制"，还是"确认它本就不该存在"？** —— 这是规范层的问题，不是代码层的

### 11.5 待裁决

| # | 问题 | 建议 |
|---|---|---|
| **Q-FIX-1** | 四项是否采纳？可逐项裁决 | **S-3/S-4 建议直接采纳**（零代价）；**S-2 建议采纳但带观察期**；**S-1 建议先答规范问题** |
| **Q-FIX-2** | S-1 的规范问题：BIND-19 灾难放行，实现为可授权机制，还是确认不该存在？ | 需你裁；**在答之前 S-1 保持现状**（今天的状态是"一个 bit"，不是"一个机制"） |
