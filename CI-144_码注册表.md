# CI-144 / 码注册表（单一事实来源）

> 建立：2026-09-20 ｜ 用途：**把各器官已有的"码"并入一张表**，作为`CI-144_跨链生命周期与反馈契约.md` §3 的实例。
> 依据：`CI-144_跨链生命周期与反馈契约.md` §3（形态）；各器官既有定义（逐条标注来源）。
> **状态：清点已完成，尚未覆盖全部器官**（见 §6）。
> **更新（2026-09-22）**：§1.2 并入 `RiskLevel` 词表，并**被代码消费**——`HelixECO-Glove/core/build.rs`
> 解析该块生成常量，R005 不再持有等级字面量。这是本表第一次**从"清点"转为"唯一来源"**（其余各节仍是清点）。

---

## 0. 三个命名空间必须分开（**本表存在的第一个理由**）

这个生态里"码"被用于三种**互不相同**的东西。混用它们是同词两义的第 4 例：

| 命名空间 | 是什么 | 用途 | **能否用作反馈** |
|---|---|---|---|
| **诊断码** | `E-*` / `W-*`：一次判定的结果 | 表达"发生了什么、接下来怎样" | ✅ **能** |
| **退出码** | 进程退出状态（0/1/2/3） | 表达"这一次运行对调用者意味着什么" | ✅ 能，但**只对进程边界** |
| **标识码** | `BIND-19` / `CAPABILITY-13` / `INTENT-7` / `HXR-L3` | **命名一个规范/向量/条目** | ❌ **不能** —— 它们不是结果 |

**⇒ 硬规则：标识码不得出现在"结果"位置上。** 把 `CAPABILITY-13` 当成一次失败的原因码，
与把 ADR 编号当错误码同罪 —— **它是索引，不是判据**（本生态已有一条同形教训：标签是索引，判据是闸门）。

---

## 1. 诊断码（**本表的正体**）

| code | level | 含义 | **确定性后果** | 归属 | 来源 |
|---|---|---|---|---|---|
| `E-MISSING-ID` | 错误 | 节点无可用 id | 节点无效 | 待定 | 外部协议 §11 |
| `E-META-SYNTAX` | 错误 | 元数据形态非法 / JSON 解析失败 / **重复键** | 节点无效 | 待定 | 同上 |
| `E-META-FIELD` | 错误 | 字段值非法 | **节点有效，字段降级** | 待定 | 同上 |
| `E-DUP-ID` | 错误 | 有效节点 id 重复 | 同 id **全部**节点无效 | 待定 | 同上 |
| `E-REF-NOT-FOUND` | 错误 | 边引用目标不存在或为无效节点 | 该边不生效 | 待定 | 同上 |
| `E-CYCLE` | 错误 | 全局图存在环 | 循环边全集失效 | 待定 | 同上 |
| `W-VERSION-MISMATCH` | 警告 | 文档级 `version` 非预期值 | 继续解析 | 待定 | 同上 |
| `W-DOC-META` | 警告 | 文档级元数据 JSON 不合法 / 重复键 | 忽略之，继续解析 | 待定 | 同上 |
| `W-CYCLE-DECLARED` | 警告 | 规范化声明边集合存在环 | 无结构影响 | 待定 | 同上 |
| `W-REDUNDANT-EDGE` | 警告 | 规范化后重复 | 折叠为一条边 | 待定 | 同上 |
| `W-META-PLACEMENT` | 警告 | 疑似元数据出现在非法位置/形态 | 节点无元数据 | 待定 | 同上 |
| `W-REDUNDANT-META` | 警告 | 节点内存在被忽略的后续 mddag 注释 | 忽略该注释 | 待定 | 同上 |
| `W-UPSTREAM-PENDING` | 警告 | `aligned` 节点存在非 aligned 上游 | 无结构影响 | 待定 | 同上 |
| `W-NFC-VIOLATION` | 警告 | 标题文本未采用 NFC 归一化形态 | 无结构影响 | 待定 | 同上 |

**⚠️ 归属列全部"待定"** —— 这批码定义在一份**外部协议规范**里，本生态是**消费方**。
⇒ 按「不重复发明」，**本表不得重新定义它们的含义**；归属须由该协议的所有者裁定（不得由本表代为决定）。

### 1.1 🔴 本表清点时的第一个产出（**不是登记，是新发现**）

上表 `E-META-FIELD` 的确定性后果是「**节点有效，字段降级**」，而该规范 §5.3 的降级规则含：

> `status` 值非法 → **回退为 `draft`**

**⇒ 这是"未知被默认值承载"的一个实例。** 但它的方向与 `RiskLevel::Low` **相反**：

| 实例 | 默认值倒向 | 性质 |
|---|---|---|
| `RiskLevel` 缺 `Unknown` ⇒ 默认 `Low` ⇒ **给 `read` 权限** | **向上（放行）** | 🔴 **缺陷** |
| `status` 非法 ⇒ 回退 `draft`（最低状态） | **向下（收紧）** | 🟡 **仍需标记，但不阻断** |

**⇒ 由此给契约 §2 第 3 条补一条判据（本表的实质贡献）：**

> **默认值承载未知时，必须检查它倒向哪一边。**
> **向上（把未知当成"更许可"）是缺陷；向下（把未知当成"更收紧"）仍须留下痕迹，但不阻断流程。**
> 理由：向下的默认至少不会**奖励不可读性**；但它仍然让"判不出"与"真的判成了 draft"不可分，
> 因此**必须可观测**，否则复盘时无法区分"作者写了 draft"与"我们读不懂"。

**⇒ 这解释了为什么 `RiskLevel` 那处特别危险：它的默认方向是向上的。**

---

### 1.2 `RiskLevel` 词表（**风险等级的唯一来源**；R005 从本块派生）

> 建立：2026-09-22 ｜ 依据：本节 §1.1 的方向判据 + `CI-144_跨链生命周期与反馈契约.md` §12.4、§12.6①。
> **为什么它在注册表里，而不在 `reviewer.rs` 里。** `HelixECO-Glove/core/src/reviewer.rs:359`
> 原有 `let valid = ["low","medium","critical","catastrophic"]` —— 那是**第二处手维护的清单**，
> 与 K-104(a) 同形：本表加一个值，它不会知道，而且它自己会成为下一个"落在后面的清单"。
> 按 §4 规则 3「注册表是唯一来源，各器官的码是本表的子集」，词表并入本表；
> `reviewer.rs` 改为**从本块派生**（`core/build.rs` 解析本块生成常量，`reviewer.rs` 不含任何等级字面量）。

**合法性（本版本认不认这个值）与处置（认了之后怎么办）必须分开**（§12.4）。下表两列都给出。

<!-- BEGIN CI144-RISK-VOCABULARY: 机器可解析块（列序固定：value | class | code | level | 含义 | 确定性后果 | high_risk | 归属）。改词表 = 改这里，不改 R005。 -->
| value | class | code | level | 含义 | 确定性后果 | high_risk | 归属 |
|---|---|---|---|---|---|---|---|
| `low` | value | `-` | - | 低风险（只读） | 通过 | no | HelixECO-Glove |
| `medium` | value | `-` | - | 中风险（写入） | 通过 | no | HelixECO-Glove |
| `critical` | value | `-` | - | 高风险 | 通过（R008 另行要求细节） | yes | HelixECO-Glove |
| `catastrophic` | value | `-` | - | 灾难风险 | 通过（R008 另行要求细节） | yes | HelixECO-Glove |
| `unknown` | unknown | `W-RISK-UNKNOWN` | 警告 | **明确声明"我判不出"**（携带 `FieldProvenance`） | **隔离待人工复核**（→ `staging/`）：是"一种声明"，不是"什么都没有" | yes | HelixECO-Glove |
| `-` | silence | `E-RISK-MISSING` | 错误 | 字段缺失或为空（**沉默**，不是一种声明） | **拒绝**（→ `rejected/`）—— **不得轻于 `unknown`** | no | HelixECO-Glove |
| `-` | illegal | `E-RISK-ILLEGAL` | 错误 | 既不在本块 `value` 行、也不在保留段 | **拒绝**（→ `rejected/`） | no | HelixECO-Glove |
| `X-` | reserved | `W-RISK-RESERVED` | 警告 | 保留段前缀（§4 规则 1，**永不赋予语义**） | **原样携带 + 隔离**（grease 探针：§5「保留或忽略，绝不变成通过」） | no | HelixECO-Glove |
<!-- END CI144-RISK-VOCABULARY -->

**⇒ 三档的单调性（§12.4 的"可接受度"序，逐条落成可证伪的不变量）：**

| # | 输入 | 合法性 | 处置 | 严重度 | 可接受度 |
|---|---|---|---|---|---|
| 1 | 词表 `value` | ✅ | 通过 | — | 最高 |
| 2 | `unknown` | ✅（**认**） | 隔离 | `Warning` | ↓ |
| 3 | 空 / 缺失 | ⚠️ 缺失证据 | 拒绝 | `Error` | ↓ |
| 4 | 词表外 | ❌ 违规 | 拒绝 | `Error` | 最低 |

**⇒ 不变量（由 `reviewer.rs` 的测试钉住，非纸面声明）：**

1. **单调性**：不确定性上升，处置不得更宽松 ⇒ `可接受度(value) > 可接受度(unknown) ≥ 可接受度(silence) ≥ 可接受度(illegal)`。
2. **沉默不得轻于 `unknown`**（§12.4 的关键一行）—— 修复前恰好相反（沉默 `Warning`、声明 `Error`）。
3. `unknown` 与 `silence` **不得混为一谈**：`code` 不同，且**处置不同**（隔离 vs 拒绝）。
4. **grease**：保留段 `X-` 值**不得**被判为 `illegal` —— 否则"容得下未知"变成"拒绝未知"（§5 明令禁止）。
5. 合法性判据**只有一个来源**（本块）；`reviewer.rs` 里不得再出现等级字面量。

**⚠️ 一处显式登记的覆盖缺口（规则 3「不一致处必须显式登记」）：** 本块 `silence` 类**合并**了
"字段缺失"与"字段显式为空串"两种输入 —— 消费方 `ToolManifest.risk_level: String` 带 `#[serde(default)]`，
**类型上分不开二者**。二者在本职责（决定"能不能执行"）下的确定性后果相同（都拒绝），故**处置无歧义**；
若要区分"我没有这个字段"与"我写了空串"，须把该字段改为 `Option<String>` —— **那是跨仓改动，本次不做，登记在此**。

---

## 2. 退出码（进程边界）

| 码 | 含义 | 归属 |
|---|---|---|
| `0` | 全 PASS | CI-144 检查器 |
| `1` | 有 FAIL ⇒ 阻断 | 同上 |
| `2` | AMBIGUOUS 超阈值 ⇒ 阻断 | 同上 |
| `3` | **检查器自身错误 ⇒ 阻断，绝不静默返回 0** | 同上 |

**⇒ 该表自身的注记已经点名了本契约的核心模式**（逐字）：

> 第 3 条对应本链第 **5** 个同形状：**默认值把「缺失」伪装成「一个合法的取值」**
> （`.or()` 顶 id · `Err ⇒ missing:true` · `unwrap_or(placeholder)` · `unwrap_or("local")` · 现在加上"工具缺失 ⇒ 退出 0"）。

**⇒ 这条注记是本契约「未知必须显式表示」的**既有实例清单**，本表沿用并续记，不另立。**

---

## 3. 标识码（**明确声明：不是结果码**）

| 码 | 它命名什么 | 归属 |
|---|---|---|
| `BIND-19` | 一份外部格式规范 | 待定 |
| `CAPABILITY-13` | 一项能力条目 | 待定 |
| `INTENT-7` | 一项意图条目 | 待定 |
| `HXR-L3` | 一个数据形态 | 待定 |
| `PFP-PRESENT` / `SAP-PRESENT` | **存在性谓词**（出现/未出现） | 待定 |

**⇒ §3 全体不得用作一次运行的结果码。** 其中 `PFP-PRESENT` / `SAP-PRESENT` 形态上接近谓词，
**若将来要当诊断码使用，必须先在此表登记并给出「确定性后果」**，不得就地使用。

---

## 4. 命名与扩展规则

1. **保留段**：`X-` 前缀为**保留码，永不赋予语义**。用途：契约 §5 的 grease 探针。
2. **未知码必须可被原样携带**：消费方类型必须能表示"本版本不认识这个码"，不得丢弃、不得报错、**不得折算成相邻类别**。
3. **新增码 = 先登记后使用**；注册表是唯一来源，各器官的码是本表的子集。
4. **不得留悬空引用**：不写"将来可能如此"的条款。**留位置，不留悬空引用。**

---

## 5. 引用核实状态

| 引用 | 状态 |
|---|---|
| §1 的 14 个诊断码及其 `确定性后果` | ✅ **逐条取自外部协议规范 §11 原文表格**（含「确定性后果」列） |
| §1.1 的 `status` 非法 ⇒ `draft` | ✅ 取自同规范 §5.3「字段层降级规则」原文 |
| §2 退出码 0/1/2/3 | ✅ 取自 `CI-144_形状类CI检查规格.md` §1.2 原文表 |
| §2 的"第 5 个同形状"注记 | ✅ 逐字引自同文件 §1.2 注 |
| §3 标识码清单 | ⚠️ **由全仓 grep 得到，未核对各自定义处**；`归属` 列全部待定 |
| §1.2 `RiskLevel` 词表 | ✅ 三档取自 `CI-144_跨链生命周期与反馈契约.md` §12.4 表；`unknown` 行与学习器 `RiskLevel::Unknown`（`src/ci144/mod.rs`）逐值对齐；`X-` 保留段取自 §4 规则 1 |
| §1.2 的 4 个诊断码 | ✅ 两处实现已核：`HelixECO-Glove/core/src/reviewer.rs`（R005）+ `HelixECO-Glove/core/build.rs`（派生）；**但"零字面量"这条只有测试钉住，无独立检查器**（见 §6.6） |

---

## 6. 未核（不宣称）

1. **各器官自有的诊断码尚未清点（部分进展）。** 本表目前并入了一份外部协议的码、CI 退出码，
   以及 `HelixECO-Glove` R005 的 4 个码（§1.2）；Tuck / Anaphase / Tentacle / Helix-Mind /
   学习器**各自的错误类型仍未枚举**。
   ⇒ **「唯一事实来源」目前仍是目标，不是事实**——**但 §1.2 已从目标变成事实的那一小块**：
   改 §1.2 会改变 R005 的行为（有 §1.2 的测试与派生机制钉住）。
2. **`归属` 列几乎全空。** 跨仓时"谁有定义权"未裁定。
3. **本表未取得"穷尽"钥匙**：无法声称已枚举生态内全部码。
4. **§1.1 的方向判据（向上/向下）只有两个实例**，**不足以称为已证实的分类**；需更多实例才能定级。
5. **标识码与诊断码之间没有机器检查**：目前靠纪律，不靠机制 ⇒ 违反 §0 不会被拦下。
6. **§1.2 与代码之间是"构建期派生"，不是"运行期读取"。** `core/build.rs` 在**构建时**解析本文件；
   本文件不在文件系统上可达时，构建**直接失败**（fail loud，不静默回退到任何副本）。
   ⇒ 代价是：构建本仓库需要本文件在同工作区的兄弟目录下（或由 `CI144_REGISTRY` 环境变量指定）。
   这是"单一来源"的**真实依赖**，登记在此而非藏在构建脚本里。
7. **"`reviewer.rs` 里不得出现等级字面量"这条不是通用检查器。** 它由
   `HelixECO-Glove/core/src/reviewer.rs` 里的一条**窄测试**覆盖
   （`test_reviewer_source_has_no_hand_maintained_risk_list`：源码级 grep，只查
   `"critical"`/`"catastrophic"` 两个字面量与生产段）——能拦住"把旧的
   `let valid = [...]` 抄回来"这个具体形态，**拦不住任何其他形态**。不宣称它是闸门。
8. **本表的声明范围只覆盖 `RiskLevel`。** 同一份 `reviewer.rs` 里还有两处手维护清单：
   R006 的平台清单（`macos/linux/windows/…`）与 R007 的 `host_os` 清单。
   它们**未被并入本表，也未被本次修复触碰** —— 按规则 3「覆盖范围必须与声明的范围一致；
   不一致处必须显式登记」，**此处即为登记：那两处仍是各仓自维护，本表不为其负责。**
