# CI-144 实现矩阵（B0）

> 建立：2026-09-17 ｜ 对象：**B0**
> 用途：**每个协议一张表：谁 MUST / SHOULD / MAY / N-A 实现哪一层。**
> **规则（唯一一条）**：**每个格子必须有可复核证据，或显式写 N-A。不得留白、不得写"应该"。**
> 依据：`CI-144_决策索引.md §2 B0`（列含「权威路径」「免疫归谁」、「必须能表达单向依赖」——后者由 K-019 要求）。

---

## 0. 为什么要这张表（以及它现在才存在的原因）

`CI-144_决策索引.md §2 B10` 依赖本表：**没有可比对的对象，校验器会安静通过**。
而 `CI-144_合拢坑记录.md` §6.1 已经给出本表的**第一行数据**：同一语义（`Override-Flag`）在**规范 / Anaphase / Tuck** 三处**形态不同**。
⇒ **数据一直在那儿，只是从没被提升为结构。** 本文件是那次提升。

**K-019 的追加要求**：引用是**单向**的（PFP/SAP 声明在 BIND-19 之上，BIND-19 全文不提它们）。
⇒ 因此本表**必须能表达"甲依赖乙、乙不知道"**——用 §3 的单向依赖表，而不是一张对称矩阵。

---

## 1. 协议清单与权威路径（实测，非转述）

| 协议 | 层 | 子协议 ID | 权威路径（**唯一**） | 本地仓库 | HEAD / tag | 状态 |
|---|---|---|---|---|---|---|
| **PFP-xCF14** | 冻结层 | `0x00` | `commonintents/BIND-19/docs/spec/pfp-xcf14.md` | 同左 | 317 行 | **Frozen**（自称永不变化） |
| **SAP-xCF14** | 演进层 | `0x01` | `commonintents/BIND-19/docs/spec/sap-xcf14.md` | 同左 | 262 行 | **Active** |
| **BIND-19** | 传输层 | — | `commonintents/BIND-19/spec/BIND-19.md` | 同左 | 195 行 | v1.0.0-RFC-4（spec 正文） |
| **INTENT-7** | 语义层 | — | `commonintents/INTENT-7/spec/INTENT-7.md` | 同左 | — | early draft |
| **CAPABILITY-13** | 能力层 | — | `commonintents/CAPABILITY-13/spec/CAPABILITY-13.md` | 同左 | — | early draft |
| **INTENT-7-SECURE** | 边缘安全 | — | `commonintents/INTENT-7-SECURE/spec/INTENT-7-SECURE.md` | 同左 | — | early draft |

**权威路径的判据**（可复核）：PFP/SAP 两处「权威来源」均指向 `CommonIntents/BIND-19/docs/spec/…` @ **`v2.0-rc.1`**（`pfp:10` / `sap:10`）。
⇒ 本地以 **`commonintents/BIND-19/`** 为准（带 `v2.0-rc.1` tag）；根 `BIND-19/` 是**同 remote 的另一次检出**，差异为 1 行版权行（详见 `CI-144_决策索引.md §0.1`）。
**⇒ 发布窗口 ≠ 权威源**：`commonintents/PFP-xCF14` 与 `commonintents/SAP-xCF14` **只有 README + CONTRIBUTING，无 `spec/`**（实测）。

### 1.1 🔴 **但表外还有第三个设计：24 字节 PAL**（K-024）

| 设计 | 来源 | 头部 | 在规范/源码中？ |
|---|---|---|---|
| **PAL** | `docs/v2.0-upgrade-plan.md`（自称**权威版本**，状态 `READY_FOR_ALPHA`） | **24 字节 / 192 bits**，含 `PAL-Version` | ❌ `grep PAL spec/ docs/spec/ src/` **零命中** |
| **PFP + SAP** | `docs/spec/pfp-xcf14.md` + `sap-xcf14.md` | **4 + 28 字节** | ✅ 现行 |

**⇒ 本表的"权威路径"一列以现行规范为准；但 PAL 计划未作废声明，仍活在 5 处文档中（含 `PLAN.md`、`README.md`）。**
**⇒ 这是本表 §1 必须带的一行注**：否则读表的人会以为"权威路径"是无争议的，**而它是一个待裁定项。**

---

## 2. 消费端清单（实测：谁真的有代码）

判据：`git grep -lI "0xCF14\|0xcf14"` **逐仓**（整树 grep 会超时）。

| 仓库 | 命中文件 | 实现的语言层 |
|---|---|---|
| `commonintents/BIND-19` | `src/frame.rs`、`src/pfp.rs`、`src/sap.rs`、`examples/`、`tests/test_vectors/` | **BIND-19 + PFP + SAP**（参考实现，全栈） |
| `Tuck` | `crates/tuck-core/src/frame.rs`、`sap.rs`、`proxy.rs`、`lib.rs` | **自有 BIND-19 方言 + PFP 提取 + SAP** |
| `Cellrix` | `protocol/src/pfp.rs`、`protocol/src/sap.rs` | **仅 PFP + SAP 载荷**（无帧层） |
| **`anaphase-helix`** | **零命中** | **无任何 CI-144 线层实现** |
| `helix-mind` | 仅 `docs/helixECO/ECOSYSTEM.md`（文档） | **无实现** |

**⇒ 第一个结构性事实**：**CI-144 的线层在 Anaphase 侧不存在。**
Anaphase 经 Tuck 网关接入（`ECOSYSTEM.md` 记载），**这是单向依赖**（§3）。
**⇒ 对本表的影响**：Anaphase 的格全部是 **N-A**，且 N-A 的理由必须写明"经 Tuck"，不能写"未实现"——**两者含义不同**。

---

## 3. 🔴 单向依赖表（K-019 要求）

| # | 依赖方 | 被依赖方 | 方向 | 被依赖方是否知道 |
|---|---|---|---|---|
| 1 | PFP-xCF14 `:1.2`「插入在 BIND-19 传输头之上」 | BIND-19 | → | ❌ **不知道**（`spec/BIND-19.md` 对 `PFP/SAP/xCF14/CF14` **零命中**） |
| 2 | SAP-xCF14 `:1.2`「SAP 不能单独出现」 | PFP-xCF14 | → | ❌ 不知道（PFP 不提 SAP） |
| 3 | PFP-xCF14 `:3.9`「保留位非零 ⇒ 触发版本协商流程」 | BIND-19 的协商机制 | → | ❌ **该机制不覆盖 PFP**（§4.3） |
| 4 | Anaphase → Tuck 网关 | Tuck | → | ✅ 知道（`ECOSYSTEM.md` 记载，Anaphase 侧零命中是**有意的**） |
| 5 | `sap:10`「所有规范变更的 PR 必须提在 BIND-19 仓库」 | BIND-19 仓库流程 | → | ✅ 知道（BIND-19 侧 `CONTRIBUTING.md`） |

**⇒ #1–#3 是"引用了一方不知道的依赖"，#4–#5 是"有记载的依赖"。**
**⇒ B10 的校验器必须能区分这两类**：前者是**缺契约**，后者是**有契约**。

---

## 4. 实现矩阵（**逐格证据；空格为零**）

图例：**✅ 实现** / **⚠️ 实现了但有分歧** / **N-A（理由）** / **❓ 未核**

### 4.1 帧层（BIND-19）

| 项 | 规范（`bind:2`） | commonintents/BIND-19 | Tuck | Cellrix | Anaphase |
|---|---|---|---|---|---|
| 8 字节头存在 | MUST | ✅ `src/frame.rs:152` | ✅ `frame.rs:88-96` | **N-A**（无帧层） | **N-A**（经 Tuck） |
| magic `0xCF14` 位置 | **字节 2+3**（`FrameType`/`ChannelID`） | ✅ 同规范 | ⚠️ **字节 0+1** | N-A | N-A |
| FrameType 字节 | 字节 0 | ✅ | ⚠️ **字节 3** | N-A | N-A |
| Flags 字节 | 字节 2 | ✅（v2.0 扩 `0x08`=PFP-PRESENT / `0x10`=SAP-PRESENT） | ⚠️ **字节 2 但语义是"子协议 ID + 特性位"** | N-A | N-A |
| Sequence ID | **1 字节** | ✅ | ⚠️ **2 字节**（`u16`，字节 4+5） | N-A | N-A |
| Payload Length | **4 字节，上限 64MB** | ✅ `frame.rs:152` `u32::from_be_bytes` | ⚠️ **1 字节，且截断**（见下） | N-A | N-A |
| PFP/SAP 存在位 | v2.0 用 **Flags 具名位**（`0x08`/`0x10`） | ✅ `FLAG_PFP_PRESENT` / `FLAG_SAP_PRESENT` | ⚠️ **独立字节 `present`（字节 6）+ 高位约定**（`0b1000_0000`/`0b0100_0000`） | N-A | N-A |

#### 4.1.1 Tuck 的 Payload Length 有三处硬证据（**已核，非推断**）

```
frame.rs:91    pub payload_len: u8,                                    ← 类型即 1 字节
frame.rs:115   payload_len: data[7],                                   ← 解码只取 1 字节
frame.rs:315   self.header.payload_len = payload.len().min(255) as u8; ← 写入侧静默截断
frame.rs:195-215   Frame::parse 的 payload = &data[offset..]           ← 读取侧从不使用该字段
```

**⇒ 三条结论**：
1. **上限即 255** ⇒ 扣掉 8+4+28 = 40 字节，**载荷可用约 215 字节**；规范上限 **64MB**。
2. **超过 255 的载荷被静默截断**（`.min(255)`）—— **不是报错，是改数字**。
3. **注释与实现不符**：`frame.rs:30`「if >255 use extended」与 `:90`「extended length for large payloads」—— **`grep -i extended` 全仓零实现**（仅这两行注释命中）。

#### 4.1.2 🔴 **这是"有意设计"，不是"漂移"**（决定性证据）

```
Tuck/crates/tuck-core/src/frame.rs:409-411
    fn test_header_parse_valid() {
        ...
        0xCF, 0x14, // magic        ← 单测显式断言 magic 在字节 0-1
```

**⇒ Tuck 有测试钉住自己的布局** ⇒ **它不是"忘记对齐规范"，是"按自己的设计实现且自认为是 CI-144"。**
**⇒ 这把 K-021 的修法从"改正一个 bug"改成"裁定谁是权威"** —— 与 K-022 是同一个裁定。

**⇒ 结论（可复核）**：**Tuck 与参考实现在 BIND-19 帧层上不是同一格式。**
**⇒ 且这是"同族阳性对照"**：Tuck 的 **SAP 28 字节布局与参考实现逐字段一致**（§4.2）⇒ **分歧只在帧层，不在载荷层** ⇒ **不是"Tuck 整体没对齐"，是帧层这一格没对齐。**

### 4.2 SAP 层（28 字节）

| 字段 | 规范（`sap:2`） | commonintents/BIND-19 | Tuck | Cellrix | Anaphase |
|---|---|---|---|---|---|
| Family-Magic 字节 0-1 | `0xCF14` | ✅ | ✅ `sap.rs:35` `FAMILY_MAGIC: u16 = 0xCF14` | ✅ `sap.rs` | N-A |
| Protocol-ID 字节 2 | `0x01` | ✅ | ✅ `SAP_PROTOCOL_ID = 0x01` | ✅ | N-A |
| Version 字节 3 低 4 位 | `0001` | ✅ | ✅ `SAP_VERSION = 0x01` | ✅ | N-A |
| Reserved 字节 3 高 4 位 | 强制 0 | ✅ | ✅ | ✅ | N-A |
| Seq-Counter 字节 4-5 | `u16` 大端，单调 | ✅ | ✅ 且实现轮换阈值 `65534` | ✅ | N-A |
| PAH-Hash 字节 6-19 | SHA-256 高 112 位 | ✅ | ✅ 注释逐字段一致 | ✅ | N-A |
| PAH-Signature 字节 20-27 | 64-bit 截断 | ✅ | ✅ | ✅ | N-A |

**⇒ SAP 层三实现一致**（字段级）。**⇒ 分歧被隔离在帧层。**

### 4.3 PFP 层（4 字节）——**语义分歧在这一格**

所有位解码点（逐字段实测）：

| 字段 | 位提取 | 参考实现 | Tuck | Cellrix |
|---|---|---|---|---|
| Modality | `byte2 & 0b11` | ✅ | ✅ `lib.rs:103` | ✅ `pfp.rs:249` |
| Risk-Level | `(byte2>>2) & 0b11` | ✅ | ✅ `lib.rs:109` | ✅ `pfp.rs:254` |
| Body-Stance | `(byte2>>4) & 0b11` | ✅ | ✅ `lib.rs:115` | ✅ `pfp.rs:259` |
| Proximity-Edge | `(byte2>>6) & 0b11` | ✅ | ✅ `lib.rs:121` | ✅ `pfp.rs:264` |
| Output-Dest | `byte3 & 1` | ✅ | ✅ `lib.rs:126-127` | ✅ | N-A |
| Override-Flag | `(byte3>>1) & 1` | ✅ | ✅ `lib.rs:133` | ✅ `pfp.rs:273` |
| Replay-Enable | `(byte3>>2) & 1` | ✅ | ✅ `lib.rs:139` | ✅ `pfp.rs:278` |
| Reserved | `(byte3>>3) & 0b11111` | ✅ | ✅ `lib.rs:87` | ✅ |

**⇒ 位布局三实现无分歧。分歧只在下面两条语义：**

| 语义项 | 规范 | 参考实现（BIND-19） | Tuck | Cellrix |
|---|---|---|---|---|
| 保留位非零的处置 | `pfp:5` 伪代码 `return Err`；`pfp:3.9` 正文"触发协商，不得静默忽略" | ⚠️ **解析后 `Ok`**，另提供 `has_unknown_reserved()` | ⚠️ **解析后不判**（`lib.rs:87` 解出 `reserved` 后无校验分支） | ⚠️ **严格拒绝**：`is_reserved_clean()` = `bytes[3] & 0b11111000 == 0`（`pfp.rs:234`） |
| `effective_risk_level()`（规则 6） | `pfp:3.3` / `sap:6` | ✅ 但注释写明 **`CI144_DEBUG=1` 可跳过降级** | ✅ `lib.rs:148-152` | ✅ `pfp.rs:283-289` |
| `is_catastrophic_override()` 用哪个风险 | **规范内部矛盾**：`pfp:6` 伪代码用**原始**；`sap:6` 第 1 条要求**不可触发** | ⚠️ **用原始**（`src/pfp.rs:276-279`、`src/catastrophic.rs:327-334`）⇒ **规则 6 不生效** | ✅ **用降级后**（`lib.rs:427-430`）⇒ **符合 `sap:6`** | ✅ **用降级后**（`pfp.rs:292-294`）⇒ **符合 `sap:6`** |

**⇒ 两个真实分歧（都必须进 B10 的判定）**：

1. **保留位**：规范内部本身就有三处互斥（K-014）；**三个实现分成两支（宽松 2 / 严格 1）** ⇒ **不是"一个错一个对"，是规范没定。**
2. **规则 6 的强制降级**：`sap:6` 第 1 条原文「**永远无法触发 CATASTROPHIC 硬覆盖**」⇒ **Tuck 与 Cellrix 遵守了规范；偏离的是参考实现。**
   - **且偏离被固化进测试**：`commonintents/BIND-19/src/frame.rs:752-770` 显式构造 `replay_enable = false` 并断言 `is_catastrophic_override()` **仍为真**。
   - **Tuck 反向也有测试**：`lib.rs:515-516`「→ effective MEDIUM → **Pass (not HardOverridePass)**」⇒ **它知道规则 6 的效果，并写下来了。**
   - ⇒ **裁定问题不是"谁实现对了"，是「`pfp:6` 伪代码与 `sap:6` 第 1 条谁定义规则 6 的语义」** —— **规范两份文本自相矛盾**（K-023）。
   - **⇒ 与 K-012 叠加才致命**：`Replay-Enable` 在**未签名**的 PFP 字节里 ⇒ 参考实现的行为允许"关掉防重放检查却仍触发紧急放行"。**这正是 `sap:6` 声称要杜绝的事。**
   - **⇒ 与 K-013/K-017 的关系**：向量未被加载 + 系统性避开这一格 ⇒ **两份规范文本的矛盾从未被测到。**

### 4.4 能力层 / 语义层 / 边缘安全层

| 协议 | 消费端 | 实现 | 证据 |
|---|---|---|---|
| CAPABILITY-13（HITL 共识） | Anaphase | ⚠️ **自有实现，非同源** | `src/hitl.rs:35 is_high_risk`（名字级分类）——**未核是否对齐 CAPABILITY-13 的挑战-响应队列** |
| INTENT-7（语义意图） | Anaphase | ❓ 未核 | — |
| INTENT-7-SECURE | — | ❓ 未核 | — |

> **这三格暂标 ❓**。按本表规则，❓ 必须在本文件内可追踪到**具体未核项**，不得长期停留：见 §6 待办。

---

## 5. 「免疫归谁」列（决策索引 §2 B0 要求）

| 自称 "immune system" | 位置（原文） | 层次 |
|---|---|---|
| **Tuck** | `tuck-core/src/lib.rs:3`「Tuck is the immune system of the Helix ecosystem」；`tuck/src/main.rs:1,18`「Helix ecosystem immune system (CI-144 PFP consumer, hard real-time security gate)」 | **边缘物理闸**（自称 security gate） |
| **Anaphase `ReflexArc`** | `reflex.rs:4`「Somatic reflex arc for **immune system**」 | **先天免疫**（自保语义；实际未接线，见 K-007 #3 / K-009） |

**⇒ 两者都自称免疫，只有一个自称 gate。** 采纳第五轮审查的解法：**区分层次，不改名。**
**⇒ 矩阵结论**：「免疫」这一格**有两个所有者**，且**层次不同**——Tuck 是**边缘**，ReflexArc 是**体内**。

---

## 6. 本表的自身检验（B0 的完成判据）

按 B0 的定义：

| 判据 | 状态 |
|---|---|
| 每个协议 × 每个消费端都有格子 | ✅ PFP / SAP / BIND-19 三层 × 5 消费端；能力/语义/边缘安全三层有行但标 ❓ |
| **空格必须显式写 N-A，不得留白** | ✅ 全部 N-A 均带理由（"无帧层" / "经 Tuck"） |
| 能表达**单向依赖** | ✅ §3（5 条，含被依赖方"是否知道"列） |
| 列含**权威路径** | ✅ §1 |
| 列含**免疫归谁** | ✅ §5 |

**⇒ B0 完成。** 但**本表产出了 3 条必须回填的坑**（§7），它们**不是 B0 的失败，是 B0 的产出**。

---

## 7. 本表产出的坑（待回填 `CI-144_合拢坑记录.md`）

### K-021 ｜**帧层与参考实现不是同一格式**（🔴 规范 ↔ Tuck 之间）

**证据（§4.1，逐字段）**：magic 位置（字节 0-1 vs 2-3）、FrameType 字节（3 vs 0）、Sequence ID 宽度（`u16` vs 1 字节）、Payload Length 宽度（**`u8` vs 4 字节**）。
**⇒ 最硬的一条是 Payload Length**：Tuck 的 `u8` ⇒ **载荷上限约 251 字节**（8 字节头 + 4 PFP + 28 SAP 之后仅剩 1 字节计数）；规范的 `u32` ⇒ **上限 64MB（`0x04000000`）**。
**⇒ 两者不可能解析对方的帧；而同族阳性对照（SAP 布局逐字段一致）证明这不是"整体没对齐"。**
**归属**：CI-144（BIND-19 帧层）。
**现状**：**🔴 未修**。**⚠️ 需先裁定"谁是 BIND-19 帧层的权威"** —— 规范正文（v1.0.0-RFC-4）与 BIND-19 v2.0 参考实现**自己也不同源**（v2.0 把头 8 字节全部交给 `PayloadLen`，v1.0 是 `FrameType+ChannelID+Flags+Seq`）。

### K-022 ｜**帧层"谁是权威"在规范与参考实现之间未定**（🔴 BIND-19 内部）

**证据**：`commonintents/BIND-19/src/frame.rs:1-20` 自陈「BIND-19 **v2.0** 在 v1.0 的 8 字节固定头部基础上，增加 PFP/SAP 可选扩展层」，并在 **Flags** 加 `0x08`/`0x10`；而 `spec/BIND-19.md:2`（**权威正文**）的 8 字节头是 `FrameType+ChannelID+Flags+SequenceID` + 4 字节 `PayloadLength`。
⇒ **参考实现与它自己引用的权威正文不是同一版。**
**归属**：CI-144（BIND-19）。
**现状**：**🔴 未修**。

### K-023 ｜**规则 6 的强制降级只在一个实现里没生效——而那个实现把"没生效"写成了测试**（🔴 规范内部矛盾）

**证据（§4.3）**：`sap:6` 第 1 条要求 `Replay=0` 时硬覆盖**永不触发**；**Tuck 与 Cellrix 遵守**，**参考实现用原始风险**（`catastrophic.rs:327`），且 `src/frame.rs:752-770` **显式构造 `replay_enable = false` 并断言覆盖仍触发**。
**Tuck 反向亦有测试**：`lib.rs:515-516`「→ effective MEDIUM → **Pass (not HardOverridePass)**」。
**⇒ 裁定问题不是"谁实现对了"，而是「`pfp:6` 伪代码与 `sap:6` 第 1 条谁定义规则 6」——规范两份文本自相矛盾。**
**⇒ 与 K-012 叠加才致命**：`Replay-Enable` 在**未签名**字节里 ⇒ 可关掉防重放检查而仍保留紧急放行。**这正是 `sap:6` 声称要杜绝的事。**
**⇒ 与 K-013/K-017 的关系**：向量未被加载 + 系统性避开这一格 ⇒ **该矛盾从未被测到。**
**归属**：CI-144（PFP `pfp:6` ↔ SAP `sap:6` ↔ 参考实现）。
**现状**：**🔴 未修**。（本条的早期表述"2:1 与伪代码相反"**已撤回**，见 `CI-144_合拢坑记录.md §10`）

---

## 8. 撤回留痕

| 撤回项 | 原表述 | 理由 |
|---|---|---|
| — | 「Tuck 的 BIND-19 实现与规范不同」**（本表第一版草稿中的措辞）** | **已替换为可复核的逐字段对照**（§4.1）。原措辞是无证据的印象，按本表规则不得入表。 |
| — | 「Tuck 的 Payload Length 上限 251 字节」**（本表第二版草稿中的数字）** | **算错**：8+4+28=40 ⇒ 255−40 = **215**，非 251。**且当时未核是否有 extended 实现** —— 现已核：无（§4.1.1）。 |
| — | 「Tuck 的 PFP 语义 ❓ 未核」（第二版草稿列为未核项） | **已核**（§4.3）：Tuck 确实实现规则 6，且 CATASTROPHIC 判定**用降级后风险**（`lib.rs:427-430`）。 |

---

## 9. 本表未核项（❓ 必须可追踪，不得长期停留）

1. ~~**Tuck 的 PFP 语义**~~ → ✅ **已核**（§4.3：位解码在 `tuck-core/src/lib.rs:87-152`；`decide()` 在 `:425`）。**Output-Dest 亦已核**（`lib.rs:126-127`）。
2. **CAPABILITY-13 ↔ `hitl.rs`**：是否同源，还是 Anaphase 自有实现。（未核）
3. **INTENT-7 / INTENT-7-SECURE** 在 Anaphase 的落点。（未核）
4. ~~**Tuck 的 Payload Length 扩展**~~ → ✅ **已核**：**无扩展实现**，`min(255)` 静默截断（§4.1.1）。
5. ~~**Tuck 的 `frame.rs` 是否有测试断言 magic 在字节 0-1**~~ → ✅ **已核：有**（`frame.rs:409-411`）⇒ **有意设计，非漂移**（§4.1.2）。
6. ~~**`commonintents/BIND-19` 与根 `BIND-19` 的 `src/` 是否同源**~~ → ✅ **已核：`diff -rq` 全同**。⇒ **两份检出的差异仅在 3 个 spec 文档（各差 1 行版权行）+ `README.md` + `CONTRIBUTING.md`**。
