# I7 登记 ｜「闸门在不在」可区分于「闸门放行」—— **登记为「不可断言」，未发明 API**

> **性质**：登记件，不是修复件。
> **依据**：`CI-144_跨链生命周期与反馈契约.md` §13.3（I7 行）· §13.4 · §13.5 第 3 条；执行 brief `CI-144_I567执行brief.md` §1 / §4 / §6。
> **结论一句话**：I7 的两半（两侧闸门**无法表达「判不出」**）**已一手复核并登记**；「零拒绝时可区分『没有门』与『门开了但没事』」这半**不能在不改闸门行为的前提下写成断言** —— 原因写在 §3，不做替代品。

---

## 1. 要登记的事实（**本次逐字复核，非转抄**）

| 器官 | 类型 | 定义位置 | 变体（逐字） | 有 `Err`／「判不出」变体？ |
|---|---|---|---|---|
| **anaphase-helix** | `GateVerdict` | `src/security.rs:58`（另一处 `src/run_cycle/safety_gate.rs:52`） | `Pass` / `Reject(String)` / `HitlRequired(String)` / `HardOverride` | **无** |
| **Tuck** | **`GateDecision`** | `crates/tuck-core/src/anaphase_bridge.rs:153` | `Pass` / `Reject` / `HitlRequired` / `HardOverride` | **无** |

复核命令与结果（2026-09-21）：

* anaphase：`awk 'NR>=58 && NR<=80' src/security.rs | grep -E '^    [A-Z]'` ⇒ 恰好 4 个变体；对同一区间 `grep -E 'Err|Cannot|Indeterminate|Unknown|判不出'` ⇒ **0 命中**。
* Tuck：同一手法对 `crates/tuck-core/src/anaphase_bridge.rs:153-166` ⇒ 恰好 4 个变体，**0 命中**。
* `grep -rn 'GateVerdict' Tuck --include=*.rs` ⇒ **0 命中**（Tuck 里没有这个名字）。归属订正见 **K-106**。

⇒ **这不是本次新发现，是复核**。契约 §8 L2、分类表 §5.3、K-106 都已记载该事实；本次只是把它钉在"可复核的命令 + 行号"上。

---

## 2. 已被既有断言覆盖的那半 —— **引用，不复制**

「闸门缺席 ≠ 闸门放行」在 **anaphase 的 `None` 路径上已经成立并被测试**：

* 机制：`src/pipeline/mod.rs:165`（`match &self.security_gate`）· `:212`（`None` 分支：高风险 ⇒ 记 `Blocked` 并 `Err`）。
* 断言：`tests/security_gate.rs:94 no_gate_blocks_a_high_risk_call`（且带 `// guards: no-gate-blocks-high-risk` 标记）、`:117 no_gate_still_allows_a_low_risk_call`（窄化而非撤销 legacy）、`:76 no_gate_is_legacy_compatible`。

**⇒ 按 brief 硬约束 7，这里只引用，不新增重复断言。**

---

## 3. I7 的断言形态为什么**做不到**（**明说做不到，不凑数**）

契约 §13.3 对 I7 的判据是：**「零拒绝时，能区分『没有门』与『门开了但没事』」**。

**做不到的原因是一条代码事实，不是工程量**：

1. 两个类型都只携带**结论**（`Pass` / `Reject` / `HitlRequired` / `HardOverride`），**没有任何位置**表达"我没判"或"我没运行"。
2. 管道在 `security_gate: Option<…>` 为 `None` 时（`src/pipeline/mod.rs:212`）：
   * **高风险**调用 ⇒ 记 `Blocked` ⇒ **与"门放行"可区分**（§2 已测）；
   * **低风险（正常）**调用 ⇒ 不产生任何门相关的记录，"没有门"与"门装了并返回 `Pass`"**走同一条路径**。
3. 收尾的账本记录由 `execute_calls` 之外的 `build_verdict` 统一产出（`src/pipeline/mod.rs:373`），落在 `LedgerRecord::Verdict { status, job_id, evidence_ids, check_reports, retry_due, parent_id }`（`src/ledger/mod.rs:23`）—— **该结构里没有任何字段记录「门有没有参与」**。

⇒ 所以"没有门"与"门开了但没事"**在运行时可观测面上是同一个记录**。要让它们可区分，只有两条路：

| 路 | 动作 | 为什么本次**不做** |
|---|---|---|
| A | 给 `GateVerdict` / `GateDecision` **加**「判不出」变体 | 改闸门类型 ⇒ 改行为（brief §4 明令：不新增变体，那是行为改动，需人类裁决） |
| B | 每次判定**写入可观测记录**（如"未装门"事件 / `Verdict` 记门信息） | 改账本语义与管道行为 ⇒ 同禁令；且 Tuck 侧"34 处安全判定里 20 处是死的"，接线一次整串 fail-open 同时生效（HANDOFF-0920 §7） |

**⇒ 结论：I7 的第二半（低风险口径下的可区分性）在不违反硬约束 3/4 的前提下，无法表达为可跑断言。** 不做替代品：把 §2 已有的高风险断言改名叫"I7 断言"就是拿代理冒充对象（正是本生态候选通则 C 要禁的那件事）。

---

## 4. 仍然敞开的（**不宣称**）

1. **Tuck 侧是否存在「闸门缺席 ⇒ 全放行」的同类行为，本次未核**（分类表 §5.1 留下的开放问题）。本件只登记**类型层面**的"判不出不可表达"，不得据本件认为 Tuck 侧已被扫过。
2. **未新增任何变体、未改任何闸门代码、未改账本结构** —— 本件是纯登记。
3. 契约 §13.5 第 2 条：七条不变量**未取得"穷尽"钥匙**；本件亦不声称穷尽。

---

## 5. 与 I5/I6 的关系

* **I5**（覆盖范围 = 声明范围）与 **I6**（缺失不得比显式未知更宽松）的断言已在既有形态内落地：
  * I6 ⇒ `HelixECO-Glove/core/tests/r005_risk_disposition.rs` 的 `criterion_4c_…`（在**实际被消费的** `Disposition` 上断言，而不是其代理 `Severity`；它能在既有 6 条探针全绿时单独变红）；
  * I5 ⇒ `helix-mind/tools/hooks/diff-ratio.acceptance.sh` 的 A2/A3 步（观测仪器自己声明、但此前无人观测的两个判决 `NEW` / `UNMEASURABLE`），由**既有 runner** 驱动。
* **I7 只有本件这一半**：登记"不可表达" + 引用既有高风险断言。**这不是遗漏，是按 brief §4/§6 的唯一可行落点。**

**登记完成。**
