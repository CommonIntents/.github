# HANDOFF — CI-144 合拢（交接给下一个会话）

> 2026-09-17 ｜ 写给**没有上一轮上下文的接手者**。
> 读这一份 + 同目录 `CI-144_决策索引.md`，即可开工。**其余 17 份文档是背景与证据，不必先读。**

---

## 0. 三十秒版

- **工作区**：`/Users/jason/Doubao/chats/Jasonmilk`（14 个独立 git 仓；**根目录本身不是仓库**）
- **当前前线**：CI-144「之间」的**形状已定、代码未动一行**。这是对的：形状未定前不动代码。
- **下一步不是讨论，是执行**：先答 **O1–O4** 四个决策，其余 11 项机械推进。
- **最优先的一条**：**O2**——它决定反射是否可被记忆污染卸掉，是**唯一能绕过其余全部措施的单点**。

---

## 1. 本轮实际改了什么（都已提交，工作区干净）

| 仓 | 关键提交 | 内容 |
|---|---|---|
| **commonintents/.github** | `6f2c086` → `7670b26`（多份） | 坑记录、形式计划、六轮审查答复、**决策索引**、四条更正 |
| **helix-mind** | `ca960c7` `508d834` `ea7b97a` `2fc…` | ADR-0042 T1（相对峰值闸门 / α 由 heliotropism 派生 / 收敛判据）、D3 抑制门控、D4 k-core + **种子豁免**、ADR-0043 建边、生态册 v1.99 |
| **anaphase-helix** | `291587a` `d403817` `a6ef174` `9d69d15` `79635c2` | T5b 传血缘、P0 修父指针槽错位、悬空父指针归一化、T6 白读 `activation_vector`、标题创建时冻结 |
| **Cellrix** | `1f868a9` `fa96721` `3c7a2bd` `a5520d6` `0bb8d51` `89d3f09` | 标题加秒→冻结、URL hash 纯原语、接通闲置测试、`[ENG]` 校验器、几何抽屉提交 |

**测试**：Helix-Mind **158** / Anaphase **277** / 全生态 **1630**；面板 JS 回归网 **10 suites green**（含 55 条对着真面板的断言）。
**线上**：mind `:50052` / anaphase `:50061` / 面板 `:8080`（另有 `:18932` 专供几何测试）/ tentacle `:50051` / Tuck `:60052` / FlowModus `:60053` / **Chrome `:9222`**。**服务不是我的托管作业**，重启前先 `ps` 看是否已在跑。

---

## 2. 已定的事（不要重开）

**原则 P1–P12** 见决策索引 §2。最该记住的六条：

- **P1 共享「事实」，不共享「代码」** —— 唯一事实来源 与 极致解耦 同时成立的钥匙
- **P4 冻结层可承载「生产者自陈」，但"改变审查者行为的位"必须带外签发** —— 你有权说你是谁，你无权决定我审不审你
- **P6 可选应缺省到「严」，不是缺省到「无」**
- **P7 阴性结论必须带阳性对照，且逐目标施加**（三把钥匙：可达 / 穷尽 / 规范语言）
- **P10 单引一条原则的论证不予通过** —— 必须写出张力与残余风险
- **P12 每份文档必须对应一个可执行动作，或明确的"不做，因为 X"**

**已排除、不再讨论**：扁平 vs 树、命名扫哪个仓、契约放哪、要不要生成、要不要坑 ID 门禁。

---

## 3. 待你裁决的**四项**（形式已修正，均可回答）

| # | 问题 | 输入 |
|---|---|---|
| **O1** | **紧急处置的真实动作集枚举**（停机/撤离/断电/外呼…）<br>*反射已证是 veto-only ⇒ 任何需要"放行"的紧急动作都不是反射，而是**一个尚未被命名的功能*** | 一张表 |
| **O2** | **`safety_rules` 静态还是运行时可更新？L2 是否就是记忆层？**<br>*若 L2 是记忆层 ⇒ 不是权限问题，是**可信边界画错地方**（基因锁规则与可污染内容同存） ⇒ 修法是**物理分开*** | 读 `lifecycle.rs` 与 L2 定义 |
| **O3** | **E1（闸门单向 + 白名单）还是 E2（带外签发 + reason）？**<br>**建议 E1**——**唯一不需要证明"LLM 无法影响帧字节"的选项**，而那证明在 LLM 驱动的躯体里极难给出 | O1 的结果 |
| **O4** | **SAP 28B 全文 + PFP 未通读约 259 行**（317 行中已完整读 128–185） | 一次通读 |

**PFP 规范位置**：`commonintents/BIND-19/docs/spec/pfp-xcf14.md`；SAP 同目录 `sap-xcf14.md`；主规范 `commonintents/BIND-19/spec/BIND-19.md`。

---

## 4. 机械可执行的 11 项

见决策索引 §2 **B1–B11**。摘要：

读 SAP/PFP 全文（B1–B3）→ `is_high_risk` 改**能力维度**（B4）→ `hard_reflex` 改 **allow-list**（B5）→ `HardOverride` 补 reason + 带外条件（B6）→ 闸门缺省语义（B7）→ 命名对齐**按实测计数**（B8：Cellrix 27 → Anaphase 8 → Mind 2 → FlowModus 1）→ 事实源单一化 + 生成 + **golden 对照**（B9）→ 校验器 + **基线豁免清单**（B10）→ 器官补丁：复合游标 `(period, seq)` + 物化快照（B11）。

---

## 5. 操作知识（踩过才知道，省你时间）

1. **面板资产是 `include_str!` 嵌进二进制的** ⇒ 改了 `web/assets/*` 必须 `cargo build` + 重启面板才生效。
2. **验证资产改动要 grep 主页面**：`curl -s http://127.0.0.1:8080/ | grep -c "<标识>"`。**`/assets/...` 直接取是空的**——资产被内联，取它会得到**假阴性**（踩过）。
3. **看面板真数据走 `:8080/api/...`**（`:50061` 直连要 bind HMAC）。
4. **`layout_test.js` 默认对 `:18932`**，且需 Chrome `:9222`；两个条件齐了才跑。
5. **重启前先 `ps`**：这一晚出现过"有人已经启好一个不带参数的实例在跑"（功能完整，端点有协议默认值），**杀掉它才是自伤**。
6. **`Tuck` 是 workspace**：源码在 `Tuck/crates/*/src`。**`Tuck/src` 不存在——grep 它会静默零命中**（这是本族第 4 次失败）。
7. **阴性结论必须带阳性对照**，且**逐目标施加**（第 5 次失败就是只施加于三个目标中的一个）。

---

## 6. 工作规矩（人类反复强调，务必遵守）

1. **改之前先说计划、等确认**
2. **中文回复**
3. **命名红线**：写「**证轨**」，绝不写「轨迹」
4. **活文档不出现外部项目名**（参考做法可以，名号不留）
5. **每个功能都要有测试；用变异注入证明非空转**
6. **不许静默提交错误数字**；更正**记录在案**，不重写历史
7. **绝不绕过 git hooks**（`[large]` 写真实理由；ADR 首行/状态要能被 `tools/adr_head.py` 解析）
8. **陶土不许说成钻石**：无法论证的数字标 `[ENG]` 或**留空**
9. **别在 Cellrix 上开并行编辑**（本晚已两次差点互相覆盖）
10. **不碰人类在飞文件**：`helix-mind/docs/helixECO/HANDOFF-0916.md`、`anaphase-helix/session_notes.json`、Cellrix 几何在飞改动

---

## 7. 边界：**收敛的是什么**

> **「决策链收敛」，不是「缺陷清点完成」。**

| 未读面 | 状态 |
|---|---|
| SAP 28B | **全文未读**（它才可能被设计成"签名覆盖 PFP"） |
| PFP 规范 | 317 行，完整读 128–185 |
| BIND-19 `spec/` | 未通读 |
| `reflex.rs` / `hitl.rs` / `security.rs` | **全文未读**（只读了相关行） |

**下一个读规范的人可能在第 200 行发现第 4 处缺陷。** 在 B1–B3 完成前，**不得宣称"规范已核清"**。

---

## 8. 本晚真正的资产（比结论耐用）

1. **判据集 P1–P12** —— 不依赖 CI-144 细节，下次设计任何"之间"都能用。
2. **一个错误族被命名并机制化**：**阴性结果 + 未声明的采样范围**。5 次同族失败，前 4 次靠运气，**第 5 次被机制自己抓到** ⇒ 机制有效。
3. **一条最朴素的教训**：**一条写在规范里、被逐字读出来的规则，价值胜过十轮从摘要推出来的判断。** 本链读了 30 行原文：推翻一个希望、坐实一个指控、新发现两处规范缺陷——而此前五轮都在摘要上打转。
4. **一次方法演示**：**先合拢再冻结是对的。提前冻结 = 冻结猜测。**

---

## 9. 接手提示词（可直接粘贴）

```
请读 commonintents/.github/HANDOFF.md 与同目录 CI-144_决策索引.md，按它们接手。
工作目录 /Users/jason/Doubao/chats/Jasonmilk。
规矩见 HANDOFF 第 6 节（改前先说计划等我确认、中文回复、写「证轨」不写「轨迹」、
活文档不出现外部项目名、测试要有变异证明、不静默提交错误数字、不绕过 git hooks）。
先给我计划，我确认后再动。
当前待我裁决的是 O1–O4 四项；其中 O1（紧急动作集枚举）与 O2（safety_rules 静态否 / L2 是否记忆层）需要我先给输入。
```

---

**一句话**：形状已定、原则已立、账目已归、11 项机械可执行、4 项待人类输入。**代码一行未改，这是正确的。**
